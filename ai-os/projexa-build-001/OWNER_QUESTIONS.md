# OWNER QUESTIONS - PROJEXA-BUILD-001

Date: 2026-09-25. Every row is traceable to PM_DECISIONS.md, the work order (section 10), the master handoff (part 4), ADDENDUM A, INFRA_INVENTORY.md, IDENTITY_AND_EDGE_FINDINGS.md, A08_billing_notes.md, DEV_TEST_DEPLOY_FACTS.md or an `owner_questions` entry in the journal of workflow wf_0a17375d-ea4.

How to read this file: the owner said in chat on 2026-09-25 "except for recharge of vercel you can take any and all decision" (PM_DECISIONS.md, header). So every question below already has a default chosen by the PM, or a PROPOSED default that the PM must write into PM_DECISIONS.md as a new PMD row before the item it blocks starts. Only OQ-01 to OQ-08 need the owner. OQ-09 to OQ-32 run on the default unless the owner overrides it.

Amendment 001 keeps exactly three action types owner-only: Vercel recharges and payments, anything that increases spend, and DNS changes (PM_DECISIONS.md, header). A question is marked owner-only YES only if answering it needs one of those three, or needs a value or a dashboard the PM cannot read.

Counts: 32 questions after removing duplicates. Inputs: work order Q1 to Q6, master handoff Q-ID1, Q-GATE, Q-D1, Q-D2, 12 verifier `owner_questions` lists (35 questions), 21 PM decisions. Owner-only YES: 8 (OQ-01 to OQ-08). Owner-only NO: 24. Proposed defaults that still need a PMD row: OQ-15, OQ-20 (storage place), OQ-22, OQ-24 (5 sub-decisions), OQ-26, OQ-27, OQ-28, OQ-29.

## QUESTION TABLE (owner-only items first)

| id | question | PM default already chosen (PMD id) | what it blocks | owner-only under Amendment 001 (yes/no and why) | how the owner overrides in one sentence |
|---|---|---|---|---|---|
| OQ-01 | Vercel go-live: recharge, unpause both projects, un-draft and merge PR #1808 (build gate). Also: keep main build-blocked with a manual release step, or let PR #1808's gate build every non-docs merge to main? Sources: work order s3, DEV_TEST_DEPLOY_FACTS s3, verifier line 36 first question. | PMD-11: Vercel stays locked. No deploy, no unpause, no setting or env change. ignoreCommand stays `sh -c 'exit 0'`. PR #1808 stays a draft. | The deployed-URL half of U-23 (work order test 3.4, status blocked_owner). U-48 (real per-AI acceptance run needs a reachable public URL). Any check that reads a deployed instance. | YES. Amendment 001 keeps recharges and payments owner-only; unpause needs the recharge first (DEV_TEST_DEPLOY_FACTS s3: the go-live order starts with the owner saying go live in chat). | Say in chat: `go live`. The PM then follows the go-live order in DEV_TEST_DEPLOY_FACTS s3. |
| OQ-02 | DNS for inbound email: add the Resend Inbound MX record at the DNS host for projexa-ai.com and veridian-aios.com (register item R-C17). When? Sources: work order Q4, handoff Q4. | PMD-06: DNS stays owner-only. The PM prepares the record list. Surface 4 (email) cells are status blocked_owner until the owner applies DNS. | U-30 and U-31 (email bridge), surface 4 cells of the 28-cell matrix, closure of R-C17 (status OPEN, closure_test_path empty). | YES. Amendment 001 keeps DNS owner-only; ai-os/DOMAIN_OWNERSHIP.yaml and R-C17 both say no AI session changes production DNS. | Say in chat: `DNS applied for <domain>` after adding the records, or `DNS by <date>`. See OWNER ACTIONS item A-2. |
| OQ-03 | Metered L1 provider: switch the internal L1 to openrouter (costs money on every L0 miss) so work-order test 2.8 passes as written? Also which L1 is sanctioned for multi-user chat. Sources: verifier line 8 first question, verifier line 38 third question, work order 2.8, handoff Q-GATE. | PMD-02 (i) and (iii): no metered provider. Internal chat for non-owner users stays Level 0 only, and the refusal text carries the records. PMD-09: test 2.8 becomes a provider-parameterised matrix that passes today. | Nothing in this plan. Only a future wish to have server-side AI for non-owner users (U-14 and U-49 assume the gate stays). | YES. A metered provider is a spend increase, which Amendment 001 keeps owner-only. | Say in chat: `enable openrouter for internal Level 1`. |
| OQ-04 | Approve a paid Supabase branch (USD 0.01344 per hour, about USD 0.32 per day) for the migration rollback rehearsal (work order 2.6)? Sources: verifier lines 12, 36, 38. | PMD-10: no branch. Rollback is proven with drizzle/down/*.down.sql, docs/ROLLBACK_RUNBOOK.md, an always-aborted transaction that compares schema hashes before and after, and the PGlite replay. | Nothing. The no-branch pattern covers test 2.6. | YES. It is a spend increase (DEV_TEST_DEPLOY_FACTS s4: get_cost branch = 0.01344 hourly). | Say in chat: `approve a branch`. |
| OQ-05 | Open Vercel Team Billing and check whether the Speed Insights Plus add-on is an active team subscription that must be cancelled separately from the per-project toggle. Sources: verifier line 18 first question, A08_billing_notes.md. | PMD-11: both project toggles are already disabled (disabledAt 2026-09-24 04:50 UTC); cancellation effective 2026-09-26 07:00 UTC; test 1.9 is date-gated and reads the first billing day, which starts 2026-09-26 07:00 UTC. | Test 1.9 (charge reads 0.0000 for Speed Insights Plus). If charges continue after 2026-09-26 07:00 UTC, the add-on is a team subscription (A08: flat 0.645161 per day since 2026-09-16 while project usage is 0 from 2026-09-17). | YES. It is a Vercel billing setting and Vercel stays locked to the PM (PMD-11); the PM has no Team Billing access. | Say in chat what Team Billing shows, or: `Speed Insights Plus add-on cancelled` after cancelling it in the Vercel dashboard. |
| OQ-06 | Read the two Sensitive Vercel Production env values RAJAT_USER_ID and AI_PROVIDER_PIPELINE_L1 and say whether RAJAT_USER_ID equals a compliance.users id or a PROJEXA org API key id. Source: verifier line 8, fourth question. | PMD-02 (iv): the gate identity becomes the acting person, not the org API key. The PM works from code evidence (adapter.ts:120-141, adapter.test.ts:28,65) and does not need the live value. | Only the wording of the D-09 and D3 findings (whether the live gate today matches a person or a key). No build item waits on it. | YES. The values are Sensitive (unreadable to the PM) and Vercel stays locked. | Say in chat: `RAJAT_USER_ID is a person id` or `RAJAT_USER_ID is an org key id`. |
| OQ-07 | Measure the USD 20 per month cap (PROJEXA-COST-001) on gross charges (USD 29.24 since 2026-08-26, run rate USD 40 per 31 days) or net of any Pro included credit? Sources: verifier line 18 second question, A08_billing_notes.md. | PMD-13: gross charges. The cumulative USD 29.24 is reported, not hidden. | Nothing in the build. It changes only whether the cap is reported as exceeded. | YES for the credit lookup (the credit is not visible in the FOCUS billing data, so only the owner's Vercel dashboard can show it). NO for the choice of measure, which the PM decided. | Say in chat: `measure the cap net of credit` and give the credit figure from Team Billing. |
| OQ-08 | Allow one owner-approved test deploy to measure whether `vercel deploy --prebuilt` removes Vercel build minutes. Source: verifier line 36 second question. | PMD-11: no deploy of any kind. The build-minute shift stays UNVERIFIABLE from docs (DEV_TEST_DEPLOY_FACTS s3). | Only the batch-release cost model in OQ-01. | YES. A deploy needs an unpaused project and spends credit. | Say in chat: `allow one test deploy of <project>`. |
| OQ-09 | Phase 5 scope order: document-only, extend the AI-Link MCP path, or build a real L1 extractor first (Q1)? Also U-34. Sources: work order Q1, handoff Q1. | PMD-03: extend the existing AI-Link MCP path first (redaction leak, de-advertise 13 tools, zero-server-L1, project scoping), then extend src/lib/services/document-extraction-service.ts, then the remaining registry entries. | U-34 (Phase 5 scope order). | NO. Not a recharge, spend increase or DNS action; the owner delegated all other decisions on 2026-09-25 (PM_DECISIONS.md header). | Say in chat: `Q1: build <option> first`. |
| OQ-10 | Fix the financial-redaction leak (Gap 5) on its own and now? Also: move it from Phase 2 to Phase 1, or ship before any phase? Sources: work order Q2, handoff Q2, verifier line 14 second question. | PMD-04: yes, in Phase 1. Fix all four role-dropping points: mcp/[token]/route.ts:58,68; executor.ts:372; assistant/route.ts:56,77 with API-key auth; construction-tools.ts:95 fail-open default. | Phase 1 sequencing. Two active AI links exist (last used 2026-08-29), so the leak is real while Vercel is paused. | NO. Not spend or DNS. | Say in chat: `Q2: hold the redaction fix until <phase>`. |
| OQ-11 | How much unattended authority may an email-triggered action have (Q3)? Sources: work order Q3, handoff Q3. | PMD-05: email-triggered actions create proposals only. Reads and drafts run unattended. No write runs until a human confirms on the AI-prepared page or by reply-to-confirm. | U-30 and U-31 (surface 4 wording and the email bridge write path). | NO. Policy choice, not spend or DNS. | Say in chat: `Q3: allow <named action> unattended`. |
| OQ-12 | Is a plain REST key (not an MCP tool) the sanctioned way to give access to an AI (Q5)? Sources: work order Q5, handoff Q5. | PMD-07: the sanctioned credential is the project-and-user scoped link token. Org API keys are service-to-service and are never handed to an AI. | U-16 and the universal AI link design (U-44). | NO. Not spend or DNS. | Say in chat: `Q5: also allow <credential type> for AI access`. |
| OQ-13 | Existing org-wide AI tokens: break them or grandfather them (Q6)? The verifiers restated it: the spec proposes a nullable project_id while work order 2.1/2.2 require NOT NULL; the 2 existing active links were last used 2026-08-29. Sources: work order Q6, verifier lines 14 and 38. | PMD-08: split. platform.user_ai_links.project_id is NOT NULL for links minted after the migration; the 2 legacy org-wide links (both demo/test orgs) are revoked at migration time. compliance.api_keys.project_id is NULLABLE with a key_kind check (org_service or project_ai); legacy org keys stay because the PROJEXA proxy needs them. | U-16 and the Phase 2 migration (tests 2.1, 2.2). | NO. Not spend or DNS. | Say in chat: `Q6: grandfather the 2 legacy links` (keeps the hole) or `Q6: also revoke legacy org keys`. |
| OQ-14 | Which project owns identity for browser-side reads (Q-ID1): move the 114 PROJEXA users into verdian-ai, bridge, or keep reads server-side? Also: which Supabase project the PROJEXA browser session authenticates against for BOQ tables, and whether to approve the Edge-gateway bridge. Sources: handoff Q-ID1, verifier line 26 second question, verifier line 44 second question. | PMD-01: Edge Function gateway on verdian-ai (verify the PROJEXA ES256 JWT against the projexa key set, resolve the person through compliance.users.auth_user_id, query as app_runtime with the tenant setting, return keyset-paginated JSON). Spike first (U-25a). Fallback (c): keep reads server-side and move only rendering to the browser. | Phase 4 browser-first work (U-25, U-27; tests E-06 and E-07 as rewritten). | NO. Not spend or DNS. Reversible until Phase 4 starts (PMD-01). | Say in chat: `use option (a)` (consolidate) or `use option (c)` (server-side reads). |
| OQ-15 | Link the 22 unlinked PROJEXA users to compliance.users now, or restrict the bridge to the 92 linked users for the BOQ pilot? Source: verifier line 26 third question. Live count 2026-09-25: 114 PROJEXA auth users, 92 linked in 8 compliance orgs, 22 unlinked (IDENTITY_AND_EDGE_FINDINGS s3b). | PROPOSED, no PMD row yet: restrict the pilot to the 92 linked users; test ID-01 (UNLINKED=0) stays pending until linking is done. The PM records this as a new PMD row before U-25 starts. | Test ID-01 and full-population use of the gateway. The pilot on linked users is not blocked. | NO. Not spend or DNS. It writes to compliance.users, so it is recorded in ACTIVE-CLAIMS first. | Say in chat: `link all 22 users now`. |
| OQ-16 | Gate identity on the PROJEXA proxy (D3): the gate checks the org API key id, not a person. Should the per-user identity from D-09 be threaded through, and is it acceptable for now that one org key covers every user in that org? Source: verifier line 8 third question. | PMD-02 (iv): the gate identity becomes the acting person (Phase 2, with per-request actor). PMD-09: D-09 gets a stricter test that also counts actor_role='api_key' rows. | Test D-09 (Phase 2), U-49. | NO. Not spend or DNS. | Say in chat: `D3: keep the org key as the gate identity`. |
| OQ-17 | External AI link path (D2): remove the server-side L1 call so the user's own AI does the classification and the server makes zero model calls on that path? Sources: verifier line 8 second question; owner directive 2026-09-25 (OWNER_REQUIREMENT_AI_WORK_LINK). | PMD-02 (ii): yes. compliance-tracker src/app/api/mcp/[token]/route.ts makes zero server model calls and sits outside the provider gate. On an L0 miss it returns candidate functions and missing parameters to the external AI. | U-43 (Phase 1), U-45 conformance harness. | NO. Removing a call lowers spend. | Say in chat: `D2: keep the server L1 call on the link path`. |
| OQ-18 | Which record does 'BOQ progress' mean (Q9): a BOQ line item or a progress entry against it? Source: verifier line 16 third question. | PMD-20: BOQ line item (compliance.construction_boq_line_items). Progress entries are the separate 'daily work progress' type. | Phase 4 record type (U-27, U-28). | NO. | Say in chat: `BOQ progress means progress entries`. |
| OQ-19 | Which AI link is the canonical surface 3 (Q8): PROJEXA's own org_ai_link snapshot or compliance-tracker's platform.user_ai_links route? Source: verifier line 16 second question. | PMD-16: compliance-tracker platform.user_ai_links with its MCP and REST route is canonical (it reaches the EXECUTORS registry). PROJEXA org_ai_link becomes a read-only projection that delegates to it. Universal link spec implementation starts after project scoping (U-18). | U-44 to U-47. | NO. | Say in chat: `Q8: keep the PROJEXA org_ai_link as canonical`. |
| OQ-20 | Exceptions: converge the four surfaces on a new exception-acknowledgement record or on the underlying flagged record (Q7)? Where do the 31 exception items live and under which ids? Do the 3 META items (29, 30, 31) count in the 111, or is the testable population 108? Sources: verifier line 16 first question, verifier line 10 first and second questions. | PMD-19: underlying flagged record (for example the overdue RFI); no new table. PMD-17: ids EXC-ITEM-01..EXC-ITEM-31; the 3 META items count in the 111 and are verified by register-level checks; platform.sumeet_gap (31 rows) is never the source. The storage place (platform.sumeet_requirements or a new table) is not set by a PMD: PROPOSED, rows in BOOLEAN_REGISTER.csv first, no new table. | D-02 rows and the exceptions cells of the 28-cell matrix. | NO. | Say in chat: `Q7: use a new acknowledgement table` or `count 108`. |
| OQ-21 | Add columns verify_command, evidence_ref and surface to platform.sumeet_requirements? They do not exist today (23 columns; 80 rows). Sources: verifier line 10 third question, INFRA_INVENTORY s1. | PMD-09: the columns are added first because tests 3.1, 3.6 and 4.2 query them (a migration, recorded in ACTIVE-CLAIMS). Fallback if declined: rewrite the tests against closure_test_path and closure_commit_sha. | U-22 and tests 3.1, 3.6, 4.2. | NO. A schema change on the live database is not spend or DNS. | Say in chat: `do not add columns, use closure_* columns`. |
| OQ-22 | Should audit rows for AI-link and API-key writes store both the key id and the acting user (changing audit.ts:115), or only the acting user? Source: verifier line 12 second question. | PMD-02 (iv) and PMD-09 cover the acting person and count actor_role='api_key' rows. Storing both is PROPOSED (no PMD row yet): store both, so a key id is never lost. | Test D-09 and the audit part of Phase 2. | NO. | Say in chat: `audit: store the acting user only`. |
| OQ-23 | Is the compliance schema exposed through Supabase's PostgREST exposed-schemas setting? SQL cannot read it. Source: verifier line 12 third question. | Not needed after PMD-01: the browser path goes through the Edge gateway, not PostgREST-direct. Live evidence: platform NOT exposed (PGRST106 confirmed), dpdp NOT exposed (confirmed), compliance plausible but not observed (IDENTITY_AND_EDGE_FINDINGS s4). | Nothing in the plan. Only the record of whether the anon key can reach compliance tables. | NO. A read of a dashboard setting. | Say in chat what Supabase Dashboard, API settings, Exposed schemas shows. |
| OQ-24 | Enable the 10 prepared PG_CRON jobs one at a time? Each re-enables writes not seen since about 2026-08-23 (report-schedule notifications, exchange rates, orchestra payload purge, stuck-deal digests). Five open sub-decisions: month-end clamp vs overflow, audit window 3h vs 24h, hourly report-schedules, dedup on digests, daily cap for crm-lead-scoring. Source: verifier line 24 first question. | PMD-12: port exchange-rate-refresh first (HTTP fetch plus upsert, no LLM). The others are enabled one per PR, each with a note that it re-enables writes silent since about 2026-08-23. The 5 sub-decisions have no PMD row: each is decided in its own PR, and only jobs 2 to 10 wait on them. | Phase 5 cron items (U-41: at most 2 crons in each vercel.json, none of the */N form). | NO. Not spend or DNS (pg_cron already runs on verdian-ai at no extra cost). | Say in chat: `enable cron <name> with <choice>`. |
| OQ-25 | Which database carries the projexa- cron jobs for E-04: the shared verdian-ai project (pg_cron and pg_net installed) or PROJEXA's own project (neither installed; installing them needs an ACTIVE-CLAIMS entry under E-16)? Source: verifier line 24 second question. | PMD-12: the verdian-ai project (pcrjmlpuqsbocqfwoxod), where pg_cron 1.6.4 and pg_net 0.20.3 already run. Nothing is installed on the PROJEXA project. | E-04 (cron.job jobname like 'projexa-%' count) and U-21. | NO. | Say in chat: `install pg_cron on the PROJEXA project instead`. |
| OQ-26 | Make Secret Scanning a required merge check in compliance-tracker? Keep the failing Env-1 cross-repo E2E job non-required until its two failing steps are fixed? Sources: verifier line 24 third question, INFRA_INVENTORY s3. | PMD-09: test 1.6 is a required-check assertion on the job that exists (sentinel.yml job secret-scan). PROPOSED (no PMD row yet): make secret-scan required; keep Env-1 non-required. projexa already requires Secret Scanning. | Test 1.6. Env-1 stays outside the gate. | NO. A repository setting, not spend or DNS. | Say in chat: `do not require secret-scan` or `require Env-1 too`. |
| OQ-27 | Disable or fix the failing scheduled workflows (Domain Ownership Drift Check, projexa Email Digest Poll, projexa Claude Nightly Maintenance)? Fix or make non-blocking the E2E Env-1 job? Source: verifier line 36 third question. | PMD-12 and E-05 replace the projexa Email Digest Poll workflow with one pg_cron job (email-digest-poll.yml is classed KILL in EDGE_CANDIDATES.csv). No PMD row for the other two: PROPOSED, fix later in their own items; nothing here disables them. Domain Ownership Drift Check belongs to the VERIDIAN AI-OS track. | Nothing in the build order. | NO. | Say in chat: `disable <workflow name>`. |
| OQ-28 | Fix three projexa routes that call missing compliance-tracker paths (construction-budget/lines, documents/[id]/dispose, schedule-tracker) by adding CT routes or by pointing projexa at the existing /api/v1/projexa/* aliases? Also: the 16 CT-only modules with no projexa consumer, and whether projexa's own-DB set (todos, notifications, conversations, email digests, ai-link, google sheets, org invites) stays in projexa's Supabase. Source: verifier line 20. | PROPOSED (no PMD row yet): point projexa at the existing aliases (no new CT routes); treat the 16 modules as unfinished projexa UI, not as dead code; the own-DB set stays in projexa's Supabase. The PM records these as PMD rows before the sitemap-coverage items start. | Route-coverage items built on PROJEXA_ROUTE_SITEMAP_projexa_main.md (310 routes, 109 modules). | NO. | Say in chat: `add the missing CT routes` or `move the own-DB set to compliance-tracker`. |
| OQ-29 | Has the owner decided item 13 (static export vs the Vite plus Cloudflare Pages route, NEXT-001 Step 4) anywhere outside the repo files the verifier read? Source: verifier line 44 first question. | No PMD row. PROPOSED: no new hosting is chosen in this plan; the BOQ browser-first slice runs from the existing Next.js app and the Edge gateway (PMD-01), and the hosting choice waits for the owner. | Only the hosting choice for a browser-first slice beyond BOQ. | NO, unless the chosen route adds paid hosting, which would be a spend increase and owner-only. | Say in chat: `item 13: <static export or Vite plus Cloudflare Pages>`. |
| OQ-30 | [removed from the public copy: see the private KT folder] | [removed from the public copy: see the private KT folder] | Nothing in this plan. | NO for PROJEXA (it is not a PROJEXA object). The VERIDIAN AI-OS track decides. | [removed from the public copy: see the private KT folder] |
| OQ-31 | DPDP Q-D1: are the 5 anon-callable functions (dpdp_apply_email_action, dpdp_parent_consent, dpdp_parent_consent_preview, dpdp_preview_email_action, dpdp_unsubscribe) intentionally public, and safe against a guessed or replayed argument? Sources: handoff Q-D1, ADDENDUM A5. | PMD-09 and PMD-18: a DPDP-track question, not a PROJEXA finding. PROJEXA keeps only a narrow guard: the platform and compliance schemas have 0 SECURITY DEFINER functions executable by anon or authenticated (0 today, checked 2026-09-25). | The DPDP track only. Nothing in the PROJEXA build order. | NO. Not spend or DNS. | Say in chat: `Q-D1: <function> stays public` or `restrict <function>`. |
| OQ-32 | DPDP Q-D2: should dpdp_create_client_org and dpdp_my_clients be callable by any signed-in user of either product, given both products share this database? Sources: handoff Q-D2, ADDENDUM A5. | Same as OQ-31 (PMD-18). Option (a) of OQ-14 would merge the two auth pools and widen this exposure; PMD-01 avoids it by not moving PROJEXA users into verdian-ai. | The DPDP track only. | NO. | Say in chat: `Q-D2: restrict to <rule>`. |

## DEDUPLICATION MAP (which source question went into which row)

| source | row |
|---|---|
| Work order Q1 | OQ-09 |
| Work order Q2 | OQ-10 |
| Work order Q3 | OQ-11 |
| Work order Q4 | OQ-02 |
| Work order Q5 | OQ-12 |
| Work order Q6 | OQ-13 |
| Handoff Q-ID1 | OQ-14 |
| Handoff Q-GATE | OQ-03, OQ-06, OQ-16 |
| Handoff Q-D1 | OQ-31 |
| Handoff Q-D2 | OQ-32 |
| Verifier journal line 8 (4 questions: D1, D2, D3, read the two env values) | OQ-03, OQ-17, OQ-16, OQ-06 |
| Verifier line 10 (META items, EXC ids and storage, verify_command) | OQ-20, OQ-21 |
| Verifier line 12 (branch, audit rows, exposed schemas) | OQ-04, OQ-22, OQ-23 |
| Verifier line 14 (Q6 restated, redaction leak placement) | OQ-13, OQ-10 |
| Verifier line 16 (Q7, Q8, Q9) | OQ-20, OQ-19, OQ-18 |
| Verifier line 18 (Team Billing add-on, cap measure) | OQ-05, OQ-07 |
| Verifier line 20 (projexa routes, CT-only modules, own-DB set) | OQ-28 |
| Verifier line 24 (10 PG_CRON jobs, cron database, Secret Scanning required) | OQ-24, OQ-25, OQ-26 |
| [removed from the public copy: see the private KT folder] | OQ-30, OQ-14, OQ-15 |
| Verifier line 36 (batch release, test deploy, failing workflows, paid branch) | OQ-01, OQ-08, OQ-27, OQ-04 |
| Verifier line 38 (Q6 split, PGlite vs branch, test 2.8 wording) | OQ-13, OQ-04, OQ-03 |
| Verifier line 44 (decision 13, browser session project) | OQ-29, OQ-14 |

Journal line numbers are the line index in C:/Users/Dell/.claude/projects/C--ct-ct/5ad1b41c-4c64-40a2-9d8c-7e6a4052cd5b/subagents/workflows/wf_0a17375d-ea4/journal.jsonl (12 lines carry an `owner_questions` field).

## OWNER ACTIONS THAT ONLY THE OWNER CAN DO

These are the actions the PM cannot take. Each is a plain step.

| id | action | exact steps | why only the owner | items that wait |
|---|---|---|---|---|
| A-1 | Vercel recharge, unpause, go-live | (1) Say `go live` in chat. (2) Recharge the Vercel team credit in the Vercel dashboard, Team Billing. (3) Unpause the two projects veridian-compliance-ai and projexa. (4) Tell the PM; the PM then un-drafts and merges PR #1808 in compliance-tracker first, then the projexa equivalent, verifies one build per project, then runs the post-deploy checks (DEV_TEST_DEPLOY_FACTS s3). | Amendment 001: recharges and payments are owner-only. PMD-11: the PM never changes Vercel. | U-23 deployed-URL half, U-48, all deployed-instance checks (work order 3.4). |
| A-2 | DNS records for Resend Inbound | (1) In the Resend dashboard, open the Inbound setup for projexa-ai.com and again for veridian-aios.com and copy the record Resend shows for each. R-C17 records that Resend Inbound needs a single MX record per domain and that the webhook delivers metadata only, so the route must call the Resend Attachments/Emails API for the body. (2) Add that MX record at the DNS host of each domain. The exact MX host name and priority are UNVERIFIED: no input file contains them, Resend generates them per domain, and the PM must not guess them. (3) For veridian-aios.com leave the existing MX record of the bare domain alone (it is Google Workspace; HANDOFF_FOR_RAJAT.md line 201 on origin/main). (4) Tell the PM `DNS applied for <domain>`; the PM then runs the inbound check. | Amendment 001: DNS is owner-only. ai-os/DOMAIN_OWNERSHIP.yaml: no AI session changes production DNS. Known state: send.projexa-ai.com and reply.projexa-ai.com are not configured (ACTIVE-CLAIMS.yaml line 4812 on origin/main; HANDOUT 2026-09-22 line 151 says unchanged). The four outbound records for send.veridian-aios.com in HANDOFF_FOR_RAJAT.md belong to the DPDP track and are not the inbound records. | U-30, U-31, surface 4 cells, R-C17. |
| A-3 | Team Billing check for the Speed Insights Plus add-on | (1) Vercel dashboard, team team_Iqx3zyb7sDdsdzcNskCFFsHD, Settings, Billing. (2) Look for an add-on line named Speed Insights Plus. (3) If it is active, cancel it there. (4) Read the billing day that starts 2026-09-26 07:00 UTC: the charge should read 0.0000. (5) Tell the PM what the page showed. | A08_billing_notes.md: the charge is a flat 0.645161 per day although project usage is 0 since 2026-09-17, so it is a team subscription. Vercel is locked to the PM (PMD-11). | Test 1.9 (date-gated). |
| A-4 | Metered provider decision | Decide yes or no on openrouter for internal Level 1 (OQ-03). Default is no. | Spend increase (Amendment 001). | Nothing today. |
| A-5 | Paid Supabase branch decision | Decide yes or no on a branch at USD 0.01344 per hour (OQ-04). Default is no. | Spend increase (Amendment 001). | Nothing today. |
| A-6 | Read two Sensitive Vercel env values | In the Vercel dashboard, Production environment variables of veridian-compliance-ai: read RAJAT_USER_ID and AI_PROVIDER_PIPELINE_L1 and tell the PM (a) whether RAJAT_USER_ID is a compliance.users id or an org API key id and (b) the pipeline value. Do not paste any key. | The values are Sensitive; the PM cannot read them and Vercel is locked. | Only the wording of the D-09 and D3 findings. |
| A-7 | Pro included credit and the USD 20 measure | In Team Billing, read whether the Pro plan shows an included credit and how much (OQ-07). Default: the PM measures gross. | The FOCUS billing data has no credit line (A08_billing_notes.md). | Only how the cap is reported. |
| A-8 | One test deploy (optional) | Only if the owner wants to know whether `vercel deploy --prebuilt` avoids build minutes: allow one deploy, then the PM reads list_billing_charges. Default: none. | A deploy needs an unpaused project and spends credit. | Only the batch-release cost model. |

## FACTS BEHIND THE OWNER-ONLY ROWS

- Vercel: both projects live=false, latest production deployment BLOCKED (A08_billing_notes.md). Both vercel.json files carry ignoreCommand `sh -c 'exit 0'` (INFRA_INVENTORY.md s1).
- Billing 2026-08-26 to 2026-09-25 (31 Pacific days): USD 29.2436 = Pro 17.7287 + Speed Insights Plus 6.4516 + Build CPU 4.3346 + Speed Insights Plus events 0.65 + rest about 0.05. Run rate USD 1.2903 per day (USD 40.00 per 31 days); USD 0.6452 per day (USD 20.00) if the add-on stops (A08_billing_notes.md).
- Speed Insights and Web Analytics: disabled on both projects, disabledAt 2026-09-24 04:50 UTC; cancellation effective 2026-09-26 07:00 UTC (task statement, PMD-11).
- Supabase branch price: USD 0.01344 per hour, no branch exists, nothing created (DEV_TEST_DEPLOY_FACTS.md s4).

## Findings for the governance track (no owner action needed now)

- AGENTS.md rule 9 names a Guardrail Presence Check CI job and a script `scripts/check-guardrail-presence.mjs`; neither exists on origin/main (finding F-A07-1, checked 2026-09-25). Rule 9 is therefore enforced by review only. Owner: VERIDIAN AI-OS track. PROJEXA does not create the job.

## Q3 AND Q4 AS DECIDED (register row BR-411)

| id | question | decision | file |
|---|---|---|---|
| Q3 | How much unattended authority may an email-triggered action have? (OQ-11) | PMD-05: email-triggered actions create proposals only. Reads and drafts run unattended. No write runs until a human confirms on the AI-prepared page or by reply-to-confirm. | PM_DECISIONS.md |
| Q4 | Who adds the inbound email DNS records? (OQ-02) | PMD-06: DNS stays owner-only. The PM prepared the exact record list in DNS_RESEND_INBOUND_RECORDS.md; the MX values are copied from Resend by the owner. | DNS_RESEND_INBOUND_RECORDS.md |
