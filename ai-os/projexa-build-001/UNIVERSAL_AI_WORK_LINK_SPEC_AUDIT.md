AUDIT: PASS

# UNIVERSAL AI WORK LINK SPEC - AUDIT RESULT (PROJEXA-BUILD-001, register rows BR-228 and BR-306)

Date: 2026-09-25.

| Step | Who | Result |
|---|---|---|
| Spec written | S01-spec (architect agent, opus) | UNIVERSAL_AI_WORK_LINK_SPEC.md, first draft |
| Adversarial audit | S02-audit (non-author agent, opus) | verdict PASS_WITH_FIXES, 23 defects (5 high, 10 medium, 8 low), file UNIVERSAL_AI_WORK_LINK_AUDIT.md |
| Fixes applied | LINK-FIX (opus) | 23 of 23 applied to the spec, the capability matrix, the harness and the register rows; harness self-test clean 24 of 24, all 18 deliberately broken rules detected |
| Independent re-verification | LINK-VERIFY (opus, neither the author nor the fixer) | 23 of 23 defect fixes present, 0 missing; register rows valid; self-test passes |

The verdict PASS means: every defect the non-author audit found is fixed and the fix was confirmed present by a second non-author check. It does not mean the design is proven in production: vendor behaviour marked UNVERIFIED in the spec (section 18) stays open until the owner acceptance tests OT-01..OT-18 run (register rows blocked_owner), and the audit's stricter variant for A-02 (an allow-list of non-money fields for ranks below manager) is recorded as an open follow-up (C-10 in the spec), accepted in PM_DECISIONS.md PMD-26.

## Defect-by-defect result

| Defect | Severity | Title | Fix present | Evidence (line references are to the fixed spec) |
|---|---|---|---|---|
| A-01 | HIGH | Tier-2 GET reads run `runDirectTask`, which inserts `submissions`, `pipeline_tasks`, `pill_usage` and `chain_h | yes | SPEC L226 (§4 row 13: POST /functions/{fn}, GET answers 405), L421-434 (§6.3 function read writes nothing; GET never runs a function), L699-710 (§9.9 executeRead: validate+executeTask, no submission/pipeline_tasks/pill/chain/gap/memory/intent row), L381-391 (/context counters.intents + counters.submissions), L672 (C-12), L98 (F-15), L147-148 (§2 'never happens'). No leftover 'computed reads'/'or Tier-2 read' text (grep 0). Harness ai_link_conformance.py L372-387 H13 compares both BUSINESS counters and prints 'counters.intents and counters.submissions unchanged' (L564), H14 L389-397, H21 L416-425. Selftest breaks get-submissions/get-function/read-writes DETECTED. |
| A-02 | HIGH | Dashboard redaction for rank < 3 misses `ledgerBudget` and `progressByBoqValuePct` (executor.ts:319-333 vs con | yes | SPEC L545 (§9.1 row: redactProjectDashboardFinancials nulls ledgerBudget and progressByBoqValuePct, CODE-B PR #1839), L99 (F-16), L670 (C-10), L951 (T10), AWL-S11 L1126 (checks construction-tools.ts). Read-only git: origin/feat/build-001-u01-redaction construction-tools.ts has 'ledgerBudget: null' x3, 'progressByBoqValuePct: null' x2; origin/main b2a4b20b has 0 (row pending until merge). Caveat: the audit's allow-list-for-rank<3 part is NOT applied; it is recorded as the open follow-up in C-10 L670 and the T10 residual L951, which PMD-26 accepts (A-02 fixed by PR #1839). |
| A-03 | HIGH | Level and function list are checked only at mint. A demoted user keeps level-1 writes, and executors do no rol | yes | SPEC L902-915 (§10.9 effective_level/effective_functions from live rank on every call; writes_enabled cap), L633 (§9.6 step 2 refuses with ROLE_CHANGED), L766 (§10.2 role read every call), L773, L955 (T14), AWL-H22 L1181 (LINK_D write 403). Harness H23 L512-525; selftest break 'demotion' DETECTED. |
| A-04 | HIGH | Admin/org/maker-installed connectors (ChatGPT workspace app, Claude org connector, Copilot Studio agent, M365  | yes | SPEC L293 (manual rule 8), L749 (warning sentence 'Use it only in an assistant that you alone use'), L961 (T20), L1237 (OT-18), L976-984 (§12 admin installs refused). Matrix: audit A-04 python test -> 0 unmarked admin_or_maker_setup cells; 15 of 16 admin cells shared_identity_risk=true/allowed_for_per_user_link=false; the 1 exempt cell (claude.ai Team/Enterprise L1: Owner only enables web search, link stays in the person's chat) carries an identity_note. identity_rule key present. AWL-S12 run here: PASS. Register BR-597 (OT-18) blocked_owner. |
| A-05 | HIGH | Setting `project_id`/`token_hash` NOT NULL on all `user_ai_links` rows breaks `GET /api/ai-link` (get-or-creat | yes | SPEC L815 (product column default 'veridian'), L828-829 (check constraint user_ai_links_projexa_shape: product='veridian' or project_id/token_hash/expires_at not null and token null), L830-835 (index split by product), L891 (no NOT NULL step on the whole table), L928-934 (§10.11 picker unchanged), L671 (C-11 product filter), L100 (F-17), L1300 (OD-13 option (a) per PMD-26). Tests AWL-D02 L1140 (constraint text matches the audit's ilike pattern), AWL-D03 L1141, AWL-S07 L1122. |
| A-06 | MEDIUM | The SQL layer (resolve, call log + guard, rate limit, draft/confirm, warning) is re-created beside DPDP's. D-1 | yes | Waiver option chosen per PMD-26: SPEC L1301 (row '/ OD-13b /' decided (b)), L1307 (EXC-DUP-1 table: resolve, call log+guard, log_call, intent, warning beside DPDP), L62 (D-14), L76, L112, L885. grep -c 'OD-13b' spec = 9 (audit test wants >=1). AWL-S18 PASS. |
| A-07 | MEDIUM | Option A is the automatic fallback, against Addendum A1. It sends the plaintext token to Vercel logs. `/ai-con | yes | SPEC L57 (D-09 Option A not authorised), L680-697 (§9.8 option B only, A needs quoted owner waiver of A1, token-in-Vercel-logs stated), L622 (§9.5 ai-confirm.html static on OD-3 host), L943 (T2 no Vercel hop), L1275-1277 (cost: Option A row, Vercel build minutes for C-8 and C-1..C-5/C-10/C-11), L1289 (OD-2 (B) only). Tests AWL-S15 L1130 (no 'api/mcp/' in link Edge code), AWL-H15 L1174 (static pages, no x-vercel-id). |
| A-08 | MEDIUM | 15 register rows need `psql`, which is absent on this laptop. | yes | Equivalent runner used instead of sqlq.mjs: SPEC L1096-1097 (node scripts/verify/sql-assert.mjs --equals, PR #1838; psql not used). AWL_BOOLEAN_REGISTER.csv: 0 rows contain 'psql'. Read-only git: origin/main:scripts/verify/sql-assert.mjs exists (4,002 bytes, --project/--equals flags present). |
| A-09 | MEDIUM | Record filters and sort order are unspecified, so they can recover redacted money columns. | yes | SPEC L456-461 (§6.6 per-kind filter/sort allow-list, unknown filter 400, money column filter/sort below rank 3 -> 400 'This field is hidden for your role'), L258 (§4.3), L291 (manual rule 6), L223 (§4 row 10). AWL-H23 L1182 (amount_gt and sort=rate -> 400). Harness H18 L492-496; selftest break 'money-filter' DETECTED. |
| A-10 | MEDIUM | The default idempotency key swallows legitimate repeat entries, and a failed intent locks its key for good. | yes | SPEC L589 (default key sha256 of {function, params, utc_date}), L590 (partial unique index on live/done statuses; failed/refused/expired free the key), L858-859 (DDL ai_work_link_intent_idem_live ... where status in (...)), L946 (T5). AWL-D10 L1148. |
| A-11 | MEDIUM | Same-project checks for `rosterId`/`issueId` run only at the Edge (`/check`). The executor path is UNVERIFIED. | yes | SPEC L712-722 (§9.10 executor-side same-project check for rosterId/issueId/boqLineItemId/boqId, 403 NOT_IN_PROJECT; Edge /check is early feedback only), L449 (§6.4), L634 (§9.6 step 3), L673 (C-13), L101 (F-18), L950 (T9). AWL-S13 L1128 (bun test --isolate executor-project-scope.test.ts, mutation stated). |
| A-12 | LOW | Call log has no retention; the IP source header is unspecified. | yes | SPEC L837-845 (call log range-partitioned by month), L917-926 (§10.10 pg_cron job ai-work-link-call-retention daily 04:10 UTC, 90 days, partition drop so no guard exemption needed), L789-794 (§10.5 one named header x-forwarded-for at a fixed right position, else one shared bucket; spike S-3), L787 (over-limit unknown-token calls refused before a row is written), L676 (C-16), L964 (T23). Tests AWL-D15 L1153, AWL-D16 L1154, AWL-H24 L1183. |
| A-13 | LOW | The W-C 24 h `issued_at` check is unenforceable (unsigned proposal). | yes | 'Delete the claim' option chosen: SPEC L592 (W-C proposals carry no age field and no signature; reason stated), L946 (T5 residual). Old text 'refuse any older than 24 h' absent (grep 0; AWL-S18 negative check passes). |
| A-14 | MEDIUM | Browser-driving agents can press Confirm on the Level-1 inbox and confirm page (no sign-in), so W-C/W-D give n | yes | SPEC L580 (§9.2 W-C and W-D are W-A for risk purposes), L948 (T7 browser-driving agent can press Confirm), L958 (T17), L53 (D-05), L978/L993 (§12). Matrix: 'Claude in Chrome (extension)' L6_paste_back v='one_time_user_setup' (audit A-14 test passes; AWL-S12 PASS). |
| A-15 | MEDIUM | Email-scanner detonation (headless Chrome, R08) can run the inbox page's JavaScript and click. | yes | SPEC L608-613 (§9.4 typed 4-character code, input id="confirm-code", Confirm disabled until typed, no POST to /actions or /drafts on page load), L624 (§9.5 same code on ai-confirm.html), L576-578 (W-B/W-C/W-D steps include typing the code), L962 (T21). AWL-H25 L1184 (grep -c id="confirm-code" -ge 1 on both pages). |
| A-16 | MEDIUM | AI-written text stored through the link reaches the internal L1 and other users' AIs unfenced. | yes | SPEC L724-736 (§9.11 2,000-character cap with 422 TEXT_TOO_LONG, control chars removed, backtick runs replaced, captureTaskResultMemory source 'ai_link', internal prompt builders fence it as data), L351 (§5.4), L674 (C-14), L102 (F-19), L963 (T22). AWL-S14 L1129 includes the audit's git grep for via === 'ai_link' in src/lib/pipeline. |
| A-17 | LOW | Exfiltration by a URL-composing AI is not in T7 or the warning sentence. | yes | SPEC L749 (warning sentence: 'an assistant that follows instructions in the data could send this information elsewhere'), L294 (manual rule 9), L948 (T7 'Exfiltration by browsing'). Audit test grep -c 'send this information elsewhere' spec = 3. |
| A-18 | LOW | F-12 says the executor enum is `software\ | yes | SPEC L95 (F-12: enum compliance.pipeline_task_executor allows software, ai, person; corrected by LINK-FIX), L661 (C-1). Audit test grep -c 'software, ai, person' = 4. |
| A-19 | LOW | Vendor fetch caches outlive revocation (R03 §1.5). Browser history keeps token URLs. | yes | SPEC L942 (T1 residual: Claude web fetch results cached, revoked page 'may be served from cache'; Gemini URL context cache), L943 (T2 residual: browser history keeps card.md and inbox URL; fragment after replaceState UNVERIFIED), L771 (§10.3), U-17 L1334, U-18 L1335. Audit test grep -c 'served from cache' = 2. |
| A-20 | LOW | Test hygiene: D13 placeholder and count output, D08 semantics, H15 trivially true, S04 regex, H18 vs "never 40 | yes | AWL_BOOLEAN_REGISTER.csv: 62 rows, column awl_phase present, no 'NNNN' (audit A-20 python test PASS). D13 L1151 runs bash scripts/verify/awl-rollback.sh with grep -qx ... && echo PASS; D08 L1146 counts only level1_outcome in ('resolved','error'); H15 L1174 checks the static pages for x-vercel-id; S04 L1119 regex paginate(<T>)?; SPEC L203-204 (§3.6 'Never 401 on a link-token route', app routes 401); SPEC L1091-1093 explains phase vs awl_phase. |
| A-21 | LOW | MCP `search`/`fetch` output has no stated redaction and no token-free `url` rule. | yes | SPEC L491 (§7.2 search/fetch: text built from the same redacted row set, url is a token-free PROJEXA app deep link), L60 (D-12). Harness H22 L427-449 prints 'search/fetch redacted and token-free' (L569); AWL-H27 L1186; selftest breaks search-leak and search-token DETECTED. |
| A-22 | MEDIUM | Claude web fetch reads "text, HTML, PDF only" (R03 §1.5; `application/json` UNVERIFIED, T-LIVE-3). The manifes | yes | SPEC L271-276 (§4.4 GET with no Accept header gets text/markdown on root, /context, /records, /functions, /propose, /intents, /history), L333 (§5.2 manifest URLs answer Markdown), L369 (§6.1), L49 (D-01). AWL-H26 L1185 (the audit's exact command). Harness H24 L451-462; selftest break json-default DETECTED. |
| A-23 | LOW | FETCHED-S01 vendor facts (V-1…V-5) have no archived excerpt, and several R-file labels are dropped or overstat | yes | AILINK_S01_fetches.md exists; grep -c 'modelcontextprotocol.io/specification/2026-07-28' = 4 (audit test >=1). Pins facts by line number + SHA-256 prefix instead of quoted lines (copyright rule); ailink_s01_verify.py re-checks (AWL-S17 L1132 recorded 26/26). SPEC L38-39 (label table incl. FETCHED-3P, INFERENCE, design choice), L54/L340 (paste-card bound = design choice, V-6), L583 (Safe Links FETCHED-3P, V-7), L203 (401 + WWW-Authenticate, V-8), L89 (F-6 Vercel probe UNVERIFIED). Matrix: 364 cells, 0 without label, 0 without confidence, 0 surface-confidence mismatches vs min-of-cells rule, 0 cells still sourced bare 'S01'; counts 25 UNVERIFIED / 23 PARTLY / 4 VERIFIED_FETCHED match SPEC L63/ |

Source of these results: the LINK-VERIFY agent result in the BUILD-001 link-fix workflow (run wf_58aa2dbb-da5), which read the fixed files, quoted line numbers, ran `python ai_link_selftest.py` and validated the register rows.
