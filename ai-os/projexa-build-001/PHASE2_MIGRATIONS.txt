# PROJEXA-BUILD-001 phase 2 (U-17, PMD-10): the migrations that must carry rollback evidence.
#
# One drizzle base name per line: the file name in drizzle/ without .sql (for example 0613_<short_description>).
# Lines starting with # and blank lines are ignored; a # after a name starts a comment.
#
# For every name listed here:
#   scripts/verify/rollback-rehearsals.sh (BR-206) requires drizzle/<name>.sql, drizzle/down/<name>.down.sql and a
#     PASS_ROLLED_BACK row in ai-os/projexa-build-001/ROLLBACK_REHEARSALS.md whose forward_sha256 matches the file today;
#   scripts/verify/rollback-replay.sh (BR-207) additionally requires scripts/verify/fixtures/<name>.base.sql and replays
#     forward then down in PGlite.
# Add the name in the same PR as the migration. Both checks pass with an empty list; a separate register row requires the
# list to be non-empty once phase 2 has written its first migration.
#
# Listed so far (2026-09-25): U-18a, the link project scope columns.
0613_build001_link_project_scope
# U-18b, resolve a project link by the hash of its token.
0614_build001_link_resolve_by_hash
# U-21, the PROJEXA timer (exchange-rate refresh off Vercel).
0615_build001_projexa_timer
# U-22, the evidence columns on platform.sumeet_requirements (phase 3). Its data file 0617 is not listed: it changes rows,
# not the schema, so a schema-hash rehearsal would read "forward changed nothing"; its proof is the PGlite test.
0616_build001_requirement_evidence
# U-25, the PROJEXA identity gateway (Edge Function projexa-read: switch table + projexa_read_* wrappers).
0618_build001_projexa_gateway
# U-32 part A, the surface column of compliance.audit_logs (nullable, four-key check).
0619_build001_audit_surface
# U-31, the inbound email attachments table (the Resend webhook stores each attachment of a resolved message).
0620_build001_inbound_email_attachments
