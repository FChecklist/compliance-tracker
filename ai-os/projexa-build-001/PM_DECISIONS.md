# PM DECISIONS - PROJEXA-BUILD-001 (Claude Code, project manager)

Date: 2026-09-25. Authority: the owner said in chat on 2026-09-25 "except for recharge of vercel you can take any and all decision" and "make final document and start work, start development on local". Amendment 001 still keeps three actions owner-only: Vercel recharges/payments, anything that increases spend, DNS. Where a decision below touches spend or DNS the PM chose the option that spends nothing and changes no DNS, and says so.

Every decision has: id, decision, evidence, reversible (yes/no + until when), owner override (what to say in chat).

## Identity and browser-side reads
**PMD-01 (Q-ID1) Identity bridge = Edge Function gateway, spike first.**
Decision: browser-side reads of verdian-ai data go through a Supabase Edge Function on verdian-ai that (1) verifies the PROJEXA session JWT (ES256) against the projexa project's public key set, (2) resolves the person through compliance.users.auth_user_id, (3) queries as role app_runtime with the tenant setting applied, (4) returns keyset-paginated JSON. Rejected: (a) move the 114 PROJEXA users into verdian-ai (touches every login; 22 of 114 are not linked to compliance.users); PostgREST-direct with new RLS (role authenticated has no SELECT grant on construction tables, the tenant predicate reads the server-set setting app.current_org_id which PostgREST never sets, no function in the database references auth.uid()). Fallback (c): keep reads server-side and only move rendering to the browser, used if the key-set spike (register item U-25a) fails.
Evidence: LIVE_FACTS_D09_D10_D11_RLS.md section (d); IDENTITY_AND_EDGE_FINDINGS.md; A09 verifier claims, skeptic-confirmed.
Reversible: yes, until Phase 4 starts. Owner override: "use option (a)" or "use option (c)".

## AI provider gate and the external AI link
**PMD-02 (Q-GATE, D1, D2, D3) Keep the gate; external AI is Level 1 with zero server model calls.**
Decision: (i) no switch to a metered provider (openrouter) - that is a spend increase, owner-only; (ii) the external AI link path (compliance-tracker src/app/api/mcp/[token]/route.ts) makes zero server-side model calls and is not subject to the provider gate, matching the owner directive of 2026-09-25 "we will not use our AI if the user has pasted the AI Work link"; on a Level 0 miss it returns the candidate functions and missing parameters to the external AI; (iii) internal chat for non-owner users stays Level 0 only, and the refusal text carries the records instead of a bare sentence; (iv) the gate identity becomes the acting person, not the org API key (Phase 2, with per-request actor).
Evidence: GATE_2_8_FINDINGS.md sections 0, 1, 4; adapter.ts:120-141; adapter.test.ts:28,65; compliance.submissions 6 refusals of 7 measured.
Reversible: yes. Owner override: "enable openrouter for internal Level 1" (owner-only spend decision; not needed for anything else in this plan).

**PMD-03 (Q1) Build order.** First extend the existing AI-Link MCP path (redaction leak, de-advertise 13 tools, zero-server-L1, project scoping); then document extraction by EXTENDING the existing src/lib/services/document-extraction-service.ts (it exists, 552 lines, no xlsx branch and no BOQ/project schema); then the remaining registry entries.
**PMD-04 (Q2) Redaction leak first.** Fix all four role-dropping points in Phase 1: mcp/[token]/route.ts:58,68; executor.ts:372; assistant/route.ts:56,77 with API-key auth; construction-tools.ts:95 fail-open default. Evidence: MERGE_MAP.md section 5.

## Scope, credentials, email
**PMD-05 (Q3) Email-triggered actions create proposals only.** Reads and drafts run unattended. No write executes until a human confirms on the AI-prepared page or by reply-to-confirm. Reversible: yes.
**PMD-06 (Q4) DNS stays owner-only.** The PM prepares the exact Resend inbound MX/TXT record list; surface 4 (email) cells are status blocked_owner until the owner applies DNS. No DNS change is made by the PM.
**PMD-07 (Q5) The sanctioned credential to give an AI is the project-and-user scoped link token.** Org API keys are service-to-service credentials (the PROJEXA proxy uses one per org) and are never handed to an AI.
**PMD-08 (Q6) Split.** platform.user_ai_links.project_id is NOT NULL for links minted after the migration; the 2 legacy org-wide links (both demo/test orgs, last used 2026-08-29) are revoked at migration time. compliance.api_keys.project_id is NULLABLE with a key_kind check (org_service | project_ai); legacy org keys stay because the PROJEXA proxy needs them. Evidence: A05, A11, A03 (5 of 7 measured submission actors are api_keys ids).

## Tests reworded because the original cannot run or can be gamed
**PMD-09 Amended tests (each has an AMENDMENT_LOG row).** 2.8 becomes a provider-parameterised matrix (passes today, characterises the licence gate) plus a zero-server-L1 test for the external link. 1.6 becomes a required-check assertion (the job exists). 1.7 splits: placement CSV in Phase 1, cron count in Phase 5 (E-14). 1.5/2.7 move to the DPDP track with a narrow PROJEXA guard (platform and compliance schemas: zero SECURITY DEFINER functions executable by anon or authenticated; today 0). 2.2/2.3 become nullable project_id plus per-request actor. D-09 gets a stricter test that also counts actor_role='api_key' rows. D-11 becomes keyset pagination tested on fixtures that match live data (largest real BOQ = 153 lines; the 10,907 figure is 5,924 test revisions). Tests 3.1, 3.6, 4.2 query columns that do not exist today (verify_command, evidence_ref, surface), so the columns are added first. E-06/E-07 are rewritten for PMD-01. E-08 scans the built browser bundle, not src/ comments. 4.5: surface 1 has a one-page project view today; what is missing is AI-prepared content with an approve action.
**PMD-10 No paid Supabase branch.** A branch costs $0.01344 per hour (spend increase, owner-only). Rollback is proven with the existing convention (drizzle/down/*.down.sql, docs/ROLLBACK_RUNBOOK.md) plus an always-aborted transaction that compares schema hashes before and after, plus the PGlite replay. Reversible: yes; owner override: "approve a branch".

## Vercel, cost, cron
**PMD-11 Vercel stays locked.** No deploy, no unpause, no setting or env change, no DNS. Vercel side of Speed Insights is disabled (both projects, disabledAt 2026-09-24 04:50 UTC; cancellation effective 2026-09-26 07:00 UTC). The first billing day that can show 0.0000 is the day starting 2026-09-26 07:00 UTC, so test 1.9 is date-gated; if charges continue after that day the owner checks Team Billing for a team-level add-on (A08 finding). Releases stay owner-approved manual deploys (ignoreCommand exit 0); draft PR #1808 stays unmerged.
**PMD-12 One cron first.** Phase 2 ports exchange-rate-refresh (HTTP fetch plus upsert, no LLM, no Node-only package) to pg_cron -> pg_net -> Edge Function on the verdian-ai project, where pg_cron and pg_net already run. The other prepared jobs are enabled one per PR, each with a note that it re-enables writes silent since about 2026-08-23.
**PMD-13 The $20 measure.** Measured on gross charges unless the owner says otherwise (A08: Pro fee $0.645/day and Speed Insights Plus $0.645/day; Build CPU only $4.33 total in 4 days). Cumulative $29.24 since 2026-08-26 is reported, not hidden.

## Process
**PMD-14 RAM policy.** Warn at 1.5 GB free, hard stop for new bun or agent bursts at 1.0 GB, one bun process at a time, typecheck and build only in CI, at most 12 concurrent agents, check RAM after every phase. Reason: announced limits that get crossed are not limits (Claude chat, 2026-09-25).
**PMD-15 The owner instruction "start only after Claude chat audits and you relay approval" was a scoped exception to Amendment 001. End condition met 2026-09-25 when the owner wrote "check this and your analysis. make final document and start work". Amendment 001 autonomy applies again.**
**PMD-16 One canonical AI link (surface 3).** compliance-tracker platform.user_ai_links with its MCP/REST route is canonical because it reaches the EXECUTORS registry. PROJEXA org_ai_link snapshot route (projexa src/app/api/ai/[token]/route.ts) becomes a read-only projection that delegates to it. The universal link spec (UNIVERSAL_AI_WORK_LINK_SPEC.md) starts implementation after project scoping (U-18).
**PMD-17 The 31 exceptions get ids EXC-ITEM-01..EXC-ITEM-31.** Three META items (29-31) count in the 111 and are verified by register-level checks; platform.sumeet_gap (31 rows) is a different list and is never used as the source.
> [removed from the public copy: see the private KT folder]
**PMD-19 Exceptions surface convergence.** The Exceptions record type converges on the underlying flagged record (for example the overdue RFI); no new table.
**PMD-20 Phase 4 record type = BOQ line item** (compliance.construction_boq_line_items). Progress entries are the separate "daily work progress" type.
**PMD-21 Where the plan lives.** Repo: ai-os/projexa-build-001/ (CI reads it). Drive: KT folder PROJEXA_BUILD-001_PLAN_2026-09-25 (reviewers read it). The repo copy wins if they differ.

## Added during development (2026-09-25)
**PMD-22 SQL assertions and the database role.** The runner scripts/verify/sql-assert.mjs (PR #1838) opens a READ ONLY transaction and refuses anything but one SELECT. The connection string in .env.local connects as role app_runtime, which is subject to row-level security: catalog assertions (pg_policies, pg_proc, information_schema, pg_extension) are exact under it, but a count over tenant data (audit_logs, submissions, boq tables) can read 0 for the wrong reason. Rule: register rows about tenant data name the role they need; the PM runs them through the Supabase MCP (role postgres) and records a dated SQL result as evidence; other people run them with a VERIFY_DATABASE_URL for a role that can see the rows. No new database role is created by PROJEXA work (that would be a schema and permission change owned by Phase 2).
**PMD-23 U-01 is closed in three PRs, not one.** PR #1839 (core: shared rule, four sites, dashboard list), a follow-up on the same branch (tasks and submissions routes thread the acting person's role; task-execution-engine and fde-service callers; the three construction tools that had no financial gate), and a PROJEXA-repo PR that makes the assistant proxy send the acting person. Until the PROJEXA PR merges, PROJEXA assistant users see redacted figures (fail closed); Vercel is locked, nothing is live. A caller that passes no role now sees redacted figures even when the person is a manager: this is the intended behaviour, and the register rows record it.
**PMD-24 Universal AI work link: the twelve options of UNIVERSAL_AI_WORK_LINK_SPEC.md section 17 are decided as the spec recommends.** OD-1 the link host is the direct supabase.co Edge Function URL only (no DNS change; a vanity host later is an owner DNS action). OD-2 write execution: run spike S-1 first; bundle the executors into an Edge Function (zero Vercel invocations) if all four spike checks pass, otherwise reuse the compliance-tracker route on Vercel, which only runs while Vercel is live. OD-3 the static inbox/confirm page is a Cloudflare Pages project on a *.pages.dev hostname (free tier, no DNS), created at Phase 4 entry with the existing owner-issued Cloudflare token; if that token cannot create a project, the page waits (status blocked_owner) and nothing else is affected. OD-4 default link level 0 (read). OD-5 the write-level table in spec section 9.1 is accepted as written. OD-6 the shared helper module moves to supabase/functions/_shared/ai-link/ and DPDP imports it, but only after a claim for that file is registered in ACTIVE-CLAIMS and DPDP behaviour is unchanged (tested); if the DPDP track objects, PROJEXA keeps a copy and records the duplication as a known exception. OD-7 the 2 legacy org-wide links are revoked (same as PMD-08). OD-8 maximum expiry 30 days. OD-9 hide_personal defaults on. OD-10 no extra level-1 safety now; a 24 hour undo for level-1 creates is the next increment. OD-11 the owner acceptance runs OT-01..OT-17 (real ChatGPT, Gemini, Claude, DeepSeek, Z.ai, Copilot, Microsoft AI, email AI, laptop AI) are register rows with status blocked_owner, scheduled before the endpoint phase closes. OD-12 tokens in query strings are refused; the header form covers Copilot Studio. The spec also answers work-order Q5: one link token serves REST, MCP and OpenAPI; no separate REST key is issued to an AI.
**PMD-25 Public repositories, private evidence.** Both GitHub repositories are PUBLIC (owner decision, closed), and an earlier credential-exposure report had to be removed from the compliance-tracker repository. So the plan package exists twice. The private copy (KT Drive folder PROJEXA_BUILD-001_PLAN_2026-09-25) holds everything, including the raw evidence folder (live database facts, gate analysis, identity and edge findings, AI vendor research). The repository copy (ai-os/projexa-build-001) holds the plan documents, the register and the scripts, WITHOUT the evidence folder, and every sentence, table cell or JSON/CSV field that names an operational secret or an attack surface (edge function credentials, service keys, connection strings, token-shaped strings) is replaced by a neutral marker by scripts/assemble_pkg.py, which then greps the repository copy for the deny-list and fails on any hit. Both copies must pass lint_plan.py. Fixed defects are still described in the public copy once their fix has merged.
**PMD-26 Universal AI work link: outcomes of the adversarial audit (verdict PASS_WITH_FIXES, 23 defects A-01..A-23, all applied).** Decisions on the points the audit left open: (A-05, OD-13) the live VERIDIAN chat link keeps working untouched: platform.user_ai_links gains a product column (existing rows = veridian) and a check constraint (a projexa row requires project_id and token_hash and a NULL plaintext; a veridian row is unchanged); no change to the VERIDIAN picker. (A-06, OD-13b) the database-side helpers copied from DPDP (token lookup, call log and guard trigger, rate limit, warning) are a recorded exception EXC-DUP-1 for this increment, because restructuring a live DPDP product is out of PROJEXA scope (PMD-18); the TypeScript/Deno helpers are shared only after the DPDP track agrees through ACTIVE-CLAIMS (PMD-24 OD-6); unifying the database side is a follow-up item once that agreement exists. (A-07) writes on Vercel (Option A) are NOT authorised and Addendum A1 is not waived: level-1 writes execute only through the Edge bundle (Option B) after spike S-1 passes; until then every link is level 0 (reads, dry runs, drafts); reads never route through Vercel; the confirm page is static on the OD-3 host. (A-01) every function read is POST-only and uses a read-only executor mode that writes no submission, task, pill or intent row; a GET changes nothing, including counters. (A-03) the effective level and function list are computed on every call from the person's live role (a demoted person's link loses write power at once). (A-04) a link may be installed only in a connector its owner alone uses; shared workspace connectors are refused by the manual and marked shared_identity_risk in the matrix; owner acceptance test OT-18. (A-15) a confirm link that travelled by email needs a typed 4-character code shown on the page; no POST on page load. (A-02) the dashboard money leak is fixed by compliance-tracker PR #1839 (shared redaction list includes ledgerBudget and progressByBoqValuePct).
> [removed from the public copy: see the private KT folder]

**PMD-28 Where a project-scoped link may resolve, and what U-21 removed from PROJEXA (decided 2026-09-25 while building U-18b and U-21).** (1) Register row BR-210 tests a project link on the compliance-tracker route `/api/mcp/[token]`, which runs on Vercel, while the link spec (10.11) and PMD-26 A-07 say link traffic stays off Vercel. Decision: the shared resolver (`platform.rpc_resolve_ai_link_scoped`, migration 0614) can resolve a `projexa` link, and project scope is enforced wherever a link resolves, as defence in depth. Nothing mints a `projexa` link until U-46, and U-46 mints links that point at the Edge Function, so no live traffic reaches Vercel by this path. PMD-26 A-07 is kept by what U-46 mints and by a Phase 4 check that no minted link URL points at Vercel, not by refusing the row inside the route. (2) `project_ai` API keys are held to their project only at the three pipeline routes today; all 36 keys are `org_service`. Rule for later: before a `project_ai` key is minted, `requireAuthOrApiKey` must refuse it by default or every route that accepts a key must be audited. (3) U-21 removed the PROJEXA email-digest Vercel cron, the GitHub poll workflow and the cadence route (projexa PR 320) and kept the digest logic; the schedule returns on pg_cron in U-40.
> [removed from the public copy: see the private KT folder]
**PMD-30 DONE rows outside code and database are evidenced by a dated check (BR-311, decided 2026-09-25 after applying U-22 migrations 0616 and 0617).** Four DONE rows of the Sumeet register (R-A1 a credential rotation, R-A2 a one-time hygiene action on a clone, R-A3 and R-A7 repository visibility decisions) concern things that live outside the code and the database, so no commit, PR number or SQL result can evidence them, and each row's own notes record a dated live check. Marking them not done would misstate that the work happened; leaving them without evidence would break the register rule. Decision: BR-311 also accepts `CHECK yyyy-mm-dd: what was read -> value`, only for rows with built = 'n/a', so no code row can use the new form (amendment log AM-118). The four evidence_ref cells were filled from the dates and results in the rows' own notes; no status text was changed (fills only NULL cells, so undoing it is setting the four cells back to NULL). Any open caveat stays in the rows' own notes.
**PMD-31 Requirements whose proof lives in the projexa repository are checked by presence (BR-308, decided 2026-09-25).** R-15, R-31 (the BOQ view spec) and R-92 (two component tests) are built in PROJEXA and have no check that can run from this repository. Decision: `scripts/verify/projexa-proof-present.sh` reads the named file from projexa main through the GitHub API and requires that it still names the requirement, the same strength as the existing R-B1 check (git grep for a spec title): it proves the spec or test exists and still carries the requirement id, not that it ran. A command unrelated to these rows would have made BR-308 read 0 without checking anything. The three verify_command cells were filled where NULL (amendment log AM-119); the limit is written into the script header and the U-22 notes. Running the PROJEXA specs stays the job of the Env-1 CI job, which is blocked by BR-315 (owner).
**PMD-32 The identity gateway is live with its switch OFF, and the switch stays off until three conditions hold (U-25, decided 2026-09-25).** Migration 0618 and the Edge Function projexa-read are applied and deployed (rehearsal PASS_ROLLED_BACK, live hash equals the rehearsed hash, no grant to anon, authenticated or PUBLIC, smoke calls without a valid token answer 401). The spike decision is continue_gateway (BR-324). Decisions: (1) The gateway returns the same line-item fields the REST BOQ route returns to any organisation member and none of the project-side cost fields (qtyProject, rateProject and the others in cost-visibility-service.ts PROJECT_SIDE_COST_FIELDS), so it opens nothing the REST route keeps closed; if the cost-visibility rules ever widen the redaction to other fields, the SQL and the handler list change in the same PR (a test holds the handler list to be a superset of the service list). (2) The email fallback (matching an unlinked PROJEXA account to a VERIDIAN user by email) stays off: linking the 22 unlinked accounts is owner question OQ-15, and if it is ever switched on it needs a verified-email claim check first. (3) The switch is turned on by its own reviewed migration only after: BR-322 has passed with a real PROJEXA session (owner supplies the token), the Phase 4 BOQ read path calls the gateway, and the 8-second PROJEXA call limit no longer applies to that path. Until then a verified caller gets 503 and every other caller 401. (4) `jose` is not a direct dependency of the repository; the test pins the version used by the function (npm:jose@6.2.10) against the lockfile copy, which is enough until the function moves.
**PMD-33 A personal AI link whose person is no longer an active user cannot act (found by U-39, decided 2026-09-25).** A link acts as exactly one person, but the resolver checks only the link's own status and expiry. Before this decision the link route redacted the money figures for such a link and still ran its tools. Decision: option A, the smallest fail-closed change: every tools/call on the link route answers a plain refusal before anything runs when the person has no active user row in the organisation (initialize, tools/list and ping are unchanged); the fifth test of the U-01 (a) group now proves the refusal (amendment log AM-122). Not chosen: passing the person into every write executor (touches every pipeline caller) and revoking links when a user is deactivated (does not cover a person who moved organisation). Follow-up for U-46: every endpoint of the Universal AI work link on Supabase Edge must apply the same rule, and the resolver function stays unchanged.
**PMD-34 The registry entries of U-28 carry no feature flag (decided 2026-09-25).** The plan's deploy notes named two flags for the new BOQ registry entries. `create_boq` is an additive write function that already goes through the propose-then-confirm path (PMD-05), and the revision fix makes an entry do what its parameters say instead of silently dropping them; nothing is deployed while Vercel is locked, so undoing either is a normal revert of one PR. A flag would add a second code path that needs its own tests. The flag exists only where a response shape that PROJEXA reads changes (U-27, BUILD001_BOQ_KEYSET_PAGINATION). A BOQ write or revision through a caller that names no person is refused (the timesheet write already behaves this way) instead of recording an API key id as the creator.
**PMD-35 The person of a session or link call is the audit actor (decided 2026-09-25, built in U-32).** The pipeline records writes under the task's `actorUserId`, which only the API-key path with a named person sets today. Callers whose `userId` is already a verified person (a signed-in session, a personal link) must pass it as `actorUserId` too, so a BOQ written from the AI link, the assistant or the approval page names its person. This is part of the four-surface unit (U-32), not a separate change.
**PMD-36 The keyset list returns one revision chain (U-27, decided 2026-09-25).** With BUILD001_BOQ_KEYSET_PAGINATION on, `GET /api/v1/construction/boq?projectId=` returns the headers of the selected revision chain and one page of that revision's line items with `nextCursor`; other chains are selected with `?revision=`. Reason: on the live project the headers alone are 4.3 MB, so "every header" and "under 1 MB" cannot both hold. The projexa side (U-33) must follow `nextCursor` and `?revision=` before the flag is turned on. The register text of AM-088 (project-wide walk) is superseded by BR-404 as built.
**PMD-37 Inbound email attachments are stored in the database, capped at 10 MB (U-31, decided 2026-09-25).** One table, `compliance.inbound_email_attachments`, with the bytes in a column and a CHECK at 10 MB, because the table that receives mail holds 0 rows today, a storage bucket would add a second shared object with its own policies, and a BOQ workbook is small. If inbound volume grows, moving the bytes to storage is a later migration.
**PMD-38 A proposal prepared by the system is confirmed from its stored chain (found by U-31, decided 2026-09-25, built in U-29).** `confirmSubmission()` re-derives a proposal from the words that were stored with it, and on the live database `compliance.phrase_map` holds no phrase for `create_boq`, so a BOQ proposal made from an email (or by any system step, not by a person typing words) would not resolve without a model. Decision: the surface 1 approval page confirms a system-prepared proposal with the parameters stored in its `selectedChain` (merged with what the person adds, such as a missing title), never by re-deriving from the words, and never by promoting a phrase per organisation. This keeps the confirmation deterministic and free of any model call, and it is the contract the approval route and the paste-back route (U-47) are built and tested against.
**PMD-39 Vercel is recharged last (owner, 2026-09-25).** The owner decided that Vercel credits are recharged only after all work on the local machine, in Supabase and in git is complete, merged, committed and green, so that no Vercel credit is used before then. Consequences: (1) every row that needs a running Vercel app (BR-225, BR-314, BR-416, BR-417, BR-418, BR-422, BR-423, BR-504, BR-510, BR-517, BR-521) stays blocked_owner until go-live; (2) the DNS records of BR-412 are added on go-live day, because the app that receives the inbound mail webhook must be running when the records go live (the domains use Vercel DNS, so no registrar login is involved, and only `inbound.` subdomains are added, never a root record); (3) go-live follows R72_DEPLOY_RITUAL.md and the 20 USD a month cap of PROJEXA-COST-001; (4) the PM never recharges or pays. Evidence: the owner's chat instruction of 2026-09-25. Reversible: yes. Owner override: "recharge earlier".
**PMD-40 Two ways an AI is billed, and how each is tested (owner, 2026-09-25).** (1) External AI link: the user copies the AI work link into an AI they already use; the billing of that AI is between the user and that vendor, and the server makes zero model calls on that traffic (PMD-02 part ii is unchanged and BR-586 checks it). (2) Internal AI, the AI the product provides: the cost is ours and is billed to the user separately, so every internal model call must record its user, organisation, model, tokens and estimated cost in the usage ledger; an internal call with no ledger row is a defect. (3) Before go-live, all internal Level 1 testing uses Claude Code as the internal Level 1 AI and no metered provider, with the one exception in PMD-43. After go-live the internal provider is OpenRouter; the system stays model-agnostic, so a change of model is a configuration change. PMD-02 part i ("no switch to a metered provider") therefore holds until go-live and ends there by the owner's decision. Reversible: yes.
**PMD-41 Billing claims through an AI link are proposals that a person approves (owner, 2026-09-25, "as explained").** An AI link may propose a billing claim (a row of compliance.construction_progress_claims). The claim is written only when a signed-in person with the billing role approves it, through the link's own confirm step or the approval page, and the audit row names that person. The registry entries that read billing claims stay read-only (BR-512). The propose-then-confirm entry create_billing_claim is added with U-42. BR-522 returns to pending, and its s3 cell is proven through the link's confirm step. An autonomous AI write of a billing claim stays refused. This is the decision that register_part_5_notes.md item 4 allowed the PM to record under the owner's delegation.
**PMD-42 The AI link and its acceptance are universal (owner, 2026-09-25).** The link is one mechanism for all users and all projects: nothing in its code, its database objects or its tests may name a specific user, project or organisation. The conformance harness (BR-490) runs with two projects, member and lead roles and a revoked link, and the owner acceptance rows (BR-588 to BR-597, BR-524) record the mechanism working, not one project's data. Vendor acceptance stays with the owner's own accounts (OD-11) and runs after the link exists.
**PMD-43 Extraction model for BR-509: Groq floor tier, hard cap 1 USD, then analyse (owner, 2026-09-25).** The projexa-document-extract Edge Function may call the platform floor-tier model on Groq (AGENTS.md rule 8 names GPT-OSS-120B via Groq) for extraction testing, with a hard cap of 1.00 USD of estimated spend. Built as: each model call writes its tokens and estimated cost to the usage ledger, and the handler refuses with budget_exhausted once the recorded total reaches 1.00 USD (BR-526). When the cap is reached the PM reports calls, tokens and cost per file and stops until the owner extends it. The provider key is set as an Edge Function secret by the owner with one command; the PM writes no credential. BR-509 stays blocked_owner until that secret is set. Reversible: yes.
**PMD-44 The ten prepared cron jobs: the four open decisions, taken by the PM under the owner's delegation (2026-09-26).** (1) Notification dedup stays on: a recipient with an unread notification of the same type and metadata kind gets no second one; task-nudge-digest is kept, and the two functions check each other by task id so that one owner gets one notice of one task (PMD-47). (2) The-firm recurring engagements: a month-end date clamps (31 January plus one month is 28 February), the Postgres behaviour, not the old JavaScript overflow. (3) Audit cadence: the window is 24 hours on a daily job (the TypeScript scanned 3 hours, which was a bug). (4) Report schedules: hourly at minute 5 with one run per slot, not the old daily 08:45. (5) The ten jobs are created active and named cost001-<name>; every function keeps its p_dry_run parameter and the first live call of each is a dry run; the projexa-scheduler-bridge job is created inactive and switched on only at go-live (PMD-39). (6) Order of landing: the ten pg_cron jobs go live and are verified first, and only then does the vercel.json cron cut merge. The crr-catchup-worker line of the GitHub runner stays commented until the owner adds the CRON_SECRET repository secret (BR-315) and approves it, so it is a go-live checklist item, as are the other GitHub runner crons. No cron runs on a paused Vercel project, so the cut removes no running job, and vercel.json carries an ignoreCommand that skips every build, so the cut costs no Vercel credit. Evidence: supabase/prepared/cost001/README.md and README_b.md (the questions), the owner's chat instruction of 2026-09-25 (delegation). Reversible: yes; each job has a down migration.
**PMD-45 What runs in an Edge Function for the AI work link, and what does not (S-1 spike, 2026-09-26).** (1) Tier-1 reads of the 13 record kinds are service_role-only SECURITY DEFINER SQL row-set functions called by the Edge Function ai-work-link, the pattern of projexa-read; no executor runs there. (2) Writes and computed reads run in a second Edge Function, ai-work-link-exec, after the refactor the spike listed (R-A: role helpers imported from a module that does not pull auth-guard; R-B: the executor stops importing the 147 KB task execution engine; R-C: the two new files and a bun build script; R-D: no vector embedding is computed on the Edge write path, the record is stored without one, a default the owner may override; R-E: the fire-and-forget automation trigger is dropped there). (3) The owner sets one secret, APP_RUNTIME_DATABASE_URL, on ai-work-link-exec before it can run; the PM writes no credential. (4) The CPU and wall-time criterion (67 to 86 round trips per computed read) is proven only after a deploy with the region pinned to ap-south-1, so that check waits for go-live. Reversible: yes.
**PMD-46 Where the AI work link objects live in verdian-ai (2026-09-26).** The five tables are in schema platform (ai_work_link_call, partitioned by month; ai_work_link_intent; ai_work_link_functions; ai_work_link_record_kinds; ai_work_link_settings). Every SQL function of the link is in schema public and named ai_work_link_*, because the Edge Functions reach the database through PostgREST, which exposes only public (the same reason as projexa_timer_* and projexa_read_*): 23 are executable by service_role alone (the twelve of spec 10.11, the four create, list, revoke and warning functions, and seven pure helpers) and ai_work_link_call_retention by postgres alone; the migrations revoke each function from public, anon and authenticated, and the live check after the apply is recorded in the evidence (this corrects the first count of twelve). The trigger function and the partition helper of ai_work_link_call are in public too, because the claim places every ai_work_link_* function there. This matches the claim in ACTIVE-CLAIMS; SHARED_BOUNDARY.md R4 is amended to list the public functions beside the platform tables. Reversible: yes (the down migrations drop them).
**PMD-47 Overlap of the overdue-task alert and the task nudge digest (U-41 part B verifier, 2026-09-26).** PMD-44 item (1) assumed the notification dedup removes the overlap between cost001-metric-alerts (its task_overdue check) and cost001-task-nudge-digest. The verifier showed the two write different notification kinds, so one person could get both on the same day. Decision (PM): keep both jobs and make the two functions check each other by task id inside p_dedup: the digest leaves out an overdue task the owner already has an unread task_overdue notice for, and the overdue alert skips a recipient whose unread digest lists the task; the assigner keeps the overdue notice. The two function bodies therefore differ from the prepared files 04 and 10, and the migration headers say so. Reversible: yes (remove the taskOverdue call from the wrapper and restore the two bodies).
