# Priority 6 tracker, created 2026-07-12 per the Owner's directive: "complete
# the integration, implementation and close all gaps of Priority 1, 2, 3, 4,
# 5 with FChecklist as a cognitive AI OS... at backend it should work as one
# brain. For end user the product, projects shall be different. The complete
# cognitive brain works as one component."
#
# Read literally and precisely: the BACKEND intelligence (registry, learning,
# orchestration) must be one coherent system: PROJEXA/veda-advisors/VERIDIAN
# stay separate PRODUCTS with separate UIs, but they draw on ONE brain, not
# separate ones. This tracker's investigation found that claim was NOT yet
# true in the actual code -- see verdict below.
#
# status values: complete | in_progress | not_started

priority: 6
name: "One Cognitive Brain -- close the integration gap between the UMR and the Software Orchestrator"

investigation:
  verdict: |
    Two real, concrete, previously-undiscovered gaps found by direct code
    and live-database inspection (not assumed from memory of what was
    built):

    GAP 1 -- THE UMR IS EMPTY. `SELECT count(*) FROM platform_assets`
    returns 0 live, despite Priority 3/4 building the complete
    infrastructure (schema, auto-registration triggers on 33 tables total,
    compiled cache, routing engine) across 9 merged PRs. Root cause,
    confirmed by re-reading migration 0152's trigger logic: the
    auto_register_asset trigger only fires on INSERT/UPDATE/DELETE --  it
    was never a backfill for rows that already existed when the trigger
    was attached. Priority 3's own backfill script
    (scripts/backfill-platform-assets.ts, for worker_agents/
    computation_engines/prompt_templates/dynamic_chains) was written,
    tested, and deliberately left for the Super Boss to run -- and was
    never actually run. Priority 4's 29 domain tables (documents, tasks,
    policies, automation_rules, etc.) have NO equivalent backfill script
    at all -- this was never built, a real gap this tracker's own scope
    decision below closes.

    GAP 2 -- THE SOFTWARE ORCHESTRATOR (Priority 5) NEVER TALKS TO THE
    UMR (Priority 3/4). Confirmed by grep: zero references to
    asset-query-service.ts, asset-routing-engine.ts, platformAssets, or
    registerAsset() anywhere in task-execution-engine.ts, chat-service.ts,
    or capability-audit-service.ts. Priority 5 built its own, entirely
    separate capability-memory system (task_capabilities/
    instruction_packages/capability_improvement_proposals) that neither
    reads from nor writes back to the UMR. This is literally "two brains"
    in the sense the Owner's directive names: a registry of what exists
    (UMR) and a learning system deciding what AI is needed for (Software
    Orchestrator), built in adjacent priorities, never wired together.
    Concretely: when capability-audit-service.ts's Auditor asks "is this
    fixable in software," it has no way to discover that a fix might
    already exist as one of the computation_engines catalog rows whose
    engineKey has no case in task-execution-engine.ts's dispatchEngine()
    switch statements (a real, pre-existing wiring gap this session has
    flagged before under a different name, e.g. Wave 99's "41/247
    implemented" note -- exact current count not re-verified this pass,
    dispatch agent 4's coverage report below will produce a live number
    instead of relying on a stale one) -- it can only ever propose building
    something new. And
    when Higher AI closes a gap, closeImprovementLoop() bumps the
    capability's version but never registers the new capability as a
    discoverable UMR asset, so nothing else in the platform (VERI FDE,
    the UMR's own search API, a future org's Dynamic Chain resolution)
    can ever find it.

  scope_decision: |
    Priority 6 closes exactly these two structural gaps, plus the
    genuinely actionable (not merely "tracked") open items already
    disclosed across Priorities 1-5. It does NOT re-open already-settled
    scope decisions (e.g. Priority 4's 359-of-388-tables-still-grandfathered
    scoping stands; Priority 3's UMR-covers-4-source-types-not-all-330
    scoping stands) -- "close all gaps" means close the gaps that are
    real integration/completeness defects, not re-litigate deliberate,
    already-justified scope boundaries.

    Explicitly OUT of Priority 6 (still requires the Owner, cannot be
    closed by an agent):
      - Priority 2 item 1: leaked veda-advisors credentials still need
        rotation via the Owner's own GitHub/Composio dashboards.
      - Priority 5's GITHUB_DISPATCH_PAT: confirmed this is a VERCEL
        environment variable (dispatch-repo.ts runs inside the deployed
        Next.js app, not GitHub Actions), distinct from the GitHub
        Secret PAT_FCHECKLIST (which DOES exist, confirmed via `gh
        secret list`, but is write-only by GitHub's own design --
        no agent can ever read a secret's value back to copy it
        elsewhere). This requires the Owner to either set it in Vercel
        directly or provide a fresh PAT value through a secure channel
        (never pasted into chat).

dispatches:
  - agent: 1
    scope: "Live backfill: run Priority 3's existing script for real + build and run the equivalent for Priority 4's 29 domain tables' pre-existing rows"
    owner: "Super Boss (sequential, must land before dispatch 3's coverage report is meaningful)"
    status: complete
    result: |
      The bare `bun run scripts/backfill-platform-assets.ts` could not run from
      this session's Bash sandbox -- a raw TCP connect to the Supabase pooler
      (aws-1-ap-south-1.pooler.supabase.com:6543) hung/timed out, the same
      class of local-network restriction already documented in
      veridian_e2e_test_pass_2026-07-06's "DATABASE_URL blocks local E2E"
      note. Switched to the Supabase MCP (project_id pcrjmlpuqsbocqfwoxod),
      this session's established reliable path for all live DB work.

      Built migration 0157 (compliance.backfill_registered_assets(text)) --
      a REUSABLE PL/pgSQL function, not a one-off script, mirroring
      auto_register_asset()'s exact column-mapping logic so any table added
      to asset_registration_config in the future can be backfilled the same
      way with zero new application code. Caught and fixed one real bug live
      on first run: the RETURNS TABLE column `source_table` collided with
      platform_assets.source_table inside the INSERT/ON CONFLICT clause
      (Postgres error 42702, "column reference is ambiguous") -- fixed by
      renaming the OUT columns to out_source_table/out_rows_seen/
      out_rows_upserted, which required DROP FUNCTION first (42P13, "cannot
      change return type of existing function").

      Ran it for all 29 Priority 4 domain tables: 2,305 rows registered
      (tasks=1899, documents=190, module_registry=99, product_branches=26,
      departments=63, approval_requests=5, projects=5, risks=5,
      subscription_plans=4, products=4, incidents=2, notices=3; the
      remaining 17 tables were legitimately empty in live data, e.g. no org
      has created a policy/automation_rule/bcm_plan/webhook yet).

      Then backfilled Priority 3's original 4 source tables
      (worker_agents/computation_engines/prompt_templates/dynamic_chains)
      directly via SQL mirroring scripts/backfill-platform-assets.ts's own
      buildXAssetInput()/mapXStatus() functions 1:1 (these predate Priority
      4's generic config-driven trigger and use bespoke status mapping, not
      a boolean active_column, so they were not folded into
      backfill_registered_assets()): worker_agents=27, computation_engines=
      247, prompt_templates=215, dynamic_chains=0 (no org has created one
      yet -- correct, not a bug).

      SELECT count(*) FROM compliance.platform_assets: 0 -> 2,794. The UMR
      is now genuinely populated for the first time. Also captured a live,
      current computation_engines status breakdown while here (superseding
      the stale Wave 99 memory note of "41/247 implemented"):
      implemented=211, partial=23, not_started=13, of 247 total.
  - agent: 2
    scope: "Wire the UMR into the Software Orchestrator: classification checks platform_assets, closeImprovementLoop() registers new capabilities, the Auditor searches for pre-existing unwired capabilities before proposing new work"
    owner: "subagent (parallel worktree)"
    status: complete
    result: |
      Built all 3 requested integration points in capability-audit-service.ts
      and task-execution-engine.ts: (1) runCapabilityAudit() now searches the
      UMR via asset-query-service.ts's queryByKeywords() for an existing
      active computation_engine match before dispatching to Higher AI,
      noting any match on the proposal (new existing_asset_match jsonb
      column, migration 0158) and folding it into the TightTask as a
      wiring/reuse hint rather than a from-scratch build ask -- never blocks
      dispatch. (2) closeImprovementLoop() now registers the closed
      capability as a discoverable platform_assets row via registerAsset(),
      or updateAsset()s the existing UMR row when a candidate was found
      earlier -- checks getAssetBySource() first to respect the real
      UNIQUE(source_table, source_id) constraint. (3) NOVEL-classified tasks
      in task-execution-engine.ts get one more UMR lookup before falling
      through to free-text AI planning, surfacing a hint into the planning
      prompt, never blocking. All 3 lookups degrade gracefully on failure
      (never block the primary flow). 45 new/updated unit tests, all passing.
      PR #217, merged; migration 0158 applied live by the Super Boss after
      merge (the subagent correctly declined to touch the live DB from its
      sandbox).
  - agent: 3
    scope: "Close genuinely actionable disclosed gaps: Priority 5's Chain Selector UI for new conversations, refresh the ported governance foundation in veda-advisors/projexa, resolve or re-document Priority 1's open tracking items"
    owner: "subagent (parallel worktree)"
    status: complete
    result: |
      Item 1 (Chain Selector for new conversations): extracted the existing
      private Chain Selector markup out of VeriComposer.tsx into a shared
      ChainSelector.tsx + ChainSelectorDialog, wired into AiThreadSwitcher's
      "New thread" action (previously a bare window.prompt()), feeding
      createNewAiThread()'s existing unused 3rd param. ensureAiThread() and
      the default 1:1 thread stayed untouched. 14 new tests.
      Item 2 (governance-foundation drift): no code to change in this repo
      and no access to veda-advisors/projexa from this sandbox -- delivered
      a git-archaeology-backed delta doc
      (12-priority6-governance-foundation-drift.md) instead, precise enough
      that a future dispatch with real repo access needs no research.
      Item 3 (VERI-as-participant): found VERI_CHAT_GOVERNANCE.md narrated
      this as shipped when it genuinely wasn't (a real doc/code mismatch) --
      built the narrow additive slice the doc itself scoped: a
      veriParticipant boolean flag on conversations (migration
      0159, renumbered from a colliding 0157 during independent
      verification), invite/remove API routes, and generateVeriGroupReply()
      triggered only by explicit @veri/"ask veri" addressing, reusing the
      existing policy-enforcement/reply-gate machinery, never proactive.
      PR #219, merged; migration 0159 applied live by the Super Boss after
      merge (same "subagent correctly declined live DB access" pattern as
      agent 2 above).
  - agent: 4
    scope: "Run Priority 4's deterministic audit script for real against live (now-populated) data; build a real 'one brain' coverage/health report reusing existing computeCoverageStats()/audit-asset-registry.ts rather than new machinery"
    owner: "subagent (parallel worktree)"
    status: complete
    result: |
      Confirmed the sandbox's bash tool genuinely cannot reach the live
      Postgres pooler, but discovered the Supabase MCP's execute_sql tool
      reaches the same live database over a different network path (the
      same discovery this tracker's own agent-1 dispatch made independently
      -- corroborating, not duplicated: both hit the same wall and found the
      same workaround on their own). Gathered real reconciliation data live
      and fed it through audit-asset-registry.ts's own pure functions via a
      new --from-json mode, rather than reimplementing the audit logic.
      Result: CLEAN -- 29/29 configured tables reconciled with zero
      mismatches, 0 trigger gaps, 0 uncovered tables of 391 declared.
      Built scripts/report-cognitive-brain-coverage.ts, reusing
      capability-learning-service.ts's computeCoverageStats() (Software
      Orchestrator side) and audit-asset-registry.ts's computeCoverageStats()
      (UMR side) rather than new aggregation logic. Live, freshly re-verified
      numbers (not stale memory): computation_engines 211 implemented / 23
      partial / 13 not_started of 247; task_capabilities 0 rows (no live
      classification history yet -- reported honestly, not fabricated).
      PR #218, merged.

final_status:
  merged_prs:
    - "#216 (agent 1, me): compliance.backfill_registered_assets() + live backfill, platform_assets 0 -> 2,794 rows"
    - "#217 (agent 2): UMR <-> Software Orchestrator wiring (Auditor UMR cross-check, closeImprovementLoop() UMR registration, NOVEL-path UMR hint)"
    - "#218 (agent 4): live deterministic audit (clean) + cognitive-brain coverage report"
    - "#219 (agent 3): Chain Selector for new VERI conversations, VERI-as-participant in group chat (narrow/additive), governance-drift doc for veda-advisors/projexa"
  supervisor_bugs_caught:
    - "drizzle/0157: RETURNS TABLE column source_table collided with platform_assets.source_table inside INSERT/ON CONFLICT (Postgres 42702) -- caught on first live run, fixed by renaming OUT columns"
    - "PR #219's migration was independently numbered 0157 (forked before #216/#217 merged) -- a real filename collision with the already-merged 0157/0158. Caught during independent verification, renamed to 0159, fixed the one stale in-code comment reference, before merge."
    - "PRs #217 and #219 each wrote a real migration but their subagents correctly declined to run it against the live DB (sandbox network restriction) -- both migrations (existing_asset_match column, veri_participant column + prompt template) were merged to git but NOT yet live until the Super Boss applied them directly via the Supabase MCP after merge. This is the same class of gap Priority 6 itself was created to catch: code merged != system actually running that way."
  live_verification_after_all_merges:
    - "platform_assets: 2,794 rows (was 0 at Priority 6's start)"
    - "capability_improvement_proposals.existing_asset_match: column live"
    - "conversations.veri_participant: column live, default false"
    - "prompt_templates: chat.veri_group_participant present with a production prompt_versions row"
  owner_requirements_addressed:
    - "\"One brain\" backend integration: the UMR (Priority 3-4) and Software Orchestrator (Priority 5) now genuinely read from and write to each other -- not parallel systems anymore. Verified by direct code review, not just by the subagent's own report."
    - "\"Don't duplicate, reuse, rewire\": every dispatch was explicitly instructed to and did reuse existing services (asset-query-service.ts, asset-registry-service.ts, capability-learning-service.ts, the existing Chain Selector markup, audit-asset-registry.ts's own pure functions) rather than building parallel infrastructure."
    - "End-user products stay distinct: no changes to PROJEXA/VERIDIAN dashboard separation -- this priority was scoped to backend registry/orchestrator integration only, per the Owner's own stated boundary."
  honestly_still_open (owner-blocked, cannot be closed by any agent):
    - "GITHUB_DISPATCH_PAT: confirmed to be a Vercel environment variable dispatch-repo.ts reads directly (process.env.GITHUB_DISPATCH_PAT), distinct from the GitHub Secret PAT_FCHECKLIST which exists but is write-only (no agent can ever read its value to copy it elsewhere). Higher AI's live dispatch path remains untested end-to-end until the Owner sets this in Vercel."
    - "Priority 2 item 1: leaked veda-advisors credentials still need Owner-side rotation."
    - "veda-advisors/projexa governance-file drift: a precise, no-research-needed checklist now exists (12-priority6-governance-foundation-drift.md) but applying it requires repo access this sandbox doesn't have."
    - "Full proactive VERI-as-participant (beyond explicit @-mention) and the full communication-type catalog from the original VERI Chat governance spec remain deliberately deferred, same reasoning as before: a bigger live-surface UX change, not a backend integration gap."
