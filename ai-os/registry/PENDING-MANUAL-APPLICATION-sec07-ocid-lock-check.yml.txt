# PENDING MANUAL APPLICATION -- this file is NOT yet a real, active GitHub Actions
# workflow. Real, confirmed platform constraint (documented independently, before this
# PR existed, in claude-control's SUPERBOSS_DISPATCH_PROMPT.md's own "Hard rule: never
# modify .github/workflows/** directly in a worker task"): the GitHub token available to
# this session lacks the `workflow` OAuth scope, so GitHub itself rejects any push that
# creates or updates a real .github/workflows/*.yml file ("refusing to allow an OAuth
# App to create or update workflow ... without workflow scope"). This is not a guess or
# a one-off failure -- it was hit for real, again, building this exact gate.
#
# Real, honest disclosure, not glossed over: this PR ships everything else this UMR
# asked for (the gate script, its real tests, the scope manifest, the override registry,
# the OCID-020 status block) as real, working, merged code -- but the workflow file
# itself, the piece that makes the check actually REQUIRED and RUN on every PR, cannot
# be pushed by this session. It is committed here, at this non-`.github` path, verbatim
# and ready to use, so the Owner (or a future session with a PAT that has `workflow`
# scope) can apply it with a single real action:
#
#   git mv ai-os/registry/PENDING-MANUAL-APPLICATION-sec07-ocid-lock-check.yml.txt .github/workflows/sec07-ocid-lock-check.yml
#
# then commit and push that single rename through a real, sanctioned channel (a PAT with
# `workflow` scope, or the GitHub web UI directly). Until that real step happens, this
# gate is REAL and TESTED but NOT YET ACTIVE -- it does not yet block any real PR.
# Real content below is the exact, complete, ready-to-use workflow file.

name: SEC-07 OCID Lock Check

# Real, deterministic pre-merge gate (UMR-20260805-025554-46f9, Owner corrective
# action, citing UMR-20260805-025349-a6b8 / OD-20260805-001, UMR-20260802-165606-4413,
# UMR-20260804-194323-0bc5). The real SEC-07 violation on PR #886 was only detected
# AFTER merge, by a discovery-only audit -- nothing technically blocked it at merge
# time. This is that real, required check: it fails a PR's CI (not a warning) when its
# own real diff and/or title/body touches locked OCID-038/039/040/041-046 scope while
# OCID-020's real current status (ai-os/MASTER-TRACKER.yaml's own ocid_020_status
# block) is anything other than a literal "VERIFIED", unless a real, explicit Owner
# override entry exists for that exact PR in ai-os/registry/sec07-overrides.yaml.
#
# See ai-os/GOVERNANCE_RECORD_HARD_RULE_7_VIOLATION_PR886_2026-08-05.md for the real
# incident this exists to prevent from recurring, and
# ai-os/registry/ocid-locked-scope-manifest.yaml for the real scope this checks.

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: read

jobs:
  sec07-ocid-lock-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
      - run: bun install --frozen-lockfile

      - name: Run the gate's own real regression tests first
        run: bun test ./scripts/check-sec07-ocid-lock.test.ts

      - name: Evaluate the real SEC-07 lock against this PR
        env:
          GITHUB_BASE_REF: ${{ github.base_ref }}
        run: node scripts/check-sec07-ocid-lock.mjs
