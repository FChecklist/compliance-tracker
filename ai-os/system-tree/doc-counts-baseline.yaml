# Baseline for scripts/check-doc-drift.mjs -- AI Documentation /
# Documentation Lifecycle gap-closure (VERIDIAN Review Framework,
# 2026-08-01). Recorded counts as of the last time ai-os/system-tree/'s
# headers were refreshed to match the live codebase.
#
# When scripts/check-doc-drift.mjs fails in CI: refresh the relevant
# ai-os/system-tree/*.yaml content/headers, then update `counts:` below to
# the new actual numbers in the SAME PR. Do not just bump the numbers here
# without touching system-tree -- that defeats the check's purpose.
#
# History:
#   2026-08-01: initial baseline. Previous system-tree headers (recorded
#   2026-07-11/12, see ai-os/system-tree/00-INDEX.md) claimed 377
#   tables/106 enums/614 API routes/~130 pages/~65 components -- all
#   already stale by 15-61% at the time this baseline/check was added (the
#   concrete finding that motivated this task). Counts below are the real,
#   freshly-measured values used to reset the baseline in the same PR that
#   refreshed the count-level facts in system-tree's headers.
#   2026-08-15: re-verified against live main (task-20260718-064004, resumed
#   after the original PR #685/#1039 stalled ~2 weeks on a since-resolved
#   branch-protection review deadlock -- see this task's own progress file).
#   Only api_routes had moved (991 -> 995, 0.4% drift, well within
#   tolerance); tables/enums/app_pages/components were unchanged. Refreshed
#   here as routine upkeep, not because the check failed.
#   2026-08-31: re-verified against live main while rebasing this PR onto
#   current main (replacement PR superseding #1211, which had gone stale
#   across ~2 more weeks of fast-moving main). Running scripts/check-doc-drift.mjs
#   against the actual pre-rebase counts genuinely FAILED this time (not
#   hypothetical -- verified by running it): enums moved 130 -> 146 (12.3%
#   drift) and api_routes moved 995 -> 1109 (11.5% drift), both over the 10%
#   tolerance. tables (443 -> 476, 7.4%) and app_pages/components (163 -> 169,
#   81 -> 84, 3.7% each) stayed within tolerance. Per this file's own header
#   instruction, only the counts below are refreshed here (routine upkeep,
#   matching the 2026-08-15 precedent) -- the per-domain guardrails/rules
#   text in ai-os/system-tree/11-13-*.yaml was NOT re-synthesized against the
#   new enum/route counts; that remains real, open, judgment-heavy work for
#   a future SYSTEM-AUDIT round (see SYSTEM-AUDIT-ROUND-3.md's own
#   recommended audit cadence), not something to fake here.
generated_at: "2026-08-31"
tolerance_pct: 10
counts:
  tables: 476
  enums: 146
  api_routes: 1109
  app_pages: 169
  components: 84
