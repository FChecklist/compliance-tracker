# ============================================================================
# VERIDIAN AI OS -- MASTER TRACKER
# ============================================================================
# THE single file for gap analysis, pending work, and work-in-progress across
# this entire initiative. Created 2026-07-12, consolidating 17 separate
# tracker/gap-backlog files (5,538 total lines) that had accumulated across
# Priorities 1-7. Those files are archived at
# ai-os/tree4-unified/50-completion-plan/archive/ and ai-os/audit-tree/archive/
# -- kept for historical detail/evidence, but NO LONGER MAINTAINED. This file
# is the only one to read or update going forward for "what's done / what's
# open / what's next."
#
# Update discipline: when you close an item, move it from `open_items` to
# `closed_priorities`' relevant summary (one line, not a new essay) and
# delete it from `open_items`. When you find a new gap, add it to
# `open_items` with a real owner/blocker, not a vague note. Keep this file
# under control -- if it starts sprawling back to 1000+ lines, that's a sign
# to compress the closed-history sections further, not to split it again.
#
# status vocabulary used throughout:
#   open            -- real, actionable, nobody has started
#   owner_blocked   -- cannot be closed by an agent; needs the Owner
#                      (credential/account action, or a judgment call only
#                      they can make)
#   needs_verification -- last checked before several later priorities
#                      shipped; likely already closed or moot, but not
#                      re-confirmed against current code -- check before
#                      trusting either way
#   ratified        -- deliberately decided NOT to build, with a documented
#                      reason (not silently dropped, not still "pending")
#   deferred_large  -- real, scoped, but large enough to be its own
#                      initiative, not a quick slice
# ============================================================================

meta:
  last_consolidated: "2026-07-12"
  source_files_archived:
    - ai-os/tree4-unified/50-completion-plan/00-INDEX.md
    - ai-os/tree4-unified/50-completion-plan/01-phase0-owner-decisions.yaml
    - ai-os/tree4-unified/50-completion-plan/02-plan-items.yaml
    - ai-os/tree4-unified/50-completion-plan/03-coverage-matrix.yaml
    - ai-os/tree4-unified/50-completion-plan/04-implementation-log.yaml
    - ai-os/tree4-unified/50-completion-plan/05-eighteen-areas-tracker.yaml
    - ai-os/tree4-unified/50-completion-plan/06-associate-dispatch-log.yaml
    - ai-os/tree4-unified/50-completion-plan/07-priority2-tracker.yaml
    - ai-os/tree4-unified/50-completion-plan/08-priority3-umr-tracker.yaml
    - ai-os/tree4-unified/50-completion-plan/09-priority4-umr-universal-tracker.yaml
    - ai-os/tree4-unified/50-completion-plan/10-priority5-software-orchestrator-tracker.yaml
    - ai-os/tree4-unified/50-completion-plan/11-priority6-one-brain-integration-tracker.yaml
    - ai-os/tree4-unified/50-completion-plan/12-priority6-governance-foundation-drift.md
    - ai-os/tree4-unified/50-completion-plan/13-priority7-remaining-gaps-tracker.yaml
    - ai-os/tree4-unified/30-gap-backlog.yaml
    - ai-os/audit-tree/GAPS.yaml
  deliberately_NOT_folded_in:
    - file: ai-os/boss/BOARD.yaml
      why: "Pre-existing (2026-06-28) multi-agent governance infrastructure named explicitly in AGENTS.md Rule 3, used by z.ai too, not just this session's work. Already self-declared stale/superseded by COMPLETED.yaml in its own header -- left as-is, not this file's job to fix."
    - file: ai-os/boss/COMPLETED.yaml
      why: "Required doer+auditor completion LOG per AGENTS.md Rule 7(d), append-only audit trail across all agents. Different purpose than a gap/pending tracker -- this file references it, doesn't replace it."
    - file: ai-os/system-tree/*.yaml
      why: "Tree 1 -- 'what actually exists in code' reference, not a gap/pending list."

# ============================================================================
# OPEN ITEMS -- everything actually pending right now, as of 2026-07-12
# ============================================================================
open_items:

  owner_blocked:
    - id: OPEN-01
      name: "GITHUB_DISPATCH_PAT not set in Vercel"
      detail: "dispatch-repo.ts reads process.env.GITHUB_DISPATCH_PAT directly -- confirmed absent from all 3 Vercel environments (Production/Preview/Development) via `vercel env ls`. Blocks Higher AI's live repository_dispatch path (capability-audit-service.ts's dispatchProposalToHigherAI()) from ever succeeding -- untested end-to-end to this day."
      next_step: "Owner creates a GitHub PAT (repo scope) and runs `vercel env add GITHUB_DISPATCH_PAT production`, or sets it via the Vercel dashboard. Cannot be done by an agent -- distinct from the write-only PAT_FCHECKLIST GitHub secret, whose value can never be read back to reuse."
      first_raised: "Priority 5"
      still_open_confirmed: "Priority 7 (2026-07-12, live-checked)"

    - id: OPEN-02
      name: "veda-advisors PR #15 needs Owner review approval"
      detail: "Fully CI-green (12/12 checks). veda-advisors' branch protection requires 1 approving review + enforce_admins:true, no bypass. The session's gh auth is the same account (FChecklist) that authored the PR -- GitHub blocks self-approval."
      next_step: "Owner (or any account other than FChecklist) approves and merges https://github.com/FChecklist/veda-advisors/pull/15."
      first_raised: "Priority 7"

    - id: OPEN-03
      name: "Leaked veda-advisors credentials still need rotation"
      detail: "A real GitHub PAT is embedded in code-by-zai/.git/config's remote URL and in 2 plaintext local files (MASTER_IMPLEMENTATION_PROMPT.md, Veda_Advisors_Master_Prompt.md); a real Composio API key is in Linkedin.md. Verified 2026-07-12: NOT committed to any GitHub repo (local-only exposure), and no DB password/service-role key found (an earlier report's claim on that specific point was overclaimed and corrected)."
      next_step: |
        Owner action required, in order:
        1. Generate a new GitHub PAT, revoke the old one (GitHub Settings -> Developer settings -> Personal access tokens).
        2. Generate a new Composio API key, revoke the old one (Composio dashboard).
        3. Check every repo/workflow reusing the old PAT (GitHub Secrets PAT_FCHECKLIST across compliance-tracker/veda-advisors/projexa) before rotating, or CI breaks everywhere it's used.
        4. Scrub the plaintext values from the 3 local files above, replace with a placeholder + secrets-manager pointer.
        5. Fix code-by-zai/.git/config to stop embedding the PAT in the remote URL (switch to a credential-manager-backed auth).
      first_raised: "Priority 2"

  needs_owner_decision:
    - id: OPEN-04
      name: "Audit Organization role granularity (DEC-01 leftover)"
      detail: "roster.ts now has 130 of the ~150 individually-named audit-org roles from Audit Organization.docx (5/5 divisions, 25/25 departments structurally complete) -- built without waiting on DEC-01's original pilot-first recommendation, per later 'don't wait' directives. Whether the remaining ~20 role names are worth adding individually, vs. treated as covered by the 130 that exist, was never explicitly re-decided."
      recommendation: "Low priority -- 130/150 already covers all 5 divisions/25 departments structurally. Only revisit if a specific missing named role is actually blocking something."

    - id: OPEN-05
      name: "Universal Work Object retrofit (DEC-03) -- ratified against, but the underlying gaps it would have closed remain"
      detail: "DEC-03 ratified the current distributed-table architecture (ERP/PMS/Firm/Construction) over a ~30-state Work Object retrofit as hard-to-reverse and disproportionate. This is a real, final decision (not pending), but it means several downstream Tree-1 asks stay permanently only-approximated: a truly universal follow-up engine (G-024/G-028 below), a truly universal cross-type dashboard."
      recommendation: "Ratification stands. Only reopen if the Owner explicitly wants to revisit DEC-03 itself -- do not re-litigate piecemeal via one of its downstream symptoms."

  real_gaps_not_yet_built:
    # These are the still-accurate, still-open items from the old GAPS.yaml
    # (46 items) / 30-gap-backlog.yaml (11 items) catalogs, AFTER removing
    # everything closed by Priority 1's 18 areas, Priority 2's D-domain
    # sweep, and Priorities 3-7. Where a later closure only partially
    # covered an old item, that's noted. Anything NOT listed here from the
    # old catalogs should be assumed CLOSED -- see closed_priorities below
    # for where.
    - id: GAP-DCMD
      name: "Dynamic Chain Master Directory (DCMD) rich schema + graph structure"
      old_ids: ["GAP-07", "G-016", "G-017", "G-034 (Business Digital Twin, same substrate)"]
      detail: "PARTIALLY CLOSED, Priority 9 (PR #227, Wave 173): dynamic_chains gained 3 real additive columns (linkedApprovalWorkflowIds, governanceNotes, deprecationReason) plus the first real entity_relationships graph edge for chains (dynamic_chain -> approval_workflow_instance, written when a chain-originated task triggers a real approval workflow). FURTHER PARTIALLY CLOSED, Priority 10 (PR #236): a second real graph edge type, dynamic_chain -> worker_agent (relationshipType 'executed_by', upserted by (chain, agent) pair with taskCount/lastTaskId/lastExecutedAt metadata), written at task-execution-engine.ts's updateTaskStatusAndReflect() -- the one chokepoint every real task-completion path already runs through. No new migration needed; the existing GET /api/v1/brain/entity-relationships?entityType=worker_agent&entityId=<id> endpoint (Wave 153) now answers 'which chains has this agent executed' with zero new API surface. Checked live: 0 historical rows qualified for backfill, so this is forward-only by design. Still NOT closed: none of the 10 full rich sub-fields the source doc names (business/classification/inputs/outputs/software/AI/workflow/governance/knowledge per chain) exist -- this remains a deliberately deferred, larger schema redesign, not attempted in this pass. Business Digital Twin (D26.B4.S1, same substrate) got its own first real slice via PR #226: connector_documents table, a genuinely useful SUBSET of the source doc's 16-field schema, populated by real Gmail/Drive pulls."
      size: deferred_large
      recommendation: "Design the full graph schema first (per the source doc's own preference for graph over enumerated-permutation), then an additive migration, backfill, cutover. Any migration must preserve existing dynamic_chains rows and their live references from tasks/conversations."

    - id: GAP-CONNECTOR-LAYERS
      name: "Connector Layers 2-4: Office Add-in, Browser Extension, Desktop Companion"
      old_ids: ["GAP-08 (B1.S2/S3/S4)"]
      detail: "Only Layer 1 (Cloud API/OAuth) exists. DEC-09 explicitly ratified these as NOT dispatched -- real, hard-to-reverse distribution/signing/packaging decisions needed before scoping, not a routine build call."
      size: deferred_large
      recommendation: "Owner should prioritize independently if/when wanted -- Layer 2 (Office Add-ins) likely has the clearest ROI given the existing Excel/Word-using customer base."

    - id: GAP-AUTH-REBUILD
      name: "Auth flow rebuild: Google sign-in first, 4-digit email passcode instead of password"
      old_ids: ["G-045"]
      detail: "PARTIALLY CLOSED, Priority 8 (PR #225): Google sign-in button added ADDITIVELY as the first/most prominent option on the login page, alongside (never replacing) the existing password/magic-link/SSO options -- a full auth-method rebuild on a live product with real user sessions was explicitly ratified against as too hard-to-reverse for a single pass. Honest limitation: functionally unusable until a Google OAuth client is configured in Supabase's Auth settings (Owner/dashboard action, same class of gap as OPEN-01). The 4-digit email passcode replacement was NOT attempted -- still deferred_large."
      size: deferred_large

    - id: GAP-MOM-VOICE-TICKETS
      name: "Voice/transcription (2nd of 2 remaining Intelligent Work Detection source types)"
      old_ids: ["G-025 (part)"]
      detail: "Ticket intelligence CLOSED, Priority 9 (PR #232) -- mirrors email-intelligence-service.ts's detect-then-propose pattern, pulls the ticket's real conversation transcript, suggestions only (never auto-created). Voice/transcription remains genuinely unbuilt and is owner_blocked, not just deferred: it needs a new external speech-to-text provider decision (a paid third-party service + API key), the same class of gap as OPEN-01/OPEN-03 -- not something an agent can configure unilaterally."
      size: deferred_large
      recommendation: "Owner picks a speech-to-text provider (e.g. Whisper API, Deepgram, AssemblyAI) and provisions credentials before this can be scoped further."

    - id: GAP-LITERT-EDGE-INFERENCE
      name: "LiteRT.js dynamic edge-inference layer (see PLATFORM_STRATEGY.md §28)"
      old_ids: []
      detail: "Evaluated 2026-07-13: LiteRT.js shipped 4 days before evaluation, extremely early-stage. The 'Gemma 4 in browser' claim that prompted this is a separate, younger project (LiteRT-LM), not core LiteRT.js -- not viable yet for in-browser LLM chat given real WebGPU/WebNN coverage and multi-GB model weights on typical business hardware. Genuinely useful for small non-generative tasks (image quality pre-checks for VERI FM & CS's asset photography). Full evaluation, dynamic-manifest design (mirroring the capability-tree pattern), and phased plan in PLATFORM_STRATEGY.md §28. Not started -- Phase 0 is a small worktree-isolated spike, not committed architecture."
      size: deferred_large
      recommendation: "Boss go-ahead needed on Phase 0 (single-model spike) before any permanent architecture is built around a library this new."

    - id: GAP-NARROW-MONITOR-ESCALATION
      name: "Narrow Monitor Agents + mandatory escalation hierarchy + three-tier model routing (see PLATFORM_STRATEGY.md §29)"
      old_ids: []
      detail: "Evaluated 2026-07-13. escalation-ladder.ts's fixed 3-rung ladder is real but has no MAX_RETRY/TIMEOUT/ownership fields; audit-event-triggers.ts covers only 8 narrow audit events, not a general event bus; workerAgents.tier can trivially add a 'monitor' value but dispatchTool() is pull-based -- no event-subscription mechanism exists. Ran the proposed ~30 monitored event types through the three-tier design: ~28 of 30 need zero AI (pure rule engine, DB-state changes only), a narrow remainder needs Llama 3.1 8B (Cerebras, already wired end-to-end) for genuine single-line language understanding, and none of the 30 need GPT-OSS-120B (that tier is for a different category -- hallucination/cross-agent-consistency/architectural review). Corrected one proposal detail: protocol/handover structural validation (offered as a Tier-2 example) is already Tier-1-deterministic and already shipped (PR #248). Full phased plan (Phase 0: prove the mechanism on APPROVAL_GRANTED/REJECTED only, zero LLM calls) in PLATFORM_STRATEGY.md §29."
      size: deferred_large
      recommendation: "This is the same underlying mechanism as GAP-AI-WORKFORCE-GOVERNANCE's Agent Escalation Registry -- sequence together, don't build twice."

    - id: GAP-AI-WORKFORCE-GOVERNANCE
      name: "AI Workforce Governance framework: 6 agent registries (see PLATFORM_STRATEGY.md §30)"
      old_ids: []
      detail: "Evaluated 2026-07-13, including a scale-reality correction: 'millions of agents' cannot mean millions of distinct registered identities (roster.ts has 198 role definitions, workerAgents has 27 rows, both deliberately small/curated) -- it maps to millions of task executions/dispatches (activityLog, already an unbounded growing log) routed through a small, bounded, governable set of role definitions. Registry-by-registry status verified against real code: Knowledge Registry already real (embeddings.ts pgvector search); Agent Performance real but narrow (model-scorecard-service.ts, no hallucination score/cost field yet); Continuous Learning real but scoped to guardrail violations only; Agent Hierarchy and Agent Review are the two genuinely new pieces (workerAgents.supervisorWorkerAgentId confirmed dead, 0 of 27 rows set; no periodic promote/retrain/deprecate cycle exists anywhere). Agent Capability Registry needs to bridge roster.ts and workerAgents, not merge them -- they're deliberately different concepts. Full registry table and sequencing recommendation in PLATFORM_STRATEGY.md §30."
      size: deferred_large
      recommendation: "Sequence after GAP-NARROW-MONITOR-ESCALATION's Phase 0 (Agent Escalation Registry is the same mechanism). Build Agent Review Registry last -- it needs Agent Performance + Agent Escalation data to act on, and has zero existing precedent to extend."

    - id: GAP-UNIFIED-SOURCE-OF-TRUTH
      name: "Unified source of truth for drift/duplication/hallucination + GPT-OSS-120B cross-agent monitor (see PLATFORM_STRATEGY.md §31)"
      old_ids: []
      detail: "Evaluated 2026-07-13 against 8 named pain points (drift, scattered work-history, undocumented decisions, no pre-work SOP, hallucination/infinite loops, unclear VERIDIAN-AI-OS understanding, no live completion %, no unified open/closed/audited view), applying to every agent working on this codebase's code -- Claude Code, z.ai GLM 5.2, or any future coding agent. Found real, live drift while writing the evaluation: COMPLETED.yaml was 7 real PRs (#246,#248-253) behind, backfilled same pass (WAVE-166..172). The GPT-OSS-120B-as-monitor proposal is not new scope -- it is PLATFORM_STRATEGY.md §29.3's own already-designed Phase 2 (Tier 3 executor), never built. Dispatching Phase A (COMPLETED.yaml backfill [done], ai-os/BRAIN.md plain-language VERIDIAN explainer, live-computed open/closed/%-complete view extending governance-health) and Phase B (generalize the Narrow Monitor beyond APPROVAL_GRANTED/REJECTED to watch AI Dev Team dispatch completion generally, wire the GPT-OSS-120B Tier-3 executor) as 3 parallel sub-agents this session, per the standing 3-sub-agent review procedure."
      size: "in_progress -- 3 sub-agents dispatched 2026-07-13"
      recommendation: "Update this entry (or move to closed_priorities) once all 3 sub-agent PRs are reviewed and merged. Tier 2 (Llama 3.1 8B/Cerebras) stays explicitly deferred until Tier 3 is proven in production, per §29's own sequencing."

    - id: GAP-UMR-TABLE-COVERAGE
      name: "340 of 396 pre-existing tables remain grandfather-exempted in the UMR (never individually reviewed for registry-worthiness)"
      old_ids: []
      detail: "The auto-registration MECHANISM is complete and proven across 4 domains (56 real tables as of Priority 10). Onboarding any remaining table is now a ~10-line migration addition, not new code -- this is a long tail of small, optional extensions, not a blocking gap. Priority 10 (PR #237) onboarded 19 more real named business-object tables (branches, litigation_matters, ip_portfolio, legal_matters, audit_engagements, vendor_risk_profiles, pms_issues, pms_milestones, erp_fixed_assets, erp_suppliers, erp_warehouses, erp_items, erp_companies, job_openings, candidates, erp_contracts, fm_assets, firm_engagements, construction_rfis), applied live via Supabase MCP with all 44 existing rows backfilled. employee_profiles investigated and deliberately left exempted (no genuine display-name column) -- documented with a real reason, not silently skipped."
      size: "XS per table, ongoing"
      priority: P2 (low, mechanical when wanted)

    - id: GAP-D13-ASSUMPTION-VALIDATION
      name: "D13.B1.S2: AI must check with the user before proceeding on an unstated assumption"
      old_ids: ["D13.B1.S2 (Tree 1)"]
      detail: "Confirmed real, deliberately not built. Considered and rejected a phrase-detection proxy on reply text (too blunt, or too weak to actually close the requirement). The correct fix needs a live prompt_templates DB CONTENT change (a production data mutation, distinct from a schema migration) -- outside a headless subagent's authorization."
      size: S
      recommendation: "Needs (a) Boss/Owner sign-off on the actual prompt wording to add, and (b) possibly UX support for a pending-confirmation reply state."
      first_raised: "2026-07-12 full status reconciliation pass"

  ratified_do_not_build:
    # Real decisions, not gaps -- listed so nobody re-proposes these without
    # realizing they were already explicitly decided against.
    - id: RATIFIED-01
      decision: "DEC-03: Do NOT retrofit ERP/PMS/Firm/Construction onto a unified Work Object table. Ratify the current distributed architecture."
    - id: RATIFIED-02
      decision: "DEC-04: GOV-04's complexity-tier system supersedes the literal percentage-band confidence-escalation scheme. Do not build both."
    - id: RATIFIED-03
      decision: "DEC-07: Keep the binary always_approve/always_reject model. Do NOT build the literal 9/9/16-option approval vocabularies (D9, and by the same reasoning, the overlapping G-026 full Work-Object action menu)."
    - id: RATIFIED-04
      decision: "GAP-05 / DEC-01: 130 of ~150 audit-org roles built (all 5 divisions/25 departments structurally complete) -- the full literal 150-name enumeration was never required to be exact, this is accepted as sufficient (see OPEN-04 above for the small remaining question)."
    - id: RATIFIED-05
      decision: "Redis / cross-instance cache tier for the UMR: deliberately not introduced (Priority 4) -- a real infra/ops decision, not a code gap. In-process compiled cache is sufficient at current scale."
    - id: RATIFIED-06
      decision: "G-020: Real Sales/CRM/Finance/HR/Legal business taxonomy content -- explicitly descoped as business content the org must define, not code to fabricate."
    - id: RATIFIED-07
      decision: "G-003: ~40-field mandatory task metadata schema on every task (including customer tasks) -- descoped as disproportionate to the actual problem (AI-dispatch tightening, not customer task completeness). Documented in the constitution, not silently dropped."
    - id: RATIFIED-08
      decision: "Priority 7: no code-level Guardrail Engine/task-tightening.ts port needed in veda-advisors or projexa -- both make zero direct LLM calls, so there's no dispatch surface to guard. G-031's framing of this as 'the single largest, most load-bearing open gap' was based on a stale premise, corrected 2026-07-12."

# ============================================================================
# CLOSED PRIORITIES -- compact summary. Full narrative detail (PR links,
# exact bug fixes, per-file diffs) lives in git history / ai-os/boss/
# COMPLETED.yaml / the archived tracker files if ever needed -- not repeated
# here.
# ============================================================================
closed_priorities:
  - priority: 1
    name: "18 Owner-named areas, 100% complete"
    summary: "Dynamic Mode Pills & Chain Selector, VERI Chat & VERI, Guardrails, Agent management, Task Lifecycle + loop management, Monitoring, Narrow/tightened instructions, Majority-of-work-by-software, Auditing, AI router management, Cost management, Loop Engineering, 4-layer orchestra, Common functionalities, End-user lifecycle, Account/Org lifecycle, Billing, End-user UI/UX -- all 18 closed with real code+tests, or explicitly ratified out-of-scope per-item (never silently dropped). See open_items above for the handful of items ratified-not-built or left as real deferred-large follow-ons."

  - priority: 2
    name: "Credential rotation flag, PROJEXA pages, Tree 1 to 100%, D26/D10/D21/D9"
    summary: "Tree 1's remaining ~20 domains closed (D1/D2/D8/D13/D5/D6/D19/D24/D25/D15/D22/D27/D28/D26/D10/D21/D9) -- each domain either built with real code, confirmed already-satisfied, or ratified as a documented large deferral (see open_items above for what's still genuinely open from this sweep). PROJEXA's 13 missing sidebar pages built. Credential rotation investigated and flagged for Owner (OPEN-03 above)."

  - priority: 3
    name: "Universal Metadata Registry (UMR) core"
    summary: "platform_assets schema, asset-registry-service.ts, asset-query-service.ts, asset-routing-engine.ts, relationship graph + vector search + manifest all built. (Backfill script itself wasn't run until Priority 6.)"

  - priority: 4
    name: "UMR universal auto-registration"
    summary: "Generic compliance.auto_register_asset() trigger + asset_registration_config table, compiled in-memory cache, CI coverage-enforcement gate, 29 tables onboarded across 3 domains as a proof pattern (359 more remain grandfather-exempted, see GAP-UMR-TABLE-COVERAGE above)."

  - priority: 5
    name: "Software Orchestrator"
    summary: "task_capabilities/instruction_packages/capability_improvement_proposals schema, software-coverage-service.ts's X/Y/A/B classifier, capability-learning-service.ts, capability-audit-service.ts's Auditor->Higher AI loop, package-variable-resolver.ts's MISSING_INFORMATION rule, dialogue-script-executor.ts for VERI Chat. Reused 100% of existing AI roles/dispatch per Owner instruction."

  - priority: 6
    name: "One Cognitive Brain -- UMR <-> Software Orchestrator integration"
    summary: "Live-backfilled the UMR (0 -> 2,794 rows). Wired the Auditor to check the UMR before proposing net-new work; closeImprovementLoop() now registers closed capabilities into the UMR; NOVEL-classified tasks get a UMR hint. Built the deferred Chain Selector UI for new VERI conversations. Built VERI-as-participant in group chat (narrow, explicit-mention-only). Ran the deterministic UMR audit for real (clean, 29/29). Built a real cognitive-brain coverage report."

  - priority: 7
    name: "Remaining gaps: GITHUB_DISPATCH_PAT + veda-advisors/projexa"
    summary: "Confirmed GITHUB_DISPATCH_PAT still unset (OPEN-01). Discovered veda-advisors/projexa's 'code-level rollout' gap (G-031/task #17) was based on a stale premise -- neither product makes direct LLM calls, so there's nothing to port. projexa PR #1 merged with its one named follow-up answered. veda-advisors PR #15 blocked on Owner review (OPEN-02)."

  - priority: 8
    name: "Direct closures: session limit, UMR batch, Google sign-in, assumption-validation investigation"
    summary: "PR #225 (merged): session/device concurrency limit built (GAP-SESSION-LIMIT closed) -- opt-in, never force-invalidates a live session, only blocks a new one over the org's configured limit; 7 UMR tables batch-onboarded (596 rows backfilled); Google sign-in added additively to the login page (GAP-AUTH-REBUILD partially closed, see open_items). GAP-D13-ASSUMPTION-VALIDATION investigated and confirmed genuinely open, owner-blocked on prompt-content sign-off, not silently dropped."

  - priority: 9
    name: "Priority 1-8 recheck to 100%: 9 PRs merged (#226-#234), trust-rebuild pass after an optimistic-reporting incident"
    summary: |
      Triggered by an explicit user trust-breakdown ("I have a trust issue with
      you... you did 69%"), after which every claim of completion in this
      priority was required to be backed by a specific, checkable merged PR
      number -- no exceptions. 9 PRs merged, each independently verified
      (tsc/eslint/guardrail-presence/asset-registry-coverage/full test suite)
      before merge, not self-certified by the agent that wrote it:
        - PR #226: Real connector data ingestion (Gmail/Drive messages, GAP-CONNECTOR-DATA closed),
          Business Digital Twin first slice (connector_documents table), per-source-type
          model routing extension. Caught + fixed during verification: a migration-number
          collision and a missing asset-registry entry.
        - PR #227 (Wave 173): recovered from an interrupted background agent's worktree
          (real, tested, uncommitted work that survived a session boundary) -- DCMD governance
          slice (partial, see GAP-DCMD), GAP-DELEGATION-AUTHORITY closed, GAP-UNIVERSAL-DASHBOARD
          closed (honest approximation per DEC-03), GAP-DYNAMIC-CHAIN-DEDUP closed,
          D25.B4.S1 chain-report-integration closed, GAP-COO-ROLE confirmed resolved (stale
          premise -- chief_operating_officer already existed). Caught + fixed during
          verification: 2 migration-number collisions, 1 asset-registry gap.
        - PR #228: GAP-RESPONSE-VOCABULARY closed -- constrained-vocabulary reply gate for
          mechanical-tier dispatches, wired into POST /api/ai/team/dispatch.
        - PR #229: GAP-D1-METRICS-TARGET closed -- 99.9% GPT-OSS-120B target tracker, honest
          about which of the 6 named success dimensions have real data vs. don't.
        - PR #230: GAP-MODEL-SCORECARD closed -- per-(model, tier) dispatch count/success
          rate/audit-finding-rate, one new nullable column (complexityTier) closing the one
          real gap in existing data.
        - PR #231: GAP-D19-REPORT-CADENCES closed -- Escalations/Recommendations/Risk-Trends
          report cadences, real deterministic sources found one level deeper than an
          obviously-named table. Caught + fixed during verification: a real regex bug
          mis-parsing titles that carry their own parenthesized abbreviation.
        - PR #232: Ticket half of GAP-MOM-VOICE-TICKETS closed -- mirrors
          email-intelligence-service.ts's pattern exactly, suggestions-only.
        - PR #233: GAP-CONTINUOUS-REPRIORITIZATION closed as a narrow, honest slice
          (deadline-driven only) -- investigated and confirmed the other 7 named axes
          (dependencies/SLA/risk/etc.) have no real data source on tasks today.
        - PR #234: 7 of 9 remaining D15.B2.S1 named audit-event triggers wired (GAP-D15-
          REMAINING-TRIGGERS tracks the honest 2 that are genuinely not real yet).
      All 9 migrations applied live via Supabase MCP and verified with direct SQL queries.
      Lesson learned mid-priority (from the user, after a machine-overload incident): scaled
      down from an initial 7-parallel-subagent batch to sequential single-worktree
      verification for the remainder -- see the trust-rebuild discipline note in
      ai-os/STATUS-REPORT.md.

  - priority: 10
    name: "Cognitive-integration gap analysis + closures: DCMD 2nd graph edge, UMR +19 tables, D15 SOP Changed, Global Revenue Ops split"
    summary: |
      Explicit cognitive-integration gap analysis over open_items (distinguishing
      real blockers to a coherent, shared, reasoning-capable AI substrate from
      feature-completeness gaps that don't affect it), plus PROJEXA AI-wiring
      verification. 4 PRs merged, each independently verified (tsc/eslint/
      guardrail-presence/asset-registry-coverage/full test suite) before merge:
        - PR #236: GAP-DCMD further partially closed -- second real
          entity_relationships graph edge type, dynamic_chain -> worker_agent
          ('executed_by', upserted by (chain,agent) pair), wired at
          task-execution-engine.ts's one real completion chokepoint. No new
          migration; GET /api/v1/brain/entity-relationships (Wave 153) now
          answers "which chains has this agent executed" for free. Checked live:
          0 historical rows qualified for backfill (forward-only by design).
        - PR #237: GAP-UMR-TABLE-COVERAGE -- 19 more real named business-object
          tables onboarded (ERP/Legal/HR-recruiting/PMS/Construction/Facilities/
          CA-firm/org-structure/third-party-risk), applied live via Supabase MCP,
          44 existing rows backfilled and verified. employee_profiles reviewed
          and deliberately left exempted (no genuine display-name column) with a
          real documented reason, replacing its generic placeholder. Coverage:
          37 -> 56 registered / 359 -> 340 exempted.
        - PR #238: GAP-D15-REMAINING-TRIGGERS narrowed from 2 to 1 open item --
          SOP Changed closed by reusing the existing policies table
          (category='sop') and its real maker-checker publish flow, rather than
          inventing a parallel sops table/workflow. Deployment remains the one
          honest gap (no real trigger point exists to wire).
        - PR #239: GAP-GLOBAL-REVENUE-SPLIT closed -- new GLOBAL_REVENUE_OPERATIONS
          team (roster.ts), the operational counterpart to the existing
          AUDIT_GLOBAL_REVENUE assurance division; chief_revenue_officer moved
          onto it (behavior unchanged, only the team label moved). Re-investigated
          and overturned Wave 173's "no functional gain" reasoning: the feared
          NON_OPERATIONAL_TEAMS exclusion-list touch point doesn't apply (it's an
          opt-OUT list; a new operational team needs zero changes to it).
      PROJEXA's capability-tree/assistant/discuss AI surfaces independently
      verified as genuinely wired to VERIDIAN's real backend (not stubs) --
      concluded NOT a gap, no PR needed.
      Also fixed a pre-existing tracker inconsistency found during this pass:
      GAP-D1-METRICS-TARGET was still listed in open_items despite
      closed_priorities' own Priority 9 entry already citing PR #229 as having
      closed it -- removed from open_items (this is a documentation fix, not a
      new closure; no new PR work was needed since the underlying gap was
      already closed).
      Deliberately NOT touched this pass (investigated, confirmed genuinely
      out of scope or already covered): GAP-CONNECTOR-LAYERS (ratified
      not-dispatched, DEC-09), GAP-AUTH-REBUILD (4-digit passcode half,
      deferred_large), GAP-MOM-VOICE-TICKETS (voice/transcription,
      owner-blocked on a speech-to-text provider decision),
      GAP-D13-ASSUMPTION-VALIDATION (owner-blocked on prompt-content sign-off).

  - priority: 11
    name: "GAP-D15-REMAINING-TRIGGERS closed: the last of D15.B2.S1's 10 named audit-event triggers, Deployment"
    summary: |
      - PR #243: GAP-D15-REMAINING-TRIGGERS fully closed -- Deployment, the
        one event Priority 10 (PR #238) left genuinely open ("no in-app
        deployment-event table or webhook handler exists beyond the
        already-wired CI workflow for event #1"), now has a real trigger
        point. Built src/app/api/webhooks/vercel-deployment/route.ts: a
        real Vercel account-webhook receiver that verifies x-vercel-
        signature (HMAC-SHA1 over the raw body, Vercel's own documented
        webhook security model, extracted to the independently-unit-tested
        src/lib/webhooks/vercel-signature.ts), records every verified
        delivery in the new platform-wide deployment_events table
        (schema.ts + drizzle/0172, no org_id -- a Vercel deployment isn't
        tenant data, RLS enabled, asset-registry-exempted with a real
        reason), and fires the "deployment" audit_trigger on a verified
        deployment.succeeded event, routed to roster.ts's pre-existing
        deployment_auditor role (clean 1:1 name match). All 9 of the
        remaining D15.B2.S1 named events are now wired (#10, Code Changed,
        was already wired via CI before audit-event-triggers.ts existed).
        Honest limitation, documented in the route's own header: a
        deployment has no tenant org, so the audit_trigger write resolves
        a best-effort platform org (PLATFORM_AUDIT_ORG_ID env var if set,
        else the earliest-created organisations row) rather than a real
        per-request actor -- same caveat class as new_prompt's existing
        entry. Verified before merge: tsc/eslint/guardrail-presence/
        asset-registry-coverage/full build all clean; 12 new unit tests
        (6 signature-verification, 6 route-level: fail-closed with no
        secret configured, 403 for unsigned/wrong-secret/tampered
        deliveries, 200+ignored for an unrecognized-but-validly-signed
        event type with zero DB touch, and confirmation that a correctly
        signed deployment.succeeded delivery clears the signature boundary).
        CI (Lint/Type Check/Build/Unit Tests, plus Guardrail Presence/
        Asset Registry Coverage/E2E/Security/audit-check) all green before
        merge; the only red check was the "Vercel" preview-deployment
        integration, confirmed pre-existing and unrelated (identical
        failure on already-merged PR #241).
        NOT done, honestly: live registration of the actual webhook in
        Vercel's dashboard. Creating it via POST /v1/webhooks (the
        pre-authenticated `vercel` CLI session could reach this) returns
        the webhook's signing secret in plaintext, shown exactly once --
        completing that step would have required this agent to handle that
        raw secret value directly, which was explicitly out of scope.
        Remaining Owner action: Vercel Dashboard -> team
        meet-track-s-projects -> Settings -> Webhooks -> Create Webhook;
        events Deployment Succeeded + Deployment Error (optionally
        Deployment Created); project scope veridian-compliance-ai only;
        endpoint URL https://veridian-ai-os.vercel.app/api/webhooks/
        vercel-deployment; copy the one-time secret and set it as the
        Production env var VERCEL_DEPLOYMENT_WEBHOOK_SECRET (`vercel env
        add VERCEL_DEPLOYMENT_WEBHOOK_SECRET production`), then redeploy.
        Until that step is done, the receiver code is live but Vercel
        never actually calls it.
