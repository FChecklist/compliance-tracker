# ============================================================================
# ACTIVE CLAIMS REGISTRY -- read this BEFORE picking any new gap/task to work
# on in this repo (or in veda-advisors / projexa). Created 2026-07-14 in
# direct response to the Owner naming 4 parallel Claude sessions working
# across this codebase at once, with no existing mechanism for one session
# to see what another is actively touching before it starts new work.
#
# THIS FILE IS NOT A SUBSTITUTE FOR GIT'S OWN SAFETY (worktree isolation +
# PR/CI + branch protection already prevent silent data loss -- see
# AGENTS.md Rule 6 and [[fchecklist_worktree_isolation_required]]). It exists
# to prevent a DIFFERENT problem git can't solve on its own: two sessions
# independently choosing to build the SAME gap at the same time, wasting a
# full agent-hours cycle on duplicate/conflicting work that then has to be
# thrown away or painfully reconciled after the fact.
#
# PROTOCOL (every session, every time, no exceptions):
#   1. BEFORE selecting a gap/task from ai-os/MASTER-TRACKER.yaml (or any
#      other source) to work on: `git pull origin main` this file fresh,
#      read every entry under `active:` below, and do NOT start work on a
#      gap/file-area another entry already claims, unless that entry's
#      `claimed_at` is more than 4 hours stale with no matching open PR
#      (check `gh pr list`) -- treat a stale claim as abandoned, not binding.
#   2. IMMEDIATELY when you start real work (dispatching a sub-agent, or
#      starting to edit yourself): add a new entry under `active:` with your
#      session label, what you're claiming (gap id + real file/directory
#      scope, not just a vague area), and a timestamp. Commit and push this
#      addition on its own, fast, BEFORE the actual work PR -- don't batch it
#      with the real change, so other sessions see the claim as early as
#      possible.
#   3. WHEN your work merges (or you abandon it), move your entry from
#      `active:` to `recently_completed:` (keep the last ~15 entries there,
#      drop older ones -- this is a rolling log, not permanent history;
#      MASTER-TRACKER.yaml's closed_priorities is the permanent record).
#   4. If you discover an active claim that conflicts with what you were
#      about to do, do NOT silently work around it or silently pick a
#      different gap without saying so -- note it in your own session's
#      final report to your Owner/user, the same honesty standard this
#      whole initiative already holds for owner-blocked items.
#   5. Sessions cannot literally message each other -- this file IS the
#      message. Keep entries short, factual, and file/directory-scoped so a
#      session with zero other context can act on them correctly.
# ============================================================================

active:
  - session_label: "Super Boss (Claude Desktop) -- this session"
    claimed_at: "2026-07-14T00:35:00+05:30"
    claim: "GAP-UMR-TABLE-COVERAGE 4th batch -- new drizzle migration onboarding ~15-25 more tables onto compliance.auto_register_asset(), plus ai-os/registry/asset-registry-coverage.yaml update. Scope: a NEW migration file only, no existing file edits besides MASTER-TRACKER.yaml/asset-registry-coverage.yaml (both merge-friendly, append-style)."
    worktree: ".claude/worktrees/agent-ae34ecff115e571b5"

  - session_label: "Super Boss (Claude Desktop) -- this session"
    claimed_at: "2026-07-14T00:35:00+05:30"
    claim: "GAP-DCMD -- investigating whether a genuine 3rd entity_relationships graph edge type exists to wire (candidates: dynamic_chain<->conversation, dynamic_chain<->dynamic_chain parent/child). Scope: src/lib/db/schema.ts (entityRelationships area only) + task-execution-engine.ts + possibly a new small migration. Explicitly NOT touching capability-learning-service.ts or anything Priority-12/13-adjacent (see below) even if investigation surfaces a tempting edge there -- deferred to whichever session owns that area."
    worktree: ".claude/worktrees/agent-a7bff280ef03a1539"

  - session_label: "Unknown session (inferred from git history, not self-registered -- please self-register if this is you)"
    claimed_at: "~2026-07-14T00:27:00+05:30 (inferred from commit timestamps, not a real claim entry)"
    claim: "OPEN-07 (Priority 12, Universal Builder) + OPEN-08 (Priority 13, PROJEXA PM ERP integration) -- large multi-wave initiative. Evidence: PR #282 'worktree-priority12-erp-sales-enforcement' merged, and a commit 'docs: correct OPEN-08 gap analysis, mark Priority 13 in progress'. Scope appears to include: orgProductBranchEnablements/requireBranchEnabled, report-engine-service.ts/report-catalog-service.ts, capability-audit-service.ts/capability-learning-service.ts/capability-tree-service.ts, and the projexa repo directly (Budget/Vendor/Material/Expense/Schedule/Scope modules per OPEN-08's ranked gap list). ALL OTHER SESSIONS: avoid these files/areas until this entry is moved to recently_completed or its owning session updates/removes it."

  - session_label: "claude-code (this session, CLI) -- finishing up, not starting new work"
    claimed_at: "2026-07-13T19:04:00Z (originally claimed ~17:00Z, before this registry existed)"
    claim: "Priority 11 Reports & Analysis ENGINE (taxonomy, generic dispatcher, TABLE_REGISTRY/FORMULA_REGISTRY, 200 live report_definitions across Project+Construction/Interior Design/Sales) -- all code+migrations already merged (PR #272/273/275/277/278) and applied live BEFORE this registry existed. Only remaining piece: PR #279, a docs-only tracker log (ai-os/boss/COMPLETED.yaml WAVE-175, MASTER-TRACKER.yaml closed_priorities #14), currently open, CI-green, blocked only by repeated `main`-advanced races -- no code/schema left to touch. Will move to recently_completed once #279 merges.
      ID COLLISION FLAGGED: PR #279 (created 2026-07-13T18:06:53Z, first) and PR #285 'docs: log Priority 12 OPEN-07 point (8) closure (PR #282) as WAVE-175' (created 19:04:45Z, second) both independently picked the id WAVE-175 for ai-os/boss/COMPLETED.yaml -- these are two different pieces of work, git won't flag it as a text conflict since duplicate YAML keys don't collide structurally. Whichever of #279/#285 merges SECOND needs to renumber its own new entry to WAVE-176 (and closed_priorities entry 15, if it also touches MASTER-TRACKER.yaml) before merging, to avoid a silent duplicate id in the permanent record. Not fixing #285 myself since it isn't my branch -- flagging here per protocol point 4."
    worktree: ".claude/worktrees/priority11-tracker-close"

# Rolling log, last ~15 entries. Drop older ones -- MASTER-TRACKER.yaml's
# closed_priorities is the permanent record, this is just recency context.
recently_completed:
  - session_label: "Super Boss (Claude Desktop) -- this session"
    completed_at: "2026-07-13T23:38:00+05:30"
    claim: "GAP-D15-REMAINING-TRIGGERS (Deployment webhook), GAP-D13-ASSUMPTION-VALIDATION (chat prompt scoping), GAP-UMR-TABLE-COVERAGE 3rd batch (22 tables) -- PRs #241/#242/#243/#244/#280 all merged."
