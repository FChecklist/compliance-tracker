# VERIDIAN AI OS -- entry point / "start here" index.
#
# Gap closure, 2026-07-13 (Boss directive, metadata/drift investigation):
# this file was previously a 9-line stub, itself stale and inaccurate
# (claimed "db: Prisma/SQLite -> PostgreSQL" -- wrong; this codebase uses
# Drizzle exclusively, zero Prisma, per CLAUDE.md's own explicit rule).
# Investigation confirmed there is genuinely NO single unified metadata
# store for this project -- 8+ separate documents each cover a different
# facet (rules vs. what-exists vs. what-should-exist vs. gaps vs.
# completed-log vs. point-in-time health), and MASTER-TRACKER.yaml's own
# header explicitly declines to fold several of them in, for real,
# documented reasons. Merging them into one file would undo those
# deliberate decisions, not fix a gap.
#
# What this file is instead, honestly: the one place that lists every
# other governance/tracking document and what it's actually for, so
# "which file do I check for X" has one answer. scripts/check-metadata-
# index-coverage.mjs enforces that every real governance file below is
# either indexed here or explicitly exempted -- the same registered/
# exempted, nothing-in-neither pattern asset-registry-coverage.yaml
# already uses for database tables, applied here to documents.
#
# This is also, honestly, the closest thing to a "VERIDIAN Brain" pointer
# that exists today. A separate `veridian-brain` GitHub repo exists but is
# a confirmed empty scaffold (README + package.json only, nothing
# extracted from this codebase yet -- see ai-os/system-tree/40-veridian-
# brain.yaml). This file is not that repo and does not claim to be a
# working cognitive architecture -- it is an index, kept honest by CI.

id: AIOS-CT
name: VERIDIAN AI Operating System (compliance-tracker)
version: 3.0.0
repo: FChecklist/compliance-tracker
stack:
  frontend: "Next.js 16 App Router"
  runtime: "Bun"
  db: "Drizzle ORM + postgres.js -> Supabase PostgreSQL (compliance schema) -- zero Prisma, see CLAUDE.md"
  ui: "shadcn/ui"
  styling: "Tailwind CSS 4"
primary_engine: shared (zai + claude-code) # matches ai-os/LIFECYCLE.yaml's own "engine" field -- not zai-only, corrected from the previous stale value

# ===================================================================# GOVERNANCE INDEX -- every real governance/tracking document, one line
# each. If a document isn't in this list, it's either not governance-scoped
# (product docs, specs) or a genuine gap -- check `exempted` below before
# assuming the latter.
# ==========================================================================
index:
  rules_and_authorization:
    - path: CLAUDE.md
      covers: "Codebase orientation + hard rules (Drizzle-only, requireAuth() required, DO-NOT-TOUCH paths). Auto-injected into every Claude Code session's system prompt by the CLI itself."
    - path: AGENTS.md
      covers: "Which AI agents are authorized (Super Boss / Z.ai GLM / Claude Code Secondary), what each can/cannot do, numbered Operating Rules (branch protection, audit requirements, guardrail-change authorization). NOT auto-injected into the z.ai/OpenRouter dispatch pipeline by the platform itself -- scripts/ai-workforce-agent.mjs's fetchGovernancePreamble() now reads and prepends both this file and CLAUDE.md to every z.ai dispatch's system prompt, closing that gap as of 2026-07-13."
    - path: ai-os/sentinel/SENTINEL.yaml
      covers: "Supreme governance rules (validation_rules, e.g. no direct DB access from UI, all changes logged). Never bypassed per AGENTS.md."
    - path: ai-os/boss/BOSS.yaml
      covers: "VEDABOSS project-governance identity/authority definition."
    - path: ai-os/boss/ROSTER.yaml
      covers: "The 3 top-level agent identities (VEDABOSS/ZAI/SENTINEL) -- verified accurate against AGENTS.md 2026-07-09."
    - path: ai-os/engines/ENGINES.yaml
      covers: "Z.ai + Claude Code full-access agent authorization record, dated 2026-06-28 -- overlaps in purpose with AGENTS.md/ROSTER.yaml; AGENTS.md is the actively-maintained one with the numbered Operating Rules, treat this as a historical authorization record."
    - path: ai-os/SELF-CHECK.md
      covers: "GAP-UNIFIED-SOT-REMAINDER (c) mid-session drift: fixed 5-question checklist a session re-affirms periodically (not read-once-at-start like CLAUDE.md). Re-injected on a real cadence for the headless ai-workforce-agent.mjs dispatch path via loop-prevention.ts's shouldPromptSelfCheck(); no equivalent hook exists for the interactive Claude Code/Desktop path -- see VERIDIAN_TASK_GOVERNANCE_CONSTITUTION.md's 'Mid-Session Self-Check' section for the honest per-path breakdown."

  work_tracking:
    - path: ai-os/boss/ACTIVE-CLAIMS.yaml
      covers: "Real-time registry of what each parallel session is actively working on right now, added 2026-07-14 after the Owner confirmed 4 parallel Claude sessions active across this codebase with no way to see each other's in-flight work. Read FIRST, before MASTER-TRACKER.yaml, before picking any gap -- prevents duplicate/colliding work, not data loss (that's Rule 6's PR/CI gate, unaffected). Rolling log, not permanent history -- see its own header for the claim/release protocol."
    - path: ai-os/MASTER-TRACKER.yaml
      covers: "THE single tracker for open items / gaps / work-in-progress. Consolidated 2026-07-12 from 17 prior scattered tracker files (archived, not deleted). Update discipline documented in the file's own header."
    - path: ai-os/boss/COMPLETED.yaml
      covers: "Append-only doer+auditor completion log, per AGENTS.md Rule 7(d) -- every completed task gets a real entry here, by both whoever did it and whoever audited it. Different purpose from MASTER-TRACKER.yaml (a pending-work list) -- this is a permanent historical record, never edited after the fact, only appended to."
    - path: ai-os/boss/completed-work
      covers: "Directory-level entry (per this file's own directory-level indexing convention, see ai-os/audit-tree etc. above) covering per-wave detailed writeups, one .md per wave (e.g. wave9-projexa-data-migration.md, wave10-dns-cutover.md), each referenced by its corresponding brief WAVE-N summary entry in ai-os/boss/COMPLETED.yaml. Found un-indexed (a real pre-existing gap, not introduced by the wave10-dns-cutover.md addition that surfaced it) while closing Wave 10 of the VERIDIAN/PROJEXA brand-layer merge; fixed here rather than left for the next session to rediscover."
    - path: ai-os/boss/BOARD.yaml
      covers: "Older task board. Self-declared stale in its own header since 2026-07-09 (stopped tracking ~114 waves ago) -- superseded by COMPLETED.yaml. Kept for historical reference only, not maintained."
    - path: PLATFORM_STRATEGY.md
      covers: "Living strategic-direction + architecture-decision document at repo root -- 'what VERIDIAN is becoming and what has to be built to get there.' Each major evaluated technology/feature gets its own numbered section (evaluated -> design -> explicitly deferred)."
    - path: ai-os/STATUS-REPORT.md
      covers: "Point-in-time (as of its own 'updated' date) percent-complete rollup reconciling Tree 1 (requirements) against Tree 3 (what's built). Re-derive, don't trust blindly, if significant time has passed."
    - path: ai-os/DCMD-SCHEMA-DESIGN.md
      covers: "Priority 14, GAP-DCMD: per-sub-field architecture design (column vs. entity_relationships graph edge) for the source doc's 9 named DCMD rich-schema fields (business/classification/inputs/outputs/software/AI/workflow/governance/knowledge). Records which sub-fields got real chokepoint wiring vs. schema-only this pass, and 3 candidate graph edges investigated and ruled out for lack of a real chokepoint -- read this before proposing new dynamic_chains columns or entity_relationships edge types for chains."
    - path: ai-os/priority18b_stage0_design.md
      covers: "Priority 18b design doc (design only, not implemented): self-serve, no-admin-approval stage-0 VERI Chat registration off a shared conversation link, passwordless email/Gmail magic-link identity, an inbox strictly scoped to direct messages/assigned to-dos/assigned instructions addressed to that user (not general org channels), and a growth-loop recommendation modeled on the existing Sales Engine referral-link mechanism. Flags one real open decision for the Owner/supervisor (single-org-per-email v1 scope vs. a real multi-org stage-0 membership table) rather than deciding it silently -- read this before implementing Priority 18b."
    - path: ai-os/priority21_workspace_memory_design.md
      covers: "Priority 21, Layer 2 Workspace Memory design + real feasibility check for @memvid/sdk (a genuinely heavy install-time dependency tree -- langchain/llamaindex/OCR/onnxruntime-node, ~392 MB new node_modules weight -- but its actual v1 call surface, create/open with kind: 'basic', never executes those code paths at runtime; also surfaces a default-on external telemetry call, mitigated via MEMVID_TELEMETRY=0). Covers the RLS-safe capsule schema (workspace_memory_capsule_events), export/import flows (additive-only import, never a silent overwrite), and explicitly flags the cross-device sync TRANSPORT (manual download/upload vs. the existing Content Pipeline Google Drive connector vs. a new first-party sync endpoint) as an open Owner decision, not picked silently -- read this before extending Layer 2 or picking a transport."
    - path: ai-os/priority22_ling_trial_findings.md
      covers: "Priority 22 workstream 1: real OpenRouter API comparison (latency/cost/quality/failure-behavior) between inclusionai/ling-2.6-1t and deepseek/deepseek-v4-pro on 3 governance_backend_engineer/chief_audit_officer/chief_operating_officer-shaped backend prompts (corrects the dispatch brief's inaccurate claim about which roster.ts roles run which model -- neither senior_backend_engineer/devops_engineer/architecture_compliance_reviewer nor a QA Engineer/AI Response Validator role are actually on DeepSeek; deepseek/deepseek-r1-0528 does not exist in roster.ts at all). Confirms Ling-2.6-1T is single-provider (Novita only, vs. deepseek-v4-pro's 16) -- a structurally worse version of the 2026-07-10 GLM-5.2/DeepInfra incident, with zero fallback available. Recommendation: do NOT adopt for the 3 real deepseek-v4-pro roles -- 2 of 3 test prompts surfaced real correctness gaps despite Ling being ~5x faster/cheaper. No roster.ts/model-tier-eligibility.ts change made -- read this before proposing Ling-2.6-1T for any roster role."
    - path: ai-os/priority22_officecli_feasibility.md
      covers: "Priority 22 workstream 2: real feasibility check of iOfficeAI/OfficeCLI as a replacement for this repo's mammoth (docx-read)/pptxgenjs (pptx-export) usage. Corrects several prior sessions' copy-forwarded claim that this dependency pair is broken on origin/main -- a fresh isolated bun install/tsc/next build all passed cleanly this session. GO for the mammoth call site (server-side, low risk); conditional-GO for pptxgenjs (its real call site is client-side/in-browser, so replacing it means a real architecture change, not a drop-in swap). No code implemented this pass -- feasibility only, read this before starting any real OfficeCLI integration work."
    - path: ai-os/EXISTING_MODULE_ENGINE_WIRING_MAP_2026-08-02.md
      covers: "Collated + spot-verified reference (2026-08-02) answering 'does existing wiring already cover this?' for anyone working master directive UMR-20260802-034545-3388's priority list, produced by task-20260802-040131 -- collates the pre-existing module/engine/wiring/UTM infrastructure itself (MASTER_INDEX.yaml, system-tree, wiring_registry, etc.), spot-checking each claim against current source rather than copying wholesale. Collation/verification only, no code touched."
    - path: ai-os/tasks/sap_mapping
      covers: "New directory (2026-07-28), first entry under ai-os/tasks/ -- per-workstream deliverable output distinct from the ai-os/tasks/task-<id>/ per-session worker directories (which live outside this git repo, under the host's ai-os task-runner tree, not here). Contains PHASE_0_BASELINE.yaml: the SAP-informed VERIDIAN Phase 0 baseline inventory -- 213 business-domain DB tables + 508 API routes categorized across 8 SAP-mapped domains (CRM/Sales/Purchase/Inventory/Accounting-GST/HR/PM/Helpdesk/Construction-BoQ), plus PROJEXA's module-chain exposure state. Also contains PHASE_1_CLASSIFY.yaml (2026-07-29): the audit trail for classifying all 80 rows of the live /opt/veridian/ai-os/memory/sap_mapping.sqlite sap_reports table into an engine_track column (calculation/workflow/hybrid) -- the actual engine_track values live in that shared sqlite file, not in this repo; this yaml is the evidence-based verification record (per-row content check, not self-certified) plus the note that the classification work itself was done by an untracked process before this task's own checkpoints ever logged it. Read before starting any later SAP-mapping phase (gap analysis, field-level mapping, etc.) so that phase builds on this baseline instead of re-deriving it."

  what_should_exist_vs_what_does:
    - path: ai-os/audit-tree/
      covers: "Tree 1 -- what the 9 source requirement documents say VERIDIAN should be (01-09 per-document trees + 10-merged-tree.yaml)."
    - path: ai-os/system-tree/
      covers: "Tree 3 -- what's actually built in the live codebase, per real file/route grep (10-13 = compliance-tracker governance/API/DB/UI, 20/30/40 = PROJEXA/veda-advisors/veridian-brain, 50-merged-tree.yaml = the merge)."
    - path: ai-os/tree4-unified/
      covers: "Tree 1 + Tree 3 merge and the original gap backlog this analysis produced -- mostly archived into MASTER-TRACKER.yaml now (see that file's meta.source_files_archived list); 50-completion-plan/ is historical."

  health_and_compliance:
    - path: ai-os/sentinel/HEALTH.yaml
      covers: "Point-in-time spot-check (CI green, no committed secrets) -- explicitly NOT a continuous monitor per its own note. Re-run before trusting if time has passed since last_check."
    - path: ai-os/sentinel/VIOLATIONS.yaml
      covers: "Point-in-time spot-check scan results, same 'not a full sweep' caveat as HEALTH.yaml."
    - path: ai-os/registry/asset-registry-coverage.yaml
      covers: "UMR (Universal Metadata Registry) coverage manifest -- every DB table must be listed registered or exempted, enforced by scripts/check-asset-registry-coverage.mjs on every PR. Row-level registration is genuinely automatic (a live Postgres trigger, zero human/AI touch per row); table-level onboarding is auto-VERIFIED but not auto-performed -- a new table needs a one-time migration + registry entry before the trigger covers it."
    - path: ai-os/registry/ARTIFACTS.yaml
      covers: "Enterprise Asset Registry scaffold. NOT YET POPULATED -- a real, named, still-open gap, not silently dropped."
    - path: ai-os/registry/maintainability-scorecard.md
      covers: "VERIDIAN Review Framework gap-closure (2026-07-18): pointer + methodology doc for the consolidated Maintainability score (guardrail-violation rate + continuous-improvement backlog + dependency health, all real signals already persisted elsewhere) -- live number via GET /api/ai/team/maintainability-dashboard (veridian_admin-only), computed by src/lib/services/maintainability-dashboard-service.ts. This doc is not itself a data snapshot -- query the API for the current score."
    - path: ai-os/registry/stale-doc-manifest.yaml
      covers: "GAP-UNIFIED-SOT-REMAINDER (b) quarantine manifest -- every root-level stale doc moved to docs/archive/ plus every file already under the tree4-unified/audit-tree archive dirs, each must carry the exact ARCHIVED/STALE banner, enforced by scripts/check-doc-quarantine-banner.mjs on every PR."
    - path: ai-os/registry/terminology-guardrail-exemptions.yaml
      covers: "Phase 3 CI enforcement wiring (task-20260724-133911): CI-enforced exemption manifest for scripts/check-terminology-guardrail.mjs's terminology guardrail, same reviewable-diff-ratchet pattern as asset-registry-coverage.yaml above. Backfilled into this index 2026-07-26 -- pre-existing drift found while fixing PR #563's CI (unrelated to that PR's own diff)."
    - path: ai-os/registry/deterministic-first-audit-log.yaml
      covers: "VERIDIAN Review Framework gap-closure (task-20260718-115004-retry-1--ai-engineering-quality--logic), [Low] Deterministic Logic Coverage: coverage manifest for scripts/check-deterministic-first-audit.mjs, same registered-or-exempted-nothing-in-neither pattern as asset-registry-coverage.yaml above. Every src/ file that calls callLLM()/callLLMJson()/callLLMVision() must appear in the manifest's `sites` (real deterministic-alternative audit note) or `exempted` (real reason auditing doesn't apply) list; a new unaudited call-site file fails CI. Check only evaluates new/changed files (does not retroactively fail CI on the 25-file baseline census this manifest was seeded with)."
    - path: ai-os/GOVERNANCE_RECORD_HARD_RULE_7_VIOLATION_PR886_2026-08-05.md
      covers: "Real, permanent governance record of the SEC-07/Hard Rule 7 violation on PR #886 (UMR-20260805-025349-a6b8): technical fix stays merged, no retroactive authorization granted, violation stays a permanent part of audit history. Explicit status block (technical/governance/operational/audit axes). Does not alter SEC-07's own lock text in ai-os/CONSTITUTION.yaml."
    - path: ai-os/GOVERNANCE_RECORD_TEMPORARY_REVIEW_COUNT_EXCEPTION_2026-08-05.md
      covers: "Real, permanent governance record of a temporary, explicitly bounded exception to the branch-protection review-count requirement (UMR-20260805-091648-6793): required_approving_review_count reverted 1 -> 0 after independently confirming real independent review had become structurally impossible (single GitHub identity, GitHub refuses self-approval, enforce_admins blocks any bypass), unblocking ~12 real, already-fixed queued PRs. Scope explicitly bounded to that backlog only. Explicit re-enable trigger recorded: the moment UMR-20260805-091629-d8e3 (real second reviewer identity) merges."
    - path: ai-os/registry/ocid-locked-scope-manifest.yaml
      covers: "Real file-path scope manifest for scripts/check-sec07-ocid-lock.mjs's pre-merge SEC-07 enforcement gate (UMR-20260805-025554-46f9) -- lists real files known to belong to each locked OCID-038/039/040/041-046's own implementation scope, seeded from PR #886's own real diff. See that file's own header for why OCID-039/040/041-046 start with empty path lists (zero real implementation exists yet under them)."
    - path: ai-os/registry/sec07-overrides.yaml
      covers: "Real, narrowly-scoped Owner-override registry for scripts/check-sec07-ocid-lock.mjs -- each entry authorizes exactly one real PR number to bypass the SEC-07 pre-merge lock, added via its own separate, independently-reviewed PR, never the same PR requesting the bypass. Currently empty (no overrides granted)."
    - path: ai-os/registry/PENDING-MANUAL-APPLICATION-sec07-ocid-lock-check.yml.txt
      covers: "The real, complete, ready-to-use GitHub Actions workflow for the SEC-07 pre-merge gate (scripts/check-sec07-ocid-lock.mjs), staged at this non-.github path because the GitHub token available to automated sessions on this platform lacks the `workflow` OAuth scope (confirmed real, documented constraint -- see claude-control's SUPERBOSS_DISPATCH_PROMPT.md's own 'Hard rule: never modify .github/workflows/** directly in a worker task'). NOT YET ACTIVE -- needs one real manual `git mv` into .github/workflows/ by someone with real workflow-scope credentials, per this file's own header."
    - path: ai-os/registry/PENDING-MANUAL-APPLICATION-reviewer-not-author-check.yml.txt
      covers: "The real, complete, ready-to-use GitHub Actions workflow for the reviewer-not-author identity guard (scripts/check-reviewer-not-author.mjs, UMR-20260805-091629-d8e3), staged at this non-.github path for the same real workflow-OAuth-scope constraint as the SEC-07 workflow above. NOT YET ACTIVE -- needs one real manual `git mv` into .github/workflows/ by someone with real workflow-scope credentials, per this file's own header. Also cannot meaningfully fire yet regardless -- see ai-os/REVIEWER_IDENTITY_PROVISIONING_GAP_2026-08-05.md for why."
    - path: ai-os/REVIEWER_IDENTITY_PROVISIONING_GAP_2026-08-05.md
      covers: "Real, honest record (UMR-20260805-091629-d8e3) of what this session built (scripts/check-reviewer-not-author.mjs, its tests, and its staged workflow) versus the real, structural identity-provisioning gap it could not close itself (no admin:org/App-management scope available anywhere in this system) -- with a precise recommendation for the Owner's one required action: create a GitHub App with exactly `Pull requests: Read & write` + `Contents: Read` (no write), installed on compliance-tracker only."
    - path: ai-os/registry/OCID-055-repository-register.md
      covers: "OCID-055 (UMR-20260805-084109-2786, reusing UMR-20260804-035817-6300): real, evidence-based universal repository register/classification/dependency register/relationship graph/documentation audit across all 15 real repositories on the FChecklist GitHub account (read-only discovery/cataloging/classification phase only -- no visibility or ownership changes made). Includes a findings-for-Owner-decision section (4 public-visibility items) awaiting explicit Owner review."
    - path: ai-os/LIFECYCLE.yaml
      covers: "Which of the 11 lifecycle stages (UNDERSTAND..IMPROVE) the project is currently in. Currently MONITOR (ongoing operation, not a one-time build)."
    - path: scripts/check-doc-cross-references.mjs
      covers: "Gap closure, 2026-07-13: link-validity check for the 6 entry-point governance docs (CLAUDE.md, AGENTS.md, this file, docs/master/INDEX.md, ai-os/BRAIN.md, VERIDIAN_TASK_GOVERNANCE_CONSTITUTION.md) -- verifies every file path they name (YAML `path:` keys, markdown links, or backtick-quoted paths in prose) still resolves on disk, enforced on every PR. Distinct from the other 4 coverage gates in this section: those check registration coverage (is X registered in a registry); this checks link validity (does a path someone actually named still exist)."
    - path: ai-os/INCIDENT_11K_API_CALLS_RCA.md
      covers: "Point-in-time RCA (2026-07-19), same genre as HEALTH.yaml/VIOLATIONS.yaml -- a dated investigation report, not a living doc. Root-causes the Owner's 'an api called 11000 times' report to 11,165 real Anthropic Messages-API calls on 2026-07-18 alone (this box's own Claude Code session transcripts), driven by 574 parallel Claude Code sessions that single day vs. 14 the day before -- confirmed unrelated to OpenRouter/Groq/GitHub Actions/costCapEnforcementEnabled. Cites the real per-invocation --max-budget-usd cap added to worker-entrypoint.sh (server-side, not git-tracked) and the AI_TEAM_LOG_SECRET Vercel gap fixed alongside it."
    - path: ai-os/RCA_OVERNIGHT_DISPATCH_FAILURE_2026-07-20.md
      covers: "Point-in-time RCA (2026-07-20), same genre as the above. Root-causes why the overnight 2045-framework gap-closure mission's execution phase never ran (PR #487's plan merged, then zero dispatch for ~8.7 hours): the active-dispatch mechanism was a Claude Desktop LOCAL scheduled task, which only fires while that app is open -- confirmed via list_scheduled_tasks showing 1 lastRunAt in ~9 hours. Prevention implemented: queue-dispatcher.py re-enabled in crontab (server-side, cron-driven, laptop-independent), CONCURRENCY_CAP raised 3->5, build_prompt() extended for pre-scoped full_prompt items, gap_queue.yaml populated from the 25-task v2 plan -- live-verified via a manual run that dispatched exactly 5 real workers."
    - path: ai-os/SUPERBOSS_IMPLEMENTATION_PLAN_2026-07-19.md
      covers: "Point-in-time plan (2026-07-19), Super Boss (GLM-5.2) evaluation+planning ONLY -- no implementation. Real inventory of every genuinely pending actionable item across the VERIDIAN AI OS project (grounded in live-read ACTIVE-CLAIMS.yaml / MASTER-TRACKER.yaml / claude-control CONTROLLER.yaml / open-PR lists / systemctl+tasks state, not guessed), an explicit collision/duplication check (19 in-flight/done items excluded; the 9-workstream 'VERIDIAN Review Framework implementation wave' flagged BLOCKED on uncommitted laptop-worktree work the Owner must push or abandon), and 11 prioritized task-shaped dispatch units (L1-L4 per SOFTWARE_TEAM.md, all resolving to GLM-5.2 via OpenRouter per Owner instruction). A dated checklist a supervising process reads every 15 min and a future dispatch script picks the next ready task from -- not a living spec; supersede by a newer-dated plan."
    - path: ai-os/SUPERBOSS_IMPLEMENTATION_PLAN_2026-07-19_v2.md
      covers: "Point-in-time plan (2026-07-19, SUPERSEDES the v1 plan above for the subset v2 re-scoped). Super Boss (Claude, this session) REEVALUATE-2045-FRAMEWORK-AND-PLAN: re-scored all 2045 rows of claude-control/VERIDIAN_Review_Framework_evaluated_2045rows.csv against what ACTUALLY shipped since 2026-07-16 (Mother Router Phase 1 #433, Software Team L0-L5 #483, GLM-5.2/OpenRouter routing, the 9-workstream Wave B found ALREADY shipped server-side so no re-do needed), produced the real current gap count (188 No-Gap closed + 75 of 111 previously-deferred rows re-decided under Owner-delegated decision authority: 42 stays-deferred-needs-money, rest decidable/closable), the full 75-row decision log (a/b/c split), and the prioritized L1-L4 task-shaped dispatch units for every genuinely-open genuinely-closable-without-money row. Docs-only output from this task; downstream dispatch to GLM-5.2 via Mother Router software_team scope is the separate execution step. Supersede by a newer-dated plan."
    - path: ai-os/REVIEW_FRAMEWORK_DECISIONS_2026-07-19.md
      covers: "Point-in-time decision-of-record (2026-07-19), the durable evidence for the subset of SUPERBOSS v2 §2 decision-log rows that close WITHOUT code -- one decision-paragraph per row (decision + rationale + authority basis), grounded in the CSV row's own Recommendation/Alternative Solutions text. Rows recorded: D7/D11 (CRM/ERP connector hold-and-document, build Tally only on a real sales blocker), D9 (no second independent cost source at current team size), C13 (bank credential target lowered to 3, reuse ai-config-crypto.ts when bank-API prioritized), C16 (market-fit deferred -- not yet applicable, no paying-customer base), C17 (OPENAI_API_KEY provisioning = formal Owner-action request, code already exists), C18 (metadata-driven = no action, deliberate strategic non-goal), C19 (Supabase IPv4 -- do the free half; the ~$4/mo add-on stays deferred as real money per §2(a)). The decision evidence backing the CSV re-score; not a living spec."
    - path: ai-os/REVIEW_FRAMEWORK_V2-8_MOBILE_UX_CROSSREF_2026-07-20.md
      covers: "Point-in-time cross-reference close (2026-07-20), V2-8-MOBILE-UX-CROSSREF, decision D10. Closes CSV row #1790 (Mobile/Responsive: field-usable Site Diary + Attendance UX) by confirming the consuming frontend lives in the sibling projexa repo (src/app/(app)/site-diary/page.tsx -> SiteDiaryClient.tsx, src/app/(app)/labour/page.tsx 'Manpower & Attendance' -> LabourClient.tsx) which calls compliance-tracker's /api/v1/projexa/{site-diary,attendance} aliases -> the real construction-site-diary-service + attendance engines here -- the intended PLATFORM-01 repo split, not a missing build. Re-scores the row off 'Needs Owner Decision' to 'Decided -- cross-repo scope confirmed, no in-repo build.' Honest nuance: the projexa screens exist but are not yet mobile-tuned (desktop tables, no field touch-target variant, no offline queue) -- those are SEPARATE open rows #106/#1792/#1793/#1794, NOT closed by this doc, tracked there. The optional 'open in PROJEXA' deep-link was deliberately NOT added (no stable deployment URL + requires authed project context = speculative Tier-2 change with no confirmed need, same anti-scope-inflation reasoning as D7/D9). Docs-only Tier1."
    - path: ai-os/STAGING_ENV_2026-07-20.md
      covers: "Point-in-time design + smoke-test doc (2026-07-20) for SUPERBOSS v2 task V2-7 / CSV rows #37/#39 (D8 persistent staging env + per-env var scoping). Records live-infra ground truth verified against the Vercel REST API on 2026-07-20 (account plan=Hobby; Hobby caps custom environments at 0, API returns 400 'Cannot create more than 0 custom environments'; 3 system envs; 19 env vars already per-target scoped), CORRECTS the v2 plan's D8 line that a named staging custom env is 'already supported, no extra spend' (that requires Pro/$20-mo, a paid change V2-7 forbids), and specifies the tier-honest Hobby-compatible design: a long-lived `staging` git branch (Vercel auto-deploys each push as a preview) + staging-only env vars scoped via the free env-var API `gitBranch` field (target=[\"preview\"], gitBranch=\"staging\"). Includes the staging-preview smoke-test expectation (5 checks) and open items for the Owner. The decision evidence backing the CSV re-score for rows #37/#39; not a living spec."
    - path: ai-os/REVIEW_FRAMEWORK_V2-3_VERIFY_FIXED_ASSETS_CHANGE_ORDERS_2026-07-20.md
      covers: "Verification evidence note (2026-07-20) for task V2-3-VERIFY-WAVEB-SHIPPED from the v2 plan above. Confirms the already-shipped Fixed Assets surface (src/lib/services/erp-fixed-assets-service.ts: full CRUD + shared Approval Workflow Engine for disposal + depreciation-schedule business-rule validation with mid-period proration/true-up/salvage floor + draft->in_use->disposed/scrapped state machine) closes CSV rows #43 (CRUD & Approval Workflow, W4 Critical) and #44 (Business Rule & Validation, W5 Critical), and the already-shipped Change Orders e-signature auto-transition (construction-change-order-service.ts draft->pending_approval->approved/rejected via submitChangeOrderForApproval + esignature-service.ts submitSignature/declineSignature allSigned/decline transitions; one-click approve/reject bypass branches removed) closes row #59 (CRUD & Approval Workflow / e-sig auto-transition, W4 Medium). All three re-scored No-Gap. Additive behavior-preserving helper extraction (computeSignatureRequestStatusAfterSign / changeOrderTransitionAfter) + esignature-service.test.ts (17 tests) shipped alongside; no schema/auth/RLS changes (Tier1)."
    - path: ai-os/SUPERBOSS_2045_MISSION_COMPLETE_2026-07-20.md
      covers: "One-time closeout report (written 2026-08-01) for the 25-item SUPERBOSS_V2_PLAN gap-closure mission dated 2026-07-20, standing in for a recurring 15-minute auditor task that was deleted after never once producing this report itself. Real per-item verdicts against /opt/veridian/ai-os/gap_queue.yaml (4 completed, 11 stuck_needs_human, 10 skipped_possible_duplicate), re-derived from live gh pr view/checks state rather than the queue's own status labels -- corrects several mislabeled/stale entries (e.g. v2-2/v2-6/v2-14 approved-but-unmerged or already-merged-since-noted) and discloses that most of the 10 'skipped_possible_duplicate' items have no real duplicate citation, just an unretried balance-exhaustion block. Point-in-time closeout; not a living spec."
    - path: ai-os/V2-12_SERVERLESS_RESOURCE_LIMITS.md
      covers: "Point-in-time doc + audit (2026-07-26) for SUPERBOSS v2 task V2-12-SERVERLESS-LIMITS / CSV row #13 (Vertical Scalability), redispatched after an original ~2026-07-20 attempt was blocked pre-flight by a spend-governance gate. Documents the live Vercel Hobby-plan limits this repo actually runs under (300s fixed max duration -- no override possible on this plan tier, 4.5 MB request/response body cap on every plan, 2 GB memory; confirmed zero `maxDuration`/`functions` overrides anywhere in this codebase today) and audits payroll-run, report-generation, and bulk-op routes against them, widened to every awaited-DB-call-in-a-loop pattern found. Five HIGH-severity N+1 routes found (`processPayrollRun`, `bulkMarkAttendance`, `compliance/import` CSV importer -- the one with no file-size cap at all, `computeCostOverrunReport`'s ~500-project fan-out, `gst-reconciliation` batch-confirm), plus a real discovered bonus finding (four upload routes advertise a 10 MB file-size cap when Vercel's actual platform ceiling is 4.5 MB on every plan). Recommends a shared cheap batching fix for all five as the first escalation, with `computeCostOverrunReport` flagged as the one genuine queue/background-job candidate if that fix alone proves insufficient. Docs-only Tier1; the recommended follow-ups are filed, not built, in this doc."
    - path: ai-os/MIGRATION_DRIFT_AUDIT_2026-07-26.yaml
      covers: "Point-in-time audit + reconciliation (2026-07-26), same genre as the RCA docs above. Root-causes why drizzle.__drizzle_migrations had 0 rows despite hundreds of live tables: drizzle/meta/_journal.json was frozen at its original single migration-0000 entry since first commit, so drizzle-kit's readMigrationFiles() never considered migrations 0001-0263 to exist. Cross-referenced all 261 real migration files against live schema on pcrjmlpuqsbocqfwoxod (249 already present, 12 genuinely missing and applied live, 1 false positive explained as intentional later drop). Repaired drizzle/meta/_journal.json (dense 0-260 idx) and populated drizzle.__drizzle_migrations with 261 correct rows. Also documents migration 0245's mid-history relocation of dynamic_chains (and 21 other tables) from compliance to a new platform schema, which required retargeting 2 still-pending migration files (0140, 0199) and fixing an unqualified ai_provider enum reference (0253) so a fresh bootstrap replay matches what was actually applied live -- see those files' own history. Known open gap flagged, not fixed here: drizzle.config.ts's schemaFilter is still ['compliance'] only, stale for the 22 relocated platform-schema tables (application-schema decision, out of this audit's scope)."

    - path: ai-os/PREVIEW_DEPLOYMENT_SPOTCHECK_2026-07-26.md
      covers: "Point-in-time spot-check note (2026-07-26) for SUPERBOSS v2 task V2-14-PREVIEW-SPOTCHECK, verifying real PR #571 guardrail-registration coverage as the current open PR (redispatch of a 2026-07-20 task that never started)."

    - path: ai-os/VERIDIAN_V2_DEFENSE_IN_DEPTH_TOOL_EVALUATION_2026-07-26.yaml
      covers: "Point-in-time tool evaluation (2026-07-26) for VERIDIAN_Architecture_v2.0 phase_4, Defense-in-Depth Security/Guardrails tool mapping and custom-work assessment."

  brain_pointer:
    - path: ai-os/CONSTITUTION.yaml
      covers: "v2.0, 2026-07-14 (Owner directive: 'only 1 constitution... machine language... no assumption, audit everything'). THE SOLE CONSTITUTION for VERIDIAN AI OS -- every rule/guardrail from 9 previously-separate 'constitutional' documents (VERIDIAN_AI_CONSTITUTION.md, VERIDIAN_TASK_GOVERNANCE_CONSTITUTION.md, VERIDIAN_AUDIT_ORGANIZATION.md, VERIDIAN_DMP_DCF_CONSTITUTION.md, VERI_CHAT_GOVERNANCE.md, MASTER_AI_OS_ARCHITECTURE.md, SENTINEL.md, ai-os/sentinel/SENTINEL.yaml, VAIOS_ARCHITECTURE_STRATEGY.md) now lives here with a stable ID, machine-first, no narrative prose. Read this FIRST, before BRAIN.md or any prose doc. Those 9 docs keep their full narrative/evidence content and each now carries an AUTHORITY NOTE pointing back here -- read them for WHY, this file for the RULE."
    - path: ai-os/BRAIN.md
      covers: "PLATFORM_STRATEGY.md Sec31.4 Phase A item (2): the single plain-language 'what VERIDIAN AI OS is and how it works end-to-end' document that did not exist before 2026-07-13 -- traces a real request flow (VeriComposer -> POST /api/tasks -> resolveModelConfig -> callLLM) and an AI Dev Team dispatch flow, then names the 5 real separate systems (roster.ts, orchestra-model-resolver.ts+model-tier-eligibility.ts, embeddings.ts, activity_log, escalation-ladder.ts+monitor-protocol.ts) that compose into something brain-like without being one. Every claim cites a real file it was read from; start here before system-tree/ or OS.yaml itself."
    - path: ai-os/system-tree/40-veridian-brain.yaml
      covers: "Confirmed status of the separate veridian-brain repo -- empty scaffold, not yet extracted from this codebase. Read this before assuming 'the Brain' is a working system anywhere."
    - path: ai-os/AI_ORCHESTRA_HIERARCHY.md
      covers: "Owner spec (2026-07-19): formal 4-domain x L0-L5 operational hierarchy (Software Development / End User Work Management / Sales & Marketing / Customer Success), mapped to the real AiRouterScope values and registry-backed model resolution. Model names in it are live snapshots, not hardcoded bindings -- see ai_orchestra_tiers.domain_hierarchy_ref in CONSTITUTION.yaml for how this document's axis (role/domain escalation) differs from that section's axis (model-trust tier)."
    - path: ai-os/SOFTWARE_TEAM.md
      covers: "AIROUTER-01 Phase 2 (2026-07-19): plain-language explainer of the real, BUILT implementation behind AI_ORCHESTRA_HIERARCHY.md's Table 1 (Software Development) -- the L0-L5 dispatch wiring (software-team-ladder.ts), the Instruction Contract/Execution Report task register (instruction-contract.ts, task-register-service.ts, platform.task_register), and the capability-based routing matrix seeded into ai_routing_policies (drizzle/0250). Mirrors ai-os/BRAIN.md's style; includes the 3-round GLM-5.2 audit summary and disclosed remaining gaps."
    - path: ai-os/AIROUTER_SOFTWARE_TEAM_AUDIT_LOG.md
      covers: "Append-only log of all 3 mandatory GLM-5.2 independent-audit rounds for AIROUTER-01 Phase 2 (Software Team L0-L5) -- real findings, real fixes applied per round, verbatim. Read this for the honest 'what did an independent reviewer actually find, and what stayed unfixed' record, not just the PR description's own summary."

  monitoring_health_check:
    - path: /api/ai/team/governance-health
      covers: "Real, queryable governance health endpoint (veridian_admin-gated): Reasoning Quality / Dependency Health / Compliance scores from real reviewer outcomes, plus (as of 2026-07-13) stuckActivities -- AI Team dispatches that have sat in a non-terminal lifecycle stage past a stated threshold with nobody having closed them out."

  # Backfilled 2026-07-26 while fixing PR #563's CI: scripts/check-metadata-
  # index-coverage.mjs was failing on 56 top-level ai-os/ files/dirs that
  # accumulated on `main` over time without ever being added here (a
  # pre-existing gap, unrelated to PR #563's own diff -- reproduced
  # independently against bare origin/main HEAD before writing this).
  # Descriptions below are drawn from each file's own header/docstring, not
  # invented.
  reference_docs_and_catalogs:
    - path: ai-os/2045-GAP-CLOSURE-10-DOLLAR-PLAN.md
      covers: "$10 execution-budget dispatch plan for the row-level CSV gap-closure backlog, ready to fire once OpenRouter credits are available -- a spec, not yet dispatched as of writing."
    - path: ai-os/AI_CACHE_AND_TRIAGE_ARCHITECTURE.md
      covers: "AI-agent-facing (not human-narrative) reference (v1, 2026-07-20): caching + request-triage architecture for lowest-token-cost dispatch, covering both the end-user product path and software gap-closure dispatch."
    - path: ai-os/AI_ENGINEERING_POLICY.yaml
      covers: "Owner directive 2026-07-21: machine-readable, mandatory AI engineering policy applying across the whole integrated system (FChecklist, VERIDIAN AI OS, compliance-tracker, PROJEXA, server, GitHub, Vercel, Supabase)."
    - path: ai-os/AI_ROSTER_CATALOG.json
      covers: "Auto-generated (extract-ai-roster-catalog.mjs, dynamic import of the real AI_TEAM_ROSTER export) catalog of all 195 AI Team roles by team -- ground truth from src/lib/ai-team/roster.ts, not hand-written."
    - path: ai-os/COST-CONTROL.md
      covers: "As-built implementation record (2026-07-20) of the zero-waste AI-credit control mechanisms -- every mechanism documented was tested against the live server before being called done."
    - path: ai-os/DATABASE_CATALOG.json
      covers: "Auto-generated (extract-db-schema-catalog.mjs, drizzle-orm getTableConfig introspection) catalog of every table/enum in schema.ts -- ground truth from live code, not AI-written descriptions."
    - path: ai-os/FUNCTION_CATALOG.json
      covers: "Auto-generated (extract-function-catalog.mjs, TypeScript compiler AST) catalog of every top-level function/arrow-const/class-method across src/**/*.{ts,tsx} -- ground truth from live code."
    - path: ai-os/BROWSER_LITE_LLM_TECH_DECISION_2026-07-27.md
      covers: "phase_5_browser_execution_tiers required technology decision (engine-browser-lite-llm): adopt WebLLM for real browser text-generation LLM inference; keep LiteRT.js (litert-spike) unchanged in its existing vision-classifier role."
    - path: ai-os/VERIDIAN_V2_DSPY_TECH_DECISION_2026-07-27.md
      covers: "phase_8_dspy_learning_distribution_engines required technology decision (engine-dspy-integration): DSPy evaluation verdict + engine-ai-learning wiring onto capability-learning-service.ts."
    - path: ai-os/BROWSER_EXECUTION_TIERS_INCREMENT_2_STATUS_2026-07-27.md
      covers: "phase_5_browser_execution_tiers increment 2 status: WebLLM install, MCP/function/worker/transformers engine wiring, stack deepening."
    - path: ai-os/GAP_ANALYSIS_2026-07-20_HOLD.md
      covers: "Analysis-only doc on the 185 open Review-Framework CSV rows. Owner directive 2026-07-20 explicitly paused dispatch on these (mechanically enforced, see doc's own Enforcement section) -- not a live work queue."
    - path: ai-os/PROCUREMENT_ERP_GAP_ANALYSIS_2026-07-31.md
      covers: "Analysis-only doc cross-referencing the 10 procurement-erp-reverse-engineering reference phase docs against this repo's live 95 erp_*-prefixed tables. Prioritized real-gap list (Gate Pass, Divisions tier, HSN/SAC-to-GST wiring, UOM master, requisition cost-center/OPEX-CAPEX, multi-state supplier GST, GRN-optional invoices, Debit Notes) pending Owner review before dispatch -- not a live work queue."
    - path: ai-os/VERIDIAN_KERNEL_1.0_RECONCILIATION_REPORT_2026-08-02.md
      covers: "Reconciliation report for the Owner-supplied VERIDIAN_KERNEL=1.0 spec (a condensed operating-model description for the PM/Planner/Dispatcher/Worker roles and dispatch pipeline): registers it as a proposed, cross-referenced operating-model doc (see MASTER_INDEX.yaml registries: veridian_kernel_1_0), explicitly NOT supreme over ai-os/CONSTITUTION.yaml -- applies the Kernel's own KERNEL_CONFLICT rule to that specific question and stops rather than deciding it unilaterally. Includes an RCA on real pre-existing drift found during the pass (a dangling PROTOCOL_OWNER_AI.yaml citation; three incompatible task-state vocabularies) and a gap analysis distinguishing what the Kernel already has a real mechanism for vs. genuine gaps. See its own amendment_log for the 2026-08-02 PM role-clarification amendment (PM=Claude Desktop/Super Boss per AGENTS.md's existing 2026-07-10 authorization, Executor=Claude Code CLI both interactive and headless)."
    - path: ai-os/IMPLEMENTATION_MATRIX_2026-08-02.md
      covers: "Real, evidence-based 14-item implementation matrix (UMR-20260802-104058-25ba + scope extension UMR-20260802-105532-775a): the 12 top-level deliverables (ERP Modules, UI/UX, Reports, Prompt Library, Web Browser, VERI Chat, VERI assistant, Multi Tenant, Multi Brand, Kernel, End-to-End Testing, Go Live) plus 2 UMR-level verifications (module/wiring collation UMR-20260802-040056-5319, Kernel reconciliation report UMR-20260802-054239-4251). Every item cites real commit hashes/file paths/table counts gathered by 5 independent research passes against live server state -- no UMR/task status label used as evidence, no completion inferred from governance/paperwork alone. Point-in-time audit; not a living spec."
    - path: ai-os/PROJEXA_AI_COM_E2E_CERTIFICATION_REDO_2026-08-02.md
      covers: "OCID-020 end-user certification redo (UMR-20260802-165606-4413) against the real correct target: projexa-ai.com, which serves veridian-compliance-ai per the Wave 10 domain revert (UMR-20260802-134939-145d). Documents the honest 0/107 result of running PROJEXA's own 22-spec suite against the wrong DOM (target mismatch, not product bugs), plus real findings from a correctly-targeted authenticated click-through: a high-severity reproducible crash (Compliance Register / Pendency View, HTTP 500 on GET /api/departments) and medium-severity 403s for a fresh self-signup org. Point-in-time audit; not a living spec."
    - path: ai-os/PROJEXA_AI_COM_E2E_CERTIFICATION_CONTINUATION_2026-08-02.md
      covers: "OCID-020 continuation (UMR-20260802-223152-0b6a) PM-decision writeup: independently verifies the credit accountant's rejection of a blind auto-fix retry for a docs-only-diff build-gate timeout was correct (quality-gate.sh's own timeout-as-failed-gate design already covers it); corrects a premature self-citation of an in-flight PM-decision UMR as an already-applied decision; and reports real results read from an already-completed but never-read background nav-sweep run -- multi-tenant isolation PASS, GAP-ERP-CRM-403 reconfirmed, a new GAP-EMAIL-INTELLIGENCE-500-VS-403 finding, and a nav sweep mostly invalidated by a browser-process crash under host contention (not a product defect). Point-in-time audit; not a living spec."
    - path: ai-os/VERIDIAN_MOBILE_PWA_AND_VERI_CHAT_RUNTIME_2026-08-03.md
      covers: "OCID-20260803-025, documentation only. 36-section real inventory of the mobile PWA (src/app/manifest.ts, installable share-target PWA), VERI Chat, VERI the assistant, mode pills / Chain Selector, the software-first AI escalation gate (llm-routing-gate.ts -> ai-reply-gate.ts), the deterministic task model, and offline/cache/sync/push/session-recovery -- each area graded honestly against real code (real IndexedDB composer-history cache and conflict-resolution primitives exist and are largely unwired; push notifications and session recovery do not exist at all; one working offline photo-capture/sync flow exists but lives in the sibling projexa repo, not here). Amends ai-os/IMPLEMENTATION_MATRIX_2026-08-02.md in place rather than duplicating it. No implementation performed."
    - path: ai-os/VERIDIAN_END_USER_EXPERIENCE_FOUNDATION_2026-08-03.md
      covers: "OCID-20260803-022 (UMR-20260803-040844-4a33, real dispatch UMR -- corrected 2026-08-05 per UMR-20260805-083603-9efa, see GAP-SELF-MINTED-ARTIFACT-UMR-FABRICATION; parent UMR-20260802-173631-ca85, the ERP Functional Completeness Master Program): the first canonical end-user-experience foundation doc -- a real, evidence-based synthesis of what an end user actually sees today (nav shell, Dynamic Mode Pills/Chain Selector, VERI Chat/VERI, multi-tenant isolation, multi-brand/PROJEXA reality) plus a catalogue of already-documented and newly-named end-user-facing gaps. Documentation only -- no code/DB/UI/UX change. Point-in-time; will go stale as real work lands."
    - path: ai-os/PROJEXA_AI_COM_E2E_CERTIFICATION_RESUME_2026-08-03.md
      covers: "OCID-020 resume (UMR-20260803-073007-06a1) PM-decision writeup: independently re-verifies PR #755 is genuinely merged (task-20260802-231454's own checkpoint was a stale pre-merge read, not a real merge failure -- no re-merge attempted); real, measured host-load check (load average 10.23 on an 8-core box, 3.7Gi/4Gi swap in use) taken before resuming the multi-navigation Playwright sweep, per the prior continuation doc's own explicit recommendation to check host load first after 2 consecutive sweep failures under contention; decision to defer the heavy sweep rather than risk a 3rd invalidated run, with a concrete handoff (per-batch browser health-check/restart, ~10-15 navigations per instance) for whoever resumes it once load allows. Point-in-time audit; not a living spec."
    - path: ai-os/PROJEXA_AI_COM_E2E_CERTIFICATION_NAV_SWEEP_COMPLETE_2026-08-03.md
      covers: "OCID-020 nav-surface sweep completion (UMR-20260803-081331-af0b): real host-load re-check confirming the prior deferral's condition cleared; a real per-batch-browser-instance harness (~12 navigations/instance) that swept all 113 remaining real nav items with zero uncovered and zero unrecovered batch failures, completing 115/115 (100%) real discovered nav-surface coverage for the first time. Real findings with evidence: GAP-ERP-REPORTS-CLIENT-CRASH-ON-403 (new, high severity, screenshot), GAP-403-VS-500-CLM-HR-PERFORMANCE (new, extends the resolved email-intelligence 500-vs-403 pattern to 5 more endpoints), GAP-NAV-TIMEOUT-ORCHESTRA-PROMPTEVAL-SALESHQ (new, honestly flagged as possibly a host-contention test artifact, not a confirmed defect), GAP-ERP-CRM-403-NO-UX-EXPLANATION reconfirmed at larger scale. Point-in-time audit; not a living spec."
    - path: ai-os/VERIDIAN_OCID_047_052_BUSINESS_CERTIFICATION_PLANNING_2026-08-03.md
      covers: "OCID-020's Business Certification phase, OCID-047 through OCID-052 (UMR-20260803-115333-dab8 / -115452-a35d / -115513-c990 / -115534-af31 / -115558-170e / -115620-29c6), planning-only real task breakdowns: Roles/Rights/Responsibilities Certification (11 real roles, 73 centrally-registered actions), Multi-Org/Tenant/Brand Isolation Certification, Subscription Plan Entitlement Certification (27 real product branches, real plan-tier mapping honestly flagged as not yet located), Data State Certification (empty/sample/large), Cross-Surface Certification (desktop gap-check + Mobile PWA -- this section's own Mobile PWA finding was superseded same day, see VERIDIAN_OCID_051_CROSS_SURFACE_CERTIFICATION_PLANNING_2026-08-03.md below), VERI Chat AI Escalation Certification (corrects a real conflation risk -- gateway.py/OWNER_ENGINE is this session's own server-ops tooling, not the product's real Mother Router). Amended same day by a real duplicate-dispatch session (same UMR) that found and closed a genuine gap in the OCID-047 section: the original covered RIGHTS (action permissions) but not the separate, already-built RESPONSIBILITY/data-scope layer (dashboard rollup scope, client-list visibility, risk-register visibility, classification-clearance ceiling -- 3 real same-rank/different-clearance divergences named) or the real per-role provisioning-path gap (6 of 11 roles have no real product onboarding path, DB-seed only). No testing or implementation performed. Point-in-time audit; not a living spec."
    - path: ai-os/VERIDIAN_OCID_052_VERI_CHAT_AI_ESCALATION_CERTIFICATION_PLANNING_2026-08-03.md
      covers: "OCID-052 (UMR-20260803-115620-29c6, parent OCID-020 UMR-20260802-165606-4413) dedicated deepening of the merged OCID-047-052 batch doc's own OCID-052 section: corrects that section's own placeholder guess (mother-router.ts) after actually reading it -- mother-router.ts is a real AI model/provider registry across scopes, not a deterministic-vs-AI decision gate. Real mechanism found instead, grounded in file:line: chat-service.ts's generateAiReply() runs tryDeterministicRoute() (llm-routing-gate.ts, 2 of 5 intent-engine.ts intents have zero-LLM-call handlers: check_status/generate_report) then runDialogueScriptTurn() (dialogue-script-executor.ts) before ever reaching resolveModelConfig()/callLLM(). Real, honest gap found: ThreadView.tsx renders every senderId-null reply identically (deterministic, scripted, refused, or AI-generated) -- the only real distinguishing signal is the incidental presence/absence of the confidenceLabel confidence badge, which was designed as an AI-confidence heuristic, not a deterministic-vs-AI indicator; no explicit label exists today. Planning only -- no testing, no implementation, no certification performed."
    - path: ai-os/VERIDIAN_OCID_057_UNIVERSAL_KNOWLEDGE_GRAPH_2026-08-04.md
      covers: "OCID-057 (task's own dispatch label trusted per the OCID-031/036/038 precedent). Original pass's claim that parent chain OCID-053 through OCID-056 'do NOT EXIST anywhere' was accurate for repo state as of 2026-08-04 (early) but is now SUPERSEDED/stale, per a real independent Superboss review + 2026-08-05 reverification: OCID-053/054/055/056 all now have real content (open PRs #867/#869/#868/#870, OCID-056 also merged via #906) -- see the document's own section 0 CORRECTION and ai-os/MASTER-TRACKER.yaml's GAP-OCID-FABRICATED-PARENT-CHAIN-REFERENCES reverification_2026_08_05 field for the full re-verification (a separate, real duplicate-UMR-mint complication across two dispatch waves for OCID-055/056 specifically is disclosed there and deferred to dedicated reconciliation PR #916, not resolved by this correction). The OCID-012 portion of the original finding is unaffected (separately under correction via PR #939). Real Universal Knowledge Register/Graph/Deduplication/Broken-Reference/Orphan report: cross-references rather than re-derives OCID-027's already-real DATABASE_CATALOG.json (444 tables)/FUNCTION_CATALOG.json (5,019 functions)/AI_ROSTER_CATALOG.json (195 roles)/VCEL registry (247 entries)/prompt registry; net-new contribution is the UMR/OCID governance-knowledge layer itself (116 unique UMR IDs catalogued, OCID-020 through OCID-052 parent-chain graph assembled for the first time in one place), one flagged-not-resolved UMR dedup candidate (OCID-027's own dual UMR attribution), and formalizing OCID-027's two prose-only named gaps (no report/business-rule catalog) into a tracked GAP- entry. Documentation/registry only, per SEC-07 -- no UMR renamed, merged, archived, or superseded; any such action is a PR-reviewed proposal only, consistent with the dispatch's own double-verification rule."
    - path: ai-os/VERIDIAN_OCID_051_CROSS_SURFACE_CERTIFICATION_PLANNING_2026-08-03.md
      covers: "OCID-051 (UMR-20260803-115558-170e, parent OCID-020 UMR-20260802-165606-4413), dedicated deepening of the OCID-047-052 batch doc's own OCID-051 section (task task-20260803-120639, instruction INS-20260803-115557-0ec8) -- planning only. Part 1 (desktop browser): real task breakdown to re-run the PR #794 nav-discovery pass and diff against the existing 115-item baseline, sweeping only any real delta with the existing per-batch harness. Part 2 (Mobile PWA): corrects the batch doc's own 'no PWA infrastructure exists' finding -- a live re-check found src/app/manifest.ts (merged PR #435) provides a real, installable manifest with a working Web Share Target (POST /api/veri-chat/share-target); only a service worker is genuinely absent (zero sw.js/next-pwa/workbox/serviceWorker.register() anywhere in src). Defines a real test path reusing only Playwright's built-in device-emulation presets and the existing per-batch harness: manifest/icon contract validation, Web Share Target end-to-end exercise, deterministic confirmation (not assumption) that no service worker intercepts navigation, and a mobile-viewport re-render of the nav surface. No new PWA framework, no new architecture. No testing or implementation performed. Point-in-time audit; not a living spec."
    - path: ai-os/PROJEXA_AI_COM_E2E_CERTIFICATION_OCID050_DATA_STATE_TASK_BREAKDOWN_2026-08-03.md
      covers: "OCID-050 (UMR-20260803-120723-716b), a real child of OCID-020 (UMR-20260802-165606-4413), opened under the Business Certification phase. Planning-only deterministic task breakdown for re-running the existing, reused (not rediscovered) real 115-item nav-surface list from PR #794 against three real data states: Empty (existing 'OCID-020 Continue Org A'), Sample Data (existing 'demo_org', real ERP transactional usage since the 2026-07-06 seed), and Large Data volume (confirmed NOT to exist yet -- flagged as a real open prerequisite, not fabricated). Task breakdown TASK-050-0 through -6: commit the nav list as a durable repo fixture (currently only in host-local /tmp), identify/create the large-data org, define pagination/empty-state/performance acceptance criteria, then three independent per-state passes reusing the PR #794 batched-browser harness, then synthesis and GAP-registration in MASTER-TRACKER.yaml following the same Finding 1/2/3 honesty bar. No testing, no fixing, and no org creation performed this cycle -- planning only."
    - path: ai-os/VERIDIAN_UMR_UTR_EUID_DISCOVERY_VS_LIVE_SYSTEM_2026-08-03.md
      covers: "Real discovery/analysis (UMR-20260803-174634-5a2f, UMR-20260803-175139-dedf, parent OCID-020 UMR-20260802-165606-4413) comparing the Owner's proposed Universal Knowledge and Execution Architecture (Universal Metadata Registry / UMR records, a task-level Universal Task Registry / UTR -- renamed from the Owner's original 'UTM' naming after this document's own discovery found and flagged a real, live naming collision, see below -- and an End User Identity / EUID synchronized across server/browser/PWA) against the real live system. Reads resource_governor.py/superboss-register.py directly: confirms the real live superboss-register.sqlite schemas for instructions/work_items/actions/system_index/umr_tasks. Real, honest finding: utm_source/utm_medium/utm_campaign/utm_content/utm_term are already real, live columns across those tables -- a deliberate, Owner-specified internal provenance-tagging convention (documented in the script's own header), not marketing-attribution data, but genuinely a different concept from the newly-proposed task-registry model, using the identical three-letter abbreviation. Per Owner resolution, the new concept is renamed UTR (independently re-verified as a genuinely unused, clean name); UMR and the existing utm_* columns are explicitly left unchanged. Also confirms UTR and EUID are both real, honest gaps -- not already secretly built under different names -- and that EUID's 'synchronized across... PWA' requirement is specifically blocked by OCID-051's own already-confirmed finding that zero service worker exists in this codebase. No PR #610 terminology audit was found to exist (searched thoroughly; PR #610 is real but unrelated, a Sales Pipeline Dashboard nav-link change). Amended same day: a third, real, pre-existing 'UMR' usage found and credited (ai-os/registry/asset-registry-coverage.yaml + check-asset-registry-coverage.mjs already call themselves 'Universal Metadata Registry', for DB-table asset-registration coverage specifically -- does not change the Owner's own 'UMR stays unchanged' resolution, just names what 'as it already exists' now includes). Discovery and analysis only -- no schema, code, or database change."
    - path: ai-os/MASTER_INDEX.yaml
      covers: "AI/software-audience protocol: query this file before creating any new script/table/register across compliance-tracker/projexa/veda-advisors/claude-control, to avoid building a parallel mechanism that already exists."
    - path: ai-os/VERIDIAN_OCID_058_UTR_REGISTRY_2026-08-04.md
      covers: "OCID-058 (UMR-20260804-035943-3c38 parent chain: OCID-057/056/055/054/053/020/021, all 7 independently re-confirmed live in umr_tasks), discovery/verification only, per its own 'certify existing execution, do not redesign' instruction. Enumerates 5 real, independently-keyed task-tracking record types that exist today (umr_tasks; instructions/work_items/actions; task.yaml; platform.ops_dev_tasks; platform.task_register Instruction Contract/Execution Report), with real schema fields cited file:line, and confirms none is a genuine six-context (actor/identity/governance/execution/business/evidence) UTR -- a real, honest gap, held for a PM decision, not built here. Also re-confirms OCID-012 (again present in the incoming dispatch-prompt template) has zero real matches anywhere in this repo -- flagged back to the Owner again, not registered. Companions: VERIDIAN_OCID_058_EXECUTION_ARCHITECTURE_REPORT_2026-08-04.md, VERIDIAN_OCID_058_EXECUTION_TRACEABILITY_REPORT_2026-08-04.md."
    - path: ai-os/VERIDIAN_OCID_058_EXECUTION_ARCHITECTURE_REPORT_2026-08-04.md
      covers: "OCID-058 companion: real, verified architecture map of every execution path -- interactive Owner/Super-Boss dispatch (dispatch-owner-task.sh -> resource_governor.py/umr_tasks), headless veridian-worker@ tasks (veridian-task.py cmd_create -> task.yaml + ops-task-sync), the systemd --user timer set (21 real veridian-cron-*/dispatch-tick/task-watchdog timers, cron itself confirmed fully retired), and 2 real API/integration surfaces (/api/ai/team/dispatch -> platform.task_register; GitHub repository_dispatch zai-task/claude-task -> confirmed a 31-line echo-only stub, zero real execution, matching AGENTS.md's own documented caveat). No redesign; certification only."
    - path: ai-os/VERIDIAN_OCID_058_EXECUTION_TRACEABILITY_REPORT_2026-08-04.md
      covers: "OCID-058 companion: per-path table answering whether each real execution path creates a UTR-style record referencing a real UMR object -- confirmed only ONE real, momentary link exists (work_items.ai_task_id = umr_id, at the instant of owner dispatch only); every downstream record (task.yaml, ops_dev_tasks, task_register, worker checkpoint actions rows) uses a different, unlinked id and carries no umr_id field at all. Recommends (not authorized here) 3 real implementation options for a future PM decision: thread umr_id through, build the full six-context UTR table, or formally document the seam as-is."
    - path: ai-os/VERIDIAN_OCID_022_039_STATUS_SNAPSHOT_2026-08-03.md
      covers: "OCID-040 (UMR-20260803-042918-60b8) real status rollup of the OCID-022 through OCID-039 end-user-documentation series: per-OCID real PR/merge state, real UMR-chain resolution (including the confirmed-nonexistent 'OCID-021 implementation lock' label), real canonical-artifact existence check, and a real as-built dependency map. Explicitly not a certification, not implementation, not a platform freeze -- all deferred pending the real OCID-020 unlock. Point-in-time audit; not a living spec."
    - path: ai-os/VERIDIAN_OCID_001_006_EARLIER_GENERATION_REGISTRATION_2026-08-04.md
      covers: "OCID-001 through OCID-006 (PR #907, merged): real, corrected-from-the-start registration for an earlier generation of OCID items that had never been indexed here. Documentation only -- no code/DB/UI/UX change. Added to this index retroactively by the OCID-060 dispatch (PR #910) after check-metadata-index-coverage.mjs (added after PR #907 merged) started failing unrelated later PRs over this pre-existing gap."
    - path: ai-os/VERIDIAN_OCID_060_REGISTRATION_2026-08-04.md
      covers: "OCID-060 registration only -- real, freshly-located UMR (`UMR-20260804-161339-d586`, independently confirmed via direct `umr_tasks` query against `superboss-register.sqlite` rather than self-minted; the dispatch gateway had already created it at dispatch time keyed by intent-text content, not by OCID number) linked to OCID-059 (`UMR-20260804-040122-2b4b`) as its immediate predecessor. Cross-references PR #874 (open, unmerged, the honest OCID-012-through-059 final platform audit) as real prior discovery evidence, and supplies the real UMR that PR #874's own content never received (its header field literally reads 'this task's registered UMR' as unfilled prose). Records, explicitly and prominently, the freeze gate OCID-060 as originally dispatched cannot bypass: OCID-020 complete (MET), OCID-038 -> OCID-039 -> OCID-040 complete in order (NOT MET -- OCID-038's one real blocker closed via merged PR #886 since PR #874 was written, but OCID-039 has still not started as real production certification), and a fresh explicit real-time Owner chat confirmation authorizing the freeze itself (NOT MET, not requested here). Also independently re-verifies and corrects PR #873's (OCID-059) own claim that OCID-053-057 are 'real, merged commits on origin/main' -- confirmed via `git merge-base --is-ancestor` that none of the five are ancestors of `origin/main`; PRs #866-870 are all still open, unmerged. No certification issued, nothing frozen, no code/schema/DB change -- registration only, per this dispatch's own explicit instruction."
    - path: ai-os/VERIDIAN_CONTINUOUS_PLATFORM_EVOLUTION_RUNTIME_2026-08-03.md
      covers: "OCID-035 (this task's own directive, parented to UMR-20260803-042003-5e92 OCID-034): documentation-only definition of how VERIDIAN continuously evolves without duplication, regression, or architectural drift -- request capture/classification, search-before-change (deferring to OCID-027's discovery taxonomy), per-artifact-type enhancement patterns, multi-brand/tenant propagation, generic component extraction, canonical-artifact/UMR-chain update rules, regression/duplication prevention, compatibility/impact/performance validation, release readiness, and governance/health/metrics. Documentation only, no implementation; ready for hand-off to OCID-036."
    - path: ai-os/VERIDIAN_UNIVERSAL_END_USER_WORK_ORCHESTRATION_RUNTIME_2026-08-03.md
      covers: "OCID-033 (this task's own directive, parented to UMR-20260803-041743-d271/OCID-032, citing the OCID-022 through 031 chain): documentation-only definition of how VERIDIAN continuously orchestrates end-user work by naming and sequencing already-real mechanisms (veri-todo-service.ts, work-dashboard-service.ts, task-reprioritization-service.ts, the notifications table, approval-workflow-service.ts, VERI Chat, the Chain Selector/option-chain/mode-pill pattern) rather than building new ones. Cross-references OCID-023/026/028/029/030 instead of restating their task-model/decision-engine/sync/org/discovery content. Consolidates an honest gap register (next-best-action ranking, predictive/anticipatory presentation, interruption recovery, cross-device draft continuity, push notifications, orchestration telemetry) for OCID-034. No code, no new DB objects. Flags a likely OCID-032/033 row mislabel in the OCID-022-039 status snapshot's table, consistent with that document's own already-confirmed off-by-one pattern (PR #776)."
    - path: ai-os/VERIDIAN_UNIVERSAL_TASK_LIFECYCLE_RUNTIME_2026-08-03.md
      covers: "OCID-20260803-032 (UMR-20260803-041743-d271, parent UMR-20260803-041700-a741, ERP Functional Completeness Master Program): grounds the complete task lifecycle (definition/creation/ID/classification/priority/context/owner/assignee/delegation/transfer/split/merge/dependencies/status model/approvals/escalation/AI escalation/software execution/monitoring/heartbeat/timeout/recovery/resumption/completion/verification/audit/traceability/reuse/analytics/dashboard/notification/sync/history/certification/zero-lost-task target) entirely in what already exists (activity_log, TASK-01..05, GP-01..30, AUDIT-01..04, escalation-ladder.ts, monitor-protocol.ts, approval-workflow-service.ts) -- no code/DB/UI/UX change. Corrects a real numbering error in the OCID-040 status snapshot (this task is real OCID-032, not OCID-031; live-verifies OCID-033 = Universal End User Work Orchestration Runtime). Names real, honest gaps where no mechanism exists (task transfer, splitting, merging, generic heartbeat/resumption, unified notification service). Point-in-time; will go stale as real work lands."
    - path: ai-os/VERIDIAN_UNIVERSAL_ORGANIZATION_RUNTIME_2026-08-03.md
      covers: "OCID-029 (documentation-only directive, parented to UMR-20260803-041257-e9c3/OCID-028) real, cited inventory of the existing organization/multi-brand/multi-tenant/legal-entity/department/project model (organisations, clientEntities, erpCompanies, departments, projects), the people model (users, role enum, stage_0 level-zero users), the ROLE_RANK + deny-only-ABAC rights model, the deterministic Approval Workflow Engine + delegationOfAuthority limits + scopedDelegations transfer/delegation mechanism, leave/exit, shared-link/guest access, and audit/data/work visibility -- each claim cited to a real file:line, each real gap (no Owner row, no locations/teams table, two unreconciled legal-entity tables, no unified exit workflow) named, not fixed. Implements nothing; SEC-07 unaffected. Point-in-time synthesis; not a living spec."
    - path: ai-os/VERIDIAN_DETERMINISTIC_EXECUTION_AND_AI_ESCALATION_RUNTIME_2026-08-03.md
      covers: "OCID-026 (UMR-20260803-041047-03ee) -- documentation-only map of VERIDIAN's real, already-built deterministic-execution vs. AI-escalation runtime: Mother Router/model-tier-eligibility, guardrail-engine/business-rule-validator, report-engine-service execution types (deterministic_aggregation/deterministic_formula/ai_recipe/external_service), VERI Chat/mode pills/Chain Selector, credit-accountant.py search-before-build, and the real AI-context-prep/validate/execute/audit/reuse pipeline. No new architecture, no implementation. Point-in-time governance map; not a living spec."
    - path: ai-os/VERIDIAN_UNIVERSAL_CAPABILITY_DISCOVERY_AND_EVOLUTION_RUNTIME_2026-08-03.md
      covers: "OCID-036 (UMR-20260803-042034-0c1f) -- real numbering resolved by GAP-OCID038-OCID035-DUPLICATE-PRS's real fix (see ai-os/MASTER-TRACKER.yaml): this document's own folder/branch label ('ocid-036') is authoritative, per the same trust-the-task's-own-label precedent PR #776 (UMR-20260803-052107-71fa) established for the OCID-026/027/028/029/030 cluster -- independently confirmed via PR #779 (merged) that OCID-034 is genuinely 'Universal Context and Predictive Runtime', which makes PR #777's own OCID-035 self-identification ('Continuous Platform Evolution Runtime', parented to OCID-034) correct and non-colliding; this document is therefore OCID-036, not a second claim on OCID-035. Defines how VERIDIAN continuously adds new capabilities without duplication -- a unified 'capability' definition spanning function/report/analysis/prompt/business-rule/workflow/UI/UX/screen/module/database-object, grounded against the 3 real distinct existing meanings of 'capability' already in this codebase (capability_registry, capability-learning/audit-service.ts, dynamic_chains/capability-tree-service.ts); capability classification (duplicate/enhancement/net-new); and capability versioning. Cross-references OCID-027 (search/discovery mechanics, PR #771) and OCID-035 (enhancement/propagation/certification lifecycle, PR #777) rather than restating either. Documentation only; not implementation, not a certification, not a platform-freeze unlock. Ready for hand-off to OCID-037."
    - path: ai-os/VERIDIAN_UNIVERSAL_SOFTWARE_EXECUTION_ENGINE_2026-08-03.md
      covers: "VERIDIAN Universal Software Execution Engine v1.0 (UMR-20260803-041700-a741, OCID-031, real PM decision UMR-20260803-063016-8bfc; this task's own directory was labeled ocid-031 correctly all along) -- defines how already-decided work actually executes and is tracked, grounded in real, independently-verified infrastructure: task-execution-engine.ts + engine-invocation.ts (task/function execution), construction-billing-workflow-service.ts + grc-workflow-engine.ts (per-domain workflow execution, no single WorkflowEngine class), report-engine-service.ts (report execution, 4 execution_type shapes), analytics-engine.ts (analysis execution), worker-entrypoint.sh/dispatch_core.py/the 18-timer systemd set (background + scheduled execution), webhook-deliver.ts + audit-event-triggers.ts (event-driven execution; event-bus.ts flagged as real but zero production callers), audit.ts + engine-invocation.ts (execution logging/audit), withTenantContext (multi-tenant execution), compliance-engine-registry.ts (multi-brand/jurisdiction execution), model-tier-eligibility.ts (execution priority/role-gating). Names real, honest gaps rather than claiming completeness: no generic retry wrapper, forward-only migrations (no rollback), spot-check-only monitoring, no cross-cutting performance dashboard, and several coexisting duplicate execution paths (Mother Router bypass, 3 AI-dispatch surfaces, old+new report functions, legacy DB access). Documentation only, per SEC-07's real OCID-020 implementation lock -- no code, no DB objects, no certification. Flags readiness for OCID-032 to cross-reference (not re-derive) its queueing/dependency/parallel/sequential-execution sections."
    - path: ai-os/VERIDIAN_OCID_038_UNIFIED_PLATFORM_INTEGRATION_DISCOVERY_2026-08-03.md
      covers: "OCID-038 (UMR-20260803-072014-d038, parent OCID-037 UMR-20260803-042230-180c) real, honest cross-repo/cross-layer platform-integration discovery and gap analysis -- answers, with live-tested evidence, the three certification questions this OCID's own directive named: (1) does compliance-tracker + PROJEXA + every FChecklist repo operate as one VERIDIAN backend today (real answer: NO -- 15 separate repos, projexa has its own 11-table schema, no confirmed shared API call); (2) is projexa-ai.com really a production PROJEXA thin client today (real answer: NO -- live-curled 2026-08-03, serves compliance-tracker's generic VERIDIAN shell, zero PROJEXA branding; real PROJEXA app is a separately-live deployment at projexa-smoky.vercel.app); (3) does the full browser-to-audit chain operate as one system today (real answer: NO -- task engine and Mother Router are both real but not wired to each other, VERI Chat is real but not wired to the dispatch layer, PWA and VERI Assistant do not exist in code at all). Also found and registered a live, unresolved OCID-035 duplication (PR #777 vs #782). Registers 6 new gaps in MASTER-TRACKER.yaml. Documentation/discovery only, per SEC-07 -- does not implement any fix, does not certify OCID-020, does not freeze the platform. Hands off to OCID-039."
    - path: ai-os/VERIDIAN_UNIFIED_SYNCHRONIZATION_RUNTIME_2026-08-03.md
      covers: "OCID-028 (real PM decision UMR-20260803-045159-ec55, correcting an earlier status-table mislabel; task folder was labeled OCID-028 correctly all along), parented to UMR-20260803-041211-b7b7 (OCID-027). Documentation-only synchronization runtime doc: cross-references the real sync/cache/PWA/chat/task state already discovered and documented by the OCID-022/024/025 sibling docs (browser-intent-cache.ts, sync-engine.ts's tested-but-unwired delta-sync/conflict-resolution primitives, PROJEXA's offline work-progress queue, chat/task polling, last-write-wins task updates with zero conflict detection) into one coherent picture, plus new synthesis sections (UMR continuity, sync certification gating, readiness for the next OCID) none of the siblings covers end-to-end. No new sync architecture designed or implemented. Point-in-time audit; not a living spec."
    - path: ai-os/VERIDIAN_OCID_062_SERVER_AUTHORITY_AND_MINI_VERIDIAN_EXECUTION_ARCHITECTURE_2026-08-04.md
      covers: "OCID-062, parent chain OCID-021 (UMR-20260802-173631-ca85) -> OCID-020 (UMR-20260802-165606-4413); this document's own dispatch UMR was not independently queried against superboss-register.sqlite (no host access from this task's working environment) and is disclosed as such rather than self-minted. Documentation-only canonical architecture doc defining: (1) authoritative server responsibilities and why (requireAuth()/auth-guard.ts, withTenantContext()/tenant-scoped.ts RLS, Mother Router model resolution, llm-routing-gate.ts deterministic-vs-AI dispatch, engines/workflows/reports, audit/billing), cross-referencing rather than restating OCID-031 (PR #781, merged) and OCID-034 (PR #779, merged); (2) 'Mini VERIDIAN' -- this document's own new term (confirmed absent from ai-os/ prior to this doc), scoping what already runs client-side today (the real, tested 5-tier src/lib/browser-execution/ system, independently re-confirmed as built-but-not-wired into VeriComposer.tsx's live send path beyond tier-selection telemetry; the real, live browser-FIRST/server-SECOND compileInBrowser() -> /api/prompt-compiler/execute pattern; browser-intent-cache.ts IndexedDB store; confirmed-absent service worker) versus what is explicitly proposed (narrow, low-stakes local-tier wiring, never touching auth/writes/engine computation/billing); (3) one concrete, clearly-labeled-proposed local/server execution handoff sequence generalizing the one real, live FIRST/SECOND pattern. Cites its 5 real dependencies honestly by their real, independently-re-verified current state at writing time: OCID-024 (PR #767, OPEN) and OCID-025 (PR #766, OPEN) read directly from their branches as real drafts, not settled fact; OCID-031 (PR #781, MERGED) and OCID-034 (PR #779, MERGED) as its strongest real foundation; OCID-061 (universal input runtime mapping) confirmed not started, zero findings fabricated. Also flags a real, independently-found stale claim in ai-os/CONSTITUTION.yaml (line 759, 'zero indexedDB usage', predates the merged phase_5 browser-execution IndexedDB work) without editing that file (out of scope). Registers GAP-MINI-VERIDIAN-CLIENT-EXECUTION-UNWIRED in ai-os/MASTER-TRACKER.yaml. No code, no DB objects, no service worker/manifest/PWA file, no certification -- per SEC-07's real OCID-020 gate."
    - path: ai-os/VERIDIAN_GLOBAL_KNOWLEDGE_DISCOVERY_AND_REUSE_RUNTIME_2026-08-03.md
      covers: "OCID-20260803-027 (real, correctly-labeled per UMR-20260803-045159-ec55, correcting an earlier draft mislabel), documentation only. 36-section discovery-and-reuse operating model grounded in real, already-existing infrastructure: the 4-layer search order already live in MASTER_INDEX.yaml, the mechanical DATABASE_CATALOG.json (444 tables)/FUNCTION_CATALOG.json (5,019 functions) catalogs reused across compliance-tracker/projexa/veda-advisors, the 247-entry VCEL engine registry, the 195-role AI_ROSTER_CATALOG.json, and the versioned prompt registry. States reuse/enhancement/standardization/certification criteria; names two honest gaps (no dedicated report/business-rule catalog yet, hand-maintained screen/UX narrative). Amends ai-os/IMPLEMENTATION_MATRIX_2026-08-02.md in place rather than duplicating it. No implementation performed."
    - path: ai-os/VERIDIAN_UNIVERSAL_DECISION_ENGINE_2026-08-03.md
      covers: "OCID-029 (UMR-20260803-041351-0278, 'the real OCID-029 directive'; this task's own directory was labeled ocid-030, a naming drift recorded honestly in the doc's own header, not a scope change) VERIDIAN Universal Decision Engine v1.0: defines how VERIDIAN decides what to do next, grounded in independently-verified real infrastructure -- Mother Router (AI model/provider routing, 35 known bypass sites), guardrail-engine.ts + policy-enforcement-engine.ts (opt-in rule engine + regex safety gate), approval-workflow-service.ts + task-execution-engine.ts (workflow/task engines), VCEL (function/analysis library, computation_engines table), report-catalog-service.ts (report library), the Prompt OS (prompt_templates/prompt_versions), the Chain Selector/mode-pill UI (VERI Chat), and the superboss-register.py/credit-accountant.py search-before-build mechanism. Cross-references rather than duplicates decision-relevant sections already real in open sibling PRs (#768 OCID-023 Sec19, #767 OCID-024 Sec23, #766 OCID-025 Sec14). Documentation only, per SEC-07's real OCID-020 implementation lock -- no code, no DB objects, no certification of anything beyond what this document's own discovery independently verified. Flags readiness for OCID-031 to cross-reference (not re-derive) its task-decision-flow and failure/recovery sections."
    - path: ai-os/VERIDIAN_UNIVERSAL_MULTI_BRAND_MULTI_TENANT_PLATFORM_RUNTIME_2026-08-03.md
      covers: "OCID-046 (UMR-20260803-084718-ce79, parented to OCID-045 UMR-20260803-084637-ada4, completion explicitly DECLINED for the same real reason as OCID-041 through OCID-045) VERIDIAN Universal Multi-Brand Multi-Tenant Platform Runtime v1.0: real inventory of the existing brand model (no `brands` table; `organisations`' 5 white-label columns from drizzle/0221_wave_b_white_label_branding.sql, all NULL on every live org; `productBranches`/`platformApplications` product catalogs; zero domain-routing middleware anywhere in src/), tenant model (withTenantContext()/RLS, 50 call sites, 51 RLS-enabled migrations, org_id as the real tenant unit -- no tenant_id column), organization model (organisations->branches->clients->clientEntities hierarchy; no confirmed standalone Organization Runtime doc exists under any settled OCID number), role/rights model (userRoleEnum + auth-guard.ts rank system + permission-service.ts action-lookup table -- no normalized roles/permissions tables), and function/report/analysis/prompt libraries (report-engine-service.ts TABLE_REGISTRY/FORMULA_REGISTRY/getFullReportCatalog(), capability-registry-service.ts's real similarity/dedup functions, cross-referencing rather than re-deriving OCID-027's VCEL/AI_ROSTER_CATALOG/Prompt OS discovery). Confirmed via literal repo-wide search that neither `projexa-ai.com` nor `thefirm-ai.com` has any live brand-config row anywhere. Names the real cross-cutting gap: every reuse primitive exists in production, but there is no first-class `brands` entity to route on, adopt white-label config against, or extend cross-brand dedup to. Documentation only, per SEC-07's real OCID-020 implementation lock -- no schema/code/CONSTITUTION.yaml change. OCID-046 not marked complete."
    - path: ai-os/PROMPT_METADATA_SCHEMA_2026-07-25.schema.json
      covers: "JSON Schema for compliance.prompt_versions.metadata, realizing VERIDIAN_Architecture_v2.0.docx section 2.6 ('Prompt Metadata Schema, 200+ Fields') at minimum-viable field depth."
    - path: ai-os/RULES_ARTICLES_198.json
      covers: "198 rules/articles extracted from Owner-supplied RULES.xlsx (2026-07-21) -- a distinct canonical source, not yet reconciled with ai-os/CONSTITUTION.yaml (its own field says so explicitly)."
    - path: ai-os/STANDING_DIRECTIVE.yaml
      covers: "Machine-readable standing task directive (Owner directive 2026-07-21, chat), referenced by filename per task."
    - path: ai-os/SUPERBOSS_REGISTER.md
      covers: "Reference doc for the Superboss Register -- four linked, full-text-searchable SQLite/FTS5 tables at /opt/veridian/ai-os/memory/superboss-register.sqlite, deployed 2026-07-20 so no session starts from zero."
    - path: ai-os/SYSTEM_DIAGRAM.md
      covers: "Auto-generated system diagram (23 components, 21 edges), rendered by ai-os/scripts/generate-system-diagram.py from that script's own SUBSYSTEMS/EDGES data -- do not hand-edit this file directly."
    - path: ai-os/VERIDIAN_UNIVERSAL_RESULT_VERIFICATION_AND_REINTEGRATION_RUNTIME_2026-08-03.md
      covers: "OCID-044 (UMR-20260803-084547-22fd, parented to OCID-043 UMR-20260803-084429-7a70, itself registered this same cycle discovery-only) -- still-undone substantive discovery the 2026-08-03 IMPLEMENTATION_MATRIX registration amendment explicitly flagged as outstanding. Real inventory of review engine (supervisor-entrypoint.sh, mandatory-audit-check.yml), audit engine (audit-protocol.ts, validate-audit-verdict.ts, audit.ts/audit-event-triggers.ts, SENTINEL), PR lifecycle (dispatch-repo.ts, ai-team-workforce.yml, Rule 6 branch protection), commit lifecycle (AI-Workforce commit convention, task-tightening.ts scope check), merge lifecycle (scope-check.py, the AUTONOMOUS-FULL-APPROVAL-2026-07-31 autonomous-merge block), lock framework (guardrail-engine.ts, policy-enforcement-engine.ts, check-guardrail-presence.mjs, SEC-07), and knowledge registry (MASTER_INDEX.yaml, superboss-register.py/sqlite, capability-registry-service.ts dedup functions, report-catalog-service.ts, Prompt OS). Names the one real honest gap: every individual primitive a universal result-verification-and-reintegration runtime needs already exists in production, but nothing sequences them into one pipeline, and the one mechanism aimed at external AI content (chatgpt_audit_guard.py/chatgpt_promptlib_guard.py) dead-ends in a sandbox with no promotion path into production libraries. Documentation only, per SEC-07 -- no review/audit runtime change, no CONSTITUTION.yaml change, no verification/reintegration code path wired. OCID-044 not marked complete."
    - path: ai-os/VERIDIAN_UNIVERSAL_END_USER_WORK_MODEL_2026-08-03.md
      covers: "OCID-20260803-023 (UMR-20260803-040929-9713, real dispatch UMR -- corrected 2026-08-05 per UMR-20260805-083603-9efa, see GAP-SELF-MINTED-ARTIFACT-UMR-FABRICATION; the fabricated 'artifact UMR' UMR-20260803-042019-844f previously cited here was never really registered): the first canonical VERIDIAN Universal End User Work Model, v1.0 -- documentation only. Defines what a task is, task uniqueness/identifier, state model, ownership/assignment/delegation/transfer, approval/escalation/completion/reopen, auditability/history/timeline, attachments/chat/decisions/artifacts, reports/analysis/notifications/search, permissions/visibility/retention/synchronization/recovery, and a lifecycle summary -- every claim grounded in real, live tables/services (tasks, tickets, pmsIssues, activity_log/TASK-04, comments, taskChatMessages, documents, notifications, auditLogs, scopedDelegations, approvalRequests) or an honestly-named existing gap, not invented. Builds directly on the pre-existing UNIVERSAL_TASK_WRAPPER_DESIGN.md (repo root, 2026-07-11) rather than proposing a competing mechanism. Point-in-time synthesis; not a living spec."
    - path: ai-os/VERIDIAN_UNIVERSAL_CONTEXT_AND_PREDICTIVE_RUNTIME_2026-08-03.md
      covers: "OCID-034 (UMR-20260803-042003-5e92, real dispatch UMR per UMR-20260803-063016-8bfc; parent UMR-20260803-041851-085a, real OCID-033): defines how VERIDIAN maintains, discovers, reuses, and predicts end user context (end user/task/chat/session/browser/PWA/server/role/org/project/function/report/analysis/work-history context, last known state, next best action, predictive function/report/analysis/mode-pill/option-chain/form/navigation selection, software context validation, AI context prep/minimization, global context reuse, multi-brand/multi-tenant context, context sync/traceability/governance/certification, performance targets). Real-cited inventory of what exists (withTenantContext, VeriChatContext, AssembledContext/context-assembly.ts, MotherRouterContext, mode-pill capability-key derivation, Dynamic Chains, L0/L5 cache layers) vs. named gaps (no shared context carrier, no function/analysis registry, no PWA, no next-best-action mechanism). Documentation only; corrects an off-by-one OCID numbering drift (033 vs 034) found against its own live dispatch record. Point-in-time design doc; not a living spec. **Real correction filed by `ai-os/VERIDIAN_OCID_038_039_040_REAL_DISCOVERY_AND_END_USER_VERIFICATION_2026-08-03.md`: its 'no PWA' finding used the literal filename `manifest.json` -- this repo's real PWA manifest is `src/app/manifest.ts` (Next.js native metadata-route convention), live-confirmed serving `https://projexa-ai.com/manifest.webmanifest` with HTTP 200; the separate 'no service worker' part of that same finding remains correct, live-reconfirmed (offline reload renders a blank page, no offline fallback)."
    - path: ai-os/VERIDIAN_UNIVERSAL_EXTERNAL_EXECUTION_FOUNDATION_2026-08-03.md
      covers: "OCID-041 (real UMR UMR-20260803-084109-6875, registered in commit 8cdbe5ea/PR #793, parented to UMR-20260802-173631-ca85/OCID-021), discovery only. The real dispatched discovery deliverable that registration amendment explicitly stated had not yet been produced. Decomposes the Owner directive's external-execution mission (approved providers as interchangeable execution processors against a deterministic minimum execution package, VERIDIAN as sole system-of-record/metadata/task/workflow/security/audit/review/governance authority, full UMR/UTM/worker/review/PR/commit/merge/lock/audit traceability, no new provider-specific architecture) into a real requirement-by-requirement mapping against existing components: roster.ts/llm-client.ts (partial provider abstraction, disconnected from the real worker-dispatch path), InstructionContract/ExecutionReport (closest existing execution-package analog), the existing task/workflow/security/audit/review/governance authority mechanisms (already treat the executor as non-authoritative), the real but fragmented UMR->task->branch->PR->commit->merge->audit traceability chain (superboss-register.py, task.yaml, .task.lock), and reusable extension points (ai_routing_policies/ai_routing_audit_log, model-tier-eligibility.ts, guardrail-presence manifest, mandatory-audit-check.yml). Names 5 honest gaps (GAP-041-1 through GAP-041-5), the central one being zero provider abstraction on the real, used worker-dispatch path (task-gateway.py/worker-entrypoint.sh, hardcoded to Claude Code CLI). Documentation only, per SEC-07's real OCID-020 implementation lock -- no code, no DB objects, no external provider execution path wired, OCID-041 not marked complete."
    - path: ai-os/VERIDIAN_OCID_043_UNIVERSAL_EXTERNAL_EXECUTION_RUNTIME_DISCOVERY_2026-08-03.md
      covers: "OCID-043 (UMR-20260803-084429-7a70, parented to OCID-042 UMR-20260803-084332-5b52, itself parented to OCID-041 UMR-20260803-084109-6875): real, independently-verified inventory of the existing worker runtime (veridian-worker@*/worker-entrypoint.sh/veridian-task.py/task-gateway.py/resource_governor.py), dispatch engine (model-tier-eligibility.ts, dispatch-repo.ts, Mother Router, orchestra-model-resolver.ts; ai-dispatch.yml confirmed a stub), two un-merged task-contract schemas (REQUIRED_TASK_SECTIONS prompt contract vs. InstructionContract/ExecutionReport), sentinel (SENTINEL.yaml confirmed a checked-in manifest with no automated rule evaluator), the cooperative-vs-hard lock framework split (ACTIVE-CLAIMS/SEC-05/SEC-07 cooperative; check-guardrail-presence.mjs/ddl_authorization_check.py/scope-check.py/RLS/mandatory-audit-check.yml/branch-protection hard), review pipeline (mandatory-audit-check.yml, field-presence not rigor), PR pipeline (ci.yml's 11 jobs, supervisor-entrypoint.sh's gh pr usage), and merge pipeline (risk-tier.py, review-verdict.json, scope-check.py, the 2026-07-31 full-autonomy AUTONOMOUS-FULL-APPROVAL block). Names 7 honest gaps (no live external-provider dispatcher, internal-only eligibility/fallback logic, two un-merged task contracts, aspirational SENTINEL logging, assertion-not-rigor audit check, opt-in scope-check, OCID-041/042 canonical artifacts confirmed not yet existing via live systemctl check even though their worker units were confirmed actively running concurrently with this task). Documentation only, per SEC-07's real OCID-020 implementation lock -- no worker/dispatch runtime or CONSTITUTION.yaml changes, no provider-selection code wired, OCID-043 not marked complete. Point-in-time discovery doc; not a living spec."
    - path: ai-os/VERIDIAN_UNIVERSAL_CONTEXT_PACKAGING_RUNTIME_2026-08-03.md
      covers: "OCID-042 (UMR-20260803-084332-5b52, this task's own real dispatch UMR, confirmed by direct query against umr_tasks in the Superboss Register sqlite; parent OCID-041, UMR-20260803-084109-6875, registered the same cycle and itself discovery-only). Discovery v0.1: real, file:line-grounded inventory of what a future deterministic minimum context-packaging runtime (so every external-AI-provider request is built exclusively from one standard VERIDIAN context package, never raw chat/document/task text) would need to reuse -- AssembledContext/context-assembly.ts (the one existing typed, serializable bundle, not wired to any provider call today), MotherRouterContext (resolution only, builds no payload), chat-service.ts history bounding, mode-pill/chain selection data (capability-learning-service.ts, ChainSelector.tsx), task/report/document content sources, and the central finding: one canonical low-level dispatcher (llm-client.ts) fed by ~24 independent ad hoc payload-construction call sites, plus 2 further fully independent paths (webllm-engine.ts in-browser, worker-entrypoint.sh's raw task.yaml prompt.txt text). Confirmed via grep that no ContextPackage/RequestPackage/PromptPackage/UniversalContext abstraction already exists (zero duplication risk). Cross-references, does not duplicate, the adjacent OCID-034 document (predictive end-user context vs. this document's outbound-provider-payload scope). Documentation only, per SEC-07's OCID-020 implementation lock and OCID-041's own not-yet-existing foundation -- no code, no schema, no CONSTITUTION.yaml change, not marked complete."
    - path: ai-os/VERIDIAN_OCID_048_MULTI_ORG_TENANT_BRAND_ISOLATION_CERTIFICATION_TASK_BREAKDOWN_2026-08-03.md
      covers: "OCID-048 (this task's own directive UMR-20260803-120905-029c, parented directly to UMR-20260802-165606-4413/OCID-020, the newly-opened 'Business Certification' phase) -- planning only, no implementation/test/certification this cycle. Real, deterministic 6-task breakdown (T1-T6) for certifying VERIDIAN's already-built organization/tenant/brand model: confirm data isolation across >=2 real orgs, confirm a real brand (projexa-ai.com) is a configuration of the one shared platform not separate software, confirm no cross-tenant leakage. Explicitly reuses (does not duplicate) the existing open Stream D item (IMPLEMENTATION_MATRIX_2026-08-02.md) and the Org A/Org B '/api/departments'-only cross-org-probe extension already flagged open in PROJEXA_AI_COM_E2E_CERTIFICATION_CONTINUATION_2026-08-02.md. Explicitly distinguished from the adjacent, differently-parented, still-artifact-less OCID-046 (Universal Multi-Brand Multi-Tenant Platform Runtime). No new database, table, or architecture proposed. Point-in-time planning doc; not a living spec."
    - path: ai-os/OCID_056_REGISTRATION_2026-08-04.md
      covers: "OCID-056 canonical registration -- registration + real, read-only discovery only. Mints UMR-20260804-161630-b761 for OCID-056, links it to OCID-055 as predecessor (cited UMR-20260804-161625-5bb6, real, confirmed on open PR #902, not yet merged to main), fully discloses the UMR mint method (real task creation timestamp + sha256(task_identity)-derived suffix, explicitly not a umr_tasks DB row -- resource_governor.py --submit deliberately not called to avoid a real duplicate-dispatch side effect against this task's own already-running identity). Independently re-verifies this dispatch's own 'zero duplication' premise (true for OCID-056, unlike the immediate sibling OCID-055 dispatch's identical premise, found false). Cross-references (does not fix) a real truncation defect found in PR #902's own OCID-055 registration doc. Restates the standing no-rotation rule: no credential rotation proceeds without the Owner naming that specific credential and confirming it directly in chat, and any authorized rotation still stays locked behind the OCID-020..040 gate."
    - path: ai-os/OCID_056_SECURITY_DISCOVERY_2026-08-04.md
      covers: "OCID-056's real security discovery report, for the Owner to act on personally. GitHub secret-scanning API re-queried live against compliance-tracker and veda-advisors (the 2 repos with scanning enabled per OCID-054's same-day census): 22 open, publicly-leaked Google API key alerts on veda-advisors (likely Google's own public client keys, not VERIDIAN-owned -- Owner judgment flagged, not decided) and 1 open alert on compliance-tracker itself, self-inflicted -- OCID-054's own report quoted the same leaked key in full, creating a second real exposure (root-caused and explicitly not repeated in this document, which quotes no secret value in full anywhere). Real credential-name inventory (zero values) mapped to every provider category the Owner named (GitHub/Vercel/Supabase/database/SSH-infra/OAuth/SMTP/webhook/AI providers/payment providers), via gh secret list (45 names), process.env grep across src/, and a non-git-tracked on-disk /opt/veridian/shared/.env* inventory (existence/names/hygiene only). Zero credential rotation, revocation, or modification performed."
    - path: ai-os/OCID-056-CREDENTIAL-REGISTER.md
      covers: "OCID-056's real credential register (task-20260804-040801-register-ocid-056--platform-security-rec, parent chain OCID-055/054/053/020/021). Names, providers, purposes, and GitHub-Actions-secret-store rotation age only for every real credential this session found -- zero credential values recorded anywhere in the document. Companion to (does not duplicate) `ai-os/OCID_056_SECURITY_DISCOVERY_2026-08-04.md`'s narrower urgent-finding summary. For the Owner to decide which, if any, to rotate and in what order -- no rotation performed."
    - path: ai-os/OCID-056-CREDENTIAL-EXPOSURE-REPORT.md
      covers: "OCID-056's real credential exposure report: full git-history secret scan (`gitleaks detect --source . --log-opts=\"--all\"`, 2702 commits, 403 raw matches, every unique file triaged by hand) plus targeted pickaxe searches for key-prefix patterns gitleaks' default ruleset doesn't specifically name -- zero hits on the pickaxe set. Documents the one confirmed real+urgent finding (a live Supabase `service_role` key committed in plaintext to `CLAUDE-HANDOFF.md`, the real live MeetTrack production database, not decommissioned) with file:commit-hash evidence, and the false-positive triage of the rest. Discovery/audit only -- no credential tested, used, or rotated."
    - path: ai-os/OCID-056-PERMISSION-AUDIT-REPORT.md
      covers: "OCID-056's real permission audit report: repo-level GitHub access (collaborators, PAT-based AI agent access per AGENTS.md), a requireAuth() coverage pass across all 994 `route.ts` files (930 direct call sites, remaining 64 categorized by auth mechanism), and service-role/RLS-bypass capability inventory (17 files). No permissions were changed as part of this audit."
    - path: ai-os/OCID-056-ENVIRONMENT-SECURITY-REPORT.md
      covers: "OCID-056's real environment security report: compares how credentials are scoped and shared across development, staging/preview, and production. Confirms (first-hand, this session) local dev `.env.local` points at the same live production Supabase project as prod itself -- a real shared-credential-across-environments finding. Cross-references `ai-os/STAGING_ENV_2026-07-20.md`'s finding that there is no separate staging database, only a staging deployment lever."
    - path: ai-os/OCID_067_VEDTOCP_DIGITAL_TWIN_PROGRAM_2026-08-04.md
      covers: "OCID-067, VEDTOCP -- VERIDIAN Enterprise Digital Twin and 90-Day Operational Certification Program (real Owner directive UMR-20260804-111532-3612, reaffirmed UMR-20260804-111547-0be3). LOCKED, registration/planning only -- a proposed 50-real-browser-agent, 90-day simulated-organization certification program against the live projexa-ai.com application, explicitly gated on every OCID in the range OCID-015 through OCID-066 independently reaching a real completed status before any real implementation, infrastructure, browser agent, or simulation may begin. Preserves the real, specific directive content the Owner supplied (fifty-person simulated org for AL EMARAT CONSTRUCTION AND INTERIOR DESIGN LLC, 2h-real/24h-virtual time compression, 20/40/40 cognitive-evolution/variance logic, tmux + Claude Code CLI browser isolation, foolproofing/idiotproofing/failproofing, UAE construction/tax/labor-law domain fidelity, chaos engineering/adversarial injection, live hotfixing/rollback, invisible-PM/strict-portal directive) and honestly discloses what template/framework content was not supplied and is not fabricated (daily task/report/analysis templates, task prompt templates, the real 50-role org chart, the 90-day milestone breakdown). Explicitly confirms no code, infrastructure, browser agent, or live projexa-ai.com interaction was performed under this document."
    - path: ai-os/VERIDIAN_OCID_063_MECHANICAL_HANDOFF_ENVELOPE_DISCOVERY_2026-08-04.md
      covers: "OCID-063 (real parent OCID-021 UMR-20260802-173631-ca85 / OCID-020 UMR-20260802-165606-4413, PM directive UMR-20260804-060832-9fdf, governed by the Mandatory Governance Directive UMR-20260804-051521-7099): honest comparison of a proposed mechanical handoff envelope (tool-call log with real status codes, mechanically-filtered rejected-paths list, one-sentence conclusion, capped unknowns list, strict validation) against 5 real existing mechanisms -- task.yaml's checkpoint structure, ACTIVE-CLAIMS.yaml's claim registration, resource_governor.py's reuse_check_result, credit-accountant.py's deterministic JSON verdict, and the AUDIT: PASS/FAIL comment convention -- citing real file paths and field names. Confirmed real gap: no existing mechanism is a mechanical per-tool-invocation call log with status codes. Design proposal only (extend task.yaml's checkpoint schema and/or the existing metadata_json free-form column, per the reuse_check_result precedent, rather than a new schema) -- no code, no new middleware, no schema migration performed. Discovery only; implementation held for a fresh PM decision."
    - path: ai-os/VERIDIAN_OCID_065_COMPLETENESS_AND_ZERO_DUPLICATION_AUDIT_2026-08-04.md
      covers: "OCID-065 (real parent OCID-061 UMR-20260804-044535-7214 -> OCID-021 UMR-20260802-173631-ca85 -> OCID-020 UMR-20260802-165606-4413, governed by the Mandatory Governance Directive UMR-20260804-051521-7099): real completeness + zero-duplication audit of OCID-061/062/063. Cites, rather than re-audits, 3 real merges already confirmed live on origin/main -- OCID-062's canonical document (PR #876, mergeCommit 6b60f01e), its OCID-064 Ollama fold-in (SS3.8 + one SS6 row, independently confirmed present in the merged doc), and OCID-063's real implementation (task.yaml checkpoint-schema extension, merged as PR #19 in the separate FChecklist/veridian-scripts repo, mergeCommit 81931136). Confirms one real, honest gap: OCID-061's own canonical artifact (PR #878) is still OPEN/unmerged and its primary .md deliverable is genuinely truncated (31 lines, covers 1 of 4 intake surfaces, ends mid-sentence with a literal tool-output-truncation trailer committed into the file) -- the real 4-surface discovery exists but only inside that same unmerged PR's MASTER-TRACKER.yaml diff, not the broken canonical doc. Independently re-verifies (not just cites) all four intake surfaces against live main code (Chain Selector, free chat, Whisper voice -- real but OPENAI_API_KEY-unprovisioned and not wired into the composer, webhook/API entry points -- real but no generic inbound intent surface) and certifies zero new intent/prompt/cache registry or parallel execution engine exists anywhere in OCID-061 through OCID-064; UTR is confirmed still a discovery-stage concept, not a built registry, so 'tasks stay in UTR' is certified as 'no competing registry proposed,' not 'UTR is live.' Verification/gap-analysis only, per this OCID's own explicit prohibition -- no new architecture/registry/DB/table/framework authorized or built. **Stale-finding note added 2026-08-06 during a routine origin/main merge (126 commits since this doc was written): origin/main has since merged a real OCID-061 canonical registration doc (see the adjacent VERIDIAN_OCID_061_UNIVERSAL_DETERMINISTIC_INPUT_RUNTIME_REGISTRATION_2026-08-04.md entry below, PR #911) -- this doc's 'PR #878 still open/truncated' finding needs a fresh look against that merged doc before being treated as current; not re-audited in this merge pass, flagged for a follow-up session instead of silently left stale.** **Correction 2026-08-13 (that flagged follow-up, this PR's own merge-conflict resolution cycle, governing chain UMR-20260808-183926-70b6): the 'PR #878 still open/truncated' finding was independently re-verified via `git cat-file -s`/`-p` (not `git show`, which is known-flaky in this sandbox) and found FALSE -- PR #878's canonical artifact is 8754 bytes / 139 lines, complete, covering all 4 intake surfaces, not 31 lines/1-surface/truncated. The original finding was itself a victim of the same output-truncation bug it accused PR #878 of. See the doc's own SS2 for the full correction record; `ai-os/MASTER-TRACKER.yaml`'s GAP-OCID-061-CANONICAL-DOC-TRUNCATED-UNMERGED entry closed accordingly.**"
    - path: ai-os/VERIDIAN_OCID_061_UNIVERSAL_DETERMINISTIC_INPUT_RUNTIME_REGISTRATION_2026-08-04.md
      covers: "OCID-061, real child of OCID-060 (PR #874, open; OCID-060's own document never quotes a self-minted UMR of its own, disclosed honestly rather than invented). Registration only, per this task's own explicit dispatch instruction -- captures the Owner's full real directive text verbatim (certifying one deterministic input runtime/canonical-intent layer across mode pill, free chat, speech, and machine input) and its explicit 7-OCID dependency list (024/025/027/031/033/034/058), each independently re-verified live against its own real current PR state rather than trusted from OCID-060's own same-day snapshot (024/025/033 had all merged since OCID-060 was written -- a live instance of this repo's own already-flagged state-drift pattern). Independently re-confirmed zero pre-existing OCID-061 registration via a direct read-only query against the real umr_tasks table in superboss-register.sqlite. This document's own UMR is honestly disclosed as not self-minted (no sanctioned write path from a docs-only task into the live, write-lock-protected production register) rather than fabricated, the same disclosure pattern OCID-062 already established. No runtime, browser, PWA, server, mode-pill, or option-chain code touched. Explicitly reconfirms real implementation/certification for this OCID stays locked behind OCID-038 -> OCID-039 -> OCID-040 (SEC-07), still open per OCID-060's own fresh evidence."
    - path: ai-os/VERIDIAN_LAPTOP_WEB_BROWSER_RUNTIME_2026-08-03.md
      covers: "OCID-024 (UMR-20260803-041000-70ae, parent UMR-20260803-040929-9713/OCID-023, cites OCID-022) VERIDIAN Laptop Web Browser Runtime v1.0: documents the laptop web browser as the real, live, sole delivery surface for the product today (no Electron shell, no native client, no built PWA per CONSTITUTION.yaml BROWSER-01 through BROWSER-07) -- grounded in direct reads of src/components/veri-chat/*, src/lib/prompt-compiler/*, src/lib/browser-execution/*, src/lib/services/{chat-service,capability-registry-service,permission-service,abac-policy-service}.ts, auth-guard.ts, browser-intent-cache.ts, AppSidebar/AppTopbar/AppShell, and all 99 src/app/(app)/* module directories. Carries forward OCID-022's own honest disclosure that the originating task prompt's 'OCID-021 implementation lock' framing does not match the real, already-closed/merged OCID-021 (SEC-06 production-DB governance split). Documentation only, per SEC-07 -- no code, no UI, no database change. Point-in-time audit; not a living spec."
    - path: ai-os/OCID_049_SUBSCRIPTION_PLAN_ENTITLEMENT_CERTIFICATION_2026-08-03.md
      covers: "OCID-049, Subscription Plan Entitlement Certification, parented to OCID-020 (UMR-20260802-165606-4413), the first registration of a new 'Business Certification' phase the Owner opened directly under OCID-020 (distinct from the OCID-021..040 ERP Functional Completeness chain and the OCID-041..046 Universal External Execution chain). Planning only, per SEC-07: enumerates the 4 real, seeded compliance.subscription_plans tiers (Basic/Standard/Professional/Enterprise, drizzle/0231), distinguishes them from 3 adjacent-but-different real mechanisms (organisations.plan trial flag, licensedSeats manual seat cap, product-branch module enablement), honestly maps what's actually wired (features.aiPackage -> mother-router.ts's getOrgAiPackage()/preferredModelByPackage, confirmed real but dormant -- zero seeded ai_routing_policies rows) vs. schema-only (assistants_per_user, zero consumers anywhere in src/), a 5-task implementation breakdown (A-E), and a per-tier test path reusing this session's ModuleNotEnabledCard/ServiceError 403 explanation pattern (GAP-ERP-CRM-403-NO-UX-EXPLANATION, PR #809). No implementation, testing, or certification performed."
    - path: ai-os/VERIDIAN_OCID_061_INPUT_INTAKE_DISCOVERY_2026-08-04.md
      covers: "OCID-061 (provisional parent OCID-021/UMR-20260802-173631-ca85, OCID-020/UMR-20260802-165606-4413 -- the incoming prompt was truncated and named no explicit parent; corrected once the full prompt arrives). Discovery/mapping only, no runtime built or modified. Real, cited map of the four named intake surfaces: mode pill + Chain Selector (real, wired, ChainSelector.tsx/VeriComposer.tsx), free chat (real, wired, chat-service.ts's generateAiReply()), speech-to-text (real code in whisper-client.ts, but wired only to the separate Voice Tickets feature, not the composer, and not currently operational -- OPENAI_API_KEY unprovisioned), and API/webhook entry points (real outbound webhooks + narrow real inbound surfaces like the PWA share-target, but no generic inbound intent-submission gateway). Confirms a real, honest gap: no canonical intent object or shared intent-resolution layer exists anywhere in this codebase tying the four surfaces together -- tracked as GAP-OCID-061-NO-CANONICAL-INTENT-OBJECT in MASTER-TRACKER.yaml. Point-in-time discovery doc; not a living spec."
    - path: ai-os/VERIDIAN_OCID_059_UNIVERSAL_BROWSER_PWA_SYNC_CERTIFICATION_2026-08-04.md
      covers: "OCID-059, real discovery/verification-only certification (SPEC explicitly prohibits redesigning runtime architecture) of the browser execution runtime, PWA manifest/service worker, and synchronization engine/offline queue. Parent-chain note: the SPEC's own claimed direct parent, OCID-058 (UMR-20260804-040009-09bc), was independently checked and found not yet real (a currently in_progress sibling task, zero commits/PRs/branches) -- flagged rather than silently accepted, same treatment as the already-known-fake OCID-012 (re-flagged again here); real parent used is OCID-057, the actual highest real merged OCID at time of writing. Re-confirms browser-execution's 108/108 passing test suite live; adds a narrower-than-previously-stated finding that tier SELECTION (src/lib/browser-execution/tier-orchestrator.ts::planExecution(), genuinely live-wired via client-compile.ts into VeriComposer.tsx) and tier EXECUTION (webllm-engine.ts/npu-engine.ts/builtin-ai-engine.ts/transformers-engine.ts/worker-pool.ts/cross-tier-storage.ts/model-cache.ts, all real and unit-tested) are two different things -- only selection is live-wired; zero non-test callers exist anywhere for the actual per-tier inference code. Independently re-confirms (rather than re-derives) OCID-051's PWA finding (real manifest.ts + Web Share Target, zero service worker) and OCID-028's sync-engine.ts finding (real, tested OfflineQueue/coalesceQueuedChanges/syncQueue/pullDeltaSync/SyncMutex primitives, zero live callers repo-wide; task-service.ts::updateTask() still unconditional last-write-wins) are both still accurate as of 2026-08-04. No implementation performed."
    - path: ai-os/VERIDIAN_OCID_038_039_040_REAL_DISCOVERY_AND_END_USER_VERIFICATION_2026-08-03.md
      covers: "Registers OCID-038 (UMR-20260803-072810-a3c1), OCID-039 (UMR-20260803-072825-3706, this task's own dispatch title), and OCID-040 (UMR-20260803-072840-52dd) together, per this dispatch's own explicit instruction, under SEC-07's real lock (OCID-020/UMR-20260802-165606-4413, confirmed still open). Real discovery cross-referencing OCID-022..037's existing findings (via git ls-files/git grep -- bare recursive find/grep -r were independently found to silently cap at 51 results in this sandbox, a real methodology gotcha disclosed here) plus one real, disclosed correction to OCID-034's 'no PWA' claim. Real, evidenced live end-user testing against https://projexa-ai.com (2 of 3 real signup+admin-bypass+login sessions succeeded; the 3rd was rejected by a real Supabase signup rate-limit, not a product bug): confirms a real installable PWA manifest with share_target, a real user-facing 'VERI, Your AI Assistant' onboarding surface, a real mode-pill/option-chain composer UI ('Discuss/Chats/To Do' pills, a literal 'chain' selector, a VERI Chat panel with Overview/Tasks/Chats/Meetings/Approvals/Voice/To Do tabs), one real reproducible 'VERI AI isn't ready yet' toast, real Sign Out UI (self-corrected after an initial false-negative automated probe), and real offline behavior (blank page, no service worker, but clean recovery on reconnect). Registers 3 real child-UMR gaps (stuck-loading/not-ready error, no offline fallback, blank mobile content area) plus the PWA documentation correction, all with implementation explicitly deferred pending OCID-020. Explicitly discloses items not reached this session (org switch, attachments, voice, delegate/transfer/approve/reject, search palette, true cross-device continuity, native install, reports/analysis) rather than silently narrowing scope. Documentation + real testing only -- no code, no DB objects, no certification, no freeze. Point-in-time audit; not a living spec."
    - path: ai-os/VERIDIAN_UNIVERSAL_KNOWLEDGE_AND_SERVICE_CATALOG_2026-08-03.md
      covers: "OCID-037 (UMR-20260803-042230-180c, real dispatch UMR per OCID-036's own §36 handoff note; parented to UMR-20260803-042144-e83f, real OCID-036): the catalog-of-catalogs front door mapping every named artifact class (function/report/analysis/prompt/business-rule/workflow/screen/mode-pill/option-chain/module/UI/UX) to its real existing catalog mechanism, cross-referencing rather than restating the two directly adjacent open-PR siblings (OCID-027 PR #771 for search mechanics, OCID-036 PR #782 for classification/versioning). Genuinely new content: the §3 catalog-of-catalogs table; §11-12 Mode Pill/Option Chain catalog entries grounded in real file:line evidence (VeriComposer.tsx, ChainSelector.tsx, capability-tree-service.ts, dynamic_chains table, CONSTITUTION.yaml DMP-01..06) confirming 'option chain' = the real Chain Selector, not a new concept; §18-19 dependency/version mapping; §26-29 browser/PWA/server/AI usage split (confirms no PWA exists to catalog); §31 auditability. Documentation only, per SEC-07's real OCID-020 implementation lock. Flags readiness for OCID-038, naming DMP-06's unpopulated Dynamic Chain Master Directory as a real, scoped example gap for that OCID's own E2E verification work."
    - path: ai-os/VERIDIAN_OCID_054_UNIVERSAL_REPOSITORY_DISCOVERY_2026-08-04.md
      covers: "OCID-054 (UMR-20260804-035759-1eb2), real org-wide read-only discovery pass across all 15 real FChecklist repos: branches, open PRs, GitHub secret-scanning coverage/alerts, task registry, and scheduled jobs. §8's real Platform Security Report is the headline finding: `veda-advisors` has 22 open, publicly-leaked `google_api_key` GitHub secret-scanning alerts sitting unresolved in its git history (single accidentally-committed AI-tool session-output file, already removed from `main`'s HEAD but still reachable via history on that public repo); 12 of the other 14 repos have secret scanning disabled outright, so no automated history coverage exists there at all. No credential rotation or repository history rewrite was performed by OCID-054 itself -- explicitly withheld pending a real Owner decision (registered as GAP-VEDA-ADVISORS-EXPOSED-GOOGLE-API-KEYS and GAP-12-REPOS-SECRET-SCANNING-DISABLED in ai-os/MASTER-TRACKER.yaml). Note: an earlier draft of this document quoted the live veda-advisors secret's raw value verbatim as evidence, which briefly became this repo's own GitHub secret-scanning alert #1 (compliance-tracker); this version never contains that raw value in any commit -- see the file's own §8 for the redaction note and independent-reverification path."
    - path: ai-os/VERIDIAN_OCID_001_006_EARLIER_GENERATION_REGISTRATION_2026-08-04.md
      covers: "OCID-001 through OCID-006 (real batch UMR UMR-20260804-162430-d156, corrected by UMR-20260804-163645-e196 before publication, further corrected post-merge re: umr_tasks store access -- see PR #912). Registration only: records that all six numbers have real, genuine, pre-existing single-row UMRs in the real umr_tasks table (independently, directly re-verified via a read-only query against superboss-register.sqlite, not cited on the PM's authority alone), predate the active OCID chain entirely, are superseded/non-active/historical only, and require no real implementation. Real active work begins at OCID-020 (superseding this doc's own earlier 'OCID-012' framing, since confirmed by the Owner as never-real -- see commit b4a09563, 'PM decision: OCID-012 confirmed by Owner as never-real', the authoritative source, and ai-os/boss/ACTIVE-CLAIMS.yaml for the corroborating cleanup pass). No code, discovery, or repository action performed."
    - path: ai-os/VERIDIAN_OCID_068_UNIVERSAL_GOVERNANCE_RUNTIME_CONSOLIDATION_OWNER_REVIEW_PACKAGE_2026-08-04.md
      covers: "OCID-068 (UMR-20260804-164614-bc46 nine-state-execution-machine addendum + UMR-20260804-170055-a069 structured-traceability-requirement addendum, parent UMR-20260804-164106-3fb8, grandparent UMR-20260804-095259-325a/OCID-066, cites governance directive UMR-20260804-051521-7099). States 1-6 only (discovery/call-graph/canonical-selection/merge-spec/validation/owner-review-package) across 21 real governance scripts + the real systemd unit inventory. Real findings: the live umr_tasks table lives in superboss-register.sqlite (2,227 rows), not the dead ai-os/umr_tasks.db decoy; names the architectural gap behind this session's own live duplicate-PR incidents (no dedup keys off interactive-session state); finds a live duplicate systemd timer for dispatch-tick.py; finds veridian-task-watchdog.timer disabled; finds 4-5 independent paths reaching veridian-task.py create with inconsistent gating; includes a real design proposal (new linked table vs. additive columns) for structured OCID/UMR/PR/commit/file-path traceability. State 7 gate checked and confirmed false (OCID-020 not verified) -- stops there, zero implementation, zero database/schema/registry changes."
    - path: ai-os/VERIDIAN_OCID_060_FINAL_PLATFORM_AUDIT_REPORT_2026-08-04.md
      covers: "OCID-060 (this task's own UMR, real confirmed parent chain OCID-059 UMR-20260804-040122-2b4b back through OCID-053 UMR-20260804-033853-2a17, OCID-020 UMR-20260802-165606-4413, OCID-021 UMR-20260802-173631-ca85). PM decision: OCID-060 as dispatched (platform constitution freeze + completion certification) is NOT authorized this phase -- CONSTITUTION.yaml SEC-07 requires OCID-038 -> OCID-039 -> OCID-040 to genuinely clear in that order first, and they haven't (OCID-038: 1 real Owner-decision-blocked gap open; OCID-039: not started as real production certification; OCID-040: only a non-certifying status snapshot). Delivered instead: an honest, item-by-item audit of OCID-012 through OCID-059's real status (real PR numbers, real merge/open state, real UMR ids where they exist), gathered via 3 independent research passes. Flags OCID-012, OCID-013, and OCID-014 as not real/unregistered as sequential OCIDs (OCID-013's first draft mislabeled a citation of the date-based Owner-directive OCID-20260802-013 as evidence for sequential OCID-013 -- corrected before merge, see PR #874's fix commit) and surfaces a real UMR chain-integrity anomaly around OCID-053-057 (near-simultaneous concurrent dispatch produced conflicting UMR citations) rather than smoothing it over. No certificate issued, nothing frozen, platform engineering not declared complete. Point-in-time audit; not a living spec."
    - path: ai-os/VERIDIAN_OCID_053_UNIVERSAL_KNOWLEDGE_AND_REFERENCE_GRAPH_2026-08-04.md
      covers: "OCID-053 (discovery/normalization/repair; own UMR `UMR-20260804-033853-2a17`, the real dispatch UMR this OCID was registered under, same convention as every other OCID -- no second/separate UMR minted beyond it); real parents OCID-020 UMR-20260802-165606-4413 + OCID-021 UMR-20260802-173631-ca85 -- 'OCID-012 Governance Foundation' named in the incoming spec excluded, zero real matches found, flagged to Owner). Four real deliverables in one doc: (1) Universal Knowledge Graph across OCID-015..037 and Group F OCID-047..052, live-verified PR/UMR state per OCID via `gh pr view` (not narrated from the 2026-08-03 status snapshot alone -- found most of OCID-022..037's canonical docs still open/unmerged, same scope as the in-flight Group C merge pass); (2) Universal Reference Graph, parent-to-child real UMR/PR chain; (3) UMR Integrity Report, resolving the UMR-20260803-115558-170e OCID-050/OCID-051 attribution ambiguity found in ACTIVE-CLAIMS.yaml (belongs to OCID-051, confirmed via 3 independent sources; no new UMR minted, neither OCID lacked one); (4) Orphan and Duplicate Detection Report (4 real closed-without-merging PRs in the OCID-047..052 range, 3 superseded/no lost result, 1 real open gap registered: GAP-OCID048-CROSS-ORG-ISOLATION-EVIDENCE-ONLY-ON-CLOSED-PR). Explicitly does not attempt platform freeze validation or final constitution certification this phase, per hard rule 7 (OCID-038/039/040 not yet genuinely cleared)."

  # Same backfill as reference_docs_and_catalogs above -- real operational
  # automation (cron/systemd entrypoints, dispatchers, extractors, guards),
  # not narrative governance docs, but still top-level items under ai-os/
  # that check-metadata-index-coverage.mjs's scan requires be accounted for.
  operational_scripts:
    - path: ai-os/scripts/anthropic_openrouter_proxy.py
      covers: "Local proxy translating Anthropic Messages API requests (Claude Code CLI via ANTHROPIC_BASE_URL) into OpenRouter chat-completions calls and back, so Claude Code's real tool-use flow can run against GLM/OpenRouter models."
    - path: ai-os/scripts/anthropic_openrouter_proxy_v2.py
      covers: "v2 of the proxy above (2026-07-20, Owner 'zero credit wastage' directive): adds two hard, code-enforced controls at the single chokepoint every GLM-5.2 call passes through."
    - path: ai-os/scripts/audit198/
      covers: "Implementation directory for the RULES_ARTICLES_198.json audit engine (category-checkers.mjs, constitution-index.mjs, evidence-engine.mjs, rules-taxonomy.mjs, results/)."
    - path: ai-os/scripts/batch-import-conversation-log.py
      covers: "Batch-imports queued conversation-log events (Claude Code hook payloads, uploaded as one NDJSON file) into the Superboss Register via direct sqlite3 inserts."
    - path: ai-os/scripts/cost-reconciliation.py
      covers: "Weekly/on-demand reconciliation report verifying the cost-control system is actually behaving as intended, not just trusted silently. Zero API cost -- reads only local logs/state."
    - path: ai-os/scripts/cost-usage-60min.py
      covers: "VERIDIAN-DEV hourly cost/usage check -- cross-checks OpenRouter's real /api/v1/credits balance against compliance.token_usage_ledger. Zero AI cost, deterministic."
    - path: ai-os/scripts/credit-accountant.py
      covers: "Owner directive 2026-07-20: hard $1-increment approval gate on metered AI credit spend (OpenRouter/GLM-5.2), the real metered pool implicated in an earlier RCA."
    - path: ai-os/scripts/dispatch-docworker-task.sh
      covers: "Atomic docworker dispatch (2026-07-20), closing a confirmed recurring gap where veridian-task.py create would auto-start the wrong GLM-routed worker unit before a manual disable could catch it."
    - path: ai-os/scripts/doc-worker-entrypoint.sh
      covers: "Entrypoint for a systemd-managed VERIDIAN DOC worker -- same checkpoint/resume/commit-push pattern as worker-entrypoint.sh below, deliberately different for browser-based external-system documentation tasks."
    - path: ai-os/scripts/extract-ai-roster-catalog.mjs
      covers: "Generator for AI_ROSTER_CATALOG.json above -- mechanical dynamic import of the real roster.ts module, not text-parsing or AI-written."
    - path: ai-os/scripts/extract-db-schema-catalog.mjs
      covers: "Generator for DATABASE_CATALOG.json above -- mechanical drizzle-orm getTableConfig introspection, reusable across repos via an argv schema-path."
    - path: ai-os/scripts/extract-function-catalog.mjs
      covers: "Generator for FUNCTION_CATALOG.json above -- mechanical TypeScript-compiler-AST extraction (parse-only, no type-checker) of every function/arrow-const/class-method under src/**/*.{ts,tsx}."
    - path: ai-os/scripts/gap-status.py
      covers: "Quick X/Y status check for /opt/veridian/ai-os/gap_queue.yaml, plus anything needing an Owner decision."
    - path: ai-os/scripts/generate-system-diagram.py
      covers: "Generator for SYSTEM_DIAGRAM.md above -- Owner directive 2026-07-20 ('make a diagram or tree of the whole system'); diagram data lives in this script's own SUBSYSTEMS/EDGES, rendered to Mermaid."
    - path: ai-os/scripts/generate_quick_reference.py
      covers: "Owner directive 2026-07-20 ('make MASTER_INDEX.yaml logical for you or any AI'): mechanically generates a compact, scannable quick-reference list (id + path + status + auto-extracted one-line summary)."
    - path: ai-os/scripts/generate_task_checklist.py
      covers: "Owner directive 2026-07-20: software-generates the self-use task checklist prompt (files/process/instructions per task) instead of a hand-maintained one."
    - path: ai-os/scripts/health-check-15min.py
      covers: "VERIDIAN-DEV 15-minute health check (L0 tier, zero AI cost): systemd unit status + in_progress task checkpoint staleness."
    - path: ai-os/scripts/import-memory-history.py
      covers: "One-time bulk import of local MEMORY.md history into the server-side Superboss Register's work_items, per Owner directive 'document all sessions ... so it becomes the real memory.'"
    - path: ai-os/scripts/master-decompose.py
      covers: "Master role, on-demand (not a standing daemon) task-decomposition trigger for the Master/Supervisor pilot."
    - path: ai-os/scripts/module-queue-dispatcher.py
      covers: "Master/Supervisor pilot dispatcher reading module-scoped queue files (ai-os/queues/<module>.yaml) and dispatching them the same way queue-dispatcher.py dispatches the general gap queue."
    - path: ai-os/scripts/preflight-guard.py
      covers: "Pre-flight guard run before the main claude -p invocation in worker-entrypoint.sh (Owner zero-waste directive, 2026-07-20) -- catches instant-environment-failure and other zero-value-spend classes before they cost anything."
    - path: ai-os/scripts/quality-gate.sh
      covers: "Runs whatever quality gates are actually detectable in a workspace (lint/build/test) before a worker's changes reach pending_review; gracefully skips gates that don't apply."
    - path: ai-os/scripts/queue-dispatcher.py
      covers: "Autonomous cron-run dispatcher for /opt/veridian/ai-os/gap_queue.yaml -- syncs dispatched-item status and pulls new items under the concurrency cap."
    - path: ai-os/scripts/recover-failed-workers.py
      covers: "RCA recovery script (2026-07-20): converts veridian-worker@ systemd units stuck in permanent 'failed' state (pre-dating the preflight-guard.py fix) into a clean 'blocked' state."
    - path: ai-os/scripts/repair_controller.py
      covers: "Rebuilds ai-os/CONTROLLER.yaml from the authoritative per-task task.yaml files -- safe because corruption was in the shared aggregate index, not the individual task records."
    - path: ai-os/scripts/risk-tier.py
      covers: "Deterministic (non-AI) risk-tier classifier for a task's diff against its base branch (tier1 = autonomous-mergeable, tier2 = holds for human sign-off) -- deliberately simple and auditable."
    - path: ai-os/scripts/run-logged.sh
      covers: "Universal execution wrapper for the Superboss Register (Owner directive 2026-07-20: ensure tasks/actions are properly logged with id and timestamp) -- middleware/aspect-pattern instrumentation."
    - path: ai-os/scripts/scope-check.py
      covers: "Enforces the Master/Supervisor pilot's no-collision rule: every file a worker's diff touches must belong to its declared module (file-ownership.yaml) or an explicit files_allowed override. Deterministic, path-glob based."
    - path: ai-os/scripts/software-request-analyzer.py
      covers: "The mandatory triage gate specified in ai-os/AI_CACHE_AND_TRIAGE_ARCHITECTURE.md section 1 -- rule-based, $0-cost, deterministic; run before dispatching anything to an AI."
    - path: ai-os/scripts/superboss-register.py
      covers: "Superboss Register implementation -- three searchable trees, server-side, SQLite+FTS5, deployed 2026-07-20 so no session starts from zero. Scope deliberately distinct from the AI-work cost-control system."
    - path: ai-os/scripts/supervisor-entrypoint.sh
      covers: "VERIDIAN-DEV server-side Superboss entrypoint: reviews a task's branch for real (not a self-report), classifies risk tier deterministically, and merges/holds/blocks accordingly."
    - path: ai-os/scripts/supervisor-sweep.sh
      covers: "Safety net catching any task stuck in pending_review with no review.json (its immediate supervisor trigger was missed -- crash/systemd hiccup)."
    - path: ai-os/scripts/sync-controller-back.py
      covers: "Automates SYNC rule #14 from SUPERBOSS_DISPATCH_PROMPT.md: records a terminal-state task's summary into the master CONTROLLER.yaml (claude-control repo) and pushes it, idempotent via task.yaml's master_synced_at marker."
    - path: ai-os/scripts/sync-repos.sh
      covers: "Pulls latest commits for all mirrored repos. Fast-forward only, never overwrites local changes."
    - path: ai-os/scripts/sync-vercel-env.sh
      covers: "Refreshes .env.local for the 3 linked Vercel projects with current production env vars."
    - path: ai-os/scripts/sync-verdian-ai-data.sh
      covers: "Full refresh of the verdian-ai Supabase local mirror (schema + data) -- destructive to the local mirror only (drops/recreates public+compliance schemas each run), does not touch the remote database."
    - path: ai-os/scripts/system-sync.py
      covers: "Single unified sync/audit mechanism for 'one server, one system' (Owner directive 2026-07-20, tasks 2-6 of the 6-part systems-design mandate) -- CONSTITUTION.yaml staleness detection plus files/registry integration checks."
    - path: ai-os/scripts/task-template.py
      covers: "Renders the Master/Supervisor pilot's standardized task dict (TASK ID/MODULE/OBJECTIVE/FILES ALLOWED/FILES FORBIDDEN/DEPENDENCIES/INPUT/OUTPUT/STEPS/CONSTRAINTS/VALIDATION/DONE CRITERIA) into the prompt text handed to veridian-task.py create."
    - path: ai-os/scripts/tight_task_validation.py
      covers: "Faithful Python port of src/lib/task-tightening.ts's validateTightTask() for the shell-layer dispatch scripts (worker-entrypoint.sh, doc-worker-entrypoint.sh), which have no access to the TS/DB-backed original."
    - path: ai-os/scripts/veridian-task.py
      covers: "VERIDIAN-DEV AI-OS task/worker manager -- server-side, self-contained state management for async AI workers that survive client disconnects, server reboots, and resume from checkpoints."
    - path: ai-os/scripts/worker-entrypoint.sh
      covers: "Entrypoint for a systemd-managed VERIDIAN AI worker -- runs Claude Code headlessly against an isolated git worktree, checkpoints periodically, resumes on restart, pushes the branch on success (never merges/deploys)."

  exempted:
    # Real product/spec/reference docs that are NOT governance-scoped --
    # listed here so check-metadata-index-coverage.mjs's scan doesn't flag
    # them as an oversight. Add a real reason when adding an entry, same
    # discipline as asset-registry-coverage.yaml's own exempted list.
    - path: docs/
      reason: "Product technical-spec documents (ComplianceTrack_TechSpec_*.docx etc), not AI-OS governance. Corrected 2026-07-14 (DEBT-06, ai-os/CONSTITUTION.yaml): this exemption does NOT extend to docs/master/ specifically -- scripts/check-doc-cross-references.mjs already treats docs/master/INDEX.md as 1 of its 6 real governance entry points, so it was never actually exempt in practice; this note now says so explicitly instead of silently contradicting that script's own behavior."
    - path: ai-os/OS.yaml
      reason: "This file is the index itself -- it does not need to index itself."
    - path: ai-os/audits/owner_engine_reaudit_2026-07-27.md
      reason: "Point-in-time independent re-audit findings report (2026-07-27), not an ongoing governance doc -- same class as other dated audit reports, real evidence/verdicts, not a spec to keep indexed long-term."
    - path: ai-os/audits/projexa_erp_e2e_reaudit_2026-07-27.md
      reason: "Point-in-time independent re-audit findings report (2026-07-27), not an ongoing governance doc -- same class as owner_engine_reaudit above."
    - path: ai-os/audits/reporting_module_reaudit_2026-07-27.md
      reason: "Point-in-time independent re-audit findings report (2026-07-27), same class as the other two audit reports above."
    - path: ai-os/v2-7-workflow-change.patch
      reason: "A git-format patch file (2026-07-20, V2-7) carrying the .github/workflows/sync-vercel-env.yml per-env scoping edit that this session's GitHub token cannot push directly (token lacks the `workflow` scope; GitHub rejects workflow-file pushes from such tokens). The design + rationale it implements live in the indexed ai-os/STAGING_ENV_2026-07-20.md; the patch is a delivery artifact for an Owner-push handoff, not a governance document -- indexed here only so check-metadata-index-coverage.mjs's scan does not flag it as an oversight. Remove once the Owner applies it and the synced .yml lands on main."
    - path: ai-os/fixtures/ocid020-nav-surface-115.json
      reason: "Real, real-mechanically-discovered test fixture (OCID-050 TASK-050-0) -- the 115-item internal-path nav-surface list used by OCID-020/050's own page-sweep test harnesses. A test-data artifact, not a governance policy/spec document -- exempted here only so check-metadata-index-coverage.mjs's scan does not flag it as an oversight. Its own provenance/usage is documented in ai-os/PROJEXA_AI_COM_E2E_CERTIFICATION_OCID050_DATA_STATE_TASK_BREAKDOWN_2026-08-03.md and ai-os/PROJEXA_AI_COM_E2E_CERTIFICATION_NAV_SWEEP_COMPLETE_2026-08-03.md."
