NEXT_PUBLIC_SUPABASE_URL=https://pcrjmlpuqsbocqfwoxod.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InBjcmptbHB1cXNib2NxZndveG9kIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODIzMTkxNTAsImV4cCI6MjA5Nzg5NTE1MH0.nH-nltFt8M3nJG_clP9Zw3_FsXtn-8mKrFeqdE414oU

Save this anon key in your memory and the notes also so that you can recall it. 
No restart needed — the dev server isn't running.

One thing to be careful about

Then

Run the R28 prompt. Its first action is grep -c on those two variable names — it'll confirm they're present without ever reading the values, then bring the stack up and release all 21 blocked points.

=======
R28

RUN R28-22AUG. Work CONTINUOUSLY until platform.cc_queue returns zero rows.
Do not stop to ask. Do not wait to be prompted between points.

═══ 0. PROVE YOUR WRITES LAND (once, first) ═══
platform.cc_spec has RLS ENABLED and ZERO POLICIES — a non-bypassing role makes
every write silently vanish.
  update platform.cc_spec set heartbeat_at = now() where point_no = 0;
  select heartbeat_at from platform.cc_spec where point_no = 0;
Timestamp did not move? STOP. Report "cc_spec writes not landing — RLS role".

═══ 1. PROTOCOL ═══
Supabase pcrjmlpuqsbocqfwoxod.
  select what_to_do from platform.cc_spec where point_no = 0;
Protocol, not a task. Never claim it, never close it.
  test -d /work && echo CONTAINER || echo LAPTOP   → LAPTOP. Translate no paths.

═══ 2. POINT 174 IS THE GATE ═══
The queue holds ONE row: point 174. The other 21 are BLOCKED-STACK and appear
only when you clear it. Deliberate.

FIRST ACTION — one command, no file opened:
  cd "C:\Users\Dell\Projects\compliance-tracker" ; grep -c NEXT_PUBLIC_SUPABASE_URL .env ; grep -c NEXT_PUBLIC_SUPABASE_ANON_KEY .env
EITHER returns 0 → the credential was never supplied. Set
cc_status='BLOCKED-RAJAT - .env missing NEXT_PUBLIC_SUPABASE_*', attempt_count=0,
and STOP. DO NOT SPEND ATTEMPTS ON A KNOWN-MISSING CREDENTIAL.

Why it matters: compliance-tracker has NO middleware.ts — Next.js 15 renamed it
to src/proxy.ts, which calls createServerClient with non-null assertions on those
two vars and whose matcher does NOT exclude /api. It throws before any route
handler runs. That is why /api/health 500s despite importing only drizzle.

  T1: cd "C:\Users\Dell\Projects\compliance-tracker" ; bun run dev            (3000)
  T2: cd "C:\Users\Dell\Downloads\Claude Code\projexa"
      set VERIDIAN_API_BASE_URL=http://localhost:3000/api/v1/projexa
      bun run dev                                                             (3100)
bun install IS PERMITTED. The @sentry/node-core gap is already resolved.

NEVER read, print, paste, create or edit .env. grep -c returns a count, never a
value — the only permitted contact.
DO NOT remove the non-null assertions in src/proxy.ts to force a boot. That
starts the app with no auth layer — worse than not booting.

IDENTITY CHECK before declaring the stack up — STABLE markers only:
  (a) select count(*) from compliance.organisations where id='projexa_demo_org' → 1
  (b) select count(*) from information_schema.tables where table_schema='compliance' → >400
  (c) select count(*) from platform.cc_spec where point_no=0 → 1
NEVER use a report_definitions count — it was 216, is now 218. Counts drift.

WHEN 174 PASSES, release the 21 yourself:
  update platform.cc_spec
     set cc_status = replace(cc_status,'BLOCKED-STACK - ',''),
         attempt_count = 0, cc_updated_at = now()
   where not cc_closed and cc_status like 'BLOCKED-STACK%';
Re-read cc_queue — 22 rows — and continue.

IF 174 FAILS THREE REAL ATTEMPTS (not a missing credential): leave the 21
blocked, mark 174 FAILED-3, STOP.

═══ 3. THE WALK ═══
  select * from platform.cc_queue;
Lowest read_order first. Work, write back, next, until zero rows.
'RESUME' = a prior session abandoned it; pick it up.
CLAIM: update platform.cc_spec set cc_status='IN_PROGRESS - R28', run_id='R28',
  heartbeat_at=now(), started_at=coalesce(started_at,now()) where point_no=<n>;
Heartbeat every 20 min; 30-min stale returns the row automatically.
WHEN FINISHED cc_status MUST START WITH 'DONE - '. Any other prefix and the next
session re-works it.

═══ 4. NO DEPLOYED SERVER, NO SSH ═══
veridian-aios.com and projexa-ai.com return 500. Use no Vercel URL, do not
redeploy. DO NOT SSH to 167.233.220.35, DO NOT run docker — veridian-server-gate
blocks it and is a safety control. DB failure → report ONLY the SQLSTATE, stop
that point.

═══ 5. EVERY POINT ═══
 1. AUDIT BEFORE — gate1_static, record every number, before changing anything.
                   Rows say DISCRIMINATOR or WATCHDOG.
 2. DO THE WORK  — exactly the row. Nothing outside it.
 3. AUDIT AFTER  — re-run gate1_static.
 4. THREE GATES  — static, behaviour, negative. Each BOOLEAN. Gate 3 must fire.

GATE HYGIENE — every affected row now carries its own warning, read them:
 · COUNT COMMANDS: use PowerShell. Git Bash gave 294 and 51 against a real 293
   here. Affects points 1, 2, 117, 135, 186, 191, 192.
 · UNANCHORED grep -c counts COMMENTS as code (E-111). Affects 1, 2, 117, 143,
   147, 151, 152, 153, 173, 197. Re-run anchored before reporting a mismatch.
 · grep -c exits 1 on zero. All && chains have been removed from gates; keep it
   that way — use ;
 · Points 2, 149, 173 carry "UPDATED AFTER R26" notes. A number disagreeing with
   a baseline may mean an earlier point already fixed it. Check that point's
   proof_of_work before calling it a regression.

═══ 6. WRITE BACK ═══
Claimed row only: cc_status, gate1_actual, gate2_actual, gate3_actual,
proof_of_work, attempt_count, files_changed, pr_url, errors_hit, cc_updated_at,
finished_at, heartbeat_at.
proof_of_work = RAW MACHINE OUTPUT verbatim, never a summary. Confident status +
empty proof = UNPROVEN, reopened.
NEVER set cc_closed or chat_verified. NEVER work an owner=CHAT or HUMAN row.

═══ 7. THREE ATTEMPTS, DIFFERENT LOGIC ═══
Three genuinely different approaches, not one command retyped. Then set
attempt_count=3, cc_status='FAILED-3 - <all three ways>', errors_hit=<blocker>,
and MOVE ON. Do not stop (except the 174 rule).
INTERNET IS UNRELIABLE HERE. A network drop is NOT an attempt — poll every 5
seconds until it returns, continue the SAME point at the SAME attempt_count,
then RE-RUN YOUR LAST WRITE AND CONFIRM IT LANDED.

═══ 8. DO NOT DRIFT ═══
The row is the instruction. Read every do_not_assume. Traps in this queue:
 · FOUR points say VERIFY, NOT BUILD — 151, 153, 32, 33.
 · 171: 24 Playwright specs exist. RUN them; do not write a 25th.
 · 188: count the pills before hiding anything. "402" is unverified.
 · 158: no drawing table exists, BOQ has no Dwg Code column. Report, don't build.
 · 175: compliance.organisations is BRITISH SPELLING; currency table is
   compliance.erp_currencies; construction_boq_line_items has NO org_id.
 · 186: a page that renders is NOT a pass. Confirm writes by querying Supabase.

═══ 9. PUSH BACK ═══
Premise looks wrong → STOP, write it in errors_hit, move on. You have been right
every time: point 142's impossible gate, point 194's wrong database, point 174's
misdiagnosed boot path — all chat's defects, not yours.

═══ REPORT ═══
POINT: <n> / FILES CHANGED: <paths or NONE> / PR URL: <url or N/A> /
RESULT: PASS or FAIL per named gate / BLOCKER: <one line> or NONE
Nothing else.

Begin with the write test, then point 0, then the .env grep.

===================

YOU CAN TAKE DECISION THAT ARE LOGICAL AND HELPS TO COMPLETE THE EACH POINT
YOU NEED TO ENSURE THAT QUALITY IS MAINTAINED
DONT ASSUME ANYTHING
VERIFY BEFORE EXECUTION
YOU CANNOT RELY ON YOUR MEMORY ONLY, YOU NEED TO READ DOCUMENTATION
YOUR OBJECTIVE IS TO COMPLETE THE WORK WITH AUDIT AND QUALITY