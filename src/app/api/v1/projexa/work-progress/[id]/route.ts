// R67 D-28: same re-export shape as ../route.ts -- PROJEXA's work-progress
// namespace is an alias over the construction one, never a second handler.
export { GET, PATCH, DELETE } from "@/app/api/v1/construction/progress/[id]/route"
