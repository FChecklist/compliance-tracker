// Stub of src/lib/email.ts for the ai-work-link-exec bundle: the email sender. A link write sends no email.
import { thrower } from "./unavailable"
export const notifyAssigned = thrower("the email sender (notifyAssigned)")
