// Stub of next/headers for the ai-work-link-exec bundle: there is no incoming request to read cookies or headers from.
import { thrower } from "./unavailable"
export const cookies = thrower("next/headers cookies")
export const headers = thrower("next/headers headers")
