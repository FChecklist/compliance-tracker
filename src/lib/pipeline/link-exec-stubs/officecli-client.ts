// Stub of src/lib/officecli-client.ts for the ai-work-link-exec bundle: the vendored docx/pptx reader (a spawned binary). No link function reads a document.
import { thrower } from "./unavailable"
export const extractDocxRawText = thrower("the docx reader (extractDocxRawText)")
export const extractPptxRawText = thrower("the pptx reader (extractPptxRawText)")
