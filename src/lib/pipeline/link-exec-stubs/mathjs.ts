// Stub of mathjs for the ai-work-link-exec bundle (scripts/awl-exec-aliases.mjs). Nothing in the closure imports it today; if a later change does, the
// bundle build fails on the missing export, which is the point: add the name here only after deciding the link path never calls it.
export {}
