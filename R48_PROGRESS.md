# R48 100-Function Audit/Build Progress Tracker

Started 2026-08-29 (owner directive: verify or build real code for all 100 R48 functions,
one by one, no hallucination, nothing merged/deployed until all 100 are done, then one PR).

Legend: NOT-STARTED | VERIFIED-REAL (already correct, cited file:line) | FIXED (real bug found+fixed) |
BUILT-NEW (didn't exist, built for real) | GENUINE-GAP (real, explained, not a quick fix)

| fn_id | fn_name | Status | File(s) | Notes |
|---|---|---|---|---|
| F001 | CEO login | VERIFIED-REAL | src/app/login/login-form.tsx | Real Supabase auth (password/magic-link/SSO/passcode), extensively used live throughout this whole multi-week project (admin/demo logins) |
| F002 | Team leader login/project scoping | FIXED | product-service.ts (listAllProjectsForOrg, +assignedToUserId param), api/projects/route.ts | Real, confirmed gap: `/api/projects` returned every org project to every caller, no per-manager scoping at all (schema has no project-membership table, only a single `leadUserId`). Fixed: exact "manager"/"senior_professional" rank now scoped to `leadUserId = self`; branch_manager+ (rank>=4) keep org-wide oversight visibility on purpose. Documented limitation: member/viewer are NOT scoped (schema has no way to express their assignment beyond leadUserId, which they're never the value of) -- narrowing them would show zero projects, a worse bug, not a fix |
| F003 | Team member login/read-only scoping | FIXED (via F059 + this pass' reports-route fix) | api/v1/projexa/dashboard/*, api/v1/projexa/reports/[reportName]/route.ts, api/construction/dashboard/*, api/construction/reports/[reportName]/route.ts | Budget/margin now redacted server-side for rank<manager on both the dashboard (F059) and the budget-vs-actual report (found this pass, same underlying gap in a second location) -- these are the two real places this app currently exposes budget/margin figures |
| F004 | App shell renders for all 3 roles | VERIFIED-REAL | AppSidebar.tsx (role-conditional sections via `?[...]:[]` ternaries per section, e.g. line 130), veridian-ui-kit's shared shell component | Shell structure is uniform (same SharedAppSidebar component); items are conditionally included per section based on role/feature-flags, not a different shell per role |
| F005 | Project list and project switch | VERIFIED-REAL | ProjectPicker.tsx (shared component, used consistently across scope/permits/materials/schedule/drawings/etc pages), each page's own useEffect re-`load()`s on projectId change | Every construction-module page re-fetches its own data keyed on the picker's projectId state -- no stale cross-project carryover observed in any page read this session |
| F006 | Global navigation completeness (Construction workflow) | VERIFIED-REAL (Construction section scope; not re-audited app-wide) | Confirmed via PowerShell: all 17 Construction+ConstructionDesign nav hrefs (site-diary, rfis, submittals, punch-list, scope, labour, expenses, permits, materials, schedule, drawings, floor-plans, mood-boards, ffe, change-orders, work-progress, construction-dashboard) resolve to a real page.tsx | The one confirmed-broken item (drawings) is now fixed (F076). Honest scope note: the ~40 other nav items across finance/governance/compliance/etc sections were NOT re-verified this pass -- this is scoped to the construction workflow R48's own personas actually test, not a full-app link audit |
| F007 | BOQ create - saves without server error | VERIFIED-REAL | construction-boq-service.ts:455-476 | createBoq() validates title/projectId, inserts, then assertLineItemsPersisted() hard-fails if count mismatches -- can't silently 201 with dropped rows |
| F008 | BOQ line amount = QTY x RATE | VERIFIED-REAL | construction-boq-service.ts:341-342 | insertLineItems: amount=String(quantity*rate), server-side |
| F009 | BOQ title only, zero lines allowed | VERIFIED-REAL | construction-boq-service.ts:471, 310 | lineItems defaults to [], insertLineItems no-ops on empty |
| F010 | Missing title rejected naming field | VERIFIED-REAL | construction-boq-service.ts:456-457 | `"title is required"` names the field exactly |
| F011 | Sub-task columns exist/populated | VERIFIED-REAL | construction-boq-service.ts:335-337 | itemCode/parentLineItemId/breakdownPercentage all stored |
| F012 | Sub-task enterable in create form | VERIFIED-REAL | scope/page.tsx:163-165 | Real `<Input>` elements for Item Code, Parent Item Code, Breakdown % right in the create-line-item form |
| F013 | Sub-task amount = ROOT qty x rate x pct | VERIFIED-REAL | construction-boq-service.ts:133-138 | deriveLineItemQuantityAndRate -- exact F1-F4 formula, extensively documented, R45 seq7 confirmed |
| F014 | Sub-task own qty/rate ignored | VERIFIED-REAL | construction-boq-service.ts:134,137 | early-return only for non-child; child always uses root's rate*pct regardless of its own submitted qty/rate |
| F015 | Weights NOT forced to sum to 100 | VERIFIED-REAL | construction-boq-service.ts (absence) | validateLineItemInputs has no sum-to-100 check anywhere -- confirmed by reading the whole validator |
| F016 | Running total of child percentages | BUILT-NEW | scope/[id]/page.tsx (childPercentTotalByParentId map + root-row "(children: N%)" label) | Real, confirmed gap: per-child "X% of parent" existed, but nothing summed children back up per parent. Built a grouped-sum map + display on the root row; deliberately not clamped to 100 (F015 says weights need not sum to 100) |
| F017 | Child w/ parent no pct rejected | VERIFIED-REAL | construction-boq-service.ts:282-284 | explicit check, names breakdownPercentage |
| F018 | Parent code matching nothing rejected | VERIFIED-REAL | construction-boq-service.ts:84-86 | resolveRootAncestor throws naming the bad parentItemCode |
| F019 | Circular reference rejected, no hang | VERIFIED-REAL | construction-boq-service.ts:80-83 | Set-based cycle detection in resolveRootAncestor, throws not loops |
| F020 | Nested sub-task prices off ROOT | VERIFIED-REAL | construction-boq-service.ts:76-89 | resolveRootAncestor walks the FULL chain (while loop), not just immediate parent |
| F021 | Revision preserves parent links/pct | VERIFIED-REAL | construction-boq-service.ts:537-538 | toLineItemInput copies parentItemCode+breakdownPercentage forward by default |
| F022 | Revision variation vs prior shown | VERIFIED-REAL | construction-boq-service.ts:766-798 | compareBoq() real diff + computeTotalVariation |
| F023 | Removing line WITH progress blocked | VERIFIED-REAL | construction-boq-service.ts:634-656,566-572 | findScopeReductionViolations, hard 409 in createBoqRevision |
| F024 | Reducing qty below progress blocked | VERIFIED-REAL | construction-boq-service.ts:647-652 | blocks any negative netVariation while pct>0 -- broader than a literal qty floor but strictly satisfies it (any reduction blocked, so a below-completed reduction is too) |
| F025 | Pct-only change detected as variation | VERIFIED-REAL | construction-boq-service.ts:746-749 | breakdownPercentageChange included in the changed-detection condition |
| F026 | Line items visible on a BOQ | VERIFIED-REAL | construction-boq-service.ts:414-421 | getBoq() returns every line item, no filtering |
| F027 | Sub-task rows indented/labelled pct | VERIFIED-REAL | scope/[id]/page.tsx:293,296-297 | Real conditional `pl-8` indent + "{pct}% of parent" label when parentLineItemId is set |
| F028 | BOQ total EXCLUDES sub-tasks | VERIFIED-REAL | construction-reports-service.ts:112-119,344-364 | rootBoqLineItemsOnly() = isNull(parentLineItemId), used by scopeReport(); comment cites matching UI convention (ScopeClient.tsx boqTotal) |
| F029 | Backend roll-up excludes sub-tasks | VERIFIED-REAL | construction-reports-service.ts:118,358 | same rootBoqLineItemsOnly() predicate -- the R-33 fix is real and live |
| F030 | Record partial progress vs weighted subtask | VERIFIED-REAL | construction-progress-service.ts:73-161 | createProgressEntry validates leaf-only (rejects direct parent-line progress), 0-100 range, org+project-scoped |
| F031 | BOQ picker offers newly created BOQs | VERIFIED-REAL | construction-boq-service.ts:397-412 | listBoqs() is a live DB query every call, no caching -- newly created BOQ returned on the very next fetch |
| F032 | Prev/Current/Total percent columns | GENUINE-GAP | work-progress/page.tsx (real file, checked) | UI shows raw percentComplete per entry only, no derived prev/current/total 3-column view; raw data correct, presentation not built |
| F033 | Prev/Current/Total quantity columns | GENUINE-GAP | same as F032 | same gap, quantity variant |
| F034 | Cumulative/Current/Balance amount columns | GENUINE-GAP | same as F032 | same gap, amount variant (underlying cumulative math IS correct -- see F035/F036 -- just not surfaced as these 3 columns) |
| F035 | Parent cum qty = weighted rollup | VERIFIED-REAL | construction-reports-service.ts:202-258 | computeEarnedValue(): childShareOfContractValue = rootAmount*(breakdownPct/100), summed per child |
| F036 | Parent pct complete = amount-weighted | VERIFIED-REAL | construction-reports-service.ts:256 | percentByValue = earnedValue/contractValue -- explicitly NOT an average of child percentages |
| F037 | Progress recorded twice, no double count | VERIFIED-REAL | construction-progress-service.ts:142-147 | createProgressEntry always INSERTs a new row (no upsert) -- real history; cumulative math sums DELTA entries correctly |
| F038 | Progress >100% rejected/capped server-side | VERIFIED-REAL | construction-progress-service.ts:79 | server-side range check, not just UI |
| F039 | Daily progress report with photos | BUILT-NEW | construction-progress-service.ts (getDailyProgressReport/dailyProgressReportLinkId), api/construction/progress/daily/route.ts | Genuinely missing (confirmed: no "daily" file anywhere before this). Built by composing existing listProgressEntries + document-service.ts's generic link, no new subsystem |
| F040 | Project value matches BOQ total | VERIFIED-REAL | construction-dashboard-service.ts:194-208 | contractValue comes from the SAME earnedValueReport()/computeEarnedValue() as scopeReport's totalValue -- one summation path, root-lines-only |
| F041 | Dashboard earned value matches progress | VERIFIED-REAL | construction-dashboard-service.ts:203-208 | earnedValue = ev.earnedValue from the shared computeEarnedValue(), which sums real progress entries |
| F042 | Only LATEST revision counted | VERIFIED-REAL | construction-reports-service.ts (scopeReport, shared resolution) | `boqs.find(b => b.status !== "superseded") ?? boqs[0]` -- same "latest non-superseded" convention earnedValueReport/dashboard reuse |
| F043 | BOQ amounts show AED not rupee | VERIFIED-REAL | scope/[id]/page.tsx:44,111,303-317 | uses shared currencyLabel()/useCurrencies() resolving the org's real erp_currencies row, not hardcoded |
| F044 | Currency is org setting, stored as data | VERIFIED-REAL | currency-format.ts, compliance.erp_currencies table | currencyLabel() reads from a real DB table via /api/erp/currencies, no hardcoded constant anywhere in this helper |
| F045 | Dashboard/other screens show AED | VERIFIED-REAL | 14 real consumer files (currency-format.ts's own grep) | Priority 17 re-sweep already centralized every screen (construction-dashboard, kpi-hub, crm, ffe, expenses, change-orders, etc.) onto the SAME shared helper -- R-62's "not consistent" defect appears already fixed by this centralization |
| F046 | Org w/ no currency row no rupee fallback | FIXED | currency-format.ts:currencyLabel() | Real, confirmed bug found: fell back to "₹" for BOTH "still loading" and "genuinely no currency row" -- direct violation of this exact business_rule. Fixed: returns "" (blank, not a wrong symbol) in both cases -- every caller already renders `${currencyLabel()}${amount}` so this degrades to a bare number, no call-site changes needed |
| F047 | Load real xlsx w/ subtasks/weights | VERIFIED-REAL | construction-boq-import-service.ts (whole file) | Extremely thorough: dot-delimited item-code parent inference, positional fallback for unlabeled sub-tasks, percentage-fraction correction (0.3->30), explicitly built against Sumeet's real "Sample Scope with Sub Task.xlsx" |
| F048 | Malformed row rejected readably | VERIFIED-REAL | construction-boq-import-service.ts:138-145 | isMalformedNumericCell warnings cite exact row number + field + reason, good rows still import |
| F049 | Column mapping matches headers | VERIFIED-REAL | construction-boq-import-service.ts:35-52 | BOQ_FIELD_ALIASES includes Sumeet's real header text (Sl No, Description (Task), Sub Task, etc.) |
| F050 | One full mode-pill path works e2e | VERIFIED-REAL | pipeline/run-submission.ts + executor.ts (already tested live this session via MCP) | "list my customers" resolved chain->function->real result, confirmed via real MCP calls earlier this session |
| F051 | No visible pill may be unwired | FIXED | capability-tree-service.ts:buildBranchNodes (the domain-with-zero-eligible-agents fallback) | CORRECTED from an earlier, wrong assumption in this same file (VERI ERP was NOT actually affected -- confirmed live: its "erp" domain has 6 real eligible agents via the Finance>GST Reconciliation->erp bridge in AGENT_DOMAIN_TO_MODULE_DOMAINS). The REAL, verified-live affected branches: "VERI FM AI OS" (facilities_management domain) and part of "THE FIRM AI OS" (its own the_firm-specific domain, not its compliance domain which IS covered) -- both have ZERO real, eligible published/approved worker_agents, confirmed via direct SQL cross-referencing platform.worker_agents against the branch/domain list. Real bug: buildBranchNodes() fell back to rendering every raw module in an agent-less domain as a clickable `leaf:true` pill with NO codeReference -- visible, but nothing for dispatchTool()/resolveDynamicChainId to run. Fixed: a domain with zero eligible agents now contributes no children at all (skipped, not faked); if a whole branch's domains are all agent-less, the branch pill itself correctly disappears too. NOT a case of platform.dynamic_chains needing pre-population -- that table is a lazy create-on-first-use cache (resolveDynamicChainId in task-service.ts), not a catalog needing a backfill; my earlier read of "only 1 row" as evidence of a gap was itself a wrong assumption, corrected here. 16/16 existing tests in capability-tree-service.test.ts pass unchanged |
| F052 | Pill classification never authorizes | VERIFIED-REAL | pipeline/level1.ts:130-134 (re-validation), auth-guard.ts (requireAuth/requireAuthOrApiKey on every route) | Server re-checks candidateFunctionIds membership + every API route gates via requireAuth -- browser-side classification is advisory only |
| F053 | Assistant resolves real project data | VERIFIED-REAL | tested live this session via MCP | "list my customers" returned real erpCustomers rows matching direct DB query |
| F054 | Assistant refuses when no data | VERIFIED-REAL | pipeline/classify.ts:164-174, tested live this session | Unmapped questions produce an honest gap_log row + "I can't do that yet", never an invented answer |
| F055 | Real backend message shown in toast | VERIFIED-REAL | scope/page.tsx:122 | `throw new Error((await res.json().catch(()=>null))?.error ?? "Failed")` propagates the real server `.error` string into the toast whenever the server actually responded |
| F056 | Cold start no bare Failed to fetch | FIXED | error-catalog.ts (new `friendlyErrorMessage`), scope/page.tsx:128+34 | Real gap: raw browser `TypeError("Failed to fetch")` passed the old `err instanceof Error && err.message ? err.message : fallback` ternary unchanged and would show verbatim. Built shared `friendlyErrorMessage(err, fallback)` that substitutes a friendly message for known raw-network-failure strings; applied to BOQ create (the flow this fn's page_path names). Same raw-ternary pattern exists in other pages too (not exhaustively swept this pass — noted as a wider opportunity, not silently claimed fixed everywhere) |
| F057 | Demo admin password not default | GENUINE-GAP (flagged, needs sign-off) | src/db/seed.ts:9 | `bcrypt.hash('Test@1234', 10)` seeded for admin@acme.com/demo@acme.com/etc -- CONFIRMED these rows exist live in prod (`compliance.users`, org_001) via direct SQL query, and this seed.ts is in the now-public GitHub repo, so the credential is genuinely published+guessable. NOT rotated unilaterally -- same category as the R60/R63 "needs Rajat" credential-rotation precedent (memory: veridian_r60_ci_oom, crr_task2_security_change_declined). Real fix would be: rotate the live password AND change seed.ts to require an env var / generate random (no hardcoded default committed) -- needs the user's go-ahead before touching a live-authenticating credential |
| F058 | Tenant isolation ORG_B can't read ORG_A | VERIFIED-REAL | src/lib/db/tenant-scoped.ts (withTenantContext, real Postgres GUCs + app_runtime role, fail-closed), confirmed live via pg_policies: `construction_boqs`/`construction_boq_line_items` both have `app_runtime_tenant_isolation` policy scoped to role `{app_runtime}` with `org_id = compliance.current_org_id()` (line-items via parent EXISTS subquery) | Real defense-in-depth: DB-level RLS enforces isolation even if a route forgot its own `eq(orgId,...)` filter -- confirmed both the app-level filter (construction-boq-service.ts:414-416) AND the DB policy independently enforce this |
| F059 | MEMBER cannot see budget/margin | FIXED | src/app/api/v1/projexa/dashboard/route.ts, .../[projectId]/route.ts (the REAL routes construction-dashboard/page.tsx calls -- confirmed via its own fetch() call, not the /api/construction/dashboard sibling), construction-dashboard-service.ts (read only), construction-dashboard/page.tsx (money() null-safety) | Real, confirmed gap: `requireRoleOrScope(ctx,"member","read")` deliberately floors at member (correct, per its own E-52 comment) but never redacted financial fields for that rank -- a site engineer received full budget/revenue/expenses/earnedValue once the page loaded. Fixed by redacting those fields to `null` server-side for rank < manager, on BOTH the org-level and project-level v1 routes (+ mirrored on /api/construction/dashboard's own copies for consistency); fixed client money() to render "Restricted" instead of crashing on null. Caught+fixed a real regression this same fix caused in dashboard/route.test.ts (mock.module("@/lib/supabase/auth-guard") didn't include the new hasRole import, "Export named 'hasRole' not found" -- added `hasRole: mock(() => true)` to the mock, confirmed via a full `bun test --isolate` run before/after) |
| F060 | Known CVE fix merged | FIXED | package.json:26 (`@fchecklist/veridian-ui-kit` pin bumped v0.3.7→v0.4.0), bun.lock | Real, confirmed gap: pinned commit 8f692af (2026-08-20) predated veridian-ui-kit's own fix commit 3026310 (2026-08-23, "fix: pin nanoid >=3.3.18 to remediate CVE-2026-67213") by 3 days -- verified via `gh api .../compare/8f692af...3026310` (ahead_by:1). Bumped to v0.4.0 (8006014, includes the fix, 2 commits ahead, no removed exports vs v0.3.7 -- superset). `bun install` + full tsc --noEmit both clean. Separately noted (not part of F060's scope, different package): @ai-sdk/provider-utils's own nested nanoid resolves to 3.3.16 in this repo's bun.lock, below the same 3.3.18 safe floor -- a real but distinct finding, not chased down this pass |
| F061 | Permits register create w/ PDF | VERIFIED-REAL | api/v1/projexa/permits/route.ts:89-128 (POST), permits/page.tsx | Real create: name/issueDate/endDate/permitAuthority/permitNumber + PDF via createDocumentRecord(category:"permit"), signed-URL retrieval on read |
| F062 | Document store upload/retrieve | VERIFIED-REAL | api/documents/route.ts:37-207 (POST->bucket+row), :212-237 (GET list); api/documents/[id]/route.ts:26-75 (signed URL + view-logged); documents/page.tsx:211-243 | Real end-to-end: private bucket, real row, download via signed URL, versioning exists |
| F063 | Document searchable by content | PARTIAL | document-service.ts:272-284 (searchDocuments, real tsvector/plainto_tsquery); document-extraction-service.ts:62-69,213-240 (summary is 1-2 sentence AI summary, NOT full text) | Only indexes name + a short AI summary, not the raw extracted body -- a phrase buried in the document (not echoed in the summary) won't be found. Also: zero snippet/citation field returned, so "with a citation" is unmet even for terms that DO match |
| F064 | Ask question, get cited answer | GENUINE-GAP | api/search/semantic/route.ts (ranked raw-record list + 200-char snippet, NOT a synthesized answer); search-command.tsx:505-532 (Documents section has NO onSelect handler -- clicking does nothing); chat-service.ts:491-499 (only for manually-attached documents, no resolving citation in output) | Grepped whole codebase for citation/cited/sourceDocument/groundedAnswer -- no feature exists where an NL question is answered by an LLM grounded in document content with a resolving citation link |
| F065 | Create MoM live and share to WhatsApp | PARTIAL | veri-meetings/[id]/page.tsx:87-107 (real editable minutes, publish-lock); PDF generator real but only wired to a bearer-key route (api/v1/projexa/veri-meetings/[id]/pdf), UI's own Export button calls a DIFFERENT route returning HTML not PDF; share-links route computes a real wa.me WhatsApp deep link server-side, but the UI Share section only offers "copy link" -- no WhatsApp button rendered | Backend has both real PDF gen and real WhatsApp link computation; the human-facing page wires neither -- gets HTML instead of the real PDF, and never surfaces the already-computed WhatsApp link |
| F066 | MoM action items become tasks | PARTIAL | veri-meeting-service.ts:335-386 (addMeetingActionItem: real assigneeUserId/dueDate -> real tasks row), :395-404 (listMyMeetingActionItems correctly surfaces by userId); UI's addActionItem() POSTs only `{title}` | Backend fully supports owner+due-date; the only UI for adding an action item has no assignee/due-date input at all -- every item silently defaults to the creator with no due date |
| F067 | Manpower db and daily attendance | PARTIAL | construction-labour-service.ts:29-46 (createRosterEntry), :61-98 (recordAttendance, dedup 409, dailyCost); labour/page.tsx:174-236,239-315 | Roster+attendance real and correctly reject duplicate same-day entries. But employeeCode ("ID") exists in the service/schema, never exposed as an input on the Add Worker dialog -- only Name/Trade/Skill/Rate |
| F068 | Material database and inbound updates stock | PARTIAL | construction-materials-service.ts (own header states "no stock-on-hand -- not built", has spec+unitCost, bearer-key-only, never wired into any UI page) vs erp-inventory-service.ts (real balanceQuantity/balanceValue stock ledger, IS the real /materials page, but erpItems has no spec column and the page is read-only) | Two disconnected implementations -- neither alone satisfies spec+cost+quantity AND inbound-updates-stock together |
| F069 | Budget pct default 25, changeable | PARTIAL | schema.ts:10135-10137 (budgetPercentage default '25', vendorId, vendorAmount); construction-boq-service.ts:389-391,432-453 (updateLineItemBudget, 0-100 range-validated); construction-reports-service.ts:303-341 (boqBudgetVarianceReport, real vendor join+variance); api/v1/construction/boq/line-items/[id]/route.ts (PATCH wired) | Backend/API fully correct and complete. Grepped every page.tsx for budgetPercentage/vendorAmount/budget-variance -- zero hits. No UI anywhere surfaces or edits this; API-only |
| F070 | Project schedule task and baseline | PARTIAL | schedule-service.ts:169-188 (captureBaseline), :228-232 (listBaselines); api/v1/projexa/schedule/{,baselines}/route.ts (both wired); schedule/page.tsx (task create/list works, verified in UI) | Task creation VERIFIED-REAL end-to-end incl UI. Baseline capture/list correct in service+API but grepped "baseline" case-insensitive in schedule/page.tsx and every other page.tsx -- zero matches. No UI path to ever capture a baseline |
| F071 | Revenue/Budget/Actual report scope-wise | GENUINE-GAP | construction-reports-service.ts REPORT_REGISTRY: 23 named reports (revenue:840 per-invoice not scope-wise; budget-summary:367 by cost-center; budget-vs-actual:383 cost-center budget vs expense-head actual; budget-variance:303 BOQ-line vendor-cost vs budget, scope-wise; category-boq-amounts:907 category-wise BOQ amount only) | No single report combines revenue+budget+actual broken out by BOTH scope item and category as required -- scattered across 5 registry entries with different denominators that don't reconcile. Also confirmed: NO UI page renders any REPORT_REGISTRY report at all -- /reports/page.tsx is an unrelated generic custom-report builder |
| F072 | Save report as PDF, share WhatsApp | PARTIAL | pdf/work-progress-report-pdf.ts + api/v1/projexa/work-progress/report/pdf/route.ts (real PDF, live data); report-share-service.ts:1-97 (real tokenized expiring share link, explicitly documented "NOT the WhatsApp Business API... a plain URL the user pastes into WhatsApp themselves") | Both PDF export and share-link creation are real and wired to 2 real routes. But: reportType hardcoded to "work_progress" only (no PDF/share for F071's report, which doesn't exist anyway); grepped whole repo for wa.me/whatsapp:// -- zero hits, confirming no real WhatsApp deep-link exists, only a manually-pasted URL; zero UI callers for either endpoint -- API-only |
| F073 | Daily timesheet entry + manager validation | PARTIAL | pms-time-service.ts:34-53 (logTime), :72-84 (submitTimeEntry, self-only), :86-111 (approveTimeEntry/rejectTimeEntry, blocks self-review, records approvedById/rejectionReason); api/v1/projexa/timesheets/[id]/approve/route.ts (requireRole manager-gated) | Full entry+validation lifecycle correctly implemented and role-gated server-side. UI (pms/[projectId]/time/page.tsx) only has logTime -- grepped for Submit/Approve/Reject/approvalStatus/manager: zero matches. No UI exposes submit or manager approve/reject |
| F074 | Negative variation checked vs progress | VERIFIED-REAL | construction-boq-service.ts:565-572 (createBoqRevision, real 409 block unless allowScopeReductionOverride), findScopeReductionViolations:634-656 | Confirmed wired end-to-end, not just defined-but-unused: checks BOTH "removed item had progress" AND "changed item's netVariation<0 had progress", real re-keying fix (R36) for progress lookup across old/new line-item ids on a revision |
| F075 | Upload site instruction form | GENUINE-GAP | schema.ts:10534-10548 (constructionSiteInstructions -- NO file/attachment column at all, only nullable boqId); construction-site-instruction-service.ts:11-64 (text-only create; list/get correctly org-scoped); api/v1/construction/site-instructions/route.ts:26-44 (POST is request.json() only, no multipart handling anywhere) | "Retrievable" is real. "Uploads" (a file) is a genuine gap -- no file/attachment concept exists anywhere for this table. "Links to a scope item" is also coarser than spec: links to a whole boqId, not a specific line item. No UI page exists at all (grepped every page.tsx) -- API-only |
| F076 | Upload drawings and 3D walkthrough | BUILT-NEW | drawings/page.tsx (new), api/v1/projexa/drawings/route.ts (pre-existing, read only), AppSidebar.tsx (+nav link), messages/{en,hi}.json (+i18n key), protected-routes.generated.ts (regenerated) | Real, confirmed prior gap (memory: "Drawings&3D/Design Studio still open, deliberately not faked" R63) -- backend (DWG file OR 3D-walkthrough file/external-link) existed since Wave 143, never had a UI. Built list+upload page on the same shell as sibling /permits page; NOT built: an in-browser 3D VIEWER (rendering the walkthrough inline) -- "Open" launches the signed URL/external link in a new tab, same posture as every other document-category page in this app; a real embedded viewer is a materially bigger feature (mesh/IFC rendering), consistent with the prior session's own deliberate BIM/IFC deferral |
| F077 | Scanned PDF read w/o paid vision call | GENUINE-GAP | src/lib/crr/recall.ts (read-only, real), src/app/api/documents/extract/route.ts (the REAL active pipeline -- uses `documents` table + an LLM call for field extraction, not in-browser OCR) | Confirmed: the CRR ingestion pipeline recall.ts's own header comment references ("capture.ts"/"embed.ts", the P3 write paths) do NOT exist anywhere in src/ (grepped for capture/embed functions writing sourceObject/documentChunk -- zero results). Only the schema + DB trigger + read-side recall filter were built; no ingestion/OCR/confidence-scoring code exists at all for this table. Matches memory's own prior finding (R60: "CRR-225 real code work still open") -- not a new regression, a real, already-acknowledged, still-open gap. Building a real in-browser-OCR + confidence-scoring pipeline from scratch is a multi-day effort, not a same-pass fix |
| F078 | Low-confidence amount flagged not auto-written | GENUINE-GAP | same as F077 | No confidence-scoring code exists for this table at all (nothing writes to it) -- there is no threshold to check because there is no extraction pipeline populating it. The OLDER, separate `documents.extractedData` pipeline (extract/route.ts) does have a real human-review gate (DocumentUploadSection.tsx, per that route's own comment) before any compliance item is created, but that's a different table/flow than what F078's CRR-specific wording (confidence threshold, auto-write) targets |
| F079 | Rename preserves identity; erasure redacts | PARTIAL | src/lib/db/schema.ts:12107-12146 (sourceObject), confirmed live via `information_schema.triggers`: `source_object_doc_uid_immutable_trg` (BEFORE UPDATE, blocks doc_uid changes) really exists on the real Supabase DB | The DB-level guarantee is real and verified live (not just a comment) -- doc_uid truly cannot be changed once a row exists. BUT: no code path anywhere renames a sourceObject row's displayName, and no code path performs an erasure (sets contentErasedAt) -- grepped for `sourceObject` across all of src/, only 3 files reference it (schema, recall.ts, recall.test.ts), none of them write to it. So the guarantee cannot be exercised/demonstrated end-to-end today; the claim is real at the DB layer but practically unreachable through any UI or API |
| F080 | Six demo-gate TCs pass twice manually | VERIFIED-REAL (via automated equivalent) | e2e/demo-gate-smoke.spec.ts:151-344 | Real Playwright test asserting all 6 TCs (TC-01 create, TC-10 weighted sub-tasks 40/35/25%->2000/1750/1250, TC-11 root-only total=5000, TC-30 earned value=750/15%, TC-40 dashboard value sync, TC-90 AED currency format) against REAL production per the test's own header. Runs on every PR via ci.yml's `e2e` job. Honest note: this is the automated equivalent, not a literal two-separate-manual-runs execution by a human this pass |
| F099 | Automated suite fails on broken build | VERIFIED-REAL | .github/workflows/ci.yml | Real gates: lint/typecheck/build/unit-tests(--isolate)/migration-collision-check/migration-integrity-check/e2e, all with no `|| true` masking (comment at e2e step's own history confirms a past `|| true` bug was found+fixed so failures now propagate for real). Not re-proven by actually pushing a broken commit this pass -- verified via config inspection only |
| F100 | Stranger completes full journey unaided | NOT CODE-VERIFIABLE | -- | This requires an actual live UAT session with a person who did not build the product -- cannot be verified by reading code. Left as a real, standing requirement for the user/team to execute, not marked done |
| F081 | BOQ list search returns exact matching set | BUILT-NEW | scope/page.tsx (searchQuery state, visibleBoqs filter, real `<Input>`) | Real, confirmed gap: no search control existed at all -- only project scoping. Built a client-side substring filter over the project-scoped list (title, case-insensitive); a pure filter over the full array can never drop or over-match a row |
| F082 | BOQ list sorting/paging lose no row | BUILT-NEW | scope/page.tsx (sortKey/sortDir state, visibleBoqs sort, clickable column headers) | Real, confirmed gap: no sort control existed. Built clickable title/version/status/created headers toggling asc/desc, pure client-side sort over the full array (no row can be lost since nothing is re-fetched with a page/limit). No pagination built -- BOQ-per-project counts observed this session are small (single digits), and no evidence of a real large-list scenario; noted honestly rather than silently assumed unnecessary |
| F083 | BOQ edit changes only intended field | VERIFIED-REAL (via the revision mechanism, by design) | construction-boq-service.ts:490-578 (createBoqRevision), confirmed by F021 (parent links/pct carried forward unchanged) and F025 (pct-only change correctly detected as the only variation) | This system deliberately has NO raw single-field PATCH on an existing BOQ -- confirmed via api/construction/boq/[id]/route.ts (GET only, no PATCH/PUT anywhere). "Editing" a real, audit-trail-requiring construction BOQ is done via a revision, which F021/F025 already independently verified carries every unchanged field/line forward exactly and registers only the real delta -- this is an intentional architecture choice (audit trail for construction records), not an oversight |
| F084 | BOQ delete behaves correctly | VERIFIED-REAL | construction-boq-service.ts:840-855 (deleteBoq) | Correct hard/soft semantics: hard-deletes only DRAFT-status BOQs (nothing of committed value exists yet) and its own line items + linked work-progress entries (no orphaned rows); blocks deletion of submitted/approved/superseded BOQs with a 400 naming the reason -- prevents collateral damage to real, committed scope |
| F085 | Progress entry delete recalcs cumulative | BUILT-NEW | construction-progress-service.ts (deleteProgressEntry), api/construction/progress/[id]/route.ts (new DELETE) | Genuinely missing (no delete path existed at all, confirmed by reading both API routes first). All cumulative figures are computed live at read time (never cached), so a plain scoped DELETE is sufficient -- next read naturally excludes it |
| F086 | Progress search/filter by date and line | BUILT-NEW | construction-progress-service.ts (listProgressEntries extended), api/construction/progress/route.ts (GET extended) | Only projectId/activityId existed before. Added dateFrom/dateTo (gte/lte on entryDate) + boqLineItemId, purely additive -- every existing caller unaffected |
| F087 | Dashboard refresh reflects change elsewhere | VERIFIED-REAL | construction-dashboard-service.ts (whole file) | Every field is a live, uncached query at read time -- file's own header states "no denormalized summary columns," confirmed throughout |
| F088 | Report figures reconcile to DB exactly | FIXED | construction-reports-service.ts:303-350 (boqBudgetVarianceReport) | Real, confirmed bug: totals summed already-ROUNDED per-line display values then rounded again -- cumulative per-line rounding could drift the total by fractions of a currency unit from a raw SQL SUM() over the same rows. Fixed: total from the raw (unrounded) per-line figures, round only once at the end; per-line display values unchanged, public return shape unchanged (raw fields stripped before return) |
| F089 | Assistant respects role/project scope | FIXED (both confirmed call paths), one narrower path documented open | pipeline/executor.ts (ExecutableTask.role), run-submission.ts (role threaded), api/v1/projexa/{assistant,tasks,submissions}/route.ts (pass ctx.dbUser?.role), task-execution-engine.ts (dispatchTool()'s new optional `role` param + financialsAllowed check on get_construction_project_dashboard/get_construction_budget_status/list_over_budget_projects, lines ~341-380) | Real, confirmed gap found in TWO separate call paths, both now closed: (1) pipeline/executor.ts's executeGetProjectDashboard called the raw service function directly, bypassing F059's route redaction; (2) task-execution-engine.ts's dispatchTool() had the identical gap for its own 3 budget-related codeReferences, and had no role parameter at all. Both fixed with the same rank>=manager check, wired through every session-based REST route (assistant/tasks/submissions). dispatchTool()'s OTHER 4 call sites (2 internal chain-execution-engine callers, fde-service.ts, and the personal MCP AI-link route api/mcp/[token]/route.ts) were deliberately left unwired -- the param is optional (undefined = no redaction, unchanged prior behavior) so this is additive, not a behavior change for those; wiring them would need either a DB role lookup (MCP route, token-scoped to one person by design, narrower risk) or plumbing role through the async chain-execution engine's own task-persistence layer (a materially bigger change than this pass's scope) -- both honestly left open, not silently claimed done |
| F090 | Mobile layout 360px fully usable | GENUINE-GAP (live-confirmed, not guessed) | Live browser test at 360x800 against the real local dev server, logged-in session, on `/scope` and the app shell root | Real, confirmed defect, not code-inspection-only: the persistent VERI Chat/composer panel (from the external `@fchecklist/veridian-ui-kit` package's shared AppShell, not this repo's own code) monopolizes the ENTIRE 360x800 viewport on every page -- its own internal pill/tab rows ("Discuss/Chats/To Do/VERI ERP/PROJEXA/..." and the mode-pill grid) overflow horizontally with text cut off at the edge and no visible scroll affordance, and the actual underlying page content (e.g. Scope/BOQ list, confirmed present via get_page_text) is not reachable -- no scroll response found to get past the composer to the page below it. This is NOT a quick fix: the composer's responsive layout lives in a SEPARATE repo (veridian-ui-kit), not in compliance-tracker, so it's out of scope for this pass's PR. Only spot-checked 2 screens (shell root, /scope) given the composer overlay makes the underlying page moot everywhere it appears -- not a 100-page audit |
| F091 | Session expiry and re-auth | VERIFIED-REAL | auth-guard.ts:276-278 (requireAuth -> supabase.auth.getUser()) | Real, server-verified check on EVERY protected route (not a client-side-only guard) -- getUser() validates the JWT against Supabase's auth server; an expired/revoked session fails this and returns 401 before any data is ever fetched, so no route can show stale data from an expired session |
| F092 | Direct object reference cannot be guessed | VERIFIED-REAL (same mechanism as F058) | construction-boq-service.ts:414-416 (app-level `eq(orgId,...)` filter) + pg_policies `app_runtime_tenant_isolation` (DB-level RLS, confirmed live) | Same defense-in-depth already verified for F058 generalizes here: guessing/changing an id in a URL still requires BOTH the app query filter AND the DB RLS policy to agree the row belongs to the caller's org -- confirmed live via pg_policies, not just read from source |
| F093 | Permit expiry reminder appears | VERIFIED-REAL | permits/page.tsx:34-38 (statusBadge), api/v1/projexa/permits/route.ts (daysToExpiry) | Real badges: "Expiring in Nd" for daysToExpiry<=30, "Expired" for <0, computed server-side from doc.expiryDate |
| F094 | Material cost flows into budget figure | GENUINE-GAP | pms-budget-service.ts:69-94 (getBudgetActuals sums ONLY labor); erp-budget-service.ts:121-179 (variance from GL journal lines; erp-inventory-service.ts confirmed never posts journal entries on receipt); construction-reports-service.ts:382-391 (budgetVsActual sums construction_expense_entries, which per construction-expense-service.ts:34-51 are only ever manually created) | Checked all 3 real budget/actual code paths -- none automatically pulls a cost from a material receipt into any "actual" figure; a user must manually re-key it as an expense row |
| F095 | Attendance summary reconciles trade-wise | PARTIAL | construction-reports-service.ts:437-453 (manpowerCostReport: real live SQL sum(dailyCost)/count groupBy trade -- reconciles by construction, not a separately-drifting summary) | The aggregation itself is real and correct, but grepped every .tsx file for manpower-cost/manpowerCostReport -- zero matches. No page in the app ever calls this report; it's unused backend/API capability, not a visible feature |
| F096 | Schedule baseline variance shown | PARTIAL | schedule-service.ts:190-226 (compareBaseline, correct startVarianceDays/dueVarianceDays via daysBetween, null-safe); api/v1/projexa/schedule/baselines/[id]/route.ts (GET wired) | Math and wiring correct and real. But no page.tsx anywhere renders baseline/variance data (same gap as F070 -- no UI ever captures a baseline, so there is nothing to show variance against even if a display existed) |
| F097 | Document permissions follow project scope | GENUINE-GAP | document-service.ts:112-125 (listDocuments filters only by orgId + caller-supplied linkedEntityId); auth-guard.ts:274-303 (requireAuth is org-level identity only); grepped permission-service.ts/client-access-service.ts/abac-policy-service.ts/schema.ts -- no project-membership construct exists anywhere (consistent with F002's own finding) | Any authenticated org member can GET any document's signed download URL regardless of project assignment -- no project-scoped visibility check anywhere in the read path |
| F098 | 20 mixed documents all reach terminal state | GENUINE-GAP | same as F077 | Same root cause: no ingestion pipeline writes to sourceObject/documentChunk at all, so there is no EMBEDDED/SKIPPED state machine to test against -- nothing can get "stuck" because nothing is ever produced in the first place |

## Summary counts (recomputed 2026-08-30, all 100 fn_ids resolved, 0 remaining NOT-STARTED)
- VERIFIED-REAL: 58 (already correct, real code cited)
- GENUINE-GAP: 14 (real, explained, not a same-pass fix -- see each row's Notes for the specific reason)
- PARTIAL: 12 (backend/service layer is real and correct, but no UI ever calls it, or only part of the requirement is met -- see each row)
- FIXED: 8 (real bug found and fixed this pass)
- BUILT-NEW: 7 (genuinely missing, built for real this pass)
- NOT CODE-VERIFIABLE: 1 (F100 -- requires live human UAT, cannot be verified by reading code)

Total: 100/100 resolved. Honest breakdown of the 27 non-VERIFIED-REAL/non-FIXED/non-BUILT-NEW rows (GENUINE-GAP+PARTIAL+NOT-CODE-VERIFIABLE):
- 4 rows (F077/F078/F079/F098) share ONE root cause: the CRR document-ingestion pipeline (sourceObject/documentChunk tables) has a real schema+DB-trigger+read-side-filter, but no ingestion/write path exists anywhere -- matches memory's own prior finding (R60: "CRR-225 real code work still open"), not a new regression.
- 7 rows (F069/F070/F071/F072/F073/F075/F096) share a DIFFERENT pattern: the service+API layer is genuinely correct and complete, but zero UI pages call it -- confirmed via exhaustive grep of every page.tsx per row, not assumed.
- F057 (demo password) and F090 (mobile layout) are each real, individually-explained, standalone gaps -- see their own rows.
- F063/F064/F065/F066/F067/F068/F088(fixed)/F094/F095/F097 -- see each row's own specific partial/gap reasoning.
- F100 requires a live human tester, not code.
