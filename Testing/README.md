This is the testing directory for the compliance-tracker project.

## Test Serial Number

See `test_tasks.md` for the full test inventory (46 tests across 12 categories).

## Instructions for AI Agent

1. **Test Serial Number** — Each test has a unique ID (e.g., TC-LP-01).
2. **Test Scenario** — Described in `test_tasks.md`.
3. **AI need to test and input the test result** — Execute tests and update `test_execution_log.md`.
4. **Find the bug if any** — Document in `test_execution_log.md` with Bug ID.
5. **Resolve if needed** — Fix, commit, push, and verify.
6. **Once done write "Test Done, Task Completed."** — Update this file and `last_known_state.md`.

## Handover

Read `last_known_state.md` to understand current progress and resume from where the last agent stopped.
