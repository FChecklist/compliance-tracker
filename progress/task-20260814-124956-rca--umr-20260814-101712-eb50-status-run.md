# RCA: UMR-20260814-101712-eb50 status=running but unit dead (write-back bug)

## Completed
- [x] Read `ai-os/boss/ACTIVE-CLAIMS.yaml` protocol context (no separate claim registration needed for a single-session docs-only RCA confirm; no colliding claim found for this UMR).
- [x] Checked the deterministic briefing's cited indexes first, per AGENTS.md Rule 12: `wiring_registry` match (`dispatch_event-owner-task-20260814-121823-3098625`) and the sibling memory note `veridian-umr-ec65-status-run-rca-already-self-corrected` (this task's own memory index) — that note documents the **exact same pattern one layer down**: UMR-20260814-095518-ec65's row also self-corrected via `reconcile_stale_running_workers.py` before its own RCA task started. This task (UMR-20260814-101712-eb50) is the RCA-of-that-RCA, and the same reconciler class fixed it again, one layer up.
- [x] Verified the real systemd unit state (not the SPEC's snapshot):
  `systemctl --user show veridian-worker@task-20260814-121415-rca--umr-20260814-095518-ec65-status-run.service` → `ActiveState=inactive`, `SubState=dead`, `Result=success`, `ExecMainStatus=0`. `journalctl --user -u ...` confirms clean start (12:14:18) and stop (12:18:00, "Consumed 16.785s CPU time"). SPEC's claim that the unit is dead is accurate.
- [x] Re-queried the live UMR row (`resource_governor.py --query-umr --umr-id UMR-20260814-101712-eb50`) rather than trusting the SPEC's snapshot. Result: **the row is already `status=completed_unmerged`**, `ts_completed=2026-08-14T12:19:46Z` — written by `reconcile_stale_running_workers.py` (STEP 3, task-20260807-052027) roughly **30 minutes before this RCA task even started** (this task's own `created_at` is `2026-08-14T12:49:57Z`). The row's `reason` field cites real, checkable evidence: unit confirmed `ActiveState=inactive`, real task dir resolved via `outputs_json.new_task_id`, `task.yaml` last status `blocked`, and "1 real completion candidate gathered (git ls-remote, branch still exists) — letting mark-umr-terminal's own structured-evidence gate decide completed vs completed_unmerged, never asserted here."
- [x] Independently verified every claim in that `reason` field rather than trusting it at face value:
  - `git ls-remote origin refs/heads/worker/task-20260814-121415-rca--umr-20260814-095518-ec65-status-run` → branch exists (`ac8e8aa...`), confirming real committed work.
  - `gh pr view 1148` → PR #1148 ("docs: RCA for UMR-20260814-095518-ec65 ... write-back gap already self-corrected, no fix needed") is real, `state=OPEN`, `mergeable=MERGEABLE`, not yet merged (`mergedAt=null`) — confirming `completed_unmerged` (not `completed`) is the honest status: real work exists, but the PR hasn't landed on `main` yet (its `audit-check` CI job is currently failing — no `AUDIT: PASS`/`FAIL` comment posted yet on this PR; a pre-existing, separately-tracked mechanism gap, not something this RCA's scope covers).
- [x] Conclusion: **no fix or redispatch is needed.** The write-back bug this task was dispatched to investigate had already been caught and corrected by the standing `reconcile_stale_running_workers.py` reconciler before this task started, exactly as the sibling UMR-ec65 RCA found one layer down. This is a docs-only confirmation, not an invented code change or a redundant `mark-umr-terminal` call (the row is already correctly terminal with real evidence).
- [x] Recorded completion via `agent_work_briefing.py record-completion` (no new wiring_registry entity registered — this reuses the existing `dispatch_event-owner-task-20260814-121823-3098625` entity per the briefing's dedup guidance).

## Remaining
- [ ] None. This RCA is complete — no code fix, no redispatch, no further `mark-umr-terminal` call warranted (row already correctly terminal).
- Note (out of scope, left for a correctly-scoped follow-up, same as the sibling ec65 RCA already flagged): PR #1148's `audit-check` CI job is failing (no AUDIT comment posted yet) — this blocks PR #1148 itself from merging, but is a pre-existing gap in the audit-check workflow's usual manual-comment step, unrelated to this task's chartered scope (the UMR-eb50 status-lying RCA).
