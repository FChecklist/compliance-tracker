# RCA: UMR-20260813-130245-6a26 (killed)

## Real evidence gathered
- `resource_governor.py --query-umr --umr-id UMR-20260813-130245-6a26` (queried fresh, not
  trusted from the SPEC summary): `status=killed`, `reason="stuck-task SIGKILL: no exit 60s
  after SIGTERM"`, `unit_name=veridian-worker@task-20260813-150119-remove-0-byte-decoy-register-files-that.service`.
- `journalctl --user -u veridian-worker@task-20260813-150119-....service`: confirms two real
  invocations of the same task unit:
  - Invocation 1 (15:01:22 → SIGTERM 16:01:47 → SIGKILL 16:01:49): systemd sent SIGTERM to the
    whole cgroup (worker-entrypoint, claude, python3, sh, snip, systemd-run, ps children) and it
    did not exit within the 60s grace window, so it was killed. **Genuine stuck-task SIGKILL, not
    a dispatch-gateway bug** — the underlying task (see `prompt.txt`, scope A–F) required a 4-
    parallel-subagent scan across the entire `veridian-scripts` tree for every register-path
    reference, which plausibly ran long enough to blow the stuck-task timeout.
  - Invocation 2 (16:34:01 → 16:48:47): ran to a clean exit, no SIGTERM/SIGKILL. This resumed the
    task and did **real, substantive work** (see below) but self-stopped mid-task when its own
    session budget ran out (`result.json`: *"I'm stopping here since the session budget is nearly
    exhausted at $0.98 remaining of $10"*) — before opening the required PR, before item D's
    decision, before item F's proof-run, and before the task's own `record-completion` call.
- `task.yaml` (`status: blocked`, 12 checkpoints): confirms invocation 2's real remaining_steps
  list matches `result.json`'s own honest self-report — nothing fabricated as done.
- The worker's automated checkpoint-commit mechanism (unrelated to the real deliverable) then
  committed a placeholder git submodule/gitlink (`veridian-scripts-clean`, mode 160000, no
  `.gitmodules` entry) into the task's own `claude-control` repo scratch workspace and opened
  `FChecklist/claude-control#170` — the tier1 auto-reviewer correctly **rejected** it (`review.json`:
  "claim-registration commit... diff does not match the stated task") and it's already `CLOSED`.
  This PR is a red herring, unrelated to the real veridian-scripts branch below.
- The real deliverable: invocation 2 committed (`0ca77dd6dbcaad5fe5d2bdfd477cfa4517a19073`) and
  pushed a genuinely substantial diff — 24 Python files + `worker-entrypoint.sh`, 645 insertions /
  132 deletions — to `origin/fix/register-path-resolution-decoy-cleanup-umr20260813130245-6a26`
  on `FChecklist/veridian-scripts`, rewiring every real hazard site to delegate DB-path resolution
  through the existing canonical `resolve_superboss_db_path()` (OCID-068's own hard-fail resolver:
  exists / non-zero-size / SQLite-magic-bytes / `umr_tasks`-table checks, raises
  `SuperbossDbPathError` rather than silently opening a decoy file). No PR had been opened for it.

## Root cause
Two independent, correctly-diagnosed causes, not one:
1. Invocation 1's kill was a **genuine stuck-task SIGKILL** — no bug in the dispatch/supervisor
   gateway to fix. This class of task (broad multi-subagent filesystem scan) is inherently at risk
   of the stuck-task timeout; no code change is being made to the timeout itself here (out of this
   RCA's scope, and no evidence the timeout itself is mis-tuned generally).
2. Invocation 2's real work was left **incomplete but not lost** — it was sitting on an unopened,
   unmerged branch, 77 commits behind `main`, with no PR and no terminal record. This is the actual
   gap this RCA needed to close.

## Fix + real remaining scope completed this task
1. Rebased `0ca77dd` onto current `origin/main` in an isolated worktree — **rebased clean, zero
   conflicts** (`git rebase origin/main`), new SHA `9a1809d0c9f29db1a62023874b0d2cb139e388dc`.
2. Independently re-verified: `python3 -m py_compile` on all 24 changed `.py` files (clean) +
   `bash -n worker-entrypoint.sh` (clean).
3. Completed item F (the real hard-fail proof-run, not done by invocation 2):
   - `resolve_superboss_db_path()` with no override → resolves the real
     `/opt/veridian/ai-os/memory/superboss-register.sqlite` (4.07GB, live).
   - `resolve_superboss_db_path(default_path=<genuine 0-byte scratch file>)` → raises
     `SuperbossDbPathError` ("non-zero size" check, loud, non-silent failure).
   - Ran a real rewritten caller (`owner_status.py`) from a different cwd (`/var/tmp`) — resolved
     and read real rows from the real DB, confirming the deterministic resolution works end to end
     outside the repo's own cwd.
4. Force-pushed the rebased branch (same name, `--force-with-lease`) and opened the real PR:
   **https://github.com/FChecklist/veridian-scripts/pull/357** — with the real proof-run evidence
   and an honest, explicit callout of item D (4 pre-existing fail-open exception wrappers around
   `directive_engine.py`, `plan_generator.py`, `security-check.py`,
   `prompt_gateway/gateway_persistence.py` that will now also silently swallow
   `SuperbossDbPathError`) as a deliberately deferred follow-up decision, not resolved by guessing
   under RCA time pressure.
5. Marked `UMR-20260813-130245-6a26` terminal via `superboss-register.py mark-umr-terminal
   --status completed_unmerged --commit-sha 9a1809d0c9f29db1a62023874b0d2cb139e388dc --pr-number
   357 --repo veridian-scripts`, citing this real evidence (verified via a fresh
   `resource_governor.py --query-umr` re-read after the write).

## Explicitly NOT done (honest, not fabricated as complete)
- Item D's actual decision (change vs. keep the 4 fail-open wrappers' behavior against
  `SuperbossDbPathError`) — flagged in PR #357 for dedicated follow-up review, not resolved here.
- PR #357 itself is **not yet merged** (`completed_unmerged`, correctly reflecting real state) —
  merge is a separate step, subject to the normal CI/review gate.
- `task-20260813-150119-...`'s own `task.yaml` (`status: blocked`) was intentionally left
  untouched — it belongs to that task's own worker/supervisor lifecycle, not this RCA task's file
  scope, and its own `record-completion` call (for its governing UMR) is a step that task's own
  worker performs, not something this separate RCA task should do on its behalf.

## Completed
- [x] Queried real UMR row fresh (`resource_governor.py --query-umr`), did not trust the SPEC
      summary alone.
- [x] Read `journalctl`, `task.yaml`, `result.json`, `review.json`, `supervisor-result.json` for
      the underlying task unit to build the real, evidenced timeline.
- [x] Determined two independent real root causes (genuine stuck-task SIGKILL on invocation 1;
      real-but-unfinished work stranded unopened on invocation 2).
- [x] Found and verified the real unmerged branch/commit (`0ca77dd`) in `veridian-scripts`.
- [x] Rebased it onto current `main` (was 77 commits behind) — zero conflicts.
- [x] Independently re-verified all 24 changed files compile clean.
- [x] Completed the outstanding item F real proof-run (hard-fail + real-caller-from-different-cwd).
- [x] Opened the real PR: FChecklist/veridian-scripts#357.
- [x] Confirmed the unrelated `claude-control#170` checkpoint-commit PR was already correctly
      closed by the tier1 auto-reviewer — no action needed there.
- [x] Marked `UMR-20260813-130245-6a26` terminal (`completed_unmerged`) citing real PR/commit
      evidence; re-verified the write via a fresh query.
- [x] Registered this session's claim in `ai-os/boss/ACTIVE-CLAIMS.yaml`.

## Remaining
- [ ] PR #357 merge (separate step, normal CI/review gate — not this RCA task's to force).
- [ ] Item D's real decision, deferred to a dedicated follow-up per PR #357's own callout.
