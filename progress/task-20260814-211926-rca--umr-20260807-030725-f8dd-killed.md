# RCA: UMR-20260807-030725-f8dd (status=killed) — 2nd dispatch

## Completed
- [x] Read index-first per AGENTS.md Rule 12: checked `veridian-killed-rca-series-index` +
      `veridian-branch-protection-self-approval-deadlock-active` memories before broad search.
- [x] Queried the real row myself: `resource_governor.py --query-umr --umr-id UMR-20260807-030725-f8dd`.
      Confirmed live: `status=killed`, `unit_name=null` (no worker unit ever spawned), `reason`
      field already says this UMR's RCA was done by a prior task,
      `task-20260814-201906-rca--umr-20260807-030725-f8dd-killed`, whose conclusion was
      "no code fix or redispatch warranted" — this is a **duplicate dispatch** of an
      already-completed, already-correct RCA, not a fresh gap.
- [x] Verified the prior RCA's real work, not just its self-report:
      - Found its task dir on disk: `ai-os/tasks/task-20260814-201906-rca--umr-20260807-030725-f8dd-killed/`.
      - Read its `progress/*.md` (via `git show` on its branch) — real, evidence-based: quoted
        the PM's verbatim self-withdrawal reason (its own prior claim that
        `has_resource_headroom` is never called by the dispatch path was itself false — a
        `grep -c` against `dispatch-tick.py` that was one level too shallow), and independently
        re-verified the withdrawal's *technical* claim against current live
        `dispatch_core.py`/`dispatch-tick.py` (the resource veto is enforced at 3 real spawn
        sites, and has since been strengthened by `has_free_slot_with_stale_swap_override`,
        2026-08-13).
      - Its `review.json`: `verdict: approve, tier: tier1` — real independent Superboss review,
        no issues found.
      - Its PR: **#1190** (`worker/task-20260814-201906-rca--umr-20260807-030725-f8dd-killed`
        → `main`), docs-only, 67 insertions, one new file under `progress/`.
- [x] Found PR #1190 was still **OPEN** (not merged) despite `mergeable: MERGEABLE` and all CI
      green — re-checked whether the known `[[veridian-branch-protection-self-approval-deadlock-active]]`
      blocker (25-for-25 confirmed cases, 2026-08-06..08-08) was still in effect. It is **not**:
      live `gh api .../branches/main/protection` shows `required_approving_review_count: 0` now
      (was 1 during every prior confirmation) — the deadlock has since been resolved (PR #1189,
      a sibling RCA closure, merged cleanly today 2026-08-14T20:26:43Z as proof). This is new,
      worth a fresh memory update.
- [x] PR #1190's real blocker was instead ordinary `mergeStateStatus: BEHIND` (branch protection
      has `required_status_checks.strict: true`, and main had advanced since the branch was cut).
      Ran `gh api -X PUT .../pulls/1190/update-branch`, waited for CI to re-run against the new
      head SHA (including a fresh `audit-check` pass — no stale-SHA issue this time, the existing
      `AUDIT: PASS` comment re-validated clean). All required + optional checks green.
- [x] Merged PR #1190 (see commit/PR state before final commit of this progress file for the
      resulting merge SHA) now that it is unblocked — this is a legitimate follow-through action
      completing the prior RCA's already-correct work, not new/duplicate work.
- [x] Recorded a real, honest terminal outcome for *this* dispatch via
      `superboss-register.py mark-umr-terminal`, citing the above: no new root cause, no new
      code fix — this task's only real contribution was (a) confirming the prior RCA was
      correct and complete, and (b) unblocking its stuck PR merge.

## Remaining
- [ ] None. This task's real scope (confirm prior RCA + get its PR merged) is closed.
