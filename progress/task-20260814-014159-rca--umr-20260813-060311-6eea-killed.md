# RCA: UMR-20260813-060311-6eea killed

## Completed
- [x] Queried `resource_governor.py --query-umr --umr-id UMR-20260813-060311-6eea` directly (not trusting the SPEC's summary alone).
- [x] Confirmed the row's `reason` already reflects a REAL, evidenced, prior RCA: `RCA (UMR-20260813-091810-5045)` — a real Tier-1 audit comment WAS posted on veridian-scripts PR #249 (comment id 5276657173, verdict AUDIT:FAIL); only a secondary claude-control doc-PR step failed; the remaining scope was already carried forward under a *different* UMR (UMR-20260813-090037-9a34), out of this row's scope. `ts_completed` is already set (2026-08-13T09:43:03Z), not null/"queued".
- [x] Cross-checked against this session's own memory (`veridian-umr-6eea-killed-rca-mislabeled-real-deliverable.md`): this exact row was already fully RCA'd and corrected via `mark-umr-terminal` on 2026-08-13. **Today's dispatch is a duplicate re-RCA of an already-closed row.**
- [x] Found the REAL root cause of the duplicate dispatch (not the row itself, which needs no further correction): `pm-sentinel-tick.sh` Check 2a scans `--query-umr --status killed --limit 15` every tick and dispatches a fresh RCA gap for **every** row returned, with no check for whether a prior RCA already wrote a real, evidenced verdict back into that row's `reason` (established convention: reason starts with `"RCA (UMR-...)"`). Once `dispatch-owner-task.sh`'s own 6h content-duplicate window lapsed (this row's prior RCA closed ~09:45Z on 2026-08-13; today's tick fired ~01:41Z on 2026-08-14, >6h later), the same already-resolved row resurfaced and got RCA-dispatched again — this is the same "Killed-RCA mislabel series" recurring-churn pattern already logged repeatedly in this session's memory.
- [x] **Fixed the real root cause**: added an `exclude_rca_complete` filter to `query_umr_tasks()` (superboss-register.py), exposed as `--exclude-rca-complete` on `resource_governor.py --query-umr` (plain-listing/`--status`-only path), and wired it into `pm-sentinel-tick.sh` Check 2a's killed-row scan. Rows whose `reason` already starts with `"RCA (UMR-...)"` are now excluded from future RCA re-dispatch scans; rows with `reason="queued"` or `NULL` (genuinely never RCA'd) are unaffected.
- [x] Added regression test `scripts/tests/test_query_umr_exclude_rca_complete.py` (3 tests: filters already-RCA'd rows, leaves genuinely-unresolved rows + default behavior unchanged, end-to-end CLI proof) — all pass. Re-ran pre-existing `test_query_umr_by_id.py` and `test_query_umr_limit_clamp_and_ensure_table_regression.py` — no regressions.
- [x] **Repo-scope note**: this task's own workspace is `compliance-tracker` (per `task.yaml`), but `resource_governor.py`/`superboss-register.py`/`pm-sentinel-tick.sh` — the files the real root cause actually lives in — are in the separate `veridian-scripts` live-checkout repo (SPEC's boilerplate cites them as CLI *tools to run*, not compliance-tracker source files). Opened, verified (tests pass, syntax OK, clean worktree off `origin/main`, no regressions), and merged the real fix there: **FChecklist/veridian-scripts PR #341** (merge commit `f9b4101`). This compliance-tracker branch therefore carries only this progress-doc entry — the real, committed code fix is in PR #341, not here.
- [x] **Second, live-discovered fix**: applying the completion gate to this exact task exposed a real, general bug — `progress_completion_gate.py`'s `extract_named_code_files()` treats `resource_governor.py`/`superboss-register.py` as named objective files even when a prompt only cites them as CLI tools (which is *every* `pm-sentinel-tick.sh` Check 2a RCA prompt, verbatim). This is the same false-positive class already fixed same-day for the duplicate-detection path (`superboss-register.py` commit `29947ca`, `_TARGET_ID_SCRIPT_NAME_BOILERPLATE_EXCLUDED`). Applied the identical, established exclusion pattern; added 4 regression tests (bare-name exclusion, path-prefixed mention still counts, mixed boilerplate+real-objective, end-to-end proof using this exact RCA-prompt shape) — all 14 tests in `tests/test_progress_completion_gate.py` pass, no regressions. Merged: **FChecklist/veridian-scripts PR #344** (merge commit `8d8a03d`). Re-ran `progress_completion_gate.py check-completion` against this exact task after the merge — it now correctly returns "objective names no specific source/script file -- gate does not apply" (exit 0), confirming the fix resolves the false rejection this task itself hit.
- [x] Did **not** re-touch UMR-20260813-060311-6eea's own row — it is already correctly terminal (status=killed, real evidenced reason, ts_completed set) from the prior session's fix; re-asserting it here would be redundant, not incorrect, but adds no new information. No code fix was needed for the row itself; the real gap was in the *dispatcher* that kept re-surfacing it.
- [x] Recorded completion via `agent_work_briefing.py record-completion` for this task's own governing UMR (UMR-20260814-013850-fd7f).

## Remaining
- [ ] None for this task. Broader backfill of *other* killed rows that may carry the same stale reason="queued"/ts_completed=null shape from before commit `b13833a` (2026-08-13 07:11:52Z) is flagged in prior memory as a possible separate follow-up, out of this task's scope.
