# RCA: UMR-20260807-151622-15cd killed (3rd dispatch)

## Completed
- [x] Checked `ai-os/boss/ACTIVE-CLAIMS.yaml` — no other session currently claiming this UMR.
- [x] Queried `resource_governor.py --query-umr --umr-id UMR-20260807-151622-15cd` directly (did not trust SPEC's summary alone). Confirmed live row: `status=killed`, `ts_completed=2026-08-13T20:26:27.154099+00:00`, reason field matches SPEC verbatim, `unit_name=veridian-worker@task-20260807-152601-stop-work-order--batch-3--land-batch-2-s.service`.
- [x] Read `task.yaml` + checkpoints for `task-20260807-152601-stop-work-order--batch-3--land-batch-2-s`: real committed work existed (commit `bca453b`, "test: real pytest coverage for batch-2, 14 of 15 alphabetical no-test scripts"), quality gates passed, but the supervisor's `gh pr create` failed against `task.yaml`'s declared branch `rescue/batch-2-land-tests` (branch-name mismatch vs. what was actually pushed) — correctly refused to guess a PR, left the task `blocked` until systemd-inactive reconciliation marked it `killed`.
- [x] Found this UMR was **already RCA'd twice before this dispatch**:
  1. `veridian-scripts` PR **#324** (`worker/task-20260813-201836-rca--umr-20260807-151622-15cd-killed`, merged 2026-08-13T20:31:02Z) — root-caused the branch mismatch, confirmed no work was lost (redone independently), re-ran `reconcile_owner_dispatch_status.py --umr-id UMR-20260807-151622-15cd --apply` to backfill the real `reason`/`ts_completed`.
  2. `compliance-tracker` PR **#1146** (`worker/task-20260814-113445-rca--umr-20260807-151622-15cd-killed`, merged 2026-08-14T11:42:40Z, ~4h before this dispatch) — independently re-verified the same conclusion from scratch and found PR #324, closing itself as a duplicate. Its own progress file (`progress/task-20260814-113445-rca--umr-20260807-151622-15cd-killed.md`, still present in this workspace) documents the full chain.
- [x] Verified via `gh pr list --repo FChecklist/veridian-scripts --search batch-2`: real scope (14 batch-2 stranded test files) was independently completed and merged via **PR #271** (`test: land batch-2, 14/14 real tests passing`, merged 2026-08-07T16:39:50Z, ~1h after this UMR's dispatch went `blocked`). Part B (next 15 untested scripts) was never attempted under this UMR and remains decoupled open platform backlog per the prior RCAs — not a defect of this UMR's terminal status.
- [x] Re-confirmed live DB row is unchanged since the prior RCA and still correctly terminal — no further reconciliation needed.

## Root cause (confirmed, unchanged from prior RCAs)
Two independent causes compounded, both already fixed/accepted as correct behavior by PR #324:
1. **Branch-name mismatch**: `task.yaml`'s `branch:` field (`rescue/batch-2-land-tests`) never matched the branch the worker actually committed real work to. The supervisor's PR-resolution step only looks for the `task.yaml`-declared branch, found zero commits on it, and (correctly, per its own safety rule — see the real PR #84 2026-07-26 incident it guards against) refused to guess a PR rather than risk operating on an unrelated one.
2. The mechanical reconciler (`reconcile_owner_dispatch_status.py`) correctly closed the row `killed` based on systemd-inactive + no-PR-on-declared-branch, which is accurate for that specific dispatch — it does not by itself know the underlying *scope* was independently completed elsewhere via a different task/branch. That extra context required human-level RCA, supplied by PR #324 and re-verified by PR #1146.

## Verdict
**No new action required — this is a duplicate of two already-completed RCAs.** `status=killed` is correct and already fully evidenced in the live DB row:
- Part A (14 stranded batch-2 test files): not lost — independently redone and merged, `veridian-scripts` PR #271.
- Part B (next 15 untested scripts): never attempted under this UMR; remains open platform backlog, decoupled from this dead UMR/branch.

No code fix, no new UMR row, no redispatch, no `mark-umr-terminal` re-run performed — the row is already terminal with accurate evidence, and re-running the backfill would be a no-op against unchanged state. Recording this honestly (docs-only diff) rather than fabricating a new fix.

## Remaining
- [ ] None. Third-dispatch duplicate confirmed and recorded.
