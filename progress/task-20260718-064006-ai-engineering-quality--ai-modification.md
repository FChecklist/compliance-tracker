# AI Engineering Quality / AI-Modification Readiness gap-closure

## Completed
- [x] invocation 14/20 resume: found no `progress/task-20260718-064006-ai-engineering-quality--ai-modification.md` yet (never created in prior invocations) and `PROGRESS.md` at repo root modified to a blank/reset "Not started" template — that file is a **shared** cross-task file at repo root now on a completely different task's committed content (the 5-orgs-x-10-users cost-estimate deliverable, merged via PR #416). A prior invocation of this task had wrongly edited that shared file. Discarded that bad edit (`git checkout -- PROGRESS.md`) and created this correctly-scoped per-task file instead, per protocol.
- [x] Checked `ai-os/boss/ACTIVE-CLAIMS.yaml`: no conflicting active claim over `scripts/`, `CLAUDE.md`, or `src/lib/services/` for this gap.
- [x] Re-verified both findings against current code before changing anything (per prompt.txt's own instruction not to trust the gap description blindly):
  - Finding 1 (Low, Code Readability for AI — "comment discipline not enforced by tooling"): confirmed real. `src/lib/services/*.ts` has an almost-universal real convention of a leading header comment (183/184 non-test files already had one), but nothing in CI enforced it — the gap description matches current reality.
  - Finding 2 (Medium, AI Modification Readiness — "no single readiness score; depends heavily on which file"): confirmed real. No large+untested-file signal existed anywhere (CLAUDE.md, scripts/, ai-os/) before this change.
- [x] Finding 1 fix: added `scripts/check-service-header-comment.mjs` — CI check verifying every `src/lib/services/*.ts` file (excluding `.test.ts`) has a qualifying header comment (≥40 chars of `//`/`/* */` comment before the first real statement; imports may precede it). Verified it passes on the real baseline (184/184) and correctly fails on a synthetic no-header file (tested, then removed the synthetic file before commit). Wired into `.github/workflows/ci.yml` as a new `service-header-comment` job, same style as the existing `check-*.mjs` jobs.
- [x] Added the new check's own CI wiring to `scripts/check-guardrail-presence.mjs`'s manifest (same precedent as the existing metadata-index-coverage entry), so this new gate can't be silently dropped later without a visible diff. Verified `node scripts/check-guardrail-presence.mjs` still passes (90/90 markers).
- [x] Finding 2 fix: added `scripts/report-high-risk-files.mjs` — a lightweight, non-CI-blocking report script (matches the existing `report-*.ts` precedent, e.g. `report-cognitive-brain-coverage.ts`, which also isn't CI-wired) that flags files that are both large (≥400 lines) and untested (no colocated `*.test.ts`/`*.test.tsx`) across `src/lib`, `src/app/api`, `src/components`. Ran it for real: 25 of 1347 scanned files match, dominated by `src/lib/db/schema.ts` (~10,200 lines) and an ERP-service/veri-chat-UI cluster.
- [x] Added a new "## High-Risk Files (Apply Extra Caution)" section to `CLAUDE.md` listing the real current output (grouped: schema.ts as the single biggest blast radius, `auth-guard.ts` flagged separately as security-sensitive, the ERP service cluster, other untested services, large untested UI components incl. a note that `components/ui/sidebar.tsx` is vendored shadcn/ui not hand-authored, and `api/mcp/route.ts`), with an explicit honest-limitation note (line-count + no-test-file is a proxy, not a real risk score) and a pointer back to the script for regeneration.
- [x] Did not touch `src/lib/services/permission-service.ts` or its `ERP_ACTION_ROLES` table, per prompt.txt's explicit scope constraint — this gap-closure needed no permission-service changes at all.
- [x] Verified final diff is exactly the 5 intended files (`git status --short`): `.github/workflows/ci.yml`, `CLAUDE.md`, `scripts/check-guardrail-presence.mjs` (modified) + `scripts/check-service-header-comment.mjs`, `scripts/report-high-risk-files.mjs` (new) — no accidental shared-file edits.
- [x] Sanity-checked both new scripts (`node --check`, ran them for real against the live tree) and the modified `ci.yml`'s YAML syntax (`python3 -c "import yaml; yaml.safe_load(...)"`) since a full `bun install` wasn't available in this workspace (no `node_modules`) to run `bun run lint`/`bunx tsc` locally — both scripts use only Node built-ins (`node:fs`, `node:path`, `node:child_process`), so this is a real, not partial, local verification; full `lint`/`typecheck`/`build`/`unit-tests` CI jobs will still run for real on the PR.

## Remaining
- [ ] Commit and push this branch, open the PR (one PR covering both findings, per prompt.txt's instruction to close co-located findings together), let CI run.
