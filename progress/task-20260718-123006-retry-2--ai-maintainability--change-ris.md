# PROGRESS -- task-20260718-123006-retry-2--ai-maintainability--change-ris

VERIDIAN Review Framework gap-closure: AI Maintainability / Change Risk
Management (5 findings). See prompt.txt in the task dir for the full spec.

## Completed
- [x] Read ai-os/boss/ACTIVE-CLAIMS.yaml -- no conflicting claim found, registered this session's own claim.
- [x] Investigated current state of each finding against the live codebase (not the stale evaluation text) before writing code -- see "Investigation notes" below.
- [x] [High] Impact Analysis Before Modification + [High] Dependency Graph Accuracy -- built `scripts/dependency-index.ts` (+`.test.ts`, 24 tests): a lightweight, regex-based static import scanner over all 1,539 non-test `.ts`/`.tsx` files under `src/`, resolving `@/*` alias and relative imports to real repo files. Queryable two ways: `bun scripts/dependency-index.ts` regenerates the committed snapshot `ai-os/registry/dependency-index.json` (4,366 internal import edges); `bun scripts/dependency-index.ts --impact-of=<path>` / `--deps-of=<path>` answer "what breaks if I change this" / "what does this depend on" directly, before a change -- the exact question both findings named. `bun run dep-index` added to package.json. Both findings close with this one artifact, per the evaluation's own row-74 recommendation.
- [x] [High] Rollback Readiness -- built `scripts/check-migration-reversibility.mjs` (report-only, not a CI gate -- see its own header for why), `drizzle/down/0217_payment_entries_approval_workflow.down.sql` + `drizzle/down/0220_hr_attendance.down.sql` (real, working down-migration examples for the 2 most recent non-additive migrations, each documenting its own failure/data-loss conditions honestly), and `docs/ROLLBACK_RUNBOOK.md` (decision tree, convention, and the honest scope: of 230 migrations only 5 are non-additive; the other 225 are self-rollback via code revert and deliberately do NOT get mechanical down-migrations, which would misrepresent the real risk surface).
- [x] [Low] AI Confidence Before Code Changes -- built `src/lib/services/confidence-correlation-service.ts` (+`.test.ts`, 11 tests) + `GET /api/ai/team/confidence-correlation` (veridian_admin-gated, same posture as `/api/ai/team/scorecard`). Correlates `activity_log.confidence_band` (the self-reported signal) against real `review_decision`/`lifecycle_stage` outcomes -- both already persisted (Wave 165), never previously queried together. Reports a `monotonic` verdict (does success rate actually decrease as reported confidence decreases) plus named anomalies when it doesn't -- the literal "periodically audit whether reported confidence correlates with actual outcome quality" ask, made queryable on demand (no cron -- matches the Model Performance Scorecard's own on-demand posture; audit-cadence.ts's existing L5/L6/L7 periodic-review levels are the natural place a human/agent slots running this in, not a new scheduler).
- [x] [Medium] Knowledge Synchronization Between Code and Documentation -- no code change. See "Investigation notes" below for why.
- [x] `bunx tsc --noEmit` clean (0 errors, needed `NODE_OPTIONS="--max-old-space-size=6000"` in this sandbox -- default heap OOMs on this repo's size, unrelated to these changes). `bunx eslint` clean on every new/changed file. Full suite: 1456 pass / 0 fail / 105 files (bun test).

## Remaining
- [ ] Commit + push, open PR.

## Investigation notes (findings vs. live code)
- **Dependency index**: no static dependency-index/impact-analysis tool existed anywhere in scripts/ or src/lib as of this session (confirmed via `git grep -li` across *.ts/*.md for "dependency.index", "impact.analysis", "dependency.graph" -- only prose docs mention the concept, nothing computed it). Finding was real, not stale. Row 37's own recommendation ("consider adding FK constraints... to make the data-layer graph enforceable") is explicitly a "consider", not a hard requirement, and is a much larger, separate schema-wide effort (231 migrations, hundreds of tables) that would need its own dedicated audit of which relationships are intentionally soft (this codebase has documented precedent for deliberately NOT using FKs in places, e.g. activity_log.detail_id/detail_table's own "not a real FK" comment) -- out of scope for this pass, left for a follow-up.
- **Rollback**: no `drizzle/down/` directory or down-migration convention existed. Of 230 migrations, only 5 contain non-additive SQL (DROP TABLE/COLUMN, RENAME, ALTER COLUMN TYPE, SET NOT NULL): 0005, 0117, 0196, 0217, 0220. Finding was real; most migrations are purely additive (`ADD COLUMN IF NOT EXISTS`) and are already self-rolling-back via a code revert (old code ignores an unused new column), so a mechanical down-file-per-migration approach would have misrepresented the real risk surface. Scoped the fix to (a) a reversibility-flagging script, (b) real down-migration examples for the two most recent genuinely risky migrations, both honestly documenting their own failure conditions, and (c) a runbook tying it together. The 3 older non-additive migrations (0005/0117/0196) are long-applied production history without down-migrations -- flagged by the script, left as a documented judgment call rather than force-retrofitted.
- **AI Confidence**: `activity_log` already persists `confidencePercentage`/`confidenceBand` (closure-time self-assessment, Wave 165) and `reviewDecision`/`lifecycleStage` (real outcome) -- the raw signal to correlate already existed, just never aggregated/queried together. Same pure-core/DB-shell pattern as `model-scorecard-service.ts`.
- **Knowledge Sync**: duplicate of the AI Documentation row 67 finding, and the recommendation text for both is identical: "periodic manual audit passes as the practical complement to structural CI checks." `scripts/check-doc-cross-references.mjs` already provides the structural half (verifies link validity across the 6 real entry-point governance docs). The recommendation itself is explicitly procedural, not a code gap -- inventing a script to "verify semantic doc/code sync" would mean building an LLM-judgment call with no clear pass/fail criterion, which isn't what the evaluation asked for. Documenting this rather than making an unnecessary change, per this task's own instruction ("If a finding turns out to already be resolved, or the described gap doesn't match what you find in the code, say so in PROGRESS.md rather than making an unnecessary change").

## Scope discipline
- Did not touch `src/lib/services/permission-service.ts`'s `ERP_ACTION_ROLES` table structure or any other in-flight worker's declared scope (checked `ai-os/boss/ACTIVE-CLAIMS.yaml` first; no overlap found).
- No schema/migration changes to live tables -- `drizzle/down/*.down.sql` files are documentation/tooling artifacts, never auto-applied.
