# RCA: UMR-20260807-063839-3e0e (killed)

## Completed
- [x] Queried `resource_governor.py --query-umr --umr-id UMR-20260807-063839-3e0e` directly (per protocol, did not trust SPEC summary alone). Real reason field: "Registration-only backfill for pre-existing closed record task-20260726-071221-close-worker-false-self-report-gap-in-ph (claude-control). Real prior outcome: Superboss rejected PR https://github.com/FChecklist/claude-control/pull/78; task.yaml status=not_needed, no further action pending."
- [x] Checked `ai-os/boss/ACTIVE-CLAIMS.yaml` (path is `/opt/veridian/ai-os/boss/ACTIVE-CLAIMS.yaml`, not under this repo checkout) — no conflicting active claim on this UMR.
- [x] Read `task-20260726-071221-close-worker-false-self-report-gap-in-ph/task.yaml` in full: status=`not_needed`, blocked checkpoint note ("Superboss rejected: PR#78") timestamped `2026-07-26T07:26:58Z`, `closed_at: 2026-07-27T06:23:02Z`, `closed_reason`: "Owner directive 2026-07-27: remaining blocked backlog items declared not needed -- deliberately not fixed, not redispatched."
- [x] Pulled real PR #78 (FChecklist/claude-control) via `gh pr view`: **state=MERGED, mergedAt=2026-07-26T08:34:24Z, mergedBy=FChecklist** — i.e. merged *after* the 07:26:58 rejection note, and *before* the task was declared "not needed" the next day.
- [x] Read PR #78's commit history: the rejected revision (`afbe097a`, AUDIT: REJECT — line-shift bug) was followed by two real corrective commits (`afbe097a` fix, then `fd3ddf1a` "Fix confirm_merge() ambiguous-gh-failure gap ... AUDIT:"), landing on head commit `92d54c4c` "Close real gap: worker's own PR commit could write a false phase self-report, bypassing backfill_phase_self_report.py entirely" — i.e. the two real, blocking issues from the `review.json` reject (ambiguous `gh`-failure handling, multi-violation line-shift bug) were actually fixed in later pushes to the same PR.
- [x] Verified `92d54c4c` is a real, durable ancestor of `origin/master` two independent ways: (a) GitHub compare API `master...92d54c4c` → `ahead_by:0, behind_by:377` (b) local checkout `/opt/veridian/repos/claude-control`: `git fetch origin master && git merge-base --is-ancestor 92d54c4c... origin/master` → true.
- [x] **Root cause determined**: this is another instance of the known "killed-RCA mislabel" pattern (see memory index). `task-20260726-071221`'s task.yaml was never refreshed after PR #78's rejected revision was fixed and merged — its `not_needed`/"Superboss rejected" status is a stale snapshot from before the fix landed. The Owner's next-day "declared not needed, deliberately not fixed" closure note compounds the staleness (the gap was, in fact, already fixed and merged by then). UMR-20260807-063839-3e0e then faithfully backfilled that already-stale claim as its `reason` without re-verifying the real current PR state, and was marked `killed`. The UMR's own execution status ("no new work performed under this UMR") is accurate — the defect is the inherited historical claim, not the UMR's non-action.
- [x] Recorded the corrected, honest terminal outcome via `superboss-register.py mark-umr-terminal --umr-id UMR-20260807-063839-3e0e --status completed --commit-sha 92d54c4c64adbb7554f18419a42ea6abc3f976c1 --repo claude-control --pr-number 78 --reason "..."` citing the real merged commit as evidence (see command output / DB row for confirmation).
- [x] Wrote back completion to `agent_work_briefing.py record-completion --umr-id UMR-20260814-101654-5e84`.

## Remaining
- [ ] None — this is a docs/registration-only RCA correction; no source-file scope was named in the SPEC (the underlying real code gap was already closed via claude-control PR #78, merged 2026-07-26). Nothing further to redispatch.

## Notes
- No code change required in *this* repo (compliance-tracker) — the real underlying gap and its real fix both live in the `claude-control` repo (PR #78, already merged before this task ever started). This task's own scope is purely the RCA + terminal-status correction on the UMR record itself.
- Per AGENTS.md Rule 12: checked `capability_registry`/`wiring_registry`/`ai_agent_registry` per the deterministic briefing above (1 wiring_registry hit was this UMR's own dispatch row, not prior art); no dead_ends.json / open_questions.json / CLAUDE_MEMORY_INDEX.md entry existed yet for this specific UMR id, so this file becomes that record via the memory write-up below.
