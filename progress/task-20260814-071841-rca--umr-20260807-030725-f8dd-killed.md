# RCA: UMR-20260807-030725-f8dd (status=killed)

## Completed
- [x] Read ACTIVE-CLAIMS.yaml (no conflicting active claim for this UMR/scope)
- [x] Queried the real row: `resource_governor.py --query-umr --umr-id UMR-20260807-030725-f8dd`
  - `status=killed`, `ts_completed=2026-08-07T03:19:13Z`, `task_kind=veridian_task_create`,
    `source_trigger=owner_dispatch_gateway`, `files_touched=[]` (nothing was ever changed by
    this dispatch — it self-terminated during investigation, before writing any code).
  - Full `reason` (verbatim, not the SPEC's truncated summary): a **PM self-withdrawal**. The
    PM initially suspected `has_resource_headroom` was never invoked by the dispatch path
    (based on `grep -c` against `dispatch-tick.py` returning 0), then caught its own error one
    level deeper: `dispatch-tick.py` calls `dispatch_core.has_free_slot()` at 3 real spawn
    sites (cited as lines 162/1127/1263 at withdrawal time), each inside
    `acquire_dispatch_lock()`, and `has_free_slot_detail()` itself calls
    `has_resource_headroom_detail()`. Live proof cited at withdrawal time:
    `has_free_slot_detail()` returned `False` with
    `check=swap_hard_ceiling, swap_used_pct=0.99994` against `threshold=0.99` — exactly why
    only 3 workers were running with 13 rows queued at that moment. Conclusion recorded: "the
    resource veto IS enforced at every spawn site... nothing broken, no code change warranted."
- [x] Independently re-verified the reason's technical claim against the live repo (not just
      trusted the row):
  - `/opt/veridian/scripts/dispatch-tick.py` (the live checkout — separate repo from
    `compliance-tracker`, per [[veridian-scripts-separate-repo-live-checkout]]) today wraps the
    spawn-site check in `has_free_slot_with_stale_swap_override()`, which still calls
    `dispatch_core.has_free_slot_detail()` underneath (`dispatch-tick.py:198`) inside
    `acquire_dispatch_lock()` at 3 call sites (now lines 212, 1270, 1406 — shifted from the
    162/1127/1263 cited at withdrawal time purely because the file has grown since 2026-08-07,
    not because the underlying claim was ever wrong).
  - `dispatch_core.py` confirms `HARD_CEILING_UTILIZATION_PCT = 0.99` (`dispatch_core.py:103`)
    and `has_free_slot_detail()` → `has_resource_headroom_detail()` → the `swap_hard_ceiling`
    check (`dispatch_core.py:159`) referenced in the withdrawal reason really exist and really
    wire together as described. **The withdrawal's technical claim is correct.**
  - `git log -- dispatch-tick.py` in `/opt/veridian/repos/veridian-scripts` shows the resource
    veto has been present and enforced across the file's whole history (no missing-veto period
    around 2026-08-07 that this UMR could have legitimately caught).
- [x] Checked for a related-but-distinct genuine gap, in case the withdrawal was right on its
      narrow technical point but missed something real: found one — but it is a **separate**,
      later-discovered bug (swap_backoff's veto could latch permanently closed on a *static*
      SwapFree/SwapTotal ratio, since Linux doesn't proactively reclaim swap pages), fixed via
      `resource_governor.py`'s `_override_stale_swap_backoff()` (PR #309/#314,
      UMR-20260813-155201-da76) and then wired into `dispatch-tick.py`'s 3 spawn sites via PR
      #326 (commit `bfbd3fc1`, merged 2026-08-14T03:39+05:30 — **7 days after** this UMR's
      2026-08-07 withdrawal, by a different UMR chain). That gap is about the veto never
      *reopening*; this UMR's premise was about the veto never being *called* — two different
      failure modes. This UMR did not need to (and did not) discover that later gap; it is
      already fixed and unrelated to this row's own scope.
- [x] Conclusion: **UMR-20260807-030725-f8dd's kill is correct and requires no correction.**
      It is a real, accurate PM self-withdrawal of a false premise, not a mislabeled real-work
      or a stuck task. `files_touched=[]` is consistent with "investigated, found the premise
      false, withdrew before writing code" — the honest outcome, not a bug in the row itself.
      No fix, no redispatch, no `mark-umr-terminal` correction needed (the row is already
      correctly terminal with an accurate, evidence-backed reason).
- [x] `agent_work_briefing.py record-completion` called with this summary (no new
      wiring_registry entity registered — the one match found
      (`dispatch_event-owner-task-20260814-071835-1804377`) is just this task's own dispatch
      event, not prior scope; no new capability/gtm mapping applies to a docs-only RCA).
- [x] Progress file committed + pushed.

## Remaining
- None. This RCA is closed. No source file needed changing because the original kill was
  independently re-verified as the correct, honest outcome (matches the established
  "correctly-killed, no correction needed" pattern — see
  `~/.claude/projects/-opt-veridian-repos-compliance-tracker/memory/MEMORY.md`'s
  "Killed-RCA correctly-killed / no-correction-needed group", most recently
  UMR-20260807-063911-48c3 / UMR-20260807-063918-f15d, both closed the same way this cycle).
