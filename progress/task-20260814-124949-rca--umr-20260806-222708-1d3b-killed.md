# RCA: UMR-20260806-222708-1d3b (status=killed)

## Completed
- [x] Queried `resource_governor.py --query-umr --umr-id UMR-20260806-222708-1d3b` directly (not trusting SPEC summary). Confirmed row: `status=killed`, `unit_name=veridian-worker@task-20260807-003146-critical--real-corruption-confirmed-in-w.service`, reason explicitly says "PM HOLD, NOT A VERDICT ON THE WORK" — a bulk disk-pressure hold applied retroactively to a dispatch row, not a verdict on the underlying task.
- [x] Read the real underlying task's full artifacts at `/opt/veridian/ai-os/tasks/task-20260807-003146-critical--real-corruption-confirmed-in-w/`:
  - `task.yaml`: real forensic incident-response repair of genuine corruption in `superboss-register.sqlite`'s `wiring_registry` table — forensic copy → `sqlite3 .recover` against the copy → rename-aside on live db (not DROP) → restore of 24,281 `.recover`-validated rows → FTS5 rebuild → clean `PRAGMA integrity_check`. `status: blocked` in the file, `remaining_steps` explicitly says the only remaining items are "Optional/deferred, not required for this incident to be considered resolved."
  - `review.json`: independent review verdict = **approve**, tier1, confirms the repair was careful/well-verified/honestly reported (including two mid-repair bugs and how each was confirmed safe), flags only process-improvement follow-ups (no reproducible recovery script committed; sudo/polkit gap for timer-pause; risk-tier blind spot for prose-described-but-already-executed DDL) — none of which are open functional defects in the repair itself.
  - `pr_url.txt` → PR #240 `FChecklist/veridian-scripts`.
- [x] Checked PR #240 live: `state=CLOSED`, `merged=false`. Read its timeline: audited/AUDIT:PASS on 2026-08-07, then **closed as obsolete today (2026-08-14T06:17:49Z)** by task-20260814-060159 — reason: the PR's entire diff was a shared-root `PROGRESS.md` edit, and commit `1c363b6` on `origin/main` structurally migrated every worker to per-task `progress/${TASK_ID}.md` files (adding `progress_completion_gate.py`, which now refuses PROGRESS.md-only diffs as completion evidence). Root `PROGRESS.md` has been overwritten dozens of times since PR #240 opened, so merging it would regress current state. This closure is about the **doc-workflow migration**, not a defect in the repair — the actual repair was DDL/data-migration run live against the production db (per `review.json`), never expressed as a committed file.
- [x] Verified the real production database state directly: `PRAGMA integrity_check` on `superboss-register.sqlite` → `ok`; `wiring_registry` row count = 24,967 (consistent with the 24,281 restored rows plus normal registration growth since 2026-08-07). **The incident is genuinely resolved and currently healthy**, independent of PR #240's merge status.
- [x] Checked the two other objects the hold reason references:
  - Disk pressure: `df -h /` → 83G avail / 301G, 72% used — well below the 94%/22G-threshold that triggered the bulk PM hold. Condition that caused the hold no longer holds.
  - "PR 249 write-back fix": `FChecklist/veridian-scripts#249` ("fix(worker-units): bridge worker exit status to umr_task...") → merged 2026-08-13T10:39:54Z.
  - UMR-20260807-051828-6715 (the re-adjudication UMR the hold reason cites as having "put these rows back to queued in bulk"): queried directly → `status=completed_unmerged`, itself finished 2026-08-13. This row (UMR-20260806-222708-1d3b) was never actually observed in a `queued` state during this RCA — its `status` field has stayed `killed` throughout, i.e. the bulk requeue never got back around to flipping this specific dispatch-level row even though the underlying task work had already completed+been reviewed before the hold was even applied (task work: 2026-08-07 00:31–00:53 UTC; hold `ts_completed`: 2026-08-07 07:56:52 UTC).
- [x] **Root cause**: this is another instance of the documented "killed-RCA mislabel" pattern (see repo memory) — a dispatch-level bulk PM-hold action, applied for disk-pressure reasons, stamped `status=killed` onto a row whose real underlying task had *already completed real, reviewed, approved work* by the time the hold was applied. The hold was never a verdict on the work, and its own stated reversal conditions (disk reclaimed; artifact adjudication done) are now both true. There is no remaining real scope to redispatch — the wiring_registry corruption was fixed and independently verified healthy days ago; PR #240 was correctly closed today for an unrelated, later reason (doc-workflow migration), not because the repair failed or is incomplete.
- [x] Recorded a real, honest terminal outcome via `superboss-register.py mark-umr-terminal --umr-id UMR-20260806-222708-1d3b --status completed_unmerged --commit-sha 5718af4e22d3bde119ebce64e5dceeca2777f4fd --pr-number 240 --repo veridian-scripts` — `completed_unmerged` because the real commit (branch `worker/task-20260807-003146-critical--real-corruption-confirmed-in-w`, verified as a real, fetched commit) is genuinely NOT an ancestor of `origin/main` (PR #240 closed unmerged), even though the underlying database repair it documents is real, reviewed, and independently verified live-healthy.
- [x] Called `agent_work_briefing.py record-completion` for UMR-20260814-121804-3da0 with a real summary.

## Remaining
- [ ] None. RCA complete; terminal status recorded with real evidence. No code/script fix was needed for the underlying incident (it was already fixed and verified); this task's own objective was diagnostic/RCA + terminal-status correction, not a new script.
