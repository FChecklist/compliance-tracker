# RCA: UMR-20260806-211546-cf3c status=killed

## Completed
- [x] Queried `resource_governor.py --query-umr --umr-id UMR-20260806-211546-cf3c` directly (not trusting SPEC summary alone).
- [x] Registered active claim in `ai-os/boss/ACTIVE-CLAIMS.yaml`.
- [x] Read the underlying task workspace `ai-os/tasks/task-20260807-002908-pm-overrides-a-false-positive-credit-acc/` (task.yaml, result.json).
- [x] Root-caused: real work was fully completed hours before the kill; the kill was a PM bulk disk-pressure hold action, not a verdict on the work (row's own `reason` field says so explicitly).
- [x] Verified child UMR `UMR-20260806-182453-702a` (the real work-tracking UMR this task wrote evidence to) is `status=completed`.
- [x] Verified PR FChecklist/veridian-scripts#238 is MERGED, commit `726f7c433561bc35c09084ec8a7d2aaf0d984ab9`, and confirmed that SHA is a real ancestor of `origin/main` in the local `veridian-scripts` checkout.
- [x] Marked UMR-20260806-211546-cf3c terminal (status=completed) citing the real merged commit as evidence.
- [x] Recorded completion via `agent_work_briefing.py record-completion`.
- [x] Closed out ACTIVE-CLAIMS entry.

## Remaining
- [x] None. RCA complete, terminal status recorded honestly.

## Root cause summary
`UMR-20260806-211546-cf3c` is the **outer dispatch UMR** (task_kind=`veridian_task_create`) that spawned
task `task-20260807-002908-pm-overrides-a-false-positive-credit-acc`. That task ran to real completion at
2026-08-07T00:50Z: it reused blocked task `task-20260806-205209`'s prior root-cause work, root-caused an
additional numerator-collapse bug, fixed `generate_platform_completion_checklist.py` in place (git-archive
snapshot isolation instead of live-worktree glob), proved the fix with 3 reproducible pytest runs, and
shipped it as `FChecklist/veridian-scripts#238` (merged, commit `726f7c4`). Evidence was written onto the
real child UMR `UMR-20260806-182453-702a`, which the register already shows as `status=completed`.

Roughly 7 hours *after* that real completion, PM's bulk disk-pressure re-adjudication
(`UMR-20260807-051828-6715`, triggered by free disk hitting 20G/94%) mass-marked a batch of `umr_tasks`
rows `killed` as a hold action — the row's own `reason` field is explicit that this was "PM HOLD, NOT A
VERDICT ON THE WORK" and that sampling found 40/40 of the held rows already had real completion artifacts.
This dispatch row (`UMR-20260806-211546-cf3c`) was one of that batch. Its own child work (`702a`) was
never re-queued or lost — only this outer dispatch-tracking row carried the stale `killed` label. This
matches the same self-correcting/PM-hold-mislabel pattern already catalogued for sibling RCAs `UMR-ec65`
and `UMR-eb50` (see `[[veridian-killed-rca-series-index]]`).

No code fix was needed or appropriate here — the real fix already shipped and merged days earlier under
`702a`. Action taken: record the honest terminal status on the outer dispatch row via
`superboss-register.py mark-umr-terminal --status completed --commit-sha 726f7c433561bc35c09084ec8a7d2aaf0d984ab9 --pr-number 238 --repo veridian-scripts`.
