# RCA: UMR-20260807-030725-f8dd (status=killed)

## Completed
- [x] Read index-first per AGENTS.md Rule 12: checked `veridian-killed-rca-series-index`
      memory before broad search — this UMR matches the "correctly-killed / no-correction-
      needed" bucket pattern (e.g. `veridian-umr-68ff`), not the "mislabel" series.
- [x] Queried the real row: `resource_governor.py --query-umr --umr-id UMR-20260807-030725-f8dd`.
      Full real record:
      - `task_identity`: owner-task-20260807-030724-3087306
      - `status`: killed, `ts_submitted` 2026-08-07T03:07:25Z, `ts_completed` 2026-08-07T03:19:13Z
      - `dispatched_repo` (per the row's own `cross_repo_pr_check`): veridian-ai-os
      - `reason` (verbatim, real, recorded at withdrawal time): "PM WITHDRAWAL, the premise of
        this task is FALSE and it must not be worked. I claimed has_resource_headroom is never
        called by the dispatch path because grep -c against dispatch-tick.py returns 0. That
        grep was one level too shallow. dispatch-tick.py calls dispatch_core.has_free_slot() at
        three real spawn sites, lines 162, 1127 and 1263, each inside acquire_dispatch_lock(),
        and has_free_slot_detail() itself calls has_resource_headroom_detail() and returns its
        detail dict. The resource veto IS enforced at every spawn site. Live proof at withdrawal
        time, has_free_slot_detail() returns False with check swap_hard_ceiling at
        swap_used_pct 0.99994 against threshold 0.99, which is exactly why only 3 workers run
        with 13 rows queued. Nothing is broken here and no code change is warranted. Working
        this task as written would have added a redundant second veto to an already-correct
        gate."
- [x] Root cause determined: this was a **self-correcting PM withdrawal**, not a bug/crash/
      infra failure. The dispatching PM's *own* prior claim (resource veto never enforced) was
      itself false, discovered and retracted by the same PM before any work was dispatched
      (`unit_name: null` — no worker unit was ever spawned for this UMR). The premise was false;
      withdrawing was the correct action.
- [x] Independently re-verified the withdrawal's technical claim against the **current live**
      code (not just trusting the narrated reason), per the lesson in
      `veridian-stop-work-order-lift-7th-gen-real-merged-pr-still-live-blocked`
      ("execute the real gate, don't trust a narrated claim"):
      - `/opt/veridian/repos/veridian-scripts/dispatch-tick.py` and `dispatch_core.py` (the repo
        veridian-ai-os itself has no `dispatch-tick.py`/`dispatch_core.py` of its own — this
        logic lives in `veridian-scripts`, confirmed via `git ls-files`).
      - `dispatch_core.py` defines `has_resource_headroom_detail()` (line 129),
        `has_resource_headroom()` (194), `acquire_dispatch_lock()` (219), `has_free_slot_detail()`
        (247), `has_free_slot()` (268) — exactly the call chain the withdrawal reason described.
      - `dispatch-tick.py` currently gates all 3 real spawn call sites (`supervisor_sweep_tick`
        line 212, and two further sites at 1270/1406 — line numbers have shifted since 2026-08-07
        because of later legitimate commits, not because the gate was removed) through
        `has_free_slot_with_stale_swap_override()`, which itself calls
        `dispatch_core.has_free_slot_detail()` first and then applies a narrow,
        activity-based override on top (added 2026-08-13, task-20260813-205525, closing a real
        but separate gap: a stale swap-occupancy ratio could permanently latch the veto even with
        abundant real memory and zero live swap I/O). The resource veto is not only still
        enforced at every spawn site as of today — it has since been *strengthened*, confirming
        the withdrawn task's premise remains false and no code change was ever warranted here.
- [x] Verdict: **no fix, no redispatch** — the row's own recorded terminal state is accurate.
      Re-confirmed via `superboss-register.py mark-umr-terminal --status killed` citing this RCA's
      real evidence (see command below), matching the precedent in
      `veridian-umr-68ff-killed-rca-correct-pm-withdrawal-scope-since-merged`.
- [x] Wrote memory entry `veridian-umr-f8dd-killed-rca-correct-pm-self-withdrawal` and linked it
      into the killed-RCA series index's "correctly-killed" bucket.
- [x] `record-completion` write-back to `agent_work_briefing.py` for this task's own UMR
      (UMR-20260814-201903-5a4a).

- [x] Committed (`8774a5e60`) and opened PR #1190
      (https://github.com/FChecklist/compliance-tracker/pull/1190). Docs-only diff, consistent
      with the finding that no code change was warranted. `Vercel` check failed on an unrelated
      build-rate-limit (not a real code/audit failure); all other checks pending/passing at
      push time — CI/audit-check gate resolves independently per Rule 6/10, this task does not
      block on it.

## Remaining
- [ ] None. RCA complete: correctly-killed, no code change warranted, terminal state
      re-confirmed with evidence, PR opened.
