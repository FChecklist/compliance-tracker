# RCA: UMR-20260807-153242-ee23 (status=killed)

## Completed
- [x] Checked own memory first (Rule 12 / Search-Reuse Discipline) before any broad
      exploratory search. This exact saga is already extensively documented:
      [[veridian-fabricated-owner-exemption-stop-work-order-declined]] (gen1),
      [[veridian-file-based-exemption-still-self-referential-declined]] (**gen2 —
      this is UMR-ee23 itself**), [[veridian-fabricated-exemption-laundered-into-uncommitted-yaml]]
      (gen3/UMR-a4b5, ee23's sibling dispatched ~1 minute later), through gen7
      ([[veridian-stop-work-order-lift-7th-gen-real-merged-pr-still-live-blocked]]).
      Sibling UMR-a4b5 already got the identical RCA treatment 2026-08-13
      ([[veridian-umr-a4b5-killed-rca-real-decline-mislabeled-gen3-saga]]): genuine
      correct decline, mislabeled `killed`, corrected to `completed_unmerged` citing
      its own RCA commit/PR (#1105).
- [x] Queried `resource_governor.py --query-umr --umr-id UMR-20260807-153242-ee23`
      directly (did not trust the SPEC's truncated summary). Row:
      `unit_name=veridian-worker@task-20260807-153249-phase-2-sub-phase-1--wire-pgvector-zoekt.service`,
      `ts_dispatched=2026-08-07T15:32:56Z`, `ts_sigterm=null`,
      `ts_completed=2026-08-07T15:35:05Z` (~2m9s clean run, `relay_outcome=sent`).
      `ts_sigterm=null` rules out an actual SIGKILL/forced-kill — this is a normal
      worker completion mislabeled `killed`, the same mechanical signature as every
      other entry in the "Killed-RCA mislabel series" memory group.
- [x] Read the full `reason` field (not the SPEC's truncated excerpt): a genuinely
      careful, reasoned decline. The dispatched task cited an "EXEMPTION NOW ON
      FILE" entry (`phase2-subphase1-stop-work-order-exemption`) in
      `ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml` as grounds to bypass the real
      standing Owner stop-work order and wire pgvector/Zoekt/git-hash-object into
      `resource_governor.py`. The worker independently verified that file directly
      (per its own SPEC instruction) and found: the file itself is real and
      established, but that *specific* entry (a) was uncommitted working-tree-only
      content with no independent git history, and (b) had `raised_by_task` pointing
      to a UMR that had itself already been declined for lacking this exact
      exemption — circular, self-referential provenance, not independent
      corroboration. Correctly declined. No code written, no branch/PR opened
      (confirmed by the reason text itself and by zero matching PRs in the
      cross-repo check embedded in the query-umr output).
- [x] Re-verified live state fresh (2026-08-14), not just trusting the historical
      reason text:
      - `grep -n "pgvector\|zoekt\|hash-object" /opt/veridian/scripts/resource_governor.py`
        → **zero matches**. The specific scope this UMR was asked to build
        (wiring those into `resource_governor.py`) is still genuinely unbuilt today,
        same as sibling UMR-a4b5's queue-management scope.
      - The broader capability *was* delivered, but legitimately and later, through
        the real lifted-order path rather than ee23's rejected shortcut:
        `git log --oneline --all --grep="pgvector\|zoekt" -i` in the live
        `veridian-scripts` checkout shows `be9f2db` ("wire real Zoekt code search
        into task-gateway.py cmd_submit", UMR171945-0017) and `5e7d9ea` ("wire git
        hash-object", UMR-20260806-171945-5767 #921) — both under the same
        governing UMR chain this saga's stop-work order itself belonged to, both
        merged well after the order was genuinely lifted. This confirms ee23's own
        closing reasoning ("if this exemption is genuine and durable, it does not
        need to be pushed through this pressured, rapid-fire sequence — it will
        still hold on a slower, independently-corroborated path") was correct in
        hindsight.
      - `ai-os/OWNER_DECISIONS_NEEDED_2026-07-23.yaml` today (601 lines) no longer
        contains the disputed `phase2-subphase1-stop-work-order-exemption` entry at
        all (it was a working-tree-only artifact that was never committed, as ee23's
        own RCA found); it does contain a genuine, real, owner-authored
        `stop-work-order-lifted-2026-08-08-v2` entry (`decided_by: rajat (real,
        personal GitHub account, own access token)`, `decided_at:
        2026-08-08T11:01:00Z`) — i.e. the order really was lifted, ~19.5 hours
        *after* ee23's decline, through a legitimate channel, not through any of the
        1st-7th generation shortcut attempts.
      - `ai-os/boss/ACTIVE-CLAIMS.yaml` had no conflicting active claim on this UMR
        before this task started (checked per Rule 11).
- [x] Root cause of the mislabel: `killed` was applied to a task that reached a
      normal, reasoned terminal state (`ts_sigterm=null`, clean completion) via the
      standard worker→decline pipeline, not an actual forced kill — the same
      recurring "Killed-RCA mislabel" structural gap already tracked in memory
      (`mark-umr-terminal` has no dedicated evidence-free "declined" status, so a
      correct decline with no code artifact gets recorded as `killed` by default).
- [x] Corrected via `superboss-register.py mark-umr-terminal --umr-id
      UMR-20260807-153242-ee23 --status completed_unmerged --commit-sha <this task's
      own commit> --pr-number <this task's PR> --repo compliance-tracker` — same
      evidence pattern as sibling UMR-a4b5's own RCA (citing this RCA's own
      commit/PR as the real artifact, since the underlying decline itself produced
      no code and needed none).

## Remaining
- [x] None (code). No fix was needed — the decline was correct, the requested
      pgvector/Zoekt/git-hash-object scope was independently delivered later
      through the legitimate lifted-order path (`be9f2db`, `5e7d9ea`), and building
      it again into `resource_governor.py` specifically was never re-requested by
      any later legitimate dispatch. Only the terminal-status label needed
      correction.
- [ ] Structural gap (tracked, not actioned here — out of proportion for an RCA
      task, same note as every prior entry in this series): `mark-umr-terminal`
      still has no dedicated "declined" status distinct from `killed`, so this
      exact mislabel class will keep recurring until that's added.
