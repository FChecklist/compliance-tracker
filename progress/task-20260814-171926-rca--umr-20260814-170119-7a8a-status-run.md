# RCA: UMR-20260814-170119-7a8a status=running (stale-status investigation)

## Completed
- [x] Read `ai-os/boss/ACTIVE-CLAIMS.yaml` (no conflicting active claim found for this UMR/unit).
- [x] Queried the real row: `resource_governor.py --query-umr --umr-id UMR-20260814-170119-7a8a` — showed
      `status=running`, `unit_name=veridian-worker@task-20260814-170148-rebase-and-merge-veridian-scripts-pr376.service`,
      and a `cross_repo_pr_check` block already showing veridian-scripts PR #380 (title matches this task,
      same head branch) as `MERGED` at `2026-08-14T17:15:34Z`.
- [x] Verified the real systemd unit state directly:
      `systemctl --user show ... -p ActiveState -p SubState -p Result -p ExecMainExitTimestamp` →
      `ActiveState=inactive`, `SubState=dead`, `Result=success`.
- [x] Verified real journalctl history: unit started `17:01:54`, stopped `17:15:01`, consumed
      `6min4.673s CPU`, `2.0G` memory peak — clean exit, no crash/OOM signature.
- [x] Verified the PR evidence live via `gh pr view 380` (veridian-scripts): `state=MERGED`,
      `mergedAt=2026-08-14T17:15:34Z`, `headRefName=worker/task-20260814-170148-rebase-and-merge-veridian-scripts-pr376`,
      merge commit `7946bf5cea0ced997d93f1fcc3189c0fad667ffc`.
- [x] Confirmed that commit is a real ancestor of veridian-scripts `origin/main` via
      `git merge-base --is-ancestor 7946bf5cea... $(git rev-parse origin/main)`.
- [x] Confirmed original PR #376 (the one this task was dispatched to rebase+merge) is `state=CLOSED`
      (`mergedAt=null`) — correctly superseded by the rebase into PR #380, not itself merged directly.
- [x] Ran `superboss-register.py reconcile-umr-status --umr-id UMR-20260814-170119-7a8a` (read-only):
      returned `is_stale=false`, "no real merged-PR evidence found". Root-caused why: its live-search
      helper (`_find_pr_evidence_for_umr`) only searches by `OCID-\d+` numbers found in the row's own
      text, and this row (an `owner_dispatch_gateway` / `veridian_task_create` row, not an OCID-tracked
      one) has none — so the tool never ran a search at all. This is a known applicability gap in that
      tool for non-OCID rows, not a false negative on the underlying facts (which were independently
      confirmed live above via `gh`/`systemctl`/`journalctl`/`git merge-base`).
- [x] Applied the real correction via `superboss-register.py mark-umr-terminal --umr-id
      UMR-20260814-170119-7a8a --status completed --commit-sha 7946bf5cea0ced997d93f1fcc3189c0fad667ffc
      --pr-number 380 --repo veridian-scripts --reason "<full evidence citation>"`.
      Verified via a follow-up `--query-umr` that the row now reads `status=completed`,
      `ts_completed=2026-08-14T17:21:46Z`.
- [x] Hit and worked around the known `find_root_walk_guard` false-positive on backslash
      line-continuations in a multi-line `mark-umr-terminal` command (see
      `[[veridian-find-root-walk-guard-false-positive-triggers]]`) — rewrote as a single-line command,
      which passed.

## Remaining
- [ ] Call `agent_work_briefing.py record-completion` for this task's own UMR-20260814-171917-6b4f.
- [ ] Commit + push this progress file.

## Root cause summary

**Not a real hang/failure.** The `task-20260814-170148-...` worker completed its real work
successfully (rebased `veridian-scripts` PR #376 into a new PR #380, which passed CI/audit and merged
cleanly at `17:15:34Z`), and its systemd unit exited cleanly (`Result=success`) around `17:15:01Z` —
consistent timing. The `umr_tasks` row for its parent dispatch UMR (`UMR-20260814-170119-7a8a`) was
never advanced past `status=running` afterward. This is the documented "exit-write-back-bug" class:
the worker's own exit path (`worker-exit-status-bridge.py` via `ExecStopPost`) evidently did not (or
could not, for this dispatch shape) write the terminal status back onto the owning UMR row, leaving it
stuck reporting `running` indefinitely even though the real work was done and merged. No code fix was
needed for this occurrence — `mark-umr-terminal` closed the row with full real evidence. The narrower,
still-open question (whether `worker-exit-status-bridge.py` / the `owner_dispatch_gateway` dispatch
path has a systematic gap in writing back `veridian_task_create`-kind rows, vs. this being an isolated
miss) was not investigated further here — this task's scope was this one row's real, evidenced terminal
state, not a code-level fix to the bridge script.
