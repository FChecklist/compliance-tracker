# RCA: UMR-20260806-202449-2e09 (status=killed)

## Completed
- [x] Read `ai-os/boss/ACTIVE-CLAIMS.yaml` (no conflicting active claim found for this scope).
- [x] Queried `resource_governor.py --query-umr --umr-id UMR-20260806-202449-2e09` directly (not trusting SPEC summary alone). Confirmed real row: `status=killed`, `unit_name=veridian-worker@task-20260807-002858-diagnose-real-stall--umr-20260806-135632.service`, `ts_completed=2026-08-07T07:56:52Z`, reason = bulk PM HOLD (disk at 94%/20G avail) applied by re-adjudication UMR-20260807-051828-6715, explicitly **not a verdict on the work**.
- [x] Inspected the real task directory `ai-os/tasks/task-20260807-002858-diagnose-real-stall--umr-20260806-135632/`: confirmed it genuinely carries `result.json`, `review.json`, `pr_url.txt`, `supervisor-result.json` — the "40/40 already have SUMMARY/REPORT/RESULT + review.json" claim in the bulk-hold reason checks out for this row.
- [x] Read `review.json`: real independent audit, `verdict=approve`, `tier=tier1` — reviewer spot-checked live DB rows (umr_tasks, pm_decisions_pending id=300, actions log ACT-20260807-003550-e836, live `PRAGMA integrity_check`) rather than trusting the self-report, and confirmed all claims matched.
- [x] Traced the diagnosed object, `UMR-20260806-135632-329e`: now `status=completed` (resolved 2026-08-07 via merged PR `FChecklist/veridian-scripts#212` — a stale-heartbeat reconciler false negative, not a real failure). So the thing this task diagnosed is fully resolved.
- [x] Checked disk pressure that caused the original hold: **resolved**. `df -h /` now shows 79% used / 63G avail (was 94%/20G at hold time, 2026-08-07).
- [x] Traced this task's own PR (`pr_url.txt` -> `FChecklist/veridian-scripts#236`, commit `2295c30fc73b1ef9bef286d4ccf6a5dd623034bd`): `state=CLOSED`, `mergedAt=null`. Confirmed via `git merge-base --is-ancestor` that this commit is real but **not** an ancestor of `origin/main`.
- [x] Read the PR's own timeline via `gh api .../issues/236/timeline`: it already carries a real `AUDIT: PASS` comment (2026-08-07, matches review.json verbatim) and was closed today (2026-08-14T06:17:46Z) as **obsolete** by a separate triage task (`task-20260814-060159`) — root cause: commit `1c363b6` on main structurally moved every worker off the shared root `PROGRESS.md` onto per-task `progress/${TASK_ID}.md` + added `progress_completion_gate.py`, which refuses PROGRESS.md-only diffs as completion evidence. Root `PROGRESS.md` on main has been overwritten dozens of times since this PR opened, so merging it would just regress. This closure is a format-obsolescence decision, not a rejection of the diagnostic content.
- [x] Verified the substantive finding this task produced (`pm_decisions_pending` id=300: real, reproducible `wiring_registry`/`wiring_registry_fts` corruption in the live `superboss-register.sqlite`) is durably recorded in the live DB independent of the PR/git outcome — `related_umr=UMR-20260806-135632-329e`, still `status=open`. Live `PRAGMA integrity_check` on that DB now returns `ok` — the corruption has since been repaired by other work; the pending row itself is stale bookkeeping, out of scope for this RCA to close (no evidence in hand of which task performed the repair) — noting it here rather than acting on it.
- [x] **Root cause determined**: this row's `killed` status is a disk-pressure-driven PM hold applied in bulk, not a real failure of the underlying work. The diagnosed target is now fully resolved (separate merged PR), the diagnosis itself was real and audit-passed, and its own artifact PR was independently and correctly closed as obsolete today for an unrelated structural reason (shared-PROGRESS.md deprecation). Re-queuing this row would be pure duplicate work — the hold's own stated risk — with no real work left to do.
- [x] Recorded terminal disposition: `mark-umr-terminal --umr-id UMR-20260806-202449-2e09 --status completed_unmerged --repo veridian-scripts --commit-sha 2295c30fc73b1ef9bef286d4ccf6a5dd623034bd --pr-number 236` (real commit, real PR, correctly not-merged-to-main — `completed_unmerged` is the honest terminal status, not `completed`).
- [x] Called `agent_work_briefing.py record-completion` for `UMR-20260814-161806-6d05`.

## Remaining
- [ ] None for this task's scope. (Not in scope: closing stale `pm_decisions_pending` id=300 — flagged above for whoever owns that queue, since I have no evidence in hand of which task actually repaired the corruption.)
