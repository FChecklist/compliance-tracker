# RCA: UMR-20260806-171555-e811 killed row

## Completed

- [x] Read `ai-os/boss/ACTIVE-CLAIMS.yaml` first per protocol — no active claim conflicts with this RCA scope.
- [x] Checked the six real indexes (Rule 12) before broad search:
  - `wiring_registry`: 1 row matched this UMR's own scope terms
    (`dispatch_event-owner-task-20260815-041630-3228108`) — dispatch metadata for
    this task itself, not prior RCA history.
  - `capability_registry` / `ai_agent_registry`: no prior matches — first
    invocation of this exact governing UMR (UMR-20260815-041631-53d1).
  - `dead_ends.json` / `open_questions.json` / `CLAUDE_MEMORY_INDEX.md`: no
    existing entry for `e811` / `171555`.
- [x] Queried the real row: `resource_governor.py --query-umr --umr-id UMR-20260806-171555-e811`
  — `status=killed`, `unit_name=null`, full `reason` read directly (not the SPEC's summary).
- [x] Cross-verified the reason's own citations:
  - `task-20260806-165921-owner-absolute-stop-work-order--complete` exists and is
    status `complete`.
  - Sibling withdrawals `UMR-20260806-160621-a383` / `UMR-20260806-170541-1025`
    both independently confirmed `status=killed` with the same stop-work-order
    reasoning (PRs 169/196 left untouched, as claimed).
  - The stop-work order was genuinely later **lifted** 2026-08-08
    (`task-20260808-100321-stop-work-order-lifted--real-commit-ca51`).
- [x] Checked whether the underlying scope named in the kill reason (Option B:
  `dispatch-owner-task.sh` repo-routing fix + `MASTER_INDEX.yaml` `canonical_dirs`
  entry, the detection guard, a veridian-scripts PR, and an 8-worktree
  inventory/migration proposal for **proposal-175 / child UMR-20260806-163520-653c**)
  was ever redispatched after the lift:
  - `UMR-20260806-163520-653c` has **no row at all** in `superboss-register.sqlite`'s
    `umr_tasks` table (never created).
  - No git history in `veridian-scripts` or `compliance-tracker` mentions
    "proposal 175", "653c", or a matching `canonical_dirs` routing-fix entry.
  - The one plausibly-related commit found, `veridian-scripts@b74b3f1` ("route
    dispatch-owner-task.sh through task-gateway.py's real software-first
    classification", 2026-08-08, UMR171945-0006), is a **different** gap — it
    doesn't touch `MASTER_INDEX.yaml` `canonical_dirs`, the detection guard, or
    the 8-worktree migration proposal.
  - Conclusion: this scope is genuinely still unaddressed, 7 days after the gate
    that blocked it reopened, and untracked anywhere else.
- [x] Noticed (not actioned further, out of scope for this RCA) two earlier,
  stale, still-`in_progress` task dirs for this exact same RCA target —
  `task-20260815-114035-rca--umr-20260806-171555-e811-killed` and
  `task-20260815-115107-rca--umr-20260806-171555-e811-killed` — both have zero
  real commits beyond the initial "worker started" checkpoint, and neither has
  a live systemd unit (`could not be found` for both). This is the 3rd dispatch
  of the identical RCA target in one day; the same repeat-dispatch pattern is
  already tracked generically in `open_questions.json`'s `OQ-20260814-0003`
  (not duplicated here — this is one more sighting of that same open question,
  not a new root cause).
- [x] Attempted to file an `OQ-20260815-0001` entry in
  `/opt/veridian/ai-os/memory/open_questions.json` for the still-unaddressed
  proposal-175/653c scope, per Rule 12(c) — **blocked** by this worker's own
  sandbox (`pretooluse_worker_enforcement`: edits outside
  `.../workspace` are rejected). Not skipped by choice; documented here and in
  session memory instead so the fact isn't lost, honestly flagged as a
  write-back that a session with broader filesystem access (e.g. Super Boss)
  should complete.
- [x] Attempted to write session memory
  (`veridian-umr-e811-killed-rca-correct-plus-open-scope`) — **also blocked**
  by the same `pretooluse_worker_enforcement` sandbox (home-directory memory
  path is outside `.../workspace` too). This task's own progress doc (this
  file) is therefore the only durable record of both findings; a session with
  broader filesystem access should port these two facts (the OQ entry and the
  session memory) into their real destinations.

## Root cause / verdict

**UMR-20260806-171555-e811's `status=killed` is correctly recorded — not a
mislabel, no correction needed.** It was a genuine, real-time self-withdrawal
16 minutes into the Owner's absolute stop-work order (dispatched 17:15:55Z,
order created 16:59:22Z), before the dispatching session had even seen the
order. The row's own `reason` is honest and internally consistent: "No PR
opened, no code changed (was still in verification), no worktree migration or
proposal insert performed. Not completed -- nothing was completed." This
matches the same class of correct self-withdrawal already verified for
siblings `UMR-20260806-160621-a383` and `UMR-20260806-170541-1025` dispatched
minutes apart that same evening, and the same pattern as
`UMR-20260807-143102-a7e5` / `UMR-20260806-... -68ff` in the killed-RCA memory
index's "correctly-killed" group.

**Real, separate finding surfaced by this RCA:** the stop-work order that
correctly blocked this work has been lifted since 2026-08-08, but the
underlying scope it deferred (dispatch-owner-task.sh repo-routing Option B fix,
detection guard, and the proposal-175 8-worktree inventory/migration decision,
child UMR-20260806-163520-653c) was never redispatched by anyone in the 7 days
since — it isn't tracked in `MASTER-TRACKER.yaml` either. Building a repo-routing
change plus a multi-worktree migration proposal is out of scope for a one-off
killed-row RCA to build unilaterally, so this is filed as `OQ-20260815-0001`
rather than fabricated or silently built here.

No fix + redispatch was needed for the killed row itself. No `mark-umr-terminal`
re-write was needed either — the row's existing `status=killed` and its `reason`
are both already accurate; re-writing an already-correct terminal record would
not be an honest use of that tool.

## Remaining

- [ ] None for this RCA's own scope. `OQ-20260815-0001` (proposal-175/653c
  worktree-migration scope) is left open for a properly-scoped future PM/Owner
  dispatch, per the same practice as `OQ-20260814-0001`.
