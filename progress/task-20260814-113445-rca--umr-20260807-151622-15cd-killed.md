# RCA: UMR-20260807-151622-15cd killed

## Completed
- [x] Read `ai-os/boss/ACTIVE-CLAIMS.yaml` (no relevant collision found; no other session claiming this exact UMR).
- [x] Queried `resource_governor.py --query-umr --umr-id UMR-20260807-151622-15cd` directly (did not trust SPEC's summary alone). Confirmed: `status=killed`, `unit_name=veridian-worker@task-20260807-152601-stop-work-order--batch-3--land-batch-2-s.service`, reason mechanical (systemd inactive, no PR opened against `task.yaml`'s `branch:` field).
- [x] Read the full `task.yaml` + all checkpoints for `task-20260807-152601-stop-work-order--batch-3--land-batch-2-s` (the dispatched task behind this UMR). Found: `branch: rescue/batch-2-land-tests` in `task.yaml`, but the checkpoints show real committed work (`bca453b`/`6cbd222`) — 14 real pytest test files for batch-2 stranded scripts, quality gates passed, reviewer verdict `approve (tier1)`.
- [x] Read `supervisor.log`: root mechanism confirmed — `gh pr create` failed for branch `rescue/batch-2-land-tests` with `No commits between main and rescue/batch-2-land-tests` (that branch, as named in `task.yaml`, was never the one the worker actually committed to). Supervisor correctly refused to fall back to guessing a PR (citing the real PR #84 2026-07-26 incident) and left the task `blocked` until it went `killed` on systemd-inactive reconciliation.
- [x] Checked whether the real work (14 batch-2 test files) was lost. In `/opt/veridian/repos/veridian-scripts`: `git log --all --oneline --grep=batch-2` shows the same rescue scope was independently redone and landed via **PR #271** ("test: land batch-2, 14/14 real tests passing", merged 2026-08-07T16:39:50Z — ~1h after this UMR's task went `blocked`). Verified via `gh pr view 271`: `state=MERGED`.
- [x] Found this exact UMR was **already RCA'd** one day prior to this dispatch: `veridian-scripts` commit `8c00d30` (2026-08-13T20:28:53Z, same author) — "RCA for UMR-20260807-151622-15cd (killed) -- branch never pushed to origin, work redone elsewhere, backfilled real reason/ts_completed." That commit re-ran `reconcile_owner_dispatch_status.py --umr-id UMR-20260807-151622-15cd --apply` to backfill the real `reason`/`ts_completed` via `update_umr_task()`.
- [x] Verified live DB state (`superboss-register.sqlite`, `umr_tasks` row) matches that prior RCA's outcome exactly: `status=killed`, `ts_completed=2026-08-13T20:26:27.154099+00:00`, `metadata_json.reconcile_owner_dispatch_status.reconciled_at` = same timestamp. The row is correctly terminal and already carries accurate evidence — no further correction needed.

## Root cause (confirmed)
Two independent causes compounded:
1. **Branch-name mismatch**: `task.yaml`'s `branch:` field (`rescue/batch-2-land-tests`) never matched the branch the worker actually committed real work to. The supervisor's PR-resolution step only looks for the `task.yaml`-declared branch, found zero commits on it, and (correctly, per its own safety rule) refused to guess — so it never surfaced the real, already-pushed work.
2. **Mechanical reconciler is branch-name-blind, not artifact-blind**: `reconcile_owner_dispatch_status.py` correctly determined the row was a stale-labeled terminal case (systemd inactive, task.yaml blocked, no PR against the *declared* branch) and mechanically closed it `killed` — which is accurate for that specific dispatch/branch, but does not by itself capture that the underlying *scope* was independently completed elsewhere. That extra context was supplied by human-level RCA (commit `8c00d30`), not by the mechanical reconciler.

## Verdict
**No new action required.** `status=killed` for UMR-20260807-151622-15cd is **correct and already fully evidenced**:
- Part A (rescue batch-2's 14 stranded test files): real work, not lost — independently redone and merged via `veridian-scripts` PR #271 (merged 2026-08-07T16:39:50Z).
- Part B (next 15 untested scripts): never attempted under this UMR; remains decoupled open platform backlog (per prior RCA), not a defect of this UMR's terminal status.

This dispatch is a **duplicate of an already-completed RCA** (`veridian-scripts` commit `8c00d30`, 2026-08-13, ~15h before this dispatch fired). No code fix is warranted — the branch-name-mismatch/no-fallback-guess supervisor behavior is a **correct** safety behavior (see the cited real PR #84 2026-07-26 incident it protects against), not a bug to patch. No new UMR row, no redispatch, no `mark-umr-terminal` re-run needed (row is already terminal with accurate evidence).

## Remaining
- [ ] None. RCA complete, verified duplicate of already-resolved prior work; recorded honestly below rather than fabricating new completion.
