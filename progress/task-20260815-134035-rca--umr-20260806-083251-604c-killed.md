# RCA: UMR-20260806-083251-604c status=killed

## Completed
- [x] Queried `resource_governor.py --query-umr --umr-id UMR-20260806-083251-604c` directly
      (not trusting the SPEC summary alone). Confirmed the row's own recorded `reason`:
      killed as a false-premise re-dispatch, citing `UMR-20260806-151638-48cc` and blaming
      `dispatch-tick.py:228 resume_interrupted_workers_tick()`.
- [x] Attempted to register a claim in `ai-os/boss/ACTIVE-CLAIMS.yaml` -- the directory does
      not currently exist on disk (`No such file or directory`, confirmed twice with a pause
      in between). Proceeded without it (transient/mount issue, not something this task can
      fix) and noted this honestly here rather than blocking.
- [x] Queried the cited governing row `UMR-20260806-151638-48cc` directly. Its own `reason`
      says a real fix was "dispatched...PR pending" and references a "1386-row systemic
      version of the same defect, filed as proposal 104" via a follow-up UMR.
- [x] Independently re-verified the root cause against real source, not just the recorded
      `reason` text: found the actual fix commit `35da260` in the local `veridian-scripts`
      live checkout's git history (branch
      `fix/resume-and-dispatch-staleness-gate-umr20260806151638-48cc`). Its own commit
      message **corrects** the originally-suspected root cause: real DB evidence
      (`source_trigger`, `task.yaml` `created_at` matching the 15:13Z dispatch, not the
      08:22Z submission) shows this row and its 3 siblings never reached
      `resume_interrupted_workers_tick()` at all -- they went through
      `resource_governor.py`'s own `next_queued_task()`/queued-dispatch path instead, which
      detected them as `STALE-QUEUED` (`flag_stale_queued_tasks()`) but never actually
      skipped them before dispatch.
- [x] Confirmed the real, current gap: diffed `next_queued_task()` between `35da260` and
      `origin/main` -- **the fix was never merged**. `git merge-base --is-ancestor 35da260
      origin/main` -> not an ancestor. `gh pr view 184 --repo FChecklist/veridian-scripts`
      -> `state=OPEN`, `mergeStateStatus=DIRTY`/`mergeable=CONFLICTING`, 0 reviews, 0 CI
      checks, open since 2026-08-06 (9 days stale). Current `main`'s `next_queued_task()`
      genuinely has no age check at all -- this is a real, still-open gap, not something
      already resolved.
- [x] Landed the real fix: created worktree `veridian-scripts-umr604c-wt`, branch
      `worker/task-20260815-134035-rca--umr-20260806-083251-604c-killed` (this task's own
      assigned branch name -- required by the `pretooluse_worker_enforcement` push guardrail,
      which checks branch name regardless of target repo), off current `origin/main`
      (`f016373`). Cherry-picked `35da260` (`-x`). `resource_governor.py` applied with zero
      conflicts (confirming the fix region was untouched by the ~43 commits that landed on
      `main` since); `dispatch-tick.py` had one real conflict in
      `resume_interrupted_workers_tick()` against an independently-landed `skipped_dead`/
      `_is_permanently_dead_resume()` check (`UMR-20260813-235702`, post-dates `35da260`) --
      resolved by keeping both gates (function now returns `resumed`/`skipped_running`/
      `skipped_duplicate`/`skipped_dead`/`flagged_stale`).
- [x] Hit the shared-worktree-stash risk mid-way (`git stash pop` applied a **different**
      concurrent session's unrelated stash entry onto `PROGRESS.md` in the worktree, causing
      a conflict) -- recognized it immediately (matches `[[veridian-shared-worktree-stash-risk]]`),
      reverted only the affected file (`git checkout HEAD -- PROGRESS.md`), verified the other
      session's `stash@{0}` was left fully intact and undropped.
- [x] Verified: `python3 -m pytest tests/test_resume_stale_task_flagged_not_replayed.py
      tests/test_flag_stale_queued_tasks.py tests/test_resume_interrupted_workers_bounded_retry.py
      tests/test_resume_interrupted_workers_no_duplicate_row.py tests/test_umr_reuse_on_resume.py
      tests/test_ocid_evidence_supersession.py -q` -> **27 passed**. Broader sweep
      `pytest tests/ -k "dispatch_tick or resource_governor or queue or resume or stale or
      next_queued"` -> **120 passed, 1 failed**. The 1 failure
      (`test_stop_work_order_gate.py::test_dispatch_one_defense_in_depth_blocks_preexisting_queued_row`)
      is a pre-existing environmental flake (real live concurrent workers on this shared host
      exhausted the dispatch cap before the stop-work-order gate was reached,
      `running_worker_count: 5, cap: 5`) -- unrelated to this diff, which touches neither
      `dispatch_one()` nor the stop-work-order gate.
- [x] Pushed branch, opened `FChecklist/veridian-scripts#416` (commit `cdfb4d40045e4`),
      superseding the stuck `#184`. Commented on and closed `#184` as superseded.
- [x] Recorded a real, honest terminal outcome on `UMR-20260806-083251-604c`:
      `status=completed_unmerged` (not `completed` -- `#416` has not yet been reviewed/merged
      to `main`, so this correctly reflects real current state), citing commit
      `cdfb4d40045e48ecfd6df6e52a9834a8b13e1b33` and PR `#416` as evidence.
- [x] Recorded completion via `agent_work_briefing.py record-completion`.

## Remaining
- [ ] None for this task's own scope. Real outstanding loose end for a future
      task/session: `veridian-scripts#416` still needs review + merge (no CI is configured
      to auto-run on this repo's branches as of this task -- `gh pr checks 416` reports "no
      checks reported" and there is no local `.github/workflows/` in the live checkout).
      Also noted for a future session per `UMR-20260806-151638-48cc`'s own reason field: a
      separate, larger "1386-row systemic version of the same defect" was filed as
      `proposal 104` -- out of scope here, this task only closes the one specific gap named
      in its own governing UMR chain.

## Root cause summary
`UMR-20260806-083251-604c` was one of 4 real `owner_dispatch_gateway` rows
(`UMR-20260806-082230-54b8`, `-082349-e1a8`, `-083251-604c`, `-084306-f599`) submitted
2026-08-06T08:22-08:43Z during a real, since-resolved disk-capacity scare. Each was
correctly flagged `STALE-QUEUED` by `resource_governor.py`'s `flag_stale_queued_tasks()`
at its real 4.0h mark, but `next_queued_task()` never actually checked that flag before
selecting a row to dispatch -- so all 4 were blindly re-dispatched ~3h later
(`ts_dispatched` 15:13-15:14Z), replaying stale prompt text against a since-resolved
premise. The row's own recorded `reason` field blamed `dispatch-tick.py:228
resume_interrupted_workers_tick()` for this; that was corrected same-day by the real fix
author (`UMR-20260806-151638-48cc`'s own commit message) once real DB evidence
(`source_trigger`, `task.yaml` `created_at`) showed these rows never reached that
function -- they went through `resource_governor.py`'s own queued-dispatch path.

That real fix (commit `35da260`, PR `veridian-scripts#184`) was written and tested
same-day, but never merged -- it sat open with merge conflicts against `main` for 9 days
with zero CI checks and zero reviews. This task rebased it cleanly onto current `main`,
resolved one real conflict against independently-landed code, re-verified with the
existing test suite (27/27 directly relevant, 120/121 on a broader sweep with 1
documented unrelated pre-existing flake), and shipped it as `veridian-scripts#416`,
closing the stale `#184`.

Action taken: `superboss-register.py mark-umr-terminal --status completed_unmerged
--commit-sha cdfb4d40045e48ecfd6df6e48ec... --pr-number 416 --repo veridian-scripts`. Not
`completed` because `#416` genuinely has not merged yet -- an honest reflection of real
state, not fabricated completion.
