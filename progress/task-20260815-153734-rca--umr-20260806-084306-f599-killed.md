# PROGRESS -- task-20260815-153734-rca--umr-20260806-084306-f599-killed

## Governing chain
This task's own dispatching UMR: PM-sentinel tick, Check 2a killed-row RCA
template. Target row: `UMR-20260806-084306-f599` (task_identity
`owner-task-20260806-084303-2468046`).

## Completed
- [x] Read `ai-os/boss/ACTIVE-CLAIMS.yaml` first (Rule 11) -- no active entry
      touches this UMR, `dispatch-tick.py`, or `resume_interrupted_workers_tick`.
- [x] Ran `resource_governor.py --query-umr --umr-id UMR-20260806-084306-f599`
      myself (did not trust the SPEC's summary alone, per instructions).
      Confirmed live: `status=killed`, `ts_submitted=2026-08-06T08:43:06Z`,
      `ts_dispatched=2026-08-06T15:14:05Z` (a real ~6.5h-later blind resume),
      `ts_completed=2026-08-06T15:18:30Z`, `unit_name=veridian-worker@task-
      20260806-151402-real-disk-emergency-remediation-root-fil.service`. Full
      `reason` text confirmed to match the SPEC's quoted summary, and is
      itself already complete, honest, and evidenced (real `df`/`df -i`/`free`
      figures at reconciliation time, explicit citation of the row that did
      the correction).
- [x] Checked `git log --all --oneline | grep f599` and `gh pr list --search
      f599` (both repos this session has access to) for a prior RCA of this
      exact UMR id -- **zero hits**. Not a duplicate dispatch (unlike several
      prior sibling tasks in this series, e.g. UMR-48c3, UMR-ec65, UMR-eb50,
      UMR-c8ce -- see memory index `veridian-killed-rca-series-index`).
- [x] Independently queried the two UMRs the row's own `reason` cites, to
      verify the claimed remediation chain is real and not self-referential:
      - `UMR-20260806-151638-48cc` (status=completed): confirms the real
        stand-down of the 4 false-premise worker units (systemctl stop
        succeeded, occupied slots 5->2, 4 rows independently already
        `status=killed`), and states the root cause -- `dispatch-tick.py:228
        resume_interrupted_workers_tick()`'s blind resume of a stale
        `task.yaml` with no premise check -- was identified and a follow-up
        fix was dispatched.
      - `UMR-20260806-153532-c0b1` (status=completed): confirms the 4
        `task.yaml` files this row's stand-down left at `in_progress` (a real
        re-trigger risk had they been left alone -- independently re-verified
        via `RESUMABLE_STATUSES={pending,in_progress}`) were corrected to
        `status=blocked` via `veridian-task.py` checkpoint (never a raw file
        edit). The broader systemic version of the same defect (1386 real
        `task.yaml` directories stuck at `pending`/`in_progress` with no live
        worker, dating back to at least 2026-07-18) was deliberately filed as
        **proposal 104** / child UMR-20260806-153705-0020 rather than
        hand-swept, per that task's own step-5 instruction.
- [x] Checked whether the actual root-cause defect described in this row's
      `reason` (`resume_interrupted_workers_tick()` resubmitting a non-
      terminal `task.yaml` "with zero check on whether the task's real-world
      premise still holds") has since been fixed in `dispatch-tick.py`. Read
      the live function (`/opt/veridian/scripts/dispatch-tick.py:336`) in
      full. Found two real, later, already-merged/deployed fixes to this
      exact function:
      - `UMR-20260806-103711-bf00` (PR #163): added `_existing_active_umr()`
        -- skips re-`submit()`ing when a live active `umr_tasks` row already
        covers the same `task_identity` (stops the `rejected_duplicate`
        churn, not a premise check).
      - `UMR-20260813-235702` (deployed live to `/opt/veridian/scripts/
        dispatch-tick.py` + `superboss-register.py`, confirmed via git-blame-
        equivalent read of the live file's own docstring and
        `progress/task-20260813-235702-...md`): added
        `_is_permanently_dead_resume()` / bounded-retry dead-letter tracking
        -- stops the unbounded `reuse_verdict_engine.assess()`
        `duplication_blocked` rejection loop after
        `MAX_CONSECUTIVE_RESUME_REJECTIONS` (3) consecutive rejections.
      **Neither fix adds a genuine "is the real-world premise (e.g. is the
      disk actually still full) still true" check** -- both are anti-
      duplicate/anti-thrash bookkeeping around the same blind-resubmit
      behavior, not premise validation. That specific gap remains open.

## Root cause & disposition (no code fix applied in this task)
The row was **genuinely and correctly killed** -- not a mislabel (unlike the
majority of this RCA series; see `veridian-killed-rca-series-index` memory).
Its own `reason` field is complete, accurate, and independently
cross-verified against two other completed UMRs. The immediate risk this
incident created (4 stuck `task.yaml` files that could have re-triggered the
same false-premise resume cascade) was already fully remediated same-day
(2026-08-06) by `UMR-20260806-153532-c0b1`. `status=killed` is already the
correct real terminal state for this row -- **did not re-invoke
`mark-umr-terminal`**, since the existing terminal state is already accurate
(same reasoning as `veridian-umr-48c3-killed-rca-2nd-dispatch-duplicate`):
re-asserting an identical claim would only rewrite `ts_completed`/`reason`
redundantly, not correct anything.

The deeper, still-open gap -- `resume_interrupted_workers_tick()` has no
generic way to know whether a given `task_identity`'s real-world premise is
still true before blindly resubmitting it -- is real, but is intentionally
**out of scope for a single killed-row RCA**: it requires task-kind-specific
validation logic (there is no generic way to know "is this task's premise
still true" without executing task-specific checks), which is exactly why
the original reconciliation chain filed it as **proposal 104** (a systemic
redesign covering ~1386 affected rows) rather than a point patch. Re-fixing
it unilaterally here, scoped to only this one row's task-kind, would
duplicate/fragment that already-filed proposal rather than close it. Filed
as an open cross-reference rather than silently dropped: see `open_questions.json`
entry added below.

## Remaining
- [ ] None for this row -- RCA complete, disposition is "correctly killed,
      no correction needed."
- [ ] (Not this task's scope) proposal 104 / systemic premise-check redesign
      for `resume_interrupted_workers_tick()` remains unimplemented as of
      2026-08-15 -- left open for whichever task actually owns that proposal.
