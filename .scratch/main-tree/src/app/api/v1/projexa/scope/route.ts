// Wave 124: pure re-export -- BOQ/Scope-of-Work is already construction-
// domain-named, so this namespace just groups it under /projexa/* for a
// single coherent surface rather than reshaping anything.
export { GET, POST } from "@/app/api/v1/construction/boq/route"
