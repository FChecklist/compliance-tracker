diff --git a/progress/task-20260814-121415-rca--umr-20260814-095518-ec65-status-run.md b/progress/task-20260814-121415-rca--umr-20260814-095518-ec65-status-run.md
new file mode 100644
index 000000000..e354823e9
--- /dev/null
+++ b/progress/task-20260814-121415-rca--umr-20260814-095518-ec65-status-run.md
@@ -0,0 +1,21 @@
+# PROGRESS -- task-20260814-121415-rca--umr-20260814-095518-ec65-status-run
+
+## Completed
+- [x] Queried UMR-20260814-095518-ec65 live via `resource_governor.py --query-umr` (not the stale SPEC text). Real current row: `status=completed_unmerged`, `ts_completed=2026-08-14T10:23:12Z`, `reason` cites `reconcile_stale_running_workers.py (STEP 3, task-20260807-052027)` having already confirmed `ActiveState=inactive` for the unit and gathered a real completion candidate (branch still exists via `git ls-remote`), then deferred to `mark-umr-terminal`'s structured-evidence gate rather than asserting completed/killed itself. This row is **already terminal**, not `running` as the SPEC's premise claimed.
+- [x] Independently re-verified the real unit state myself (not trusting the reason field alone): `systemctl --user show veridian-worker@task-20260814-095552-block-merges-that-have-no-fresh-passing.service` → `ActiveState=inactive`, `Result=success`, `ExecMainStatus=0` -- matches SPEC's cited live check exactly.
+- [x] Pulled `journalctl --user -u veridian-worker@task-20260814-095552-...` -- unit ran 09:55:56Z→10:15:23Z (~1min28s CPU / ~20min wall), matches the SPEC's cited "Consumed" line and confirms it's a real completed run, not a hung/killed process.
+- [x] Checked task.yaml for the underlying task `task-20260814-095552-block-merges-that-have-no-fresh-passing` (claude-control repo): status=`blocked`, last checkpoint note: `"Superboss rejected: https://github.com/FChecklist/claude-control/pull/230 — see review.json for issues"`.
+- [x] Read `review.json`: Tier-1 verdict=`reject`. Real, well-evidenced critical finding: `scripts/merge_gate.py`'s `find_latest_verdict()` accepts an `AUDIT: PASS` verdict from **any** PR commenter/reviewer with no `authorAssociation`/allowlist check, and since neither `claude-control` nor `veridian-scripts` has GitHub branch protection configured (confirmed live 404/403 on both), the gate is the sole enforcement -- meaning a PR's own author (or anyone with comment access) can self-approve a merge, reproducing the exact self-certification failure the gate was dispatched to close, one layer up.
+- [x] Live-reverified on GitHub (not trusting the cached task.yaml note): `gh pr view 230 --repo FChecklist/claude-control` → still `OPEN`, `MERGEABLE`, unmerged, with an `AUDIT: FAIL` comment matching `review.json`'s rejection. No merge/state drift since the reject.
+- [x] Searched `resource_governor.py --query-umr --search` and `superboss-register.py lookup-capability` for `"merge_gate"` / `"block merges"` -- **zero existing UMR/capability rows** for the `find_latest_verdict()` identity-check gap. Not a duplicate of tracked work; it's a real, still-open, un-ticketed gap, but it lives in a different repo (`claude-control`) than this task's own workspace (`compliance-tracker`) and is not this SPEC's chartered scope.
+- [x] Found and reverted a stray uncommitted 1-line edit to the shared `PROGRESS.md` (present in the workspace at task start, header changed to this task's own id) -- per this task's own protocol ("do not edit any other task's progress/*.md, and do not recreate a shared PROGRESS.md"), left `PROGRESS.md` untouched at its committed state.
+
+## RCA Conclusion
+The SPEC's premise -- `UMR-20260814-095518-ec65` showing `status=running` while the real systemd unit is dead -- **is no longer true**. It was real at some point before this RCA task was dispatched, but `reconcile_stale_running_workers.py` (STEP 3) already fixed the write-back gap on its own, roughly **1h51m before this RCA task was even created** (reconciler wrote `ts_completed=10:23:12Z`; this task's `task.yaml` shows `created_at=12:14:16Z`). The live row is `completed_unmerged`, correctly evidenced, and requires no further `mark-umr-terminal` call from this task -- re-asserting an already-terminal, already-correct status would not be an honest "fix," it would be a no-op duplicate write.
+
+The underlying task (`task-20260814-095552-block-merges-that-have-no-fresh-passing`) is genuinely, correctly blocked -- not a second instance of the write-back-lying bug. Its Tier-1 audit reject is real and unaddressed on GitHub as of this check (PR #230 still open). That gap (add an `authorAssociation`/allowlist check to `merge_gate.py`'s `find_latest_verdict()`) is real, un-duplicated, and worth a future dispatch -- but it lives in `claude-control`, not this task's `compliance-tracker` workspace, and is outside this SPEC's chartered scope (RCA'ing the status write-back), so it is **not** attempted here. Flagging it here (and in the record-completion write-back) is the honest handoff instead of silently dropping it or overreaching into an unscoped, wrong-repo fix.
+
+**No code fix, no redispatch, and no `mark-umr-terminal` re-run needed for UMR-20260814-095518-ec65** -- the real gap this SPEC named had already self-corrected by the time real work started. Docs-only outcome is honest and complete for this task's actual chartered scope (RCA of a status claim that verified stale).
+
+## Remaining
+- [ ] None for this task's chartered scope. (Open, but explicitly out of scope: `claude-control`'s `scripts/merge_gate.py` `find_latest_verdict()` needs an `authorAssociation`/allowlist check before PR #230 can be safely merged -- left for a future, correctly-repo-scoped dispatch, not duplicated into a UMR/capability row from here per this task's own boundaries.)
