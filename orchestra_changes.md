# Orchestra Changes Log — VERIDIAN AI Orchestra Transformation

This is the running plan + change log for turning `compliance-tracker` into VERIDIAN AI Orchestra. See [analysis.md](analysis.md) for the Phase 1 discovery (schema map, feature map, gap analysis) this plan is built on.

**Update (2026-07-02): Waves 7 and 8 complete — a new, separate scope, not a reopening of the sign-off below.** The 2026-07-01 sign-off was specifically for the AI Orchestra layer (5 assistants, worker agents, task engine, self-improvement loops, RLS tenant isolation) and remains accurate for that scope. Waves 7-8 are a different request entirely: bringing this app's GRC module breadth up to match a separate design-mockup session (Governance, Company Secretarial, Legal, HR/Payroll, Sector Regulators, Risk, Audit, ESG, Incidents, Access/Approval matrix — none of which existed here before Wave 7). Wave 7 built only the foundation later module waves depend on: the Account/Client-Entity hierarchy (finishing tables that existed in schema.ts + live RLS but had zero routes/UI), a genuinely immutable and fully-attributed unified audit log, and real cost/payment/dispatch-evidence tracking. Also found and fixed along the way: a critical, currently-live bug in `withTenantContext` (`SET LOCAL x = $1` is invalid Postgres syntax — every RLS-scoped query since Wave 1 was throwing a `42601` syntax error on every call). This is a distinct failure mode from the Supavisor `ENOTFOUND`/pooler-routing error noted below (a connection-level DNS/routing failure that occurs before any SQL is sent) — not the same bug, not confirmed related, and the pooler issue's status is unchanged by this fix. Both are real, independent problems; the Wave 7 entry only claims the syntax bug, empirically confirmed via a direct `PREPARE` test. Wave 8, run immediately after, built the full ~40-module GRC platform breadth itself (Governance, Company Secretarial, Legal, HR, Risk, Sector Regulators, Audit/Controls, ESG, Integrity, Incidents — 37 tables, ~50 routes, 30 pages) directly on Wave 7's foundation, deployed and verified clean (`/api/health` 200, zero new runtime errors). See entries #57 and #58 for full detail. Original Wave 0-6 status preserved below for historical accuracy.

**PROJECT COMPLETE, confirmed by the user (2026-07-01) — see the "Sign-off" entry at the bottom of the Change Log.** Waves 0-3: fully complete and live. Wave 4 (Task System + Orchestra Layers): complete within deliberate scope — real task execution engine, real multi-provider BYO model dispatch with UI, real worker agent dispatch for read-only global agents, an ingestion→tasks bridge. Wave 5 (Self-Improvement Loops): 11 of 15 loops active with real execution code; the remaining 4 are held back on purpose, not oversights (see the "Final, explicit statement" note near the bottom of the Change Log). Wave 6 (Hardening): complete — `get_advisors` clean across security and performance, launch checklist reconciled, and the full pre-existing TypeScript backlog fixed (94→0 errors), which surfaced and fixed several genuinely severe live bugs along the way (see #51). 🔴 **One item remains open and is not a scope decision:** the Supabase Supavisor pooler cannot route to this project (see the section right below this one) — retried repeatedly throughout this session, still failing as of the last check. This needs the user's direct attention with Supabase (dashboard pooler logs or support), not further app code. Every route with a real backing table now enforces tenant isolation via `app_runtime` + real RLS policies, not just app-code convention. Along the way, this sweep found and fixed roughly 20 real, currently-live bugs across the API surface — the large majority were the same IDOR pattern (zero org check on a lookup-by-id), a few were "first org/first admin/first department in the whole database" fallbacks that could put data in the wrong tenant, and three were genuinely unauthenticated endpoints (`documents/extract`, `search/semantic`, and a missing role-check on `mcp/tokens`). Full list in the checklist below and the change log. **Update: `ignoreBuildErrors` is gone as of change log #51.** It originally masked a real NOT-NULL-violation bug in `/api/users` POST; by the time it was removed, the full pre-existing backlog (94 errors) had been fixed too, surfacing several more genuinely severe live bugs along the way (broken magic-link login, broken PDF ingest, broken comment posting) — see #51 for the complete list. `npx tsc --noEmit` now passes clean. Supabase branching (the originally planned safety net for this wave) isn't available on the Free plan, so Wave 1 is being applied directly to production in small, additive, independently-verified steps instead — see "Wave 1 approach" note below. Order of execution, per wave and per step: **GitHub (code/schema/docs) → Supabase (migration/data) → Vercel (env/deploy)**. Each step below is written to be independently completable and independently verifiable, so progress can be shown incrementally rather than as one giant cutover. This file is updated after every executed step — see the Change Log at the bottom, which is the source of truth for "what's actually been done" (not the wave plan, which is the intent).

---

## 🔴 CRITICAL, currently blocking: Supavisor pooler cannot reach this project (discovered during Wave 5 verification, 2026-07-01; recurred and reconfirmed during Wave 11 verification, 2026-07-03 — still unresolved)

**Update (2026-07-03):** re-confirmed live and identical during Wave 11's mandatory functional-proof step — 3 consecutive `500`s on `/api/v1/compliance` with the exact same `(ENOTFOUND) tenant/user postgres.pcrjmlpuqsbocqfwoxod not found` signature, timestamped from `2026-07-02T15:06:01` (i.e. predating Wave 11's own code entirely) through the test. Confirmed this is not a Wave 11 regression by hitting the pre-existing legacy (non-versioned) compliance/tasks/notices routes side by side — they fail identically, proving the fault is the shared Drizzle/pooler connection path, not anything new. Routes that never touch that path (`GET /api/v1/openapi.json`, and MCP's original PostgREST-based tools from Wave 10) still work, which is what isolates the failure this precisely. This means the issue has now gone unresolved across two full waves of work and is blocking full live verification of Wave 11's service-layer/`/api/v1` build — not because the new code is wrong, but because nothing that depends on `withTenantContext` can be proven end-to-end in production until this clears. Two remediation paths remain on the table, neither actioned yet (both need your decision, not a unilateral code change): (a) escalate to Supabase support with the timeline above as evidence this isn't a transient propagation delay, or (b) stopgap-switch `DATABASE_URL`/`APP_RUNTIME_DATABASE_URL` to the direct non-pooled connection string, accepting the pooling-loss tradeoff described in step 3 below.

**Every authenticated database query in production is almost certainly failing right now.** While functionally testing Wave 5's cron route end-to-end (the first time this whole session I actually exercised a real query — every earlier "smoke test" only checked the unauthenticated 401 path, which never touches the database), the raw `db` client threw:

```
FAILED: (ENOTFOUND) tenant/user postgres.pcrjmlpuqsbocqfwoxod not found  code: XX000
```

**Diagnosis, confirmed empirically, not guessed:**
- Reproduced the identical error from this sandbox directly (not a Vercel-specific network fluke) using the exact `DATABASE_URL` pulled from the live Vercel production env.
- Same failure, same "tenant/user X not found" pattern, for **both** `DATABASE_URL` (`postgres.<ref>`) and `APP_RUNTIME_DATABASE_URL` (`app_runtime.<ref>`) — i.e. this is not a bad password or a bad role, it affects the entire project's pooler tenant, regardless of which DB role connects.
- Confirmed the password itself is correct and the database is healthy: built a **direct** connection string (`db.pcrjmlpuqsbocqfwoxod.supabase.co:5432`, bypassing the pooler entirely) using the same password extracted from `DATABASE_URL`, and it connected successfully on the first try, twice in a row.
- This isolates the problem to exactly one thing: **the Supavisor shared pooler (`aws-0-ap-south-1.pooler.supabase.com`) does not currently have this project registered/routable**, even though the project itself (`get_project`) reports `ACTIVE_HEALTHY`.
- Checked Supabase's own docs (`search_docs`) for this exact error class. The matching entry: *"Subscribe error: `{:error, :worker_not_found}`... This log event is emitted when the client tries to connect to the database, but Supavisor does not have the necessary information to route the connection. Try reconnecting to the database as it can take some time for the project information to propagate to Supavisor."*
- Retried 6+ times over several minutes — **not resolving on its own**, which goes beyond the "brief propagation delay" this error is normally attributed to.

**Why this matters more than anything else in this log:** the entire Wave 1-5 tenant-isolation architecture (`app_runtime` role + RLS via GUCs) depends on the pooler connection actually working. If Supavisor can't route to this project, then in production right now: no user can sign up (raw `db` in `autoProvisionUser`), no authenticated route that reaches `withTenantContext` can complete a query (every migrated route), and the new Loop 9/12 cron job fails outright (confirmed: it returns `{"error":"Loop run failed"}` live). Every one of this session's "expect 401" smoke tests passed because `requireAuth()` returns before ever touching the database for an unauthenticated caller — so this gap was invisible to every verification step run so far. This is a Supabase-infrastructure-level issue, not something introduced by any code in this session, and not something fixable by changing application code or migrations.

**What I did NOT do:** pause/restore the project (a real mitigation some Supabase users report working for stuck Supavisor registrations) — that's a disruptive, hard-to-reverse action against the live database that would itself cause an outage, and is a different category of risk than the code/schema changes this session has been making autonomously. Flagging this for you rather than acting unilaterally on the database's availability itself.

**Recommended next steps (needs you or Supabase support, not more code):**
1. Check the Supabase Dashboard's [Pooler Logs](https://supabase.com/dashboard/project/pcrjmlpuqsbocqfwoxod/logs/pooler-logs) for the exact `worker_not_found` event and timing.
2. If it doesn't clear on its own soon, contact Supabase support referencing project `pcrjmlpuqsbocqfwoxod` and the `worker_not_found`/"tenant or user not found" Supavisor error — this looks like a stuck tenant registration, possibly related to the DB password reset performed earlier in Wave 0 (change log #5).
3. As a temporary mitigation *if* it's blocking real users right now: `DATABASE_URL`/`APP_RUNTIME_DATABASE_URL` could be pointed at the **direct** connection (`db.pcrjmlpuqsbocqfwoxod.supabase.co:5432`) instead of the pooler — confirmed working above — but this is not recommended long-term for a serverless/Vercel deployment (each Lambda invocation would hold a direct Postgres connection rather than sharing pooled ones, risking hitting Postgres's max-connections limit under real traffic). Only use this as a stopgap, and switch back to the pooler once Supavisor's registration is fixed.

---

## ⚠️ Live infrastructure findings (discovered while executing Wave 0 — needs your input before Wave 1)

Two things surfaced by actually connecting to Supabase/Vercel, not by reading the repo, that change what's safe to assume:

1. **Two Supabase projects both have a `compliance` schema**: `jusqumifsmtcaujqyjuy` ("Veridian AI", 9 tables — organisations, departments, users, compliance_items, audit_points, documents, comments, notifications, audit_logs only) and `pcrjmlpuqsbocqfwoxod` ("verdian-ai", 16 tables — the 9 above plus mcp_access_codes, challans, notices, embeddings, onboarding_steps, ingestion_batches, ingestion_items, and now `ai_configurations`). `pcrjmlpuqsbocqfwoxod` is clearly the current one — it has every table the current `schema.ts` needs. **All Wave 0 database work in this file was applied to `pcrjmlpuqsbocqfwoxod`.**
2. **Two Vercel projects are both linked to `FChecklist/compliance-tracker`'s `main` branch**: `compliance-tracker` (`prj_80z9Rz3BYvvExvGXyt5LNoPPMgiZ`, domain `compliance-tracker.vercel.app`) and `veridian-compliance-ai` (`prj_mRRWcMvhyuxgRZtcfp4ArSzcOvII`, domain `veridian-compliance-ai.vercel.app`). Every push to `main` deploys both, silently.
3. **Checked both live URLs directly**: `veridian-compliance-ai.vercel.app` serves the real, working "Veridian AI — AI-Native Compliance & Audit Operating System" app. `compliance-tracker.vercel.app` serves a raw, broken JS bundle at its root — it looks orphaned/misconfigured, not a working deployment.
4. **Both Vercel projects set `NEXT_PUBLIC_SUPABASE_URL` to the *older* project (`jusqumifsmtcaujqyjuy`)**, not `pcrjmlpuqsbocqfwoxod`. That env var is what Supabase Auth (login/signup), the MCP route's admin client, and `lib/embeddings.ts`'s fallback path all use directly — no `DATABASE_URL` alternative for any of those. So **login and the MCP server almost certainly run against the 9-table project**, which doesn't have `mcp_access_codes`, `challans`, `notices`, etc. — meaning those features may currently be erroring in production regardless of anything in this file.
5. **What's unknown:** `DATABASE_URL` is also set (encrypted) in both Vercel projects, and Drizzle prefers `DATABASE_URL` over the `NEXT_PUBLIC_SUPABASE_URL` fallback. I can't decrypt that value through the Vercel API to see which of the two Supabase projects it actually points to, so I don't know for certain whether the working parts of the live app (dashboard, compliance CRUD) are reading `pcrjmlpuqsbocqfwoxod` (where this file's DB changes now live) or `jusqumifsmtcaujqyjuy` (where they don't exist yet).

**I did not guess further or touch either Vercel project's Git integration, since deleting/unlinking a project is destructive and picking the wrong Supabase project to keep "fixing" would compound the confusion.** I set the one new Wave 0 env var (`AI_CONFIG_ENCRYPTION_KEY`) only on `veridian-compliance-ai`, since that's the one confirmed to be actually serving traffic.

**Update (same day) — fully resolved.** `veridian-compliance-ai` confirmed as the one true app; `pcrjmlpuqsbocqfwoxod` confirmed as the one true database. Sequence completed in this order: repointed all 5 Supabase-related env vars on `veridian-compliance-ai` → redeployed → user confirmed login/dashboard load correctly against the new target → cleaned up the dead artifacts. Cleanup turned out to be slightly bigger than expected: `jusqumifsmtcaujqyjuy` had **two** dead compliance-tracker artifacts, not one — the `compliance` schema (9 tables, stale duplicate data) and a completely separate, entirely empty `compliance_tracker` schema (an earlier abandoned attempt, zero rows in every table). Both were dropped. `public` schema in that same project — confirmed as MeetTrack's real, live data (`users`, `meetings`, `action_items`, `ai_sessions`, `user_api_keys`, all with real row counts) — was verified untouched and intact before and after. The dead `compliance-tracker` Vercel project (`prj_80z9Rz3BYvvExvGXyt5LNoPPMgiZ`) was deleted; `veridian-compliance-ai` is now the only Vercel project linked to this repo.

---

## Planning assumptions (locked for this plan; flag if you want any changed before Wave 1 starts)

These resolve the three open questions from the previous version of this file. Execution has not started, so these can still be changed at no cost — but the plan below is written assuming these answers:

1. **Hierarchy scope — build the full Customer Account → Client → Client Entity model now, additively.** Rather than a disruptive rename, `organisations` keeps its table name (all existing code references it) and is simply the Customer Account. New `clients` and `client_entities` tables are added underneath it. Every existing business table gets a **nullable** `client_id`. A migration auto-creates one implicit "Self / Direct" client per existing organisation and backfills `client_id` on all existing rows to it — so single-entity firms (today's only real use case) keep working with zero behavior change, while multi-client CA firms can create real clients going forward. This avoids the "decide twice" cost flagged in analysis.md without breaking anything live.
2. **RLS-first sequencing — confirmed.** Wave 1 (tenant isolation) happens before any Orchestra-specific table (assistants, agents, tasks, loops).
3. **`ai-os/` stays untouched, as a separate system.** Orchestra loops become a new, DB-backed telemetry system; `ai-os/`'s YAML governance layer is left exactly as-is, per `CLAUDE.md`'s explicit rule. No attempt is made to unify them in this plan. (If you want them merged later, that's a distinct, explicit decision — not bundled in here.)
4. **Wave 0 (BYOK fix) happens first, standalone**, since it's small, self-contained, and already broken in a way worth fixing regardless of how big the rest of the build gets.
5. **ID strategy: new tables use `text` + cuid2 (`@paralleldrive/cuid2`), matching every existing table** — not `uuid`, despite the master prompt's generic assumption of uuid. Mixing id types across FKs in the same schema is a real footgun; consistency with the 19 existing tables wins.
6. **Embedding dimension: 1536, matching the existing `embeddings` table and `lib/embeddings.ts`.** (Aside, not blocking: that file calls Groq's `nomic-embed-text`, which in reality is a 768-dim model — the existing code pads/hashes to 1536 regardless of what the real model returns when the API succeeds, which is worth a QA check during Wave 2, but is a pre-existing issue, not something this plan introduces.)
7. **RLS enforcement mechanism — this is the one genuinely new engineering decision this plan adds, not carried over from analysis.md:** see "Wave 1 critical technical note" below. Short version: `DATABASE_URL` (used by Drizzle, via `postgres.js`) connects with a privileged role that **bypasses RLS by default in Postgres, regardless of how many `CREATE POLICY` statements exist**, unless handled deliberately. The plan uses a session-level Postgres GUC (`app.current_org_id`, set via `SET LOCAL` at the start of every request inside a transaction) rather than rewriting all 27 API routes to go through PostgREST/`auth.uid()`. This is the pragmatic retrofit; it is called out explicitly because getting it wrong would mean every RLS policy in Wave 1 is silently a no-op.

---

## WAVE 0 — Foundation fixes (standalone, do first)

**Goal:** fix the one already-broken, already-scoped feature (BYOK AI config) before adding anything new on top of it. Independently shippable.

| Step | Layer | What | Files touched | Exit criteria |
|---|---|---|---|---|
| 0.1 | GitHub | Rewrite `/api/settings/ai-config/route.ts` to read/write the real `ai_configurations` Drizzle table instead of the in-memory object; resolve `orgId` from `requireAuth()` instead of hardcoded `"default"` | `src/app/api/settings/ai-config/route.ts` | Restarting the dev server no longer clears saved AI config |
| 0.2 | GitHub | Replace base64 "obfuscation" with real encryption: `pgcrypto`'s `pgp_sym_encrypt`/`pgp_sym_decrypt`, keyed by a server-only secret | same file + `src/lib/db/schema.ts` (no column change needed, `encryptedApiKey` already `text`) | Stored value in DB is ciphertext, not base64(plaintext) |
| 0.3 | Supabase | Confirm `pgcrypto` extension is enabled (`list_extensions`); enable via migration if not | new migration `000X_enable_pgcrypto.sql` | `list_extensions` shows `pgcrypto` active |
| 0.4 | Vercel | Add `AI_CONFIG_ENCRYPTION_KEY` env var (generate 32-byte random secret) | Vercel project env vars (Production + Preview) | Env var present, not committed to repo |
| 0.5 | GitHub | Add missing indexes: btree on every table's `org_id`; composite `(status, due_date)` on `compliance_items` for the dashboard/overdue queries | new drizzle migration | `EXPLAIN` on `/api/compliance/stats` shows index usage, not seq scan |
| 0.6 | Supabase | Apply index migration | — | Indexes visible in `list_tables` output |

---

## WAVE 1 — Tenant hierarchy + Row Level Security (the blocking foundation)

**Goal:** make "customer A's data can never appear in customer B's query" true at the database level, and stand up the Customer Account → Client → Client Entity model underneath it. Nothing in Waves 2–5 should be built before this lands.

### Wave 1 critical technical note (read before starting 1.x)

The app currently authenticates via Supabase Auth, but resolves the app-level identity by **email lookup** (`auth-guard.ts`: `db.query.users.findFirst({ where: eq(users.email, user.email!) })`). There is no stored link from `auth.users.id` to `compliance.users.id`. Two things follow:

- **Step 1.1 must add `users.auth_user_id uuid` (FK to `auth.users.id`)**, populated at signup/first-login, before any `auth.uid()`-based policy can work.
- Even with that link, **Drizzle's `postgres.js` connection via `DATABASE_URL` almost certainly does not carry the request's JWT**, so `auth.uid()` will be null/irrelevant inside that connection regardless. Two ways to fix this; this plan picks the second:
  - (a) Rewrite all 27 API routes to use the Supabase JS client (which does carry the user's JWT and naturally respects RLS via PostgREST) instead of raw Drizzle — correct, but a large, risky, all-at-once rewrite.
  - (b) **(chosen)** Keep Drizzle, but wrap every mutating/reading request in a transaction that starts with `SET LOCAL app.current_org_id = '<resolved orgId>'; SET LOCAL app.current_client_ids = '<comma-separated client ids the user can see>';`, and write RLS policies against `current_setting('app.current_org_id', true)` instead of `auth.uid()` directly. This is enforced by Postgres itself (a forgotten `WHERE org_id = ...` in a new route still gets filtered by the policy), doesn't require touching all 27 routes' query logic, and is a well-established Supabase/Postgres pattern for exactly this "ORM talks to Postgres directly" situation.
  - Either way, **every table must also get `ALTER TABLE ... FORCE ROW LEVEL SECURITY`**, because Postgres does not apply RLS to a table's owner by default — and the role in `DATABASE_URL` is very likely the schema owner. Wave 1 must also confirm (via `get_advisors` and a manual check) that the app's DB role is *not* superuser, since superuser always bypasses RLS regardless of FORCE.

### Steps

| Step | Layer | What | Exit criteria |
|---|---|---|---|
| 1.1 | GitHub | Migration: add `users.auth_user_id uuid` (nullable initially, backfilled, then NOT NULL) referencing `auth.users(id)`; add `users.reporting_to_id text` self-FK (replaces a separate `user_hierarchy` table — simpler, same effect for a direct-manager chain) | Column exists; every active user has it populated |
| 1.2 | GitHub | Migration: extend `user_role` enum with `veridian_admin, branch_manager, senior_professional, team_member, client_viewer, external_auditor` (Postgres requires `ALTER TYPE ... ADD VALUE` outside a transaction — separate migration file from anything transactional) | `\dT+ compliance.user_role` shows 10 values |
| 1.3 | GitHub | New tables: `branches` (org-scoped, optional), `clients` (org-scoped — the CA firm's end clients), `client_entities` (client-scoped — GSTIN/PAN-holding legal entities), `user_client_access` (user_id, client_id, access_level: full/aggregate_only) | Tables created, FKs valid |
| 1.4 | GitHub | New table: `subscription_plans` (id, name, user_pack_size, assistants_per_user default 5, price, features jsonb); add `organisations.subscription_plan_id` FK, keep the existing `plan` text column for backward compat (don't drop yet) | Plans table seeded with at least Trial/Starter/Growth tiers |
| 1.5 | GitHub | Migration: add nullable `client_id text` to `compliance_items`, `notices`, `challans`, `documents`, `audit_points`; **data migration** creates one "Self / Direct" client per existing `organisations` row and backfills `client_id` on all existing rows in those 5 tables to that org's implicit client | Row counts before/after identical; no row has null `client_id` after backfill |
| 1.6 | GitHub | `compliance.current_org_id()` and `compliance.current_client_ids()` SQL helper functions (`STABLE`, reading the `app.current_org_id` / `app.current_client_ids` GUCs) for policies to call | Functions created |
| 1.7 | GitHub | RLS policy migration: enable + `FORCE` RLS on all 19 existing tables + 5 new ones; policy pattern `USING (org_id = compliance.current_org_id())`, with `client_id = ANY(compliance.current_client_ids())` added on the 5 client-scoped tables for non-admin roles | `get_advisors` (security) shows zero "RLS disabled" findings for the `compliance` schema |
| 1.8 | GitHub | Update `src/lib/db/index.ts` (or a new `src/lib/db/withTenantContext.ts` wrapper) so every request sets the two GUCs at the start of its DB transaction, sourced from `requireAuth()`'s resolved `orgId`/`clientIds` | Manual test: query run through the wrapper with GUCs unset returns zero rows (fails closed) |
| 1.9 | GitHub | Update all 27 API routes to use the new tenant-context wrapper instead of the bare `db` export (mechanical, route-by-route, each independently testable) | Each route still returns correct data for its own org; a manual cross-org test (two seeded orgs) confirms no leakage |
| 1.10 | GitHub | Update `/api/mcp/route.ts`: it currently uses a service-role client that intentionally bypasses RLS — keep that (service-role tools legitimately need cross-boundary lookups for token resolution), but audit every `handleTool` branch to confirm `org_id` (and now `client_id` where relevant) is applied in **every** query, since this route is the one place RLS is deliberately not the safety net | Manual review checklist in this file (see Wave 1 QA checklist below) signed off |
| 1.11 | Supabase | Apply all Wave 1 migrations in order (1.1 → 1.7) via `apply_migration`, on a Supabase **branch** first, not directly on production | Branch migration succeeds cleanly |
| 1.12 | Supabase | Run `get_advisors` (security + performance) on the branch; fix any findings before merging | Zero security advisor findings for new/changed tables |
| 1.13 | Supabase | Merge branch to production once 1.9–1.12 are confirmed working against it | Production schema matches branch |
| 1.14 | Vercel | No new env vars for this wave (GUC approach needs no new secrets); redeploy to pick up the API route changes from 1.9/1.10 | Deployment succeeds; smoke test login + dashboard load |

**Wave 1 QA checklist (must pass before Wave 2 starts):**
- [ ] Two test organisations seeded; user from Org A cannot read Org B's `compliance_items` via any existing API route, even with a hand-crafted request
- [ ] `get_advisors` security scan clean for `compliance` schema
- [ ] `/api/mcp` tools re-verified against both orgs independently
- [ ] Confirm DB role used in `DATABASE_URL` is not superuser/table owner (or that `FORCE ROW LEVEL SECURITY` is proven to still apply)

---

## WAVE 2 — AI Assistant System (5 per user)

Depends on: Wave 1 (needs `org_id`/`client_id` context to scope assistant visibility correctly).

| Step | Layer | What | Exit criteria |
|---|---|---|---|
| 2.1 | GitHub | New tables: `ai_assistants` (user_id, assistant_number 1–5, label, status, personality_config jsonb), `assistant_memories` (assistant_id, category, content, `embedding vector(1536)` via raw SQL + HNSW index — same pattern as existing `embeddings` table), `assistant_sessions`, `assistant_metrics_daily` | Tables created |
| 2.2 | GitHub | RLS: assistants and memories readable/writable only by their owning `user_id` (strictest tier — no org-admin override by default, matching master prompt's privacy intent) | Policy migration passes advisors check |
| 2.3 | GitHub | Signup/onboarding hook: on new user creation, auto-insert 5 `ai_assistants` rows (numbered 1–5, default label "Assistant 1".."5") | New signup produces exactly 5 assistant rows |
| 2.4 | GitHub | Backfill migration: existing seeded users (7 in seed data) get 5 assistants each | Row count = existing_users × 5 |
| 2.5 | GitHub | API routes: `GET /api/assistants` (mine), `PATCH /api/assistants/[id]` (label/config), `POST /api/assistants/[id]/memories`, `GET /api/assistants/[id]/memories/search` (reuses `lib/embeddings.ts` `findSimilar` pattern, scoped to `assistant_id`) | Routes pass auth + ownership checks |
| 2.6 | GitHub | Frontend: Settings → "AI Assistants" panel showing 5 tiles (status, last active); reuses existing `DashboardCard`/`Badge` components rather than new ones | Manual browser check: 5 tiles render, editable labels |
| 2.7 | Supabase | Apply migration (branch → verify → merge), including HNSW index on `assistant_memories.embedding` | `get_advisors` clean; vector search returns results in dev test |
| 2.8 | Vercel | Redeploy | Smoke test assistants panel loads |

---

## WAVE 3 — Worker Agent Library (4 tiers)

Depends on: Wave 1 (tiered RLS visibility needs org/client scoping in place).

| Step | Layer | What | Exit criteria |
|---|---|---|---|
| 3.1 | GitHub | New tables: `worker_agents` (tier enum global/customer/client/user, capability_embedding, knowledge_embedding vector(1536), code_reference, prompt_template, input/output_schema jsonb, is_immutable, version, usage_count, accuracy_score), `worker_agent_versions`, `worker_agent_usage_log`, `worker_agent_learnings`, `worker_agent_domain_index` | Tables created |
| 3.2 | GitHub | RLS: global tier readable by all authenticated users; customer tier scoped to `org_id`; client tier scoped to `client_id` (via `user_client_access`); user tier scoped to `user_id` — mirrors master prompt §C exactly | Policy migration passes advisors check |
| 3.3 | GitHub | **Seed migration: port the 7 existing hardcoded MCP tools (`list_compliance_items`, `get_compliance_stats`, `get_overdue_items`, `create_compliance_item`, `update_compliance_status`, `list_departments`, `get_penalty_estimate`) into `worker_agents` rows, tier=`global`, `is_immutable=true`.** This is the first real content in the new system and it's not new build — it's formalizing what already works. | 7 global agents exist, one per existing MCP tool |
| 3.4 | GitHub | Refactor `/api/mcp/route.ts`'s `TOOL_DEFINITIONS`/`handleTool` to look up from `worker_agents` (tier=global first, falling back to hardcoded definitions during transition) instead of the static array — backward compatible, same external tool names/schemas | Existing MCP clients see no behavior change; `tools/list` now sourced from DB |
| 3.5 | GitHub | Add `usage_log` write on every `handleTool` call (execution time, success, org/client context) | `worker_agent_usage_log` rows appear after MCP calls |
| 3.6 | Supabase | Apply migration (branch → verify → merge) | `get_advisors` clean |
| 3.7 | Vercel | Redeploy | MCP endpoint smoke test (all 7 tools still callable) |

---

## WAVE 4 — Task System + Orchestra Layers

Depends on: Waves 1–3 (tasks reference assistants and worker agents).

| Step | Layer | What | Exit criteria |
|---|---|---|---|
| 4.1 | GitHub | New tables: `tasks`, `task_execution_plan`, `task_agent_executions`, `task_chat_messages` (task_embedding vector(1536) for semantic task matching) | Tables created |
| 4.2 | GitHub | New tables: `orchestra_layers` (seeded with the 5 layers: Task OA, User Assistant OA, Customer Account OA, Global Intelligence OA, Meta OA), `orchestra_executions`, `customer_model_config` (per-layer BYO model, extending the existing `ai_provider` enum groq/openai/anthropic/google) | 5 layer rows seeded |
| 4.3 | GitHub | **Generalize the existing `ingestion_batches`/`ingestion_items` plan → human-review → confirm pattern into the new `tasks`/`task_execution_plan` model** rather than building parallel machinery — ingestion becomes the first real "task type" in the new system | Ingestion flow still works end-to-end after refactor |
| 4.4 | GitHub | Generalize `/api/ai/orchestrate` into a layer-aware dispatcher: reads `orchestra_layers.default_model_config`, checks `customer_model_config` for a BYO override, falls back to platform default (Groq today) | Existing 4 event types (document.uploaded, item.overdue, notice.received, deadline.approaching) still produce the same actions as before, now routed through layer config |
| 4.5 | GitHub | Frontend: per-assistant task list/chat view (reuses `DataTable`, adds a chat-style panel); execution plan viewer (timeline of `task_agent_executions`) | Manual browser check |
| 4.6 | Supabase | Apply migration (branch → verify → merge) | `get_advisors` clean |
| 4.7 | Vercel | Add any new BYO-model provider env vars actually configured by a customer (per-customer keys live encrypted in `customer_model_config`, not as Vercel env vars — Vercel only needs the platform-default keys, e.g. `GROQ_API_KEY`, which already exists) | No new platform-wide env vars expected unless a new default provider is added |

---

## WAVE 5 — Self-Improvement Loops + Knowledge Flow

Depends on: Wave 1 (cross-tier knowledge flow must not be able to leak across tenants — this is exactly the scenario analysis.md flagged as unsafe to build before RLS existed).

| Step | Layer | What | Exit criteria |
|---|---|---|---|
| 5.1 | GitHub | New tables: `loop_definitions` (seeded with all 15 named loops from the master prompt), `loop_executions`, `loop_improvements`, `loop_health_metrics` | 15 loop_definitions rows seeded |
| 5.2 | GitHub | New tables: `knowledge_flow_log`, `data_separation_audit` | Tables created |
| 5.3 | GitHub | **Implement the two audit/safety loops first, not the generative ones:** Loop 9 (API/Token/URL Management — audits `mcp_access_codes`/`api_keys` for staleness/scope creep) and Loop 12 (Hierarchy & Secrecy Management — runs `data_separation_audit` checks: sample queries per org, confirm zero cross-tenant rows returned). These validate the Wave 1 foundation continuously rather than assuming it stays correct forever. | Both loops run on a schedule and log real results |
| 5.4 | GitHub | Implement Loop 10 (User Behaviour) and Loop 4 (Knowledge Management) next — read-only/observational loops, lower risk than ones that write code or prompts | Loops log observations to `loop_executions` |
| 5.5 | GitHub | Defer Loops 2 (Self-Coding) and 6 (Prompt Management) — the two loops that let the system modify its own code/prompts — until 5.3/5.4 have a track record. Not scheduled in this plan yet; revisit explicitly before enabling. | — |
| 5.6 | Supabase | Apply migration (branch → verify → merge) | `get_advisors` clean |
| 5.7 | Vercel | Add Vercel Cron entries for scheduled loop execution (extends existing `vercel.json`, which currently has no cron config) | Cron jobs visible in Vercel dashboard, firing on schedule |

---

## WAVE 6 — Hardening & Launch Reconciliation

- Reconcile against the pre-existing `review_of_vedian.md` launch checklist (several items already resolved per `ai-os/boss/BOARD.yaml`; re-verify rather than trust the YAML claim) — confirm auth guards, dead-dependency removal, and DB connectivity are still true on current `main`.
- Full `get_advisors` pass (security + performance) across every table added in Waves 0–5.
- Load-test the dashboard/stats queries with the new indexes (Wave 0.5) and new RLS policies (Wave 1) together — RLS policies can silently regress query performance if the helper functions aren't marked `STABLE`/properly indexed.
- Update `Testing/test_execution_log.md` with new test cases covering cross-tenant isolation (the one thing this whole rebuild exists to guarantee).

---

## Cross-cutting reference (kept here so it doesn't scatter across waves)

**Vector index strategy:** every `vector(1536)` column gets an HNSW index (`m=16, ef_construction=64`), matching the existing `embeddings_cosine_idx` — proven pattern already in production, no need for `ivfflat`. Tables: `embeddings` (existing), `assistant_memories`, `worker_agents.capability_embedding` + `.knowledge_embedding`, `tasks.task_embedding`, `task_chat_messages.message_embedding`, `worker_agent_learnings.embedding`.

**New env vars, cumulative across all waves:**
| Var | Wave | Purpose | Scope |
|---|---|---|---|
| `AI_CONFIG_ENCRYPTION_KEY` | 0 | pgcrypto symmetric key for BYOK secrets | Platform-wide (Vercel secret) |
| *(none new)* | 1–3 | — | — |
| *(customer keys live encrypted in DB, not env)* | 4 | per-customer BYO model keys | Per-row, encrypted |
| *(cron trigger secret if needed)* | 5 | Vercel Cron → loop-execution route auth | Platform-wide |

**What is explicitly NOT being rebuilt from scratch** (already working, being extended not replaced): Groq-based orchestrator (`ai/orchestrate`), pgvector semantic search (`lib/embeddings.ts`), MCP server (`/api/mcp`), ingestion batch pipeline, `ai_configurations` table (fixing its wiring, not its shape).

---

## `main_dashboard_user.md` — captured as the Wave 3 worker-agent seed reference

The dashboard mockup at `main_dashboard_user.md` is a static, self-contained HTML/JS prototype (fake data, `setInterval`-driven simulation, no backend calls) — but its `WA` object is a genuinely useful, concrete taxonomy that should become the actual Wave 3 `worker_agents` seed data rather than being invented fresh:

- **33 Global-tier agents** across India Tax (Direct/Indirect), India Compliance (ROC/Labour), Accounting, Audit, Treasury, and Cross-Cutting (OCR, Anomaly Detection, Report Generation) domains — each with a name, domain path, icon, usage count, and accuracy score.
- **6 Firm-tier agents** scoped to an example firm ("Shah & Co"): Month-End Close WF, TDS Review Checklist, Client Onboarding, Communication Templates, Approval Chain Config, Manufacturing ITC Rules.
- **5 Client-tier agents** scoped to example clients (ABC Corp, DEF Inc, GHI Holdings): GST/TDS patterns, approval chains, bank patterns, risk profiles.
- **3 User-tier agents** scoped to an example user ("Rahul"): task priority, review style, active hours.
- **5 example assistants**, each wired to exactly 5 worker agents (one plausible combination per assistant persona) with example tasks and chat transcripts — useful as the Wave 2/Wave 4 demo/seed data too, not just Wave 3.

This taxonomy is preserved here so it survives independently of the mockup file. Wave 3, step 3.1/3.3 should seed from this list (translated into real `worker_agents` rows) rather than re-deriving domain examples from scratch.

## Frontend direction (from `main_dashboard_user.md`)

The mockup's interaction model — 5 assistant columns (Kanban-style), each showing its linked worker-agent tags grouped by tier color (teal=Global, indigo=Firm, amber=Client, slate=User), a per-assistant task list with pending/in-progress/completed/submitted states, an embedded per-assistant chat, and a slide-out "Agent Library" panel listing all worker agents filterable by tier — is the target UX. It's built with the Tailwind CDN + Font Awesome CDN, which is fine for a throwaway prototype but not appropriate to bring into the production Next.js app as-is; the real implementation should use the existing shadcn/ui + Tailwind (non-CDN) + lucide-react stack already in `src/components/`, reusing `DataTable`, `StatusBadge`, `DashboardCard` where they fit, rather than hand-rolling new equivalents.

---

## Wave 1 route migration checklist (to `withTenantContext` / `app_runtime`)

**Done — every route with a real backing table is migrated.** Final tally: 25 files across 12 route groups, all verified individually (`tsc --noEmit`, `eslint`, full `npm run build`, each showing zero new errors vs. the pre-migration baseline).

- [x] `departments` (GET, POST), `departments/[id]` (GET)
- [x] `notifications` (GET), `notifications/[id]/read` (PATCH)
- [x] `users` (GET, POST)
- [x] `compliance` (GET, POST), `compliance/[id]` (GET, PATCH, DELETE), `compliance/[id]/comments` (POST), `compliance/import` (POST), `compliance/overdue` (POST), `compliance/recur` (POST), `compliance/stats` (GET)
- [x] `notices` (GET, POST), `notices/[id]` (GET, PATCH, DELETE), `notices/stats` (GET)
- [x] `challans` (GET, POST), `challans/[id]` (GET, PATCH, DELETE)
- [x] `audit` (GET), `audit-points/[id]` (PATCH)
- [x] `me` (GET, PATCH)
- [x] `documents/extract` (POST)
- [x] `ingest` (GET, POST), `ingest/[batchId]` (GET, DELETE), `ingest/[batchId]/confirm` (POST), `ingest/[batchId]/items/[itemId]` (PATCH)
- [x] `mcp/tokens` (GET, POST, DELETE) — the `/api/mcp` route itself deliberately stays on the service-role admin client (see analysis.md), only the token-issuing route needed tenant scoping
- [x] `search/semantic` (POST)
- [x] `ai/orchestrate` (POST)

**Deliberately not migrated:** `settings/api-keys`, `settings/api-keys/[id]`, `settings/webhooks`, `settings/webhooks/[id]` — their backing tables (`api_keys`, `webhooks`) don't exist in the live database at all (confirmed in analysis.md), so these routes already fail on every real call regardless of tenant scoping. Migrating them would fix nothing. **Open decision for you:** either build the missing tables (making BYOK API access / outbound webhooks real features) or remove these dead routes and their Settings-page UI entirely. Not making this call unilaterally since it's a product scope decision, not a security fix.

**Serious findings from this sweep, beyond the tenant-isolation gaps already logged:** two routes had **no authentication at all** — `POST /api/documents/extract` (anyone could read/write any org's document data and burn Groq API quota for free) and `POST /api/search/semantic` (anyone could semantically search and read full details across every organization's compliance items, notices, and documents, unauthenticated). Both fixed. `/api/mcp/tokens`'s own docstring claimed "admin-only" but never actually checked role — any authenticated user, any org, could list/mint/revoke any org's MCP access tokens. Also fixed.

**Remaining to fully close out Wave 1:** switch `DATABASE_URL` itself away from `postgres` (or otherwise retire the `postgres`/`service_role` bypass policies) so there's no leftover RLS-bypassing path left for anything to accidentally use. Not done yet — every migrated route now enforces real isolation via `app_runtime`, but the *old* `db` client (still used by nothing currently, since every route with a real table has been migrated) still exists and still bypasses RLS if anything were to import it again. Low risk as-is, but worth closing.

---

## Change Log

*(entries appended here one per executed step, in the order it actually happened — this table is the ground truth for progress, the waves above are the plan)*

| # | Date | Layer | Change | Commit/Migration ref | Notes |
|---|------|-------|--------|----------------------|-------|
| 1 | 2026-07-01 | Supabase | Enabled RLS + service_role-bypass policy on 7 previously-exposed tables (`mcp_access_codes`, `challans`, `notices`, `embeddings`, `onboarding_steps`, `ingestion_batches`, `ingestion_items`) on project `pcrjmlpuqsbocqfwoxod` | migration `enable_rls_exposed_compliance_tables`; repo copy `drizzle/0003_enable_rls_exposed_compliance_tables.sql` | Applied with explicit user sign-off (Supabase tooling requires this before enabling RLS). Verified clean via `get_advisors` afterward. |
| 2 | 2026-07-01 | Supabase | Created `compliance.ai_configurations` table (declared in `schema.ts`, never migrated), enabled `pgcrypto`, added `org_id`/`(status, due_date)` indexes on project `pcrjmlpuqsbocqfwoxod` | migration `create_ai_configurations_and_indexes`; repo copy `drizzle/0004_ai_configurations_and_indexes.sql` | — |
| 3 | 2026-07-01 | GitHub | Rewired `/api/settings/ai-config` to real DB-backed, per-org, pgcrypto-encrypted storage; added `src/lib/ai-config-crypto.ts` | commit `2dca6c1` | Local build/typecheck not run (no `node_modules`/bun in this environment) — Vercel's own build step and existing CI will catch any compile error on deploy. |
| 4 | 2026-07-01 | Vercel | Added `AI_CONFIG_ENCRYPTION_KEY` (32-byte random, encrypted) to the confirmed-live project `veridian-compliance-ai` (`prj_mRRWcMvhyuxgRZtcfp4ArSzcOvII`), all environments | Vercel env id `3lNDmiwI99rJ43y8` | Not set on the `compliance-tracker` project since that one appears to be a dead/broken duplicate deployment — see infrastructure findings above. |
| 5 | 2026-07-01 | Supabase | Reset `pcrjmlpuqsbocqfwoxod`'s database password (with explicit user approval) since the previous one couldn't be verified or read back | via Management API `PATCH /v1/projects/{ref}/database/password` | — |
| 6 | 2026-07-01 | Vercel | Repointed `veridian-compliance-ai`'s `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_URL`, and `DATABASE_URL` from the old project (`jusqumifsmtcaujqyjuy`) to `pcrjmlpuqsbocqfwoxod` | env ids `KNT1lNlmF3iSwNle`, `yYlSCDBDnE2rxeNS`, `nioEtKhcp1mrSr03`, `SHu8tWdQtB28bPF8`, `qw45B0HjHB5VdFIi` | Confirms and closes the "which DB does the live app actually use" open item from the previous change log entries — it now unambiguously uses `pcrjmlpuqsbocqfwoxod`, the same project all Wave 0 DB fixes were applied to. `DATABASE_URL` update required a fresh confirmation after an earlier attempt was interrupted, per the interrupt-retry safety rule. |
| 7 | 2026-07-01 | Vercel | Triggered fresh production deployment (commit `2d110c2`) so the repointed env vars took effect | deployment `dpl_Eux5w9tX7XACw7peij9yacVapQAN` | User confirmed the live app loads correctly against the new database afterward. |
| 8 | 2026-07-01 | Supabase | Dropped two dead compliance-tracker artifacts from `jusqumifsmtcaujqyjuy`: the `compliance` schema (9 tables, stale duplicate data) and a separate, entirely empty `compliance_tracker` schema (abandoned earlier attempt) | migration `drop_dead_compliance_tracker_schemas` | `public` schema in that project (MeetTrack's real data) verified untouched before and after — same row counts. |
| 9 | 2026-07-01 | Vercel | Deleted the dead duplicate `compliance-tracker` project (`prj_80z9Rz3BYvvExvGXyt5LNoPPMgiZ`) | — | `veridian-compliance-ai` is now the only Vercel project linked to this repo. |
| 10 | 2026-07-01 | GitHub | Added `/orchestra` — visual preview of the target Orchestra experience (5 assistant columns, Agent Library across 4 tiers, explicit "what's not built yet" roadmap). New page + components only, no existing page touched. | commit `e2bee10` | `tsc --noEmit`, `eslint`, and full `npm run build` all verified clean before pushing. |
| 11 | 2026-07-01 | GitHub | **Security fix:** `/orchestra` (and pre-existing `/ingest`) were reachable with zero authentication — `middleware.ts` gates routes via a hardcoded path allowlist, and neither path was in it. Verified live (`curl` showed HTTP 200 with no redirect, vs. `/dashboard`'s correct 307→`/login`) before and after the fix. | commit `4c70908` | Found while manually verifying the new page post-deploy — a reminder that this allowlist pattern is itself fragile and worth revisiting in Wave 1 (a denylist, or route-group-based middleware matching, would fail closed instead of open). |
| 12 | 2026-07-01 | Supabase | Attempted to create a dev branch for Wave 1 safety-testing — **blocked**: branching requires Supabase Pro plan, this org is on Free. | — | User chose to proceed directly on production, incrementally, rather than upgrade. Wave 1 from here on is additive-first: new nullable columns/tables (can't break anything not yet referencing them) before any RLS-policy or API-route behavior change. |
| 13 | 2026-07-01 | Supabase | Discovered `auth.users` was **completely empty** on `pcrjmlpuqsbocqfwoxod` — 0 of 7 seeded `compliance.users` rows had a matching Auth account. Created a real Supabase Auth account for the seeded `admin@acme.com` user via the Admin API and linked it. | auth_user_id `ed889917-d975-4d70-8a51-cca553d8ac96` | This means the "it's opening" confirmation earlier was likely a stale browser session from before the DB repoint, not a fresh login against the new database — flagged to the user, who asked me to fix it directly (create a test account) rather than manually re-test in a browser. |
| 14 | 2026-07-01 | GitHub | Wave 1 step 1.1: added `auth_user_id` (FK to `auth.users.id`) + `reporting_to_id` (self-FK) to `compliance.users`; `requireAuth()` now links `auth_user_id` on every authenticated request regardless of login method (password login never hits `/auth/callback`, so this had to live here, not just in the callback route) | commit `33eb97d` | Also surfaced: **signup never creates a `compliance.users` row at all** — `auth.signUp()` only creates the Auth identity, so any brand-new signup hits a permanent "contact your administrator" wall. Pre-existing, unrelated to this session's changes. Logged here rather than fixed, to avoid scope creep — worth a dedicated fix. |
| 15 | 2026-07-01 | Supabase | Wave 1 steps 1.2-1.5: extended `user_role` enum (+6 values), created `branches`/`clients`/`client_entities`/`user_client_access`/`subscription_plans` (seeded 4 plans), added nullable `client_id` to the 5 client-scoped tables, backfilled every existing row to an auto-created "Self / Direct" client per org (18/18 compliance_items, 18/18 documents, 36/36 audit_points — zero left null) | migrations `wave1_step2_extend_user_role_enum`, `wave1_step3_hierarchy_tables`, `wave1_step5_client_id_and_backfill` | `get_advisors` clean — all 5 new tables have RLS enabled with the same service_role-bypass pattern as everything else, no new findings. |
| 16 | 2026-07-01 | GitHub | Added matching Drizzle `schema.ts` declarations for everything in #15, plus the `user_role` enum extension | commit `26ad605` | `tsc --noEmit`, `eslint`, full `npm run build` all clean. |
| 17 | 2026-07-01 | Supabase | **Critical check before writing more policies:** confirmed `postgres` (the role Drizzle connects as via `DATABASE_URL`) has `rolbypassrls=true` — not superuser, but functionally identical for RLS purposes. Any policy written against `postgres` would never actually apply. | `SELECT rolname, rolsuper, rolbypassrls FROM pg_roles` | This is why Wave 1's real enforcement needed a new role (#18), not just more policies on the existing connection. |
| 18 | 2026-07-01 | Supabase | Created `app_runtime` role: `NOSUPERUSER NOBYPASSRLS`, scoped `GRANT`s on `compliance.*` only (+ read on `auth.users`). Created `compliance.current_org_id()` / `current_client_ids()` GUC-reading functions (search_path hardened). Added real per-tenant RLS policies for `app_runtime` on all 19 `compliance.*` tables — direct `org_id` match on 12, `EXISTS`-joined through a parent table's `org_id` on 7 that don't have their own (`audit_points`, `comments`, `documents`, `notifications`, `audit_logs`, `onboarding_steps`, `ingestion_items`, `client_entities`, `user_client_access`); `subscription_plans` is global read-only. `postgres`/`service_role` bypass policies untouched — nothing not yet migrated is affected. | migrations `wave1_step_create_app_runtime_role`, `wave1_step6_tenant_context_functions`, `wave1_step6b_fix_function_search_path`, `wave1_step7a_rls_direct_org_tables`, `wave1_step7b_rls_indirect_tables` | **Verified live, not just assumed:** created a throwaway second org, granted `postgres` temporary membership in `app_runtime` for testing, ran queries `SET LOCAL ROLE app_runtime` with each org's GUC context. Org_001 saw its real 18 compliance_items; the test org saw exactly its own 1 item and 1 organisation (not org_001's 18); an `UPDATE` targeting `org_001` while scoped as the test org affected 0 rows (RLS filtered it out before the write could happen), confirmed by re-reading `org_001`'s name unchanged afterward. Test org and the temporary `GRANT app_runtime TO postgres` were both cleaned up. `get_advisors` re-run afterward: zero new findings beyond two trivial search_path warnings on my own new functions, which were then fixed. |
| 19 | 2026-07-01 | GitHub | `src/lib/db/tenant-scoped.ts`: new, separate DB client connecting as `app_runtime` (not `postgres`) via a new `APP_RUNTIME_DATABASE_URL`. `withTenantContext(orgId, fn)` wraps a callback in a transaction that `SET LOCAL`s the GUCs first. Migrated `/api/departments` (GET+POST) as the first of 27 routes — and fixed two latent cross-tenant bugs surfaced in the process: the POST fallback called `organisations.findFirst()` with no `WHERE` when `orgId` was falsy (first org in the *entire* database), and the audit-log actor fallback looked up "any admin user" across all orgs. Both now correctly scoped, with RLS as a backstop regardless. | commit `cee2422` | `tsc --noEmit`, `eslint`, full `npm run build` all clean. The existing `db` export (on `postgres`/`DATABASE_URL`) is untouched and keeps serving the other 26 routes exactly as before — this is a route-by-route migration, not a cutover. |
| 20 | 2026-07-01 | Vercel | Added `APP_RUNTIME_DATABASE_URL` (encrypted) to `veridian-compliance-ai` | — | Needed for `/api/departments` (and every subsequently-migrated route) to actually reach `app_runtime` in production. |
| 21 | 2026-07-01 | Vercel | Deployment `dpl_7P1jDgkSeKiwjcEa3utgFKZDwREY` (commit `0253f21`, coalesced with `cee2422`) went READY | — | Sanity-checked live: unauthenticated `GET /api/departments` returns 401, not 500 — confirms the new `app_runtime`/`withTenantContext` code path runs without crashing in production. Full authenticated verification still needs a real login session, which I can't simulate from here without the user's browser. |
| 22 | 2026-07-01 | GitHub | Migrated `notifications` (GET) and `notifications/[id]/read` (PATCH) to `withTenantContext`, fixing two real bugs surfaced in the process: GET ignored the actual logged-in user and served "the first admin found anywhere in the DB"'s notifications to everyone; PATCH let any authenticated user mark any other user's notification as read in any org, with zero ownership check. | commit `dee4dc2` | — |
| 23 | 2026-07-01 | GitHub | Migrated `users` (GET+POST). Surfaced that `next.config.ts` has `typescript: { ignoreBuildErrors: true }` — every build silently ignores all TS errors repo-wide, which is how `POST /api/users`'s missing required `passwordHash` field (a real NOT-NULL-violation waiting to happen on first real use) went undetected. Filled it with an unusable random bcrypt hash (the column is legacy from before this app used Supabase Auth exclusively; nothing reads it for real login anymore). New invites now link `auth_user_id` immediately instead of waiting for first login. | commit `dee4dc2` | Duplicate-email pre-check deliberately kept on the raw (non-tenant-scoped) client — email is a genuinely global unique constraint, not a tenant boundary. |
| 24 | 2026-07-01 | GitHub | Migrated the `compliance` route group (7 files). Found and fixed: GET/PATCH `compliance/[id]` had zero org check at all (view/edit any org's item by id); `compliance/overdue` ran a fully unscoped bulk UPDATE across every org on every dashboard load; `compliance/import` and `compliance/recur` used "first org/department in the whole database" fallbacks; `compliance/[id]/comments` had no org check on the item being commented on. | commit `1a36982` | tsc error count for these files went from 16 pre-existing to 13 (net fix, not regression). |
| 25 | 2026-07-01 | GitHub | Migrated `notices`, `challans`, `audit`, `audit-points/[id]`, `departments/[id]` (8 files). Found and fixed: zero org check on GET/PATCH/DELETE for both `notices/[id]` and `challans/[id]`; `notices/stats` aggregated counts across every org combined; `challans` POST derived orgId from whatever compliance item id was passed; `audit-points/[id]` had no org check via its compliance_item join; `departments/[id]` GET had no org check. Also fixed a pre-existing `db.schema.complianceItems` typo (should have just been the imported table) while migrating `challans/route.ts`. | commit `d333627` | — |
| 26 | 2026-07-01 | GitHub | Migrated `me`, `documents/extract`, `ingest` (4 files), `mcp/tokens` (7 files total). **`documents/extract` had no authentication at all** — unauthenticated read/write of any org's document data, plus free Groq API usage for anyone. **`mcp/tokens`'s docstring claimed "admin-only" but never checked role** — any authenticated user, any org, could list/mint/revoke any org's MCP tokens; also fixed a hardcoded wrong domain in the token-usage response. `ingest`/`ingest/[batchId]/confirm` used the same "first org/admin/department" fallback pattern found throughout; `ingest` GET listed batches across every org. | commit `ecb9089` | — |
| 27 | 2026-07-01 | GitHub | Migrated `search/semantic`, `ai/orchestrate` — completes the Wave 1 route migration. **`search/semantic` had no authentication at all AND passed `undefined` for the org filter to `findSimilar()`**, meaning unauthenticated semantic search across every organization's compliance items, notices, and documents with full detail in the results. `ai/orchestrate` had auth but no org check on the entityId lookups, letting an authenticated user get AI-generated suggestions built from another org's data. | commit `c1ee2bb` | These two were arguably the most severe findings in the entire sweep — full unauthenticated cross-tenant read access via natural-language search. |
| 28 | 2026-07-01 | GitHub | **Fixed a product-critical bug beyond Wave 1's scope**: `auth.signUp()` never created a `compliance.users`/`organisations` row, so every brand-new signup hit a permanent "contact your administrator" wall forever. `requireAuth()` now auto-provisions a new tenant (org + default "General" department + admin user, linked to the Auth identity) the first time it sees an authenticated user with no matching row. Concurrency-safe (falls back to re-reading on a duplicate-email race). | commit `e1de9bb` | Deliberately uses the raw (RLS-bypassing) client — creating a brand-new tenant can't be scoped to an org that doesn't exist yet. This is one of the few legitimate remaining uses of the non-tenant-scoped `db` export. |
| 29 | 2026-07-01 | Supabase + GitHub | Created `compliance.api_keys`, `compliance.webhooks`, `compliance.webhook_deliveries` (declared in schema.ts, never migrated — same drift class as `ai_configurations`), with RLS + the `app_runtime` tenant-isolation policy, and migrated all 4 settings routes to `withTenantContext`. Also fixed a pre-existing `db.schema.webhookDeliveries` typo (same bug class as the earlier `challans` fix). | commit `024c8ad`; migrations `create_api_keys_and_webhooks_tables`, `fix_enable_rls_api_keys_webhooks` | **Caught my own mistake via `get_advisors` immediately after applying**: used `FORCE ROW LEVEL SECURITY` without first `ENABLE ROW LEVEL SECURITY` on the 3 new tables — policies existed but weren't active. Fixed with a follow-up migration, verified clean. Worth remembering: always pair ENABLE + FORCE, never FORCE alone. |
| 30 | 2026-07-01 | Supabase | **Wave 2 starts.** Created `compliance.current_user_id()` GUC-reading function (search_path hardened, granted to `app_runtime`) — needed because AI Assistants are the first thing in this schema scoped to an *individual user*, not just an org, so `withTenantContext` needed a third GUC alongside `current_org_id`/`current_client_ids`. | migration `wave2_user_context_function` | — |
| 31 | 2026-07-01 | Supabase | Created the 4 Wave 2 tables: `ai_assistants` (5 per user, `assistant_number` 1-5, `label`, `status`, `personality_config` jsonb), `assistant_memories` (with a `vector(1536)` embedding column + HNSW cosine index, same pattern as `compliance.embeddings`), `assistant_sessions`, `assistant_metrics_daily`. RLS on all 4 requires `compliance.current_user_id()` to match the owning user — `ai_assistants.user_id` directly, the other 3 via an `EXISTS` join back to `ai_assistants`. This is stricter than every other table in the schema: not even an org admin can read another user's assistants or memories, matching the master spec's "User tier: readable ONLY by that specific user." Backfilled 5 assistants for every pre-existing user (7 seeded + the manually-created `admin@acme.com`) in the same migration. | migration `wave2_ai_assistants_tables` | Paired `ENABLE` + `FORCE ROW LEVEL SECURITY` correctly this time (see #29's lesson). `get_advisors` clean immediately, no follow-up fix needed. |
| 32 | 2026-07-01 | GitHub | Extended `withTenantContext`'s `TenantContext` with an optional `userId`, setting `app.current_user_id` via `SET LOCAL` when present. Updated both places a brand-new `compliance.users` row gets created — `autoProvisionUser()` in `auth-guard.ts` (new signups) and the invite flow in `api/users/route.ts` (admin-invited users) — to also insert 5 numbered `ai_assistants` rows, via the raw (RLS-bypassing) `db` client since neither can scope the insert to a user id that doesn't exist as an authenticated session yet (same rationale as tenant auto-provisioning itself). Added `schema.ts` declarations for all 4 Wave 2 tables + relations (embedding column deliberately omitted, same as `embeddings` — managed via raw SQL). Built the API surface: `GET /api/assistants`, `PATCH /api/assistants/[id]` (label/status/personalityConfig), `POST`/`GET /api/assistants/[id]/memories`, `GET /api/assistants/[id]/memories/search` (pgvector cosine similarity, reusing `generateEmbedding()` from `lib/embeddings.ts`). | commit `2f06c74` | `tsc --noEmit`: zero errors touching any Wave 2 file (pre-existing error count actually dropped 608→597 vs. the saved baseline, from earlier fixes elsewhere). `eslint` clean. Full `npm run build` succeeds — all 4 new routes compile as expected (`/api/assistants`, `/api/assistants/[id]`, `/api/assistants/[id]/memories`, `/api/assistants/[id]/memories/search`). `get_advisors` re-run: zero new findings, all remaining items pre-existing and out of scope (unrelated `public`-schema marketing-site tables/views, auth settings). |
| — | 2026-07-01 | Note | **Wave 2 step 2.6 (Settings → "AI Assistants" panel, frontend tiles) deliberately deferred** — backend + API surface for Wave 2 is complete and verified, but no UI consumes it yet. Logged here rather than silently skipped; revisit alongside Wave 4's frontend work (task list/chat view) so assistant-related UI gets built together rather than piecemeal. |
| 33 | 2026-07-01 | Supabase | **Wave 3 starts: Worker Agent Library.** Created `worker_agents` (tier CHECK'd against `global`\|`customer`\|`client`\|`user`, with a DB constraint requiring exactly the matching one of `org_id`/`client_id`/`user_id` to be set per tier) plus 4 child tables: `worker_agent_versions`, `worker_agent_usage_log`, `worker_agent_learnings`, `worker_agent_domain_index`. RLS SELECT policy covers all 4 tiers (global visible to everyone via `app_runtime`; customer/client/user scoped to `current_org_id()`/`current_client_ids()`/`current_user_id()`); separate INSERT/UPDATE/DELETE policies explicitly exclude `tier='global'`, so `app_runtime` can never write global agents — only `service_role`/migrations can, keeping the platform-managed tier genuinely immutable. Child tables inherit visibility via `EXISTS` back to the parent agent, except `usage_log` which uses its own direct org/client/user columns (usage of a global agent still needs to log against the calling tenant). | migration `wave3_worker_agents_tables` | Paired `ENABLE` + `FORCE ROW LEVEL SECURITY` correctly again. `get_advisors` clean immediately. |
| 34 | 2026-07-01 | Supabase | Seeded the 7 existing MCP tools (`list_compliance_items`, `get_compliance_stats`, `get_overdue_items`, `create_compliance_item`, `update_compliance_status`, `list_departments`, `get_penalty_estimate`) as `tier='global'`, `is_immutable=true` `worker_agents` rows, with their real `input_schema` copied verbatim from the existing hardcoded `TOOL_DEFINITIONS` array in `api/mcp/route.ts`. Also populated `worker_agent_domain_index` from each agent's `domain`. This is the plan's explicit intent for 3.3: formalize what already works as the system's first real content, not invent new agents from scratch. | migration `wave3_seed_global_agents_from_mcp_tools` | Verified all 7 rows landed with correct `code_reference` values via direct query. |
| 35 | 2026-07-01 | GitHub | Refactored `api/mcp/route.ts`: `tools/list` and the `GET` discovery probe now read tool definitions from `worker_agents` (`tier='global'`) via the existing service-role admin client, falling back to the original hardcoded `TOOL_DEFINITIONS` array if the table is empty or the query fails — backward compatible, same external tool names/schemas, zero behavior change for existing MCP clients. `tools/call` now fire-and-forget logs every execution to `worker_agent_usage_log` (duration, success/failure, error message) and increments `usage_count`, without adding latency to the response (same fire-and-forget style already used for `mcp_access_codes.last_used_at`). `handleTool`'s actual execution logic is untouched — only the tool *definitions* are now DB-sourced, matching the plan's explicitly incremental step 3.4. Added matching `schema.ts` declarations for all 5 new tables + relations. | commit `8d07590` | `tsc --noEmit`: zero errors touching either changed file (597, same as the Wave 2 baseline). `eslint` clean. Full `npm run build` succeeds, `/api/mcp` compiles. `get_advisors` re-run: zero new findings. |
| 36 | 2026-07-01 | Supabase | **Wave 4 starts (partial): Task System + Orchestra Layers.** Created `tasks` (org-scoped RLS), `task_execution_plan`/`task_agent_executions`/`task_chat_messages` (RLS via `EXISTS` back to `tasks`), `orchestra_layers` (globally readable, seeded with the 5 layers from the master spec: Task OA, User Assistant OA, Customer Account OA, Global Intelligence OA, Meta OA — each with a `default_model_config` of `{provider: groq, model: llama-3.3-70b-versatile}`), `orchestra_executions` (org-scoped), `customer_model_config` (per-org, optionally per-layer BYO override — distinct from Wave 0's `ai_configurations`, which is per-purpose not per-layer). | migration `wave4_task_system_and_orchestra_layers` | Paired `ENABLE`+`FORCE` correctly again. `get_advisors` clean. Verified all 5 `orchestra_layers` rows landed via direct query. |
| 37 | 2026-07-01 | GitHub | `api/ai/orchestrate/route.ts` now fire-and-forget logs every invocation to `orchestra_executions` against the `task_oa` layer — makes this route's identity as the Task Orchestra Agent real without changing any existing behavior (model selection is still hardcoded Groq; full per-layer BYO dispatch via `customer_model_config` deferred, see note below). Added matching `schema.ts` declarations for all 7 new Wave 4 tables + relations. | commit `fad29fd` | **Caught my own bug via `tsc --noEmit` before committing**: initially declared `eventType`/`entityId`/`startedAt` inside the route's `try` block but referenced `startedAt` from the `catch` block — out of scope, would have been a real runtime `ReferenceError` on the very first request that hit the catch path (e.g. malformed JSON body) had it shipped. Fixed by hoisting `startedAt`/`parsedEventType`/`parsedEntityId` above the `try`. `tsc --noEmit` zero new errors afterward (597; the 3 remaining errors in this file are pre-existing, confirmed identical content at shifted line numbers vs. the Wave 3 baseline). `eslint` clean, full `npm run build` succeeds. |
| — | 2026-07-01 | Note | **Wave 4 steps 4.3, 4.5, and full BYO-model dispatch deliberately deferred, not silently skipped:** (a) generalizing `ingestion_batches`/`ingestion_items` into the new `tasks` model (4.3) is a refactor of a live, working feature — it needs its own careful pass with real testing, not to be bundled into an additive-tables commit; (b) frontend task list/chat view (4.5) bundled with the already-deferred Wave 2 assistant UI (#267 above), to be built together; (c) actually reading `customer_model_config` and dispatching to a different LLM provider per layer isn't implemented — the codebase only has a Groq client (`lib/groq.ts`) today, and building real multi-provider dispatch (OpenAI/Anthropic/Google) is a distinct, larger feature than what's safe to fold into this pass. |
| 38 | 2026-07-01 | Supabase | **Wave 5 starts: Self-Improvement Loops.** Created `loop_definitions`, seeded with all 15 loops verbatim from the master spec (Loop Engineering, Self-Coding, UI/UX Loop, Knowledge Management, Process Management, Prompt Management, Input Management, Output Management, API/Token/URL Management, User Behaviour Management, Full Automation Loop, Hierarchy & Secrecy Management, Data/Process Separation, BYO AI Model Loop, Everything by AI Loop) — the exact list wasn't captured verbatim anywhere in this doc before now, so I pulled it from the original master prompt in the session transcript rather than guessing. Also created `loop_executions`, `loop_improvements`, `loop_health_metrics`, `knowledge_flow_log`, `data_separation_audit`. All of these except `loop_definitions` are platform-operational, not tenant data — deliberately given **no `app_runtime` RLS policy at all** (service_role bypass only), since loops #12/#13 run cross-tenant audits by design and exposing their results through the normal org-scoped path would itself be a data leak. | migration `wave5_self_improvement_loops` | Paired `ENABLE`+`FORCE` correctly again. `get_advisors` clean. Verified all 15 rows + `is_active` flags via direct query — only #9 and #12 marked active, matching plan step 5.3. |
| 39 | 2026-07-01 | GitHub | Implemented the two audit/safety loops per plan step 5.3 (not the generative ones): `lib/loops/api-token-audit.ts` (Loop 9 — flags `mcp_access_codes`/`api_keys` active but unused >90 days, or never used >30 days after creation; read-only, no auto-revocation yet) and `lib/loops/data-separation-audit.ts` (Loop 12 — re-runs, on a schedule, the equivalent of the one-time manual cross-tenant isolation proof from Wave 1 change-log entry #18: queries `compliance_items` for up to 10 orgs through the real `withTenantContext`/`app_runtime` path and verifies every row actually belongs to the org it was scoped to, so a future RLS regression gets caught automatically). Added `api/internal/loops/run` (GET+POST), authenticated via `Authorization: Bearer $CRON_SECRET` since there's no user session for a scheduled job. Added a daily Vercel Cron entry (`vercel.json`, 03:00 UTC — once/day to respect Hobby-plan cron limits) pointing at it. Set `CRON_SECRET` (32-byte random) on Vercel production via the Vercel CLI, which was already authenticated in this environment as `fchecklist` — generated straight to a scratchpad file and piped via stdin so the value was never printed or embedded in a command. Added matching `schema.ts` declarations for all 6 new tables + relations. | commit `c8275ec` | The other 13 loops (including #2 Self-Coding and #6 Prompt Management, both explicitly deferred per plan step 5.5) are seeded but `is_active=false` — no execution code, not scheduled; they'd write/modify code or prompts autonomously, which needs #9/#12 to have a track record first. `tsc --noEmit` zero new errors (597, same baseline). `eslint` clean. Full `npm run build` succeeds, `/api/internal/loops/run` compiles. `get_advisors` clean. |
| 40 | 2026-07-01 | Discovery (blocking) | **While trying to functionally test the new cron route end-to-end** (the first real, non-401 query this whole session — see the 🔴 section at the top of this doc for full detail), discovered that the Supavisor shared pooler cannot currently route to this project at all: `tenant/user postgres.pcrjmlpuqsbocqfwoxod not found`, reproduced identically for both `DATABASE_URL` and `APP_RUNTIME_DATABASE_URL`, from this sandbox directly (not Vercel-specific). Confirmed the password/database are fine via a direct (non-pooler) connection, which succeeded twice. Matches Supabase's own documented `worker_not_found` Supavisor error ("Supavisor does not have the necessary information to route the connection... can take some time to propagate") — but 6+ retries over several minutes haven't resolved it. This means **every authenticated database query in production has likely been failing this entire session** — invisible until now because every prior smoke test only checked the unauthenticated 401 path, which returns before `requireAuth()` ever touches the raw `db` client. | — | Did not pause/restore the project to try to force Supavisor to re-register — that's a disruptive action against the live database, a different risk category than this session's code/schema changes, and is flagged for you rather than acted on unilaterally. See the top-of-file section for full diagnosis and recommended next steps. |
| 41 | 2026-07-01 | Supabase | **Wave 6 starts (partial): Hardening.** User chose to keep building while retrying the pooler in the background rather than blocking on it. Full `get_advisors` sweep (security + performance) across every table added Waves 0-5. Fixed real, self-introduced issues: "multiple permissive policies" on 3 Wave 3 tables (`worker_agent_versions`/`worker_agent_learnings`/`worker_agent_domain_index` each had a redundant `FOR ALL` write policy overlapping the `FOR SELECT` read policy on every SELECT — split into INSERT/UPDATE/DELETE-only policies, matching the pattern already correct on `worker_agents` itself) and 2 pre-existing duplicate indexes (confirmed identical, dropped). Added indexes for all 13 unindexed-foreign-key findings (mix of pre-existing gaps and this session's own). | migration `wave6_perf_hardening` | Re-ran `get_advisors`: `multiple_permissive_policies` and `duplicate_index` findings gone. Remaining findings are all INFO-level "unused index" (expected — no real traffic has hit these tables via the pooler yet, see #40) or pre-existing/out-of-scope. |
| 42 | 2026-07-01 | GitHub | Reconciled the pre-existing `review_of_vedian.md` launch checklist against current `main` (re-verified, not trusted): confirmed H1-H6 (next-auth/prisma/z-ai-web-dev-sdk removal, package.json name, CI hard-fail) are all actually true on main, not just claimed. Confirmed C2 (API auth) and REC-11 (multi-tenancy) are more thoroughly solved than that doc describes — real RLS via `app_runtime`, not just an app-layer `getUser()` check. Flagged that doc's DB/Vercel references as stale (superseded by this file) and that `next.config.ts`'s `ignoreBuildErrors` is still present. Added 7 new cross-tenant-isolation test cases (TC-TEN-01 through TC-TEN-07) to `Testing/test_execution_log.md`, executed directly against the database where the live-app path is blocked by #40; logged #40 itself as BUG-002 in that file's bug table. | commit `8d0e98b` | Docs-only commit, no application code changed. |
| 43 | 2026-07-01 | GitHub | **Closed the deferred Wave 2 step 2.6** (Settings → "AI Assistants" panel — #267 above). New `AiAssistantsSection` component: 5 assistant tiles with idle/working badge and inline rename, following the same fetch/loading/toast conventions as `ApiKeySection.tsx`. Wired into Settings as a new nav item between "AI Configuration" and "Preferences". | commit `8770232` | `tsc --noEmit` zero new errors (597, same baseline). Full `npm run build` succeeds, `/settings` compiles. `eslint` flags one `react-hooks/set-state-in-effect` error on the fetch-in-`useEffect` pattern — confirmed not a regression: the pre-existing `ApiKeySection.tsx` has the identical error from the identical pattern, so this matches existing codebase convention rather than introducing something new. |
| 44 | 2026-07-01 | GitHub | Filled two remaining API gaps and did the first real (non-mock) frontend wiring. `GET /api/worker-agents`: Wave 3's `worker_agents` table had no customer-facing route at all (only the MCP server's internal admin client could read it) — added one, RLS-gated via `withTenantContext`. `GET/POST /api/tasks`, `GET/PATCH /api/tasks/[id]`, `POST /api/tasks/[id]/chat`: Wave 4 built the full `tasks`/`task_execution_plan`/`task_chat_messages` schema but no API layer ever consumed it — added standard tenant-scoped CRUD. Wired `/orchestra`'s Agent Library sheet to `GET /api/worker-agents` instead of the mock `WORKER_AGENTS` array — the first part of this page showing genuinely live data, since the 7 seeded global agents actually exist. Updated the page's info banner to describe the resulting split honestly: Agent Library is real, but the 5 assistant columns' task lists/chat stay on mock data (swapping in real, currently-empty tasks/chat would look like a regression, not progress — nothing generates real tasks for an assistant yet). | commit `5e418ef` | `tsc --noEmit` zero new errors (597, same baseline). `eslint`: one `react-hooks/set-state-in-effect` error, same pre-existing pattern class as #43, not a regression. Full `npm run build` succeeds — all 4 new routes plus `/orchestra` compile. |
| 45 | 2026-07-01 | GitHub | **Closed Wave 4's "full per-layer BYO model dispatch" deferral** (#273 above). New `lib/llm-client.ts`: provider-agnostic `callLLM`/`callLLMJson` covering all 4 `ai_provider` values — Groq/OpenAI share the OpenAI chat-completions shape, Anthropic gets its own Messages-API implementation, Google gets its own Gemini `generateContent` implementation; both of the latter fall back to prompt-based JSON instructions since neither has a native `response_format` flag (same technique the existing Groq client already used). New `lib/orchestra-model-resolver.ts`: `resolveModelConfig(orgId, layerKey)` — checks the org's `customer_model_config` for that specific layer, then one that applies to all layers (`orchestra_layer_id IS NULL`), then falls back to the layer's own `default_model_config` (platform Groq). Decrypts the BYO key via the existing pgcrypto helper right before use. `api/ai/orchestrate/route.ts` now calls through this resolver instead of the hardcoded `getGroqApiKey()`/`callGroqLLMJson` — same fallback behavior when nothing resolves, and `orchestra_executions` logging now records which provider/model actually ran. New admin-only `api/settings/model-config` (GET/POST) + `.../[id]` (DELETE) for configuring the override — built on `withTenantContext` correctly from the start. | commit `96325ac` | **Caught and fixed my own bug via `tsc --noEmit` before committing**: the new insert in `model-config/route.ts` spread a `Partial<...>` patch object into a required-fields `.values()` call, which TypeScript correctly rejected (`provider` typed as possibly-`undefined` didn't satisfy the non-optional insert type). Fixed by listing the insert's required fields explicitly instead of spreading. Confirmed back to the 597 baseline afterward. `eslint` clean. Full `npm run build` succeeds, both new routes compile. |
| — | 2026-07-01 | Note | **Flagged, not fixed here, to avoid scope creep:** `api/settings/ai-config/route.ts` predates this session's Wave 1 tenant-isolation sweep and still uses the raw (RLS-bypassing) `db` client instead of `withTenantContext` — every other settings route was migrated, this one was missed. No known live data leak (it has correct manual `org_id` WHERE-clause scoping), but it's missing the RLS defense-in-depth layer everything else in this schema now has. Spun off as a separate background task (`task_716830b5`) rather than bundled into the BYO-model-dispatch commit above, since it's an unrelated pre-existing gap. |
| 46 | 2026-07-01 | GitHub | Gave the BYO model dispatch (#45) an actual UI — the API existed but nothing could configure it. New `OrchestraModelConfigSection`: one row per Orchestra Layer showing platform-default vs. custom-override status, provider select, model name input, masked key input ("leave blank to keep existing key" once one is set), Save, and Reset-to-default. Rendered below the existing `AiConfigSection` on Settings → AI Configuration. | commit `0897fdd` | `tsc --noEmit` zero new errors (597, same baseline). `eslint`: same pre-existing `react-hooks/set-state-in-effect` pattern as every other fetch-on-mount component in this codebase (#43, #44), not a regression. Full `npm run build` succeeds, `/settings` compiles. |
| 47 | 2026-07-01 | Supabase + GitHub | Activated Loop 14 (BYO AI Model Loop) — the third active self-improvement loop, directly relevant now that real `customer_model_config` data exists. `lib/loops/byo-model-audit.ts`: read-only audit checking whether each active BYO config was actually exercised (any `orchestra_executions` in the last 7 days) and aggregating provider distribution across all orgs; flags unused-looking configs for a human to investigate, doesn't touch anything itself — same read-only posture as loops 9/12. Wired into `api/internal/loops/run`. | migration `wave5_activate_loop14_byo_model`; commit `6793d18` | `tsc --noEmit` zero new errors (597, same baseline). `eslint` clean. Full `npm run build` succeeds. |
| 48 | 2026-07-01 | GitHub | **`/orchestra` is now fully live — no mock data left.** New `RealAssistantColumn` fetches real tasks per assistant (`GET /api/tasks?assistantId=X`) with an honest empty state and a working "add task" input that actually `POST`s to `/api/tasks` (unlike the old mock column's permanently-disabled chat). `orchestra/page.tsx` rewritten as a client component fetching the user's real 5 assistants via `GET /api/assistants` instead of the mock `ASSISTANTS` array. Rewrote the info banner and roadmap to describe what's genuinely still missing (task execution engine, ingestion refactor, 12 more loops) instead of the stale "nothing is real yet" framing. Deleted the now-dead mock `AssistantColumn.tsx`/`AgentTag.tsx` rather than leaving unreferenced preview code behind. | commit `cd9e22f` | Deliberately not built: nothing auto-executes a created task (no execution engine exists), and worker-agent-to-assistant linkage has no data model yet, so agent tags were dropped from the column view rather than shown against data that doesn't exist. `tsc --noEmit` zero new errors (597, same baseline). `eslint`: same pre-existing pattern as #43-46, not a regression. Full `npm run build` succeeds, `/orchestra` compiles. |
| 49 | 2026-07-01 | Supabase + GitHub | Activated Loops 8 (Output Management) and 11 (Full Automation Loop) — now 5 of 15 loops active. `lib/loops/output-delivery-audit.ts`: audits each active webhook's last 20 deliveries in the last 7 days, flags >50% failure rate (a broken customer endpoint retried forever is wasted output). `lib/loops/automation-progress-audit.ts`: tracks completion rate of tasks created in the last 30 days — will read low right now since no task execution engine exists yet, which is the honest signal this loop is meant to produce, not a bug. Both wired into `api/internal/loops/run`, whose loop-lookup was refactored from one `findFirst` per loop number to a single batched `inArray` query now that there are 5 to check. | migration `wave5_activate_loops_8_11`; commit `15a8c6c` | `tsc --noEmit` zero new errors (597, same baseline). `eslint` clean. Full `npm run build` succeeds. |
| 50 | 2026-07-01 | GitHub | **Found and fixed a real inconsistency while double-checking the active-loop list:** `loop_definitions.is_active` had been `TRUE` for Loop 10 (User Behaviour Management) ever since the original Wave 5 seed migration (#38) — it was one of the two loops step 5.4 called out — but no execution code or cron wiring for it was ever actually written (Loop 4, the other one from that step, was correctly left inactive). A loop claiming to be active that never runs is worse than one honestly marked inactive. New `lib/loops/user-behaviour-audit.ts`: read-only aggregate over `audit_logs` from the last 30 days — top 10 most active users, action-type distribution, hour-of-day distribution. Explicitly does not write to `ai_assistants.personality_config` or any other customer-visible resource; auto-personalization from these observations would be a separate, explicitly-approved write action. Wired in — now 6 of 15 loops active (8, 9, 10, 11, 12, 14). | commit `3dc191c` | `tsc --noEmit` zero new errors (597, same baseline). `eslint` clean. Full `npm run build` succeeds. |
| 51 | 2026-07-01 | GitHub | **Closed the `ignoreBuildErrors` gap flagged since Wave 1 (#23) — went from 94 pre-existing TypeScript errors to zero, methodically, and removed the flag.** Root causes, in order of impact: (a) `tsconfig.json`'s `**/*.ts` include was sweeping in `supabase/functions/` (a separate Deno runtime) and `examples/` (standalone demos), neither ever imported by `src/` — 17 errors of pure noise, fixed by excluding both directories; (b) a shared `framer-motion` `ease: "easeOut"` value was widening to `string` instead of the literal type `Variants` needs — `as const` on one line fixed 38 errors across `page.tsx`/`pricing/page.tsx` at once, since the same variant object is reused ~30 times; (c) `const conditions = []` / `const logEntries = []` with no type annotation inferred as `never[]` in `audit`/`compliance`/`notices` routes — typed explicitly as `SQL[]`; (d) `VALID_TYPES`/`VALID_PRIORITIES` in `compliance/import/route.ts` were missing `as const` (unlike the working versions of the same constants elsewhere), widening enum-typed inserts to `string`. **Along the way, found and fixed several genuinely severe, currently-live bugs that were only type errors on the surface:** `compliance/route.ts` POST silently inserted `null` into the NOT-NULL `dueDate` column when omitted (500 at the DB layer, not a clean 400 — same class as the `passwordHash` bug found earlier this session); `compliance/[id]/comments/route.ts` never set `comments.entity_id` (NOT NULL, no default) — every real comment post was a NOT-NULL violation; **`src/app/auth/callback/route.ts` imported and called `createClient` from `@supabase/ssr`, which doesn't export any such function** — every magic-link/OAuth login completed the provider round-trip and then crashed on this line, and its `setAll`/`remove` were no-ops that wouldn't have persisted the session cookie even if the import hadn't crashed; **`lib/ingest/parser.ts`'s `parsePdf()` called `pdfParse(buffer, {max:0})` as a default-exported function, but the installed `pdf-parse` version exports a `PDFParse` class instead** — every PDF ingest was throwing "pdfParse is not a function". `lib/supabase/server.ts` and `middleware.ts` both had a stray `remove()` cookie method belonging to neither the modern nor deprecated `@supabase/ssr` cookie API shape (verified the real shape directly against the installed package's `.d.ts` files, not guessed) — dead code, removed; `setAll` already covers deletion in the modern API. Capstone: removed `next.config.ts`'s `typescript: { ignoreBuildErrors: true }`. | commits `4662ff4`, `31457b6` | `npx tsc --noEmit` now passes with **zero errors** (was 94, then 56, 41, 35, 33, 14, 7, 4, 0 across the fix passes). `eslint` clean (a few now-stale `eslint-disable` comments left as harmless warnings — the casts they guarded no longer exist). Full `npm run build` succeeds with `ignoreBuildErrors` removed — every route compiles under real type checking. |
| 52 | 2026-07-01 | GitHub | **Real task execution engine — closes Wave 4's biggest remaining functional gap.** New `lib/task-execution-engine.ts`: `executeTask()` resolves the org's configured LLM (reusing `resolveModelConfig`/`callLLMJson` from the BYO model dispatch, #45), asks it for a short 2-4 step plan grounded in the org's actual worker agent roster (real names/domains/tiers), persists the plan to `task_execution_plan`, posts a summary to `task_chat_messages`, and marks the task `completed`. Failures mark the task `failed` with an explanatory system chat message rather than leaving it silently stuck at `pending` forever. Deliberately does not invoke worker agents' underlying logic yet (most global agents' `code_reference` targets MCP tool handlers scoped to a specific compliance item, not a free-text task) — noted honestly in the UI roadmap rather than faked. `api/tasks` POST now creates as `in_progress`, calls `executeTask` synchronously (same pattern as `ai/orchestrate`'s live LLM call), and returns the true final status. Updated `RealAssistantColumn`/`orchestra/page.tsx` copy and added a loading spinner to the add-task button since it now takes a few seconds. | commit `0151093` | `tsc --noEmit` zero errors (full clean project post-#51). `eslint` clean except the same pre-existing `react-hooks/set-state-in-effect` pattern already in this component. Full `npm run build` succeeds. |
| 53 | 2026-07-01 | Supabase + GitHub | Activated Loops 1 (Loop Engineering) and 7 (Input Management) — now 8 of 15 loops active. `lib/loops/loop-engineering-audit.ts` (Loop 1): the meta-loop that observes the other loops — only became buildable once there was real `loop_executions` data to observe. For every active loop, checks whether it produced any executions in the last 7 days and reports average execution time; catches the exact class of bug found manually earlier this session (Loop 10 marked active with zero execution code wired) automatically going forward. Runs last in the cron sequence deliberately, so it observes this same run's activity, not just the prior run's. `lib/loops/input-quality-audit.ts` (Loop 7): audits the last 30 days for compliance items marked `completed` with no acknowledgement/registration number on file, and notices missing their notice number or issuing authority — a data-entry-completeness signal, distinct from Loop 11's completion-rate tracking. Both wired into `api/internal/loops/run`. | migration `wave5_activate_loops_1_7`; commit `bcd4650` | `tsc --noEmit` zero errors (full clean project). `eslint` clean. Full `npm run build` succeeds. |
| 54 | 2026-07-01 | GitHub | **Real worker agent dispatch for read-only global agents** — closes the last honest gap the task execution engine's own comment called out: it planned against real agents but never invoked any of them. New `DISPATCHABLE_TOOLS` allowlist (`get_compliance_stats`, `get_overdue_items`, `list_departments`) — when a plan step's LLM-chosen agent is global-tier and matches, `executeTask` now actually runs it against the org's real data (reimplemented directly via the same Drizzle/`withTenantContext` path the rest of the engine already uses, not the Edge-runtime MCP route's `handleTool` — reusing that would mean importing across an Edge/Node boundary for no real benefit), records a real `task_agent_executions` row, and folds the actual returned data into the task's chat summary. Deliberately still not auto-dispatching write tools or non-global agents — an LLM-generated plan isn't a trustworthy source of write arguments without a human confirming first; those steps are still recorded as real rows, just not auto-invoked. | commit `b64e082` | `tsc --noEmit` zero errors. `eslint` clean. Full `npm run build` succeeds. |
| 55 | 2026-07-01 | Supabase + GitHub | Activated Loops 4 (Knowledge Management) and 5 (Process Management) — now 10 of 15 loops active. `lib/loops/process-turnaround-audit.ts` (Loop 5): measures average compliance-item turnaround (created→completed) per department over the last 30 days, flags departments running >2x the platform-wide average — distinct from Loop 7 (data completeness) and Loop 11 (task, not compliance-item, completion). `lib/loops/knowledge-flow-audit.ts` (Loop 4): audits `knowledge_flow_log` for upward flows not marked anonymized — an unanonymized upward flow would mean one customer's specifics leaking into what other customers' agents learn from, exactly what the master spec's "knowledge flows up anonymized, never sideways" rule exists to prevent; complements Loop 12's access-level audit with a knowledge-propagation-level check. Nothing writes to `knowledge_flow_log` yet, so this honestly reports zero flows until something does — the correct signal, not a bug, and the check is ready the moment real knowledge propagation exists. Both wired into `api/internal/loops/run`. | migration `wave5_activate_loops_4_5`; commit `74111a3` | `tsc --noEmit` zero errors. `eslint` clean. Full `npm run build` succeeds. |
| 56 | 2026-07-01 | Supabase + GitHub | **Ingestion→tasks bridge + Loop 13 (Data/Process Separation) — now 11 of 15 loops active.** `api/ingest/[batchId]/confirm/route.ts`: confirming a batch now also inserts a real `tasks` row (title, description, status, a system chat message with actual import counts) inside the same transaction — deliberately additive, not a replacement; the `ingestion_batches`/`ingestion_items` staging pipeline's own behavior is completely unchanged. This is the safe subset of "generalize ingestion into the tasks model": visibility in `/orchestra` for work that already happened, not a rewrite of how ingestion itself works — full generalization (an ingestion batch literally *being* a task from the start) remains deferred. `lib/loops/tier-integrity-audit.ts` (Loop 13): on reflection this is genuinely distinct from Loop 12, not a duplicate as previously assessed — Loop 12 does live query-level access proof (can org A actually read org B's data through the real path); Loop 13 checks the *data model's own structural integrity* for the tier system Loop 12 depends on (every `worker_agents` row's tier must match its scoping columns exactly — a `tier='global'` row with a leftover `org_id`, or `tier='customer'` with no `org_id`, would silently break the separation Loop 12 verifies). Catches that class of drift before it causes a leak, not after. | migration `wave5_activate_loop13`; commit `04eea3a` | `tsc --noEmit` zero errors. `eslint` clean. Full `npm run build` succeeds. |
| — | 2026-07-01 | Note | **Final, explicit statement on the 4 loops still inactive and why — these are not oversights.** Loop 2 (Self-Coding) and Loop 6 (Prompt Management): these are the two loops the master spec itself groups as needing the others to "have a track record" first (Wave 5 plan step 5.5). Beyond that: this codebase's cron-triggered loops run as stateless Vercel serverless functions with no live git/filesystem access to the deployed source and no static-analysis or prompt-versioning infrastructure — building these for real (not hollow) would mean inventing a code-review-proposal pipeline and a prompt-versioning system from scratch, which is new, disconnected infrastructure far outside "activate a loop," and Self-Coding specifically implies the system eventually *deploying its own code changes*, a categorically different and higher-risk capability than every read-only audit built in this wave. Not building it is a considered safety boundary, not a shortcut. Loop 3 (UI/UX): investigated properly this time — even the `login`/`logout` values already sitting unused in the `audit_action` enum are never written by any code path in this app (checked directly, not assumed), so there's no real interaction telemetry to observe at all; building this loop for real means building a client-side analytics pipeline, not activating existing data. Loop 15 (Everything by AI): its own seed description is explicit that this is "the aspirational end-state loop," a maturity marker for the whole system, not a loop meant to carry execution logic — giving it fake logic would be hollow, not real. Separately, also holding the line on **auto-dispatching worker agents' *write* actions** (create/update real compliance data) from an LLM-generated task plan — read-only dispatch (change log #54) is safe because a wrong query just returns wrong-but-harmless data; a wrong write would be a real, possibly customer-visible mistake with no human in the loop. This remains a deliberate boundary regardless of how many more loops get activated. |
| — | 2026-07-01 | Sign-off | **Project confirmed complete by the user.** Presented the exact same list above (4 inactive loops + reasons, the write-dispatch boundary, and the external pooler issue) directly and asked what to do next rather than guessing a 6th time. Answer: "This is done." No further feature work is planned. Summary of the final state: Waves 0-3 fully complete and live; Wave 4 (Task System, BYO model dispatch, real worker agent dispatch for read-only agents, ingestion→tasks bridge) complete within its deliberately-scoped boundaries; Wave 5 at 11 of 15 self-improvement loops active, the remaining 4 held back on purpose (see the note above); Wave 6 hardening complete including a full pre-existing TypeScript backlog cleanup (94→0 errors) that surfaced and fixed several genuinely severe live bugs along the way (broken magic-link login, broken PDF ingest, broken comment posting, a silent NOT-NULL violation on compliance-item creation). The one open item that isn't a scope decision: **BUG-002**, the Supabase Supavisor pooler outage, still unresolved as of the last check — needs the user's direct attention with Supabase (dashboard pooler logs or support), not further app code. |
| 57 | 2026-07-02 | GitHub + Supabase + Vercel | **Wave 7 (NEW scope, see the Update note at the top of this file): Account/Client hierarchy foundation, unified audit log, cost & dispatch evidence tracking.** Requested by the user directly, separate from the Wave 0-6 sign-off above — the goal is closing the gap between this app's GRC coverage (compliance items/notices/challans/audit points only) and a much broader GRC platform mockup designed in a parallel session (Governance, Company Secretarial, Legal, HR, Sector Regulators, Risk, Audit, ESG, Incidents, Access/Approval). This wave is the foundation those future module waves build on, not the modules themselves. **CRITICAL FIX FIRST:** `withTenantContext` (`lib/db/tenant-scoped.ts`) used `SET LOCAL app.current_org_id = ${value}` via postgres.js's parameterized `sql` tag — `SET LOCAL` does not accept bind parameters, only literals, so every call has thrown a `42601` syntax error since Wave 1 introduced it. Empirically confirmed via a direct `PREPARE p(text) AS SET LOCAL x = $1` test. Fixed to `SELECT set_config('app.current_org_id', ${value}, true)`, verified round-trips correctly. Committed and deployed standalone before the rest of this wave, since every other change here depends on it actually working. **Account/Client hierarchy:** added `organisations.accountType` (company/ca_firm/legal_firm/consultant); discovered `branches`/`clients`/`client_entities`/`user_client_access` already existed in `schema.ts` with live RLS (`pg_policies` confirmed) but zero API routes, zero UI, and no migration file in the repo (pure drift — applied directly in an earlier session, never committed as `.sql`) — `clients` had exactly 1 backfilled row, `client_entities` had 0. Finished wiring it: new `/api/clients`, `/api/clients/[id]/entities`, `/api/clients/[id]/access` routes, a `/clients` page (shown only for non-`company` accounts), and a retroactive migration recreating the already-live tables/policies idempotently plus backfilling the missing `client_entities` row per client. Also fixed `hasRole()`/`ROLE_RANK` in `auth-guard.ts`: it only recognized the original 4 roles, so all 6 Wave 1 hierarchy roles (including `veridian_admin`, meant to be the *most* privileged) resolved to rank 0 and failed every permission check — a real, live authorization bug, not just a missing feature. **Unified audit log:** new `src/lib/audit.ts` `logActivity()` is now the single call site every route uses (replaced 13+ inline `auditLogs.values()` calls). Added `orgId`, `clientId`, `actorName`/`actorRole` (denormalized snapshots — survive the actor being renamed/deactivated later), `userAgent` (`ipAddress` existed but was never populated). `action` changed from a fixed 10-value Postgres enum to free text, so new verbs (`view`, `payment_recorded`, `dispatch_recorded`, and whatever future GRC modules need) don't require a schema migration each time. `REVOKE UPDATE, DELETE ON audit_logs FROM app_runtime` — true DB-level append-only enforcement, confirmed via `information_schema.role_table_grants` (app_runtime has only INSERT/SELECT). Document `GET` now logs a `view` action — real usage logging, not just create/update/delete. **Cost tracking:** `compliance_costs` (government fees, consultant charges, penalties) + append-only `cost_payments` ledger — `paymentStatus`/`amountPaid` are always recomputed from real payment rows, never hand-set, so "we paid this" is provable, not just claimed. Verified end-to-end with a real partial-then-completing-payment test (₹400 + ₹600 against a ₹1000 obligation → `partially_paid` → `paid`, then cleaned up). **Notice dispatch evidence:** `notice_dispatches` — tracking number, courier, dispatch/delivery-confirmed dates, linked proof document; "a reply was filed" becomes a provable claim. **Document storage:** discovered `documents.noticeId`/`extractedData` were declared in `schema.ts` but never existed live, and `complianceItemId` was wrongly `NOT NULL` (making a notice-only or general document literally impossible to insert, not just RLS-invisible) — fixed all three, plus added `documents.orgId` (the old RLS policy only covered rows with `complianceItemId` set). Built a real Supabase Storage upload/signed-URL-download pair (`POST /api/documents`, `GET /api/documents/[id]`) — **this repo had no working file upload at all before this wave**, despite 18 seeded `documents` rows with fake URLs. **Also fixed:** `organisations` was missing `address`/`cinNumber`/`gstin`/`panNumber`, columns `PATCH /api/me` had referenced since before this session — every admin settings save touching org details was throwing a raw "column does not exist" error. Added indexes on every new table's `org_id` + FK lookup columns (the Supabase performance-advisor tool itself errored on an unrelated internal query when checked, so these were added from known query patterns rather than its output). | migrations `wave7_hierarchy_and_audit_foundation_v2`, `wave7_organisations_missing_columns`, `wave7_new_table_indexes`; commits `c4683f4` (critical fix), `9752735` (main wave), `ccc2bb7` (docs) | `npx tsc --noEmit` zero errors. Full `next build` succeeds (all 70+ routes compile, including every new one). `get_advisors` security clean on all `compliance.*` tables (all pre-existing findings are in an unrelated `public` schema). Live-DB verification: hierarchy backfill confirmed (client_entities row created for the existing org), audit_logs backfill confirmed (all 20 pre-existing rows have real org_id/actorName/actorRole), documents backfill confirmed (all 18 rows have org_id), REVOKE confirmed via `role_table_grants` (app_runtime has no UPDATE/DELETE on audit_logs or cost_payments), payment-ledger recompute logic verified with a real test-and-cleanup. Deployed to production (`dpl_2FGTCh1V5xm1VynBb6BwFDpNGnZw`), `/api/health` returns 200, `get_runtime_errors` clean in the 10 minutes following deploy. |
| 58 | 2026-07-02 | GitHub + Supabase + Vercel | **Wave 8: full GRC module breadth — brings this app's module coverage up to match the design-mockup session's complete GRC platform scope, built directly on Wave 7's foundation.** Requested by the user immediately after Wave 7 was confirmed complete: "complete everything all modules and the complete project in local... then publish." 37 new tables (9 new enums: `boardMeetingTypeEnum`, `boardMeetingStatusEnum`, `rptApprovalStatusEnum`, `policyStatusEnum`, `approvalRequestStatusEnum`, `litigationStageEnum`, `riskCategoryEnum`, `riskStatusEnum`, `incidentStageEnum`), ~50 API routes, 30 pages, across 10 new sidebar groups. **GOVERNANCE:** Board & Governance (real meetings/minutes — amending a minute never overwrites, it appends a new version), Committees, Related Party Transactions (module-level gate: `classification='board_only'`, invisible outright to anyone below board clearance, not just redacted), Delegation of Authority, Director & KMP Register, Board & Director Evaluation (Companies Act Schedule IV / SEBI LODR Reg 17(10) — stage cycle with full history retained, never overwritten), Policy Management with a real generic maker-checker (new `approval_requests` table — requesting publish sets a policy to `under_review`; the actual `status='published'` flip only happens inside `PATCH /api/approvals/[id]`'s approve branch, never at request time — Policy is the reference implementation, reusable by any future module needing the same two-person-rule pattern). **COMPANY SECRETARIAL:** Statutory Registers (read-only aggregator view over Directors/Charges/Cap Table — one screen, not a fourth copy of the data), Cap Table + corporate-action events, Charges (ROC), Secretarial Audit, MCA e-Filing (same honesty rule as the pre-existing `mcaFilings` precedent: prepare/track/SRN only — no code path files anything with the government). **LEGAL:** External Counsel & Vendors, Litigation & Disputes (stage-advance lifecycle), IP Portfolio, Legal Opinions. **PEOPLE & HR:** Payroll & HR Statutory Compliance, Leave & Holiday Compliance, POSH (module-level `confidential` gate — committee composition, case status/dates only, complaint *content* is never a stored column or logged, matching the mockup's confidentiality principle). **RISK:** Risk Register with scope-based visibility — `BROAD_SCOPE_ROLES` (admin/veridian_admin/branch_manager/senior_professional/manager) see all org risks, everyone else sees only risks matching their own department or where they're the named owner. **SECTOR REGULATORS:** SEBI/RBI/IRDAI, each gated by `organisations.regulatoryEntityType` (listed_company/bank_nbfc/insurer/general) via a new `SectorGateNotice` component — shown as an honest "not applicable to your entity type," never force-fit, admin-editable in Settings. **AUDIT:** Controls & Framework Library (pillar-grouped, COSO/NIST/PCI-DSS/HIPAA joining the pre-existing ISO27001/SOC2), Audit Management (engagements + findings/CAPA). **THIRD-PARTY & ESG:** Vendor & Third-Party Risk, ESG/BRSR — `GET /api/esg` computes its Social-pillar metrics (POSH resolution rate, POSH policy attestation) live from the real `poshComplaints`/`policies` tables at read time rather than storing a second hand-entered copy that could drift from the source of truth ("graph, not islands," matching the mockup's One Truth principle). **INTEGRITY:** Whistleblower & Ethics (module-level `confidential` gate, same content-never-stored rule as POSH), Business Continuity (BCM), Contract Compliance. **INCIDENTS:** 7-stage lifecycle (logged→triaged→investigating→contained→notified→remediated→closed) rendered as a Kanban board, per-record classification (Security/Data Breach categories auto-classify `confidential`, everything else `department`), `regulatoryNotifyRequired`/`notified`/`notifyDeadline` tracked as fields distinct from stage (an incident can be fully remediated and still be non-compliant on notification), and a `flag_as_risk` action that creates a real Risk Register row with a genuine FK link — not a note that goes stale. New `src/lib/classification.ts` (`canAccess(role, classification)`) adapts the mockup's 5-level access model (public/company_wide/department/confidential/board_only) onto the real 10-role system via a `ROLE_CLEARANCE` map, applied consistently across RPT/Board minutes/POSH/Whistleblower/Incidents. New `SimpleModulePage`/`StatusPill` reusable components carried ~18 of the simpler modules (list + inline add-form + status pill) without hand-building the same CRUD scaffold 18 times. | migration `wave8_full_grc_module_breadth`; commit `9bee808` | `npx tsc --noEmit` zero errors across all new modules. Full `next build` succeeds (100+ routes compile). Supabase `list_tables` confirms all 37 new tables present with RLS enabled; `get_advisors` security clean. Deployed to production (`dpl_HCvHgrEBwEWzmny3f2tToiTCRheQ`), reached `READY` in ~95s, `/api/health` returns 200, `get_runtime_errors` clean in the 30 minutes following deploy. **Honest scope trims, not oversights:** given the sheer breadth requested in one pass, module depth is uneven by design — some modules (Board, Incidents, Litigation, POSH, RPT) got full custom pages with stage/lifecycle actions, while ~18 simpler ones got the generic `SimpleModulePage` list+form pattern rather than a bespoke UI each; the Controls & Framework Library page has no "add control" form yet (frameworks/controls exist and are seeded-viewable, not yet editable from the UI); and the new cost/receipt-evidence columns already on Wave 7's `complianceCosts`/`costPayments`/`noticeDispatches` (`receiptDocumentId`/`proofDocumentId`) have no UI wiring yet on any Wave 8 module — attaching a document to, say, a litigation matter or a secretarial audit finding would need explicit follow-up work if the user wants that across the new breadth too, not just on the original cost-tracking tables. |

**Update (2026-07-03): Waves 9-10 — new scope, per PLATFORM_STRATEGY.md.** The user's direction shifted from "complete this GRC product" to "turn VERIDIAN AI into a platform other surfaces (mobile, ChatGPT, Claude connector, white-label resellers, custom client apps) can build on" — see `PLATFORM_STRATEGY.md` at repo root for the full strategy and a 7-wave (9-15) implementation plan, approved via plan mode. Waves 9-10 close the first, most foundational gap identified in that plan: there was no single, real, working external credential — `apiKeys` generated keys nobody validated, and `/api/mcp` used a totally separate, disconnected token system. Both are now unified.

| 59 | 2026-07-03 | GitHub + Supabase | **Wave 9: unified API-key auth.** `apiKeys` (`vk_...`, SHA-256 hashed, `read`/`write` scopes) has existed since M-03 but nothing ever validated one on an incoming request — Settings > API Keys was a pure stub. New `src/lib/api-keys.ts` (hoisted `hashSHA256`/`generateApiKey`, shared by the settings route and the new validator so the hash algorithm can't drift), `src/lib/supabase/api-key-auth.ts` (`validateApiKey(request)`), `requireAuthOrApiKey(request)`/`hasScope()` in `auth-guard.ts` (tries session cookie first, falls back to the API key; a real session always has full access, scopes are an API-key-only axis). `logActivity()` (`src/lib/audit.ts`) now takes a discriminated `dbUser`/`apiKey` actor so an API-key-driven write still produces a real, attributable audit row ("API Key: <name>", role `api_key`) instead of crashing. Schema: `audit_logs.user_id` is now nullable with a sibling `api_key_id` FK — exactly one is set per row, `actor_name`/`actor_role` stay `NOT NULL` and denormalized either way. Fixed 3 read sites (`compliance/[id]`, `compliance/stats`, `notices/[id]`) that assumed `log.user` was always present. | migration `wave9_unified_api_key_auth`; commit `d4a71b5` | `npx tsc --noEmit` zero errors. `next build` compiles clean (page-data collection fails locally only for lack of a local `DATABASE_URL`, not a code defect — Vercel's build has it). Live-verified: a real generated key resolves to the correct org via a direct SQL-level proof (insert test key → confirm lookup → confirm garbage hash returns nothing → clean up). Deployed (`dpl_Cc2Zf6jmVgR6FDgHWMPmLJy1n7u9`), `/api/health` 200, `get_runtime_errors` clean. |
| 60 | 2026-07-03 | GitHub + Supabase + Vercel | **Wave 10: retire `mcp_access_codes`, repoint `/api/mcp` at the unified `apiKeys` — and along the way, found and fixed a severe, previously-undiscovered production bug: `/api/mcp` had never actually been reachable, for any of its 7 tools, since the route was first built.** `resolveToken()` in `api/mcp/route.ts` now hashes the incoming Bearer token and looks it up against `api_keys` instead of `mcp_access_codes` (same Edge-compatible raw Supabase-JS client, Edge runtime can't use the Drizzle/postgres.js path); write tools (`create_compliance_item`, `update_compliance_status`) now require a `write`-scoped key. `api/mcp/tokens` (the old token-minting route): `POST` now returns `410` pointing at `POST /api/settings/api-keys` instead of minting a now-useless token; `GET`/`DELETE` still work for any legacy token. `mcp_access_codes` marked `@deprecated` in `schema.ts`, not dropped. `MCP_PROTOCOL.md` corrected throughout: the documented endpoint was the *wrong, dead* domain (`compliance-tracker-ai.vercel.app` — a duplicate project deleted back in the Wave-0-era infra cleanup, see the note near the top of this file) instead of the actual live one (`veridian-compliance-ai.vercel.app`), and the auth/setup sections rewritten for the unified key. **Then, doing this wave's own required live functional proof (not just the happy-path check) surfaced two real, independent, pre-existing bugs that meant MCP had silently never worked for anyone, ever:** (1) `service_role` — the credential `/api/mcp`'s admin Supabase-JS client authenticates as — had **zero SQL grants anywhere in the `compliance` schema**, confirmed directly (`SET ROLE service_role; SELECT * FROM compliance.api_keys` → `42501: permission denied for schema compliance`); `rolbypassrls=true` only bypasses RLS, a completely separate privilege system from GRANT/REVOKE, and no default privilege for `service_role` had ever been set (only `app_runtime`'s existed). Fixed via migration `wave10_grant_service_role_compliance_schema`: `GRANT USAGE`+`SELECT/INSERT/UPDATE/DELETE` to `service_role` on the schema and all its tables, plus `ALTER DEFAULT PRIVILEGES` so future tables inherit it automatically. (2) Even after that fix, still failed — the `compliance` schema was never in Supabase's PostgREST "exposed schemas" list at all (`db_schema: "public,graphql_public"`, a project-level Data API setting, not a SQL grant, confirmed via the Management API `GET .../postgrest`) — the real error the whole time was `PGRST106: Invalid schema: compliance`, but `resolveToken()` previously discarded Supabase's `error` entirely (`const { data } = await sb...`), silently swallowing it into a generic 401 regardless of who called it or why. Fixed by adding `compliance` to `db_schema` via `PATCH /v1/projects/{ref}/postgrest` — confirmed safe to expose since RLS is `FORCE`-enabled on every compliance table and `anon`/`authenticated` have no grants there either, so only `service_role` (a secret, server-only credential) gains anything. `resolveToken()` now logs unexpected Supabase errors (anything but `PGRST116`, the normal shape of "no matching key" for a garbage token) instead of silently discarding them — this is what should have surfaced the schema-exposure issue on day one. | migrations `wave9_unified_api_key_auth` (Wave 9, listed above), `wave10_grant_service_role_compliance_schema`; commits `1cf963b` (route/token retarget), `e026837` (diagnostic logging, later refined), `d90b226` (root-cause fixes + finalized error handling) | `npx tsc --noEmit` zero errors throughout. Live-verified end-to-end against production with real generated keys, after the fix, not just before it: a `read,write`-scoped key's `get_compliance_stats` call returns real data (`{"total":18,"overdue":3,...}`); a garbage token still gets a clean `401`; a `read`-only key correctly gets a JSON-RPC error attempting `create_compliance_item`. Test keys created and cleaned up via Supabase MCP `execute_sql`. Deployed (`dpl_DoKECKXrkDT6P9Sd4xnn74CN2Haf`), `get_runtime_errors` clean. **Honest framing:** this was not a regression introduced by this wave — it's a bug that predates it, only found because this wave's plan explicitly required a live functional proof rather than trusting the code looked right. |
| 61 | 2026-07-03 | GitHub + Vercel | **Wave 11: service layer + `/api/v1` + OpenAPI + expanded MCP coverage — code complete, typechecked, and deployed clean, but live-functional verification is blocked by the Supavisor pooler bug recurring (see the 🔴 section at the top of this file for the full diagnosis; not a Wave 11 defect).** Scope discipline per the plan: only the 3 highest-traffic domains named in `PLATFORM_STRATEGY.md` — compliance, tasks, notices — not all ~40 modules. New `src/lib/services/{compliance,task,notice}-service.ts`: pure functions taking a `{orgId, dbUser?}` context, each owning its own `withTenantContext` call, returning plain data (no `NextResponse` inside the service layer). Legacy `src/app/api/{compliance,tasks,notices}/**` routes rewritten as thin wrappers (`requireAuthOrApiKey()` → parse → call service → `NextResponse.json`) — behavior-identical refactor, not a rewrite. New `src/app/api/v1/**` route group re-exports the same service calls as the stable external contract, every route gated by `requireAuthOrApiKey()`/`requireRoleOrScope()` (never session-only) — internal `(app)/` UI still calls the legacy unversioned paths, no frontend forced to change. New `src/lib/schemas/{compliance,tasks,notices}.ts` (zod) feed both service-layer input validation and `src/lib/openapi/generate.ts` + `GET /api/v1/openapi.json` (OpenAPI 3.1, generated via zod v4's native `z.toJSONSchema()` rather than adding a new dependency). MCP expanded from 7 to 9 tools: `list_notices`/`get_task_status`, seeded as new `global`-tier `worker_agents` rows (migration `wave11_seed_expanded_mcp_tools`) — since `/api/mcp` runs on Vercel Edge (can't use the Node-only `postgres.js` driver the service layer depends on), these 2 new tools call `/api/v1/*` via internal `fetch()` (`getApiV1BaseUrl()`) rather than importing the service layer directly, unlike the original 7 tools' raw-Supabase-JS approach — confirmed this Edge constraint before committing further tools to the pattern, per the plan's own flagged risk. **Caught and fixed a real correctness issue during my own review, before shipping:** the first draft of the write routes used `hasScope(ctx, "write")` alone, which would let any logged-in session through regardless of role, silently dropping the original per-route `requireRole` gates (e.g. compliance POST/PATCH previously required `member`, DELETE required `manager`). Added `requireRoleOrScope(ctx, minimumRole, writeScope)` — preserves the exact original role gate for session callers, gates API-key callers on scope — applied to every write route so no route became more permissive than it was before this wave. | migrations `wave11_seed_expanded_mcp_tools`; commits (service layer + `/api/v1` + OpenAPI + MCP expansion, see repo history for exact hashes) | `npx tsc --noEmit` zero errors. `eslint` clean. `next build` compiles/typechecks clean (page-data collection fails only for lack of a local `DATABASE_URL`, the same pre-existing local-only limitation noted in earlier waves, not a code defect). Deployed (`dpl_HEHdXpSD1Kcj78D85fsBdRFrTScn`), zero new runtime error clusters in the following window. **Verification status, stated honestly:** `GET /api/v1/openapi.json` confirmed working (no DB dependency). Legacy-vs-`/api/v1` parity, both new MCP tools, and write-scope gating could **not** be confirmed live — every attempt hit the recurring pooler `ENOTFOUND` failure, reproduced identically on both the legacy and `/api/v1` compliance routes, proving the block is infrastructure-wide and pre-existing rather than something wrong with this wave's code. Per the user's own instruction, Wave 12 was explicitly not started pending this check-in. This wave's service-layer/`/api/v1` work should be treated as code-complete-but-not-yet-live-verified until the pooler issue clears and the full verification protocol can actually run. |
| — | 2026-07-03 | Note | **Stopgap attempted and reverted, cleanly.** Asked to apply the direct-connection stopgap for the pooler bug "now," `DATABASE_URL`/`APP_RUNTIME_DATABASE_URL` were switched to `db.pcrjmlpuqsbocqfwoxod.supabase.co:5432` and redeployed. This surfaced a *different, more fundamental* problem: `getaddrinfo ENOTFOUND db.pcrjmlpuqsbocqfwoxod.supabase.co` from Vercel's own runtime. Confirmed via `Resolve-DnsName` that this hostname has **only an AAAA (IPv6) record, no A (IPv4) record** — Supabase's 2024 policy for direct connections (IPv4 requires their paid add-on), and Vercel serverless functions have no outbound IPv6 path. Confirmed the pooler itself fails identically on both transaction-mode (6543) and session-mode (5432) — same `tenant/user not found` signature — so this isn't a mode/port issue either. Reverted `DATABASE_URL`/`APP_RUNTIME_DATABASE_URL` back to the original pooler strings and redeployed; confirmed production is back to its exact pre-existing documented state (same error signature, no regression). The two real remaining paths — Supabase IPv4 add-on (paid, ~$4/mo, would actually work since it sidesteps Supavisor entirely) or escalating to Supabase support for the Supavisor tenant registration itself (free, no ETA) — were presented to the user; the answer was to proceed on Waves 12-15 without live DB verification for now, clearly marked as such, and revisit once the pooler is actually fixed by one of those two paths. |
| 62 | 2026-07-03 | GitHub + Supabase | **Wave 12: Chat + instruction-tracking backend.** New `conversations`/`conversation_participants`/`messages`/`instruction_commitments`/`instruction_mismatch_detections` tables (migration `wave12_chat_and_instruction_tracking`). RLS: conversations/participants/messages are scoped via a new `compliance.is_conversation_participant(conv_id)` SECURITY DEFINER function rather than a naive self-referential policy on `conversation_participants` (a known Postgres RLS footgun — querying a table from its own policy can force the planner back through the same policy on the subquery's scan of that table; the SECURITY DEFINER function bypasses RLS for just that one internal membership check, matching Supabase's own recommended pattern for this exact shape). `instruction_commitments` is scoped to its two named parties (assigner OR assignee), narrower than plain org-scoping — an instruction between two people isn't the whole org's business. `instruction_mismatch_detections` is assigner-only, enforced at the DB layer, matching the product rule that "did they actually do what I asked" judgments never reach the assignee. New `src/lib/services/chat-service.ts`: conversations/messages/read-receipts, plus the **first real call site for the User Assistant OA layer** — seeded since Wave 4 but dormant until now, since no code path had ever invoked it; the pinned AI thread gets a real LLM reply via `resolveModelConfig(orgId, "user_assistant_oa")` + `callLLM`, not a mock. New `src/lib/loops/instruction-mismatch-audit.ts` + `/api/internal/instruction-audit/run` cron (deliberately standalone, not folded into the Wave 5 loop taxonomy — this is a product feature, not a platform self-improvement loop): for overdue `pending` commitments, pulls the assignee's real task/audit-log activity and asks the org's `task_oa` model to judge match vs. drift, marking `done_as_asked` or `drifted` + a mismatch row. **Caught a real correctness bug in my own draft before shipping:** the AI's own reply insert into `messages` needs `current_user_id()` set to a real conversation participant (the human, not the AI) to pass `is_conversation_participant()` at all — an early draft passed only `orgId` to `withTenantContext`, which would have made every AI reply silently fail its own RLS check. | migration `wave12_chat_and_instruction_tracking` (later amended with 4 missing FK indexes, see below); commit `851b068` | `npx tsc --noEmit` and `eslint` both zero issues. Live-verified the RLS/SECURITY DEFINER logic directly at the DB level (app-level verification blocked by the pooler bug, not by this wave's code): inserted a real conversation/participants/message/commitment/mismatch row, confirmed `is_conversation_participant()` returns `true` for both real participants and `false` for a non-participant, confirmed the assigner-only mismatch policy's qual evaluates correctly for assigner vs. assignee, confirmed `pg_policies` shows the exact intended quals on all 5 tables — then cleaned up all test rows. `get_advisors` (security) clean on everything added. Deployed (`dpl_9zGk7CDv5jAujoDNxzscGmsXFr3R`), zero new runtime errors. |
| 63 | 2026-07-03 | GitHub | **Wave 13: Chat Page UI.** New `/chat` page (two-pane: `ConversationList` + `ThreadView`), pinned VERIDIAN AI thread always first, "Assign as instruction" toggle with a due-date picker on the compose bar, `MismatchBubble` rendered inline (AI-styled, bot icon, never confusable with a human message) with one-tap Nudge/It's-fine wired to `PATCH /api/instruction-mismatches/[id]`. Sidebar gets a new top-level "Chat" item above the module sections with a real unread-count badge. `chat-service.getMessages()` extended to surface inline mismatch info per message (RLS-backed: a non-assigner's query simply returns none, same guarantee as the DB layer). **Deliberate deviation from the plan's own wording:** the plan assumed `@tanstack/react-query`'s `refetchInterval` since it's "already a dependency" — checked and it's listed in `package.json` but never actually wired into the app anywhere (no `QueryClientProvider` exists in the tree). Used this codebase's actual, consistent convention instead (plain `fetch` + `useState` + `setInterval`, matching every other page) rather than introducing new client-state infrastructure for one feature. **Fixed a real bug caught via `eslint`'s `react-hooks/set-state-in-effect` rule** (not pre-existing noise this time — a genuine synchronous `setLoading(true)` call directly in an effect body): switched `ThreadView` to key on `conversation.id` from the parent so a conversation switch remounts fresh (`loading` starts `true` via initial state, never needs a synchronous reset) — the officially-recommended React pattern for this exact "reset state on identity change" shape, not a workaround. | commit `282b886` | `npx tsc --noEmit` and `eslint` both zero issues after the fix. `next build` compiles/typechecks clean (same pre-existing local-only `DATABASE_URL` limitation for page-data collection). Deployed (`dpl_9hYKyfZ6bUCB63tp2arojKc7Dubj`), zero new runtime errors. |
| 64 | 2026-07-03 | GitHub + Supabase | **Wave 14: instruction mismatches reach the user proactively.** New `notification_type` enum value `instruction_mismatch` (`ALTER TYPE ... ADD VALUE`, applied as its own statement, matching Wave 1's enum-extension precedent) + a generic `notifications.metadata` jsonb column (avoids a bespoke FK column per notification type). `instruction-mismatch-audit.ts` now inserts a real notification for the assigner on every mismatch, carrying `{conversationId, mismatchId}`. **Found and fixed a real, pre-existing gap while implementing this:** `AppTopbar`'s notification bell already fetched the full notifications list from `GET /api/notifications` but only ever read `unreadCount` off it and discarded the list — the bell was a single generic redirect with a count, never actually showing what the notifications were. Rebuilt it as a real dropdown listing notifications, routing per type: `instruction_mismatch` opens `/chat?conversation={id}&highlight={mismatchId}` at the exact message, everything else keeps the prior generic overdue-items default. `ThreadView`/`/chat` read the `highlight` param and scroll that specific mismatch bubble into view once, instead of the usual scroll-to-bottom. | migration `wave14_instruction_mismatch_notifications`; commit `5a523cc` | `npx tsc --noEmit` and `eslint` both zero issues. `next build` compiles/typechecks clean. Deployed (`dpl_GjUHRAVciqdW72eaeByXbfpY73q6`), zero new runtime errors. |
| 65 | 2026-07-03 | GitHub + Supabase | **Wave 15: Home Page restructure — universal To Do/Analytics/Approval.** New `/home` page, identical 3 tabs for every rank; only the CONTENT inside each varies by role, never the tabs themselves. New `tasks.assigned_by_id` (nullable, backfilled to `assigned_by_id = user_id` for every pre-existing self-assigned row — confirmed 0 rows missed the backfill) is what makes "assigned to me" vs. "assigned by me" distinguishable at all; `listMyTodos()`/`listAssignedByMe()` added to `task-service.ts`. **Analytics tab is a real, role-branched rollup, not a single shared view with a label swap:** new `getAnalyticsRollup()` derives scope from the caller's own rank (`ROLE_RANK`, now exported from `auth-guard.ts`) server-side — never a client-supplied scope — branch_manager+ get the existing rich `/dashboard` content verbatim (renamed to `DashboardAnalytics`, reused rather than rebuilt since it was already correct for that tier), manager/senior_professional get a real team rollup scoped to their direct reports via `reportingToId` reverse-lookup, everyone else gets their own individual pace. Approval tab extracted into a shared `ApprovalTab`, used by both `/approvals` and Home. `/dashboard` now redirects to `/home` for continuity; sidebar promotes Home alongside Chat as the two top-level items (folding in the old standalone Dashboard entry); post-login/auth-callback redirects and `middleware.ts`'s protected-route allowlist updated from `/dashboard` to `/home` — **also added `/chat` to that allowlist**, which Wave 13 introduced but never added (a real gap from that wave, fixed here since this file was already open). Removed an unrelated pre-existing bug found in passing: the old dashboard rendered `<OnboardingChecklist />` a second time on top of `AppShell`'s global one. **Honest scope trim:** task creation still only self-assigns — no UI yet to assign a new task to someone else; the schema/service support is there for it, not silently faked. | migrations `wave15_tasks_assigned_by`, `wave12_missing_fk_indexes` (4 FK-covering indexes `get_advisors` flagged during this wave's final verification pass — added retroactively to the Wave 12 migration file for history accuracy); commit `36ac5a1` (+ `c754b64` for the index fix) | `npx tsc --noEmit` and `eslint` both zero issues across every file touched in Waves 9-15 (confirmed by lint-checking that exact file list directly — the 14 errors a full `eslint .` reports are all pre-existing, in vendor/scaffold files like `carousel.tsx`/`use-mobile.ts` never touched by this work). `next build` compiles/typechecks clean. `get_advisors` (security) clean on everything added across Waves 12-15. Deployed (`dpl_2e5Rqr3aKWzTeJyVz22PwACJEigg`, then `dpl_7YGqataizQ9keZft9NZ3RLav9roh` after the index fix), zero new runtime errors across the full session. |
| — | 2026-07-03 | Note | **All 7 planned waves (9-15) are now code-complete and deployed.** Re-confirmed the pooler bug one final time before closing out (`first=2026-07-02T15:06:01Z`, `last=2026-07-03T13:30:03Z` — spans the entire day's work, not self-resolved): a fresh real API key against production `/api/v1/compliance` still hits the identical `(ENOTFOUND) tenant/user postgres.pcrjmlpuqsbocqfwoxod not found`. Everything built across Waves 11-15 that depends on `withTenantContext` (which is nearly everything with real data — compliance/tasks/notices' `/api/v1` surface, and all of Chat/instruction-tracking/Home) is code-complete, typechecked, lint-clean, deployed with zero new runtime error clusters, and had its RLS/logic correctness verified directly at the database level wherever app-level verification was blocked — but **full live end-to-end proof through the actual HTTP surface remains outstanding** until the pooler is fixed via one of the two paths already presented to the user (Supabase IPv4 add-on, or a support escalation). This is the single open item blocking a complete "done" sign-off, and it is infrastructure, not application code. |
| — | 2026-07-03 | Update | **New scope: the VAIOS Master Constitution.** The user pasted a full governance-model document (now `PLATFORM_STRATEGY.md` §10, verbatim) defining a 4-layer AI authority hierarchy (Platform/Product-Enterprise-Personal) plus 12 refinement concepts (Worker Agent Library ownership, Automatic Worker Creation, Discovery, Learning Loop, Layer 4 Quality Approval, Scope-Limited Creation, Shared AI Resource Pool, Purpose-Bound Intelligence, Digital Workforce, etc.) and an "AI DNA" principle. A rigorous gap analysis against the live schema/code (`PLATFORM_STRATEGY.md` §11) found two pre-existing "4-layer" systems (`orchestraLayers` cost-tiering, `workerAgents.tier` ownership-scoping) neither of which is this governance hierarchy — no L2 "Product/Project" analog existed anywhere, and no code-change-request workflow existed at all. Three explicit user decisions shaped the resulting Waves 16-19 plan (approved via plan mode): Shared AI Resource Pool is org-to-**platform** only, never org-to-org; Purpose-Bound AI uses a system-prompt clause + hard tool allowlist; the two large structural gaps (code-change governance, Product/Project layer) get a real, scoped first slice now rather than a roadmap-only note. |
| 66 | 2026-07-03 | GitHub + Supabase | **Wave 16: Worker Agent Governance (constitution §4, refinements #1/#2/#4/#5/#6/#7).** `worker_agents` gains a real `lifecycle_status` state machine (`draft/proposed/approved/published/retired` — `is_immutable` left exactly as-is, additive not replaced, since it's a live boolean already read by the dispatch gate) plus `supervisor_worker_agent_id` (Digital Department grouping, self-FK) and `proposed_by_id`. No new proposal table — reuses `approval_requests` exactly as Wave 8's Policy-publish flow already does (`requestType='worker_agent_proposal'`). New `worker-agent-service.ts`'s `proposeWorkerAgent()` is scope-limited (plain user → `tier:'user'` only, for themselves; admin rank required for `tier:'customer'/'client'`) — `tier:'global'` isn't proposable through this service at all, since RLS itself already blocks any `app_runtime` insert with `tier='global'` (confirmed directly against the live policy: the insert/update policy's USING/CHECK only covers customer/client/user branches) — "only Layer 1 creates platform agents" was already DB-enforced before this wave, not newly built. Publish is a deliberately separate action from approve (new `PATCH /api/worker-agents/[id]/publish`), both `veridian_admin`-gated — the in-app stand-in for "Layer 1 administrative authority" since no other code-editing/publishing AI actor exists inside the running app. Closed Worker Agent Discovery's missing half in `task-execution-engine.ts`: when no approved/published agent matches a plan step, posts an actionable system chat message instead of silently dropping it or auto-creating an unattended proposal (which would violate Scope-Limited Creation — a proposal always needs a real human attributed to it). Wired the first-ever write to `worker_agent_learnings` (existed since Wave 3, confirmed unused before this): `chat-service.ts`'s `resolveInstructionMismatch()` now records a learning when a nudge corrects a worker-agent-dispatched task step — the one real "human validated and corrected an AI's work" event already in this codebase. `instruction-mismatch-audit.ts`'s LLM call now identifies which of the assignee's tasks the instruction relates to, populating `instruction_mismatch_detections.related_task_id` (previously always null) so this hook has something real to act on. | migration `wave16_worker_agent_governance` (later amended with one missing FK index, see Wave 19 entry); commit `31eef7f` | `npx tsc --noEmit` and `eslint` both zero issues. DB-level proof via Supabase MCP: full `proposed → approved → published` state transition confirmed via direct SQL. `get_advisors` (security) clean. Deployed (`dpl_3tf24kabUwYrcC21Y4JPQPE7b5PN`) — **then live-verified end-to-end over real HTTP via MCP** (not just DB-level simulation), since MCP's PostgREST-based path is unaffected by the pooler bug: see Wave 17's entry below for the actual test evidence, run against both waves together. |
| 67 | 2026-07-03 | GitHub + Vercel | **Wave 17: Purpose-Bound AI Enforcement (constitution §6/refinement #11).** New `purpose-bound-ai.ts` extends the pre-existing flat `DISPATCHABLE_TOOLS` set in `task-execution-engine.ts` into a domain-keyed allowlist (`DOMAIN_ALLOWED_TOOLS`) rather than inventing a new mechanism — "belt and suspenders" exactly as decided: a system-prompt clause (`buildPurposeClause`) AND a hard, server-enforced check (`isToolAllowedForDomain`), never relying on the model alone. Wired into `task-execution-engine.ts`'s dispatch gate, `chat-service.ts`'s AI-thread system prompt, and `/api/mcp`'s `handleTool` (new nullable `api_keys.domain_scope` column — `null` preserves every existing key's exact behavior, zero migration risk). Honest limitation: this codebase is single-domain (`compliance`) today, so nothing was actually rejected by this before now — the mechanism exists so a future product branch inherits real enforcement from day one. **Live-verified end-to-end over real HTTP, not just DB-level** (MCP's PostgREST-based path is unaffected by the still-open pooler bug): generated a real `domain_scope='sales'` test key, called `tools/call` for `get_compliance_stats` (a `compliance`-domain tool) through production `/api/mcp` — got back `{"error":{"code":-32000,"message":"Tool 'get_compliance_stats' is outside this API key's authorized domain ('sales')"}}`; confirmed an unconstrained (`domain_scope=null`) key still returns real data unchanged. Both test keys cleaned up. | migration `wave17_purpose_bound_ai`; commit `31eef7f` (same commit as Wave 16 — the two waves touch identical lines in `task-execution-engine.ts`, built together) | `npx tsc --noEmit` and `eslint` both zero issues. `get_advisors` (security) clean. Deployed (`dpl_3tf24kabUwYrcC21Y4JPQPE7b5PN`). **This is the first wave this session with genuine live HTTP proof rather than DB-level simulation for the pooler-blocked parts** — Waves 16/17 don't touch `withTenantContext`'s Drizzle path for their most security-critical check (the MCP domain gate runs through the same raw-Supabase-JS/PostgREST path as MCP's original 7 tools), so the pooler bug doesn't block them the way it blocks everything else. |
| 68 | 2026-07-03 | GitHub + Supabase | **Wave 18: Shared AI Resource Pool — org-to-platform only, never org-to-org (constitution refinement #10, built to the user's explicit correction, not the constitution's literal wording).** The user caught and corrected an early misreading mid-session: Org A's BYO key must never run Org B's workflow, full stop; what's real is Layer 1 (the platform's own internal orchestration work) borrowing an idle, explicitly opted-in org's capacity when its own default model isn't configured — "Layer 1 needs more capacity to do orchestra, so it takes from all available models as per need" (the user's own words). New `resolvePlatformModelConfig(layerKey)` in `orchestra-model-resolver.ts` deliberately takes no `orgId` parameter at all — that absence is the structural guarantee it can never be mistaken for (or reused as) a customer org's resolution path; the existing per-org `resolveModelConfig(orgId, layerKey)` is completely untouched by this wave. `customer_model_config` gains `shared_pool_eligible` (explicit, per-config opt-in required on the **lending** side — real money, even for platform housekeeping, needs real consent) and `last_used_at` (updated on every real resolution, both an org's own use and a pool borrow, so "idle" reflects genuine usage). New `shared_pool_allocations` audit table (service-role-only RLS, same posture as `loop_executions`) records every borrow; new `GET /api/settings/model-config/pool-usage` gives a lending org narrow visibility into its own history. **Real, non-hollow consumer, not a flag with nothing drawing on it:** `loop-engineering-audit.ts` (Loop 1, the existing meta-loop) now makes an actual LLM call via `resolvePlatformModelConfig('meta_oa')` to synthesize a plain-language platform-health summary — `meta_oa` has been a seeded, dormant Orchestra Layer since Wave 4 with zero call sites until this. Degrades gracefully (logged, not thrown) if neither a platform default nor any eligible pooled config exists. | migration `wave18_shared_ai_resource_pool`; commit `9b09cc5` | `npx tsc --noEmit` and `eslint` both zero issues. **This resolver depends on Drizzle/`postgres.js` (unlike Waves 16/17's MCP-domain-check path), so it's blocked from live HTTP verification by the pooler bug like everything else `withTenantContext`-dependent — verified the exact query/state logic directly via Supabase MCP `execute_sql` instead:** candidate selection correctly includes only `shared_pool_eligible=true` configs and excludes one in active use within the idle threshold; allocation audit row records correctly with the lender org, never a "borrower org" (there isn't one); confirmed via `pg_policies` that `shared_pool_allocations` has no `app_runtime` policy at all (Layer-1-only visibility, by construction). `get_advisors` (security) clean. Deployed (`dpl_75Mu4n2VncPnovdhQEMjDtQZQYVi`), zero new runtime errors. |
| 69 | 2026-07-03 | GitHub + Supabase | **Wave 19: Code-Change-Request workflow + Product/Project (L2) scope layer — the two large structural gaps, given a real first slice rather than deferred to roadmap-only.** Part A reuses `approval_requests` again (same engine as Waves 8/16) via a new `code_change_requests` satellite table — pre-generates both rows' ids in `code-change-request-service.ts` so each can reference the other directly on insert, avoiding an awkward empty-then-backfilled `entityId`. **Stated explicitly in code and here: approving a code change request does NOT cause any code to change** — no code path anywhere writes a source file from this; Implementation/Testing/Deployment remain a human directing a coding session outside the app, exactly as before this wave — this is an intake + audit trail, not a pipeline. `AGENTS.md`'s existing `repository_dispatch` mechanism is the plausible next step for actually triggering work from an approved request, explicitly not built here. Part B: new `products`/`projects` tables — the missing L2 analog the gap analysis found (`clients`/`clientEntities` are the wrong shape, a CA firm's own client companies, not a "product"). Wired into two real existing concepts (`tasks.projectId`, `worker_agents.projectId`) to prove the layer is functional, not decorative — `task-service.ts`'s `createTask()` and `worker-agent-service.ts`'s `proposeWorkerAgent()` both accept an optional `projectId` now. **Honesty section, carried into `PLATFORM_STRATEGY.md` too:** `products`/`projects` is a scope/data layer only, not an AI actor — nothing here makes an AI reason at the product/project level, and the L4→L3→L2→L1 escalation chain is one flat submit→single-human-decision shape (identical to `approval_requests` today), not a literal multi-hop AI review chain, since the L2/L3 AI actors that would need don't exist yet. | migrations `wave19_code_change_requests_and_products`, `wave16_worker_agent_governance` (amended — see below); commit `f5deb52` | `npx tsc --noEmit` and `eslint` both zero issues. DB-level proof via Supabase MCP: full submit→approve and submit→reject transitions confirmed for `code_change_requests` (denormalized status correctly mirrors `approval_requests.status` both directions); product/project creation and task attachment confirmed; all test rows cleaned up. **Found and fixed one real gap via `get_advisors` during this pass:** Wave 16's `worker_agents.proposed_by_id` FK was missing a covering index — added, folded into the Wave 16 migration file for history accuracy (everything else `get_advisors` flagged is either pre-existing in an unrelated `public` schema, or part of the large, pre-existing Wave-8-era unindexed-`client_id`-FK backlog across ~40 GRC tables, out of scope for this wave). `get_advisors` (security) clean. Deployed (`dpl_Bs1XEMq8eda65FnpgxWY5gJCKN5L`), zero new runtime errors; confirmed the MCP route (Waves 16/17's live-verification path) still returns the expected `401` for an unauthenticated request post-deploy, no regression. |
| — | 2026-07-03 | Update | **New scope: module reusability.** The user's ask, verbatim in spirit: "99% of requirements across customers/products/projects/users/end-users are similar — instead of forking a module every time, use the SAME module, have customized rules, and the module evolves over time. Worker agents are available to all product/project/company/account/user, customized to do work." Evaluated all ~40 GRC modules against this: every one is rigid today (hardcoded Postgres enums, a hardcoded `ROLE_CLEARANCE` constant in `classification.ts` with zero per-scope configurability, no module registry/catalog, no per-module settings anywhere). Worker agents already have almost the right shape (4-tier `workerAgents.tier` + Wave 19's `projectId`), but `worker_agent_domain_index` (meant to index which domains an agent serves) has been dormant since Wave 3, and `task-execution-engine.ts`'s agent-fetch query had zero domain/project filter at all. Scoped as Waves 20-21: Wave 20 (Module Registry + Product-Branch catalog, pure new infrastructure), Wave 21 (generic Module Rules Configuration resolver + wiring 3 representative modules + real Worker Agent Domain Index dispatch). Confirmed via plan-mode `AskUserQuestion`: a new `product_branches` table (not overloading Wave 19's org-scoped `products`), user-level module rules deferred to a documented future case, both waves planned together. |
| 70 | 2026-07-03 | GitHub + Supabase | **Wave 20: Module Registry + Product-Branch Enablement.** New global-read catalog tables (`app_runtime` gets SELECT only, same posture as `orchestra_layers`): `module_registry` (40 rows seeded — the 4 pre-Wave-7 core tables `is_core=true`, plus all 36 Wave 7-8 GRC modules, categorized by this file's own existing section-comment groupings), `product_branches` (1 row seeded: `branch_key='grc'` — the only real platform branch today; a **platform-wide** concept, deliberately a separate table from Wave 19's **org-scoped** `products`/`projects`, since forcing a platform branch into an `org_id NOT NULL` table would need either a nullable `orgId` breaking that table's existing invariant, or a fake sentinel-org row — an anti-pattern this codebase has already avoided elsewhere), `product_branch_modules` (40 enablement rows, all `is_enabled=true` for `grc` — today's actual behavior, made explicit rather than implicit). New read-only `module-registry-service.ts` (`listModules`, `getModule`, `listEnabledModulesForBranch`) and `GET /api/settings/modules` (`?branch=grc` filters to a branch's enabled modules). Pure additive infrastructure — no existing table or hot-path code touched. | migration `wave20_module_registry_and_product_branches`; commit *(pending)* | `npx tsc --noEmit` and `eslint` both zero issues. Seed counts confirmed via Supabase MCP: 40 modules (4 core), 1 branch, 40 enablements. `get_advisors` (security) clean — nothing new flagged against these 3 tables. **These are the one piece of Waves 20-21 provably verified over genuine live HTTP** (no `orgId`/`withTenantContext` dependency, same as `orchestra_layers`), not just DB-level simulation. |
| 71 | 2026-07-03 | GitHub + Supabase | **Wave 21: Module Rules Configuration + wiring 3 modules + Worker Agent Domain Index dispatch.** New `module_rule_configs` table generalizes `orchestra-model-resolver.ts`'s `resolveModelConfig()` "most-specific-scope-wins" pattern to module behavior — one polymorphic `scope_type`/`scope_id` discriminator (`platform`/`product_branch`/`org`/`project`/`client`/`user`) rather than 5 nullable FK columns, same unconstrained-but-service-validated scope-pointer trade-off `approval_requests.entity_id`/`entity_type` already makes. RLS splits read (platform/product_branch rows visible to everyone; org/project/client rows only when the scope resolves to the caller's own org, via joins to `projects`/`clients`) from write (platform/product_branch excluded from `app_runtime` entirely — only `service_role` may write platform/branch defaults, same discipline as `worker_agents`' `tier='global'` write-exclusion from Wave 16). New `module-rules-resolver.ts`'s `resolveModuleRule()` and `module-rule-service.ts`'s `setModuleRule()`/`listModuleRules()` (`scopeType='user'` deliberately rejected — the resolver accepts a `userId` for shape completeness, but no rule-setting API exists for it yet; most GRC rules are organizational, not personal). **3 representative modules wired, deliberately varied "kinds" of customizable behavior, not all ~40:** `risks`' `severity_matrix` rule (likelihood×impact→severity-band matrix, `GET /api/risks` now computes a `severity` label from it instead of leaving it for the UI); `incidents`' `regulatory_notify_triggers` rule (the `confidential`-vs-`department` classification trigger on POST, previously a hardcoded `/security\|breach/i` regex — the seeded platform default's `categoryRegex` is the exact same pattern, so behavior is byte-for-byte unchanged until an org/client sets its own override); `posh_complaints`' `classification_ceiling_override` rule (`classification.ts`'s `canAccess()` gained an optional `roleOverrides` parameter — omitting it, every pre-Wave-21 call site, is identical to before). The remaining ~37 modules stay on their existing hardcoded path — deliberate scope discipline, not oversight. **Worker Agent Domain Index wired for real** (dormant since Wave 3): `proposeWorkerAgent()` now inserts `worker_agent_domain_index` rows from the agent's `domain` (plus an optional `domainPaths[]` input); `task-execution-engine.ts`'s `executeTask()` agent-fetch query is now project-scoped (a project-matched agent shadows an org-wide one of the same name, mirroring the rules resolver's own most-specific-wins philosophy) — `createTask()`/`task-service.ts` now thread `projectId` through to it. **Caught and corrected a real bug before deploying:** the original design planned to also filter agent discovery by `worker_agent_domain_index.domain_path = DEFAULT_DOMAIN` ('compliance') — confirmed directly against live data that `workerAgents.domain` is actually a free-text **capability-path taxonomy** ("Cross-Cutting > Data Access", "India Compliance > Penalty Calculation"), not the same value space as `purpose-bound-ai.ts`'s single-value domain concept; that filter would have matched zero of today's real agents, a regression. Removed it and left an explicit comment — the domain-index table is genuinely populated (real progress) but its *consumption* by task dispatch is honestly deferred pending a real task-level domain concept, not shipped broken to look complete. | migration `wave21_module_rule_configs_and_domain_index_wiring` (+ 2 follow-up migrations dropping duplicate indexes `get_advisors` flagged on `worker_agent_domain_index` — one pre-existing since Wave 3 on `domain_path`, one redundant with the existing UNIQUE composite index's leftmost column on `worker_agent_id`); commit *(pending)* | `npx tsc --noEmit` and `eslint` both zero issues (two real TS errors fixed along the way: `const conditions = []` inferring `never[]`, typed explicitly as `SQL[]`, same class of bug Wave-era entry #51 fixed elsewhere in this codebase; a `module` variable name collides with Next.js's reserved webpack global, renamed to `moduleRow`). DB-level proof via Supabase MCP: inserted an org-level `risks`/`severity_matrix` override, confirmed the most-specific-wins resolution query correctly returns the org override over the platform default; confirmed `module_rule_configs`' RLS policies exist exactly as designed (`app_runtime`'s INSERT/UPDATE/DELETE policies structurally exclude `platform`/`product_branch` scope). `get_advisors` (security) clean after the two index-duplicate fixes. `next build`'s page-data-collection step fails locally with "No database connection string available" — confirmed via `ls .env*` that no `.env` file has ever existed in this repo's local working copy (`DATABASE_URL` is Vercel-only, set in its dashboard); the error originates in `db/index.ts`'s `getConnectionString()`, unrelated to anything this wave touched, so this is a local-environment gap, not a regression. Real build verification is Vercel's own build on deploy, matching this repo's established practice since the pooler bug made local `withTenantContext` testing unreliable anyway. |
| — | 2026-07-03 | Note | **All 4 VAIOS constitution waves (16-19) are now code-complete, deployed, and — for Waves 16-17 — genuinely live-verified end-to-end over real HTTP** (not just DB-level simulation), since those two waves' most security-critical checks run through MCP's PostgREST-based path rather than the pooler-blocked Drizzle path. Waves 18-19 depend on `withTenantContext` like everything else built since Wave 11, so their live HTTP proof remains outstanding for the same still-open reason — verified via direct DB-level SQL simulation instead, matching this session's established workaround. The Supavisor pooler bug itself is unchanged and still the one open item blocking a complete live-verification sign-off across Waves 11, 18, and 19. |
| — | 2026-07-03 | Research | **Open-source research pass — 21 repos studied for VERIDIAN AI OS build-vs-borrow decisions** (`PLATFORM_STRATEGY.md` §13 has the full report + Integration Matrix). User's explicit brief: study Agency Agents, OpenClaw, MemPalace, AutoResearch, Agent Skills, Awesome Claude Code, Awesome LLM Apps, Hermes Agent, Microsoft Qlib, iFixAi, LangGraph, Microsoft AutoGen, CrewAI, LlamaIndex, Haystack, Langfuse, AgentOps, Flowise, Dify, n8n, and OpenHands — understand architecture/concepts, do NOT copy code, implement only what VERIDIAN genuinely needs. 6 parallel research passes verified each repo's real existence/activity/license via WebSearch/WebFetch before analyzing (several didn't resolve exactly as named in the brief — e.g. the real repo is `ifixai-ai/iFixAi` not `iFixAI`, `karpathy/autoresearch` turned out to be a single-GPU ML-experiment-loop tool, not a citation/multi-agent research framework as assumed — flagged rather than glossed over). Findings cross-checked against VERIDIAN's actual schema to avoid recommending duplicate work: `orchestraExecutions` (Wave 4) already covers most of what an LLM "trace" needs (missing only model/tokens/cost columns); `loopExecutions`/`loopImprovements` (Wave 5) already track before/after state, deployed/rollback flags — closer to a mature self-improvement loop than most researched "self-improving agent" repos, with the real gap being analytical *depth*, not schema. Two repos scored 8/10 as genuine, additive, low-risk gaps: **Langfuse** (Prompt Operating System + LLM observability data model — VERIDIAN's biggest true gap, no prompt versioning or LLM-call tracing exists at all today) and **Hermes Agent** (trace-analysis-driven Loop Engineering improvement + completion-contract verification pattern). Everything else in the 21 either confirmed VERIDIAN's existing governance already exceeds what these frameworks treat as paid enterprise add-ons (CrewAI Control Plane, LangGraph Platform Enterprise, OpenHands enterprise tier), is out of scope until a future product line exists (Qlib — financial forecasting), is a documented security anti-pattern to avoid (Flowise's CVE history: CVSS 10.0 unauthenticated RCE, a confirmed cross-tenant secret leak on its own Cloud offering), or is legally blocked from the exact "white-label as a multi-tenant feature" use case VERIDIAN would need (n8n's Sustainable Use License, Dify's modified Apache license both explicitly prohibit it). Next: a scoped implementation plan for the Prompt OS + Observability enrichment (+ a small Worker Agent Library `toolType` taxonomy addition), proposed via plan mode rather than building all 21 repos' worth of ideas at once. |
| 72 | 2026-07-03 | GitHub + Supabase | **Wave 22: Prompt Operating System + Observability/Memory/Taxonomy schema foundations.** New `prompt_templates`/`prompt_versions` tables (Langfuse-inspired: versioned, labeled `'production'`/`'staging'` rows, global-read same posture as `orchestra_layers`/`module_registry` — only `service_role` writes platform defaults; `veridian_admin` may create new versions through the app). Seeded 8 template keys — the pinned chat AI-thread system prompt, the task planner's system prompt, the meta-loop synthesis prompt, the instruction-mismatch judgment prompt, and 4 event-specific `orchestrate` route prompts — with content copied **verbatim** from the 5 real hardcoded strings found across the codebase, confirmed byte-identical via direct DB diff so nothing in AI behavior changes yet. Two prompts (`chat.ai_thread_system`, `task_execution.planning_system`) preserve a literal `{{PURPOSE_CLAUSE}}` placeholder token marking exactly where `buildPurposeClause(DEFAULT_DOMAIN)` was already spliced into the middle of the original template literal. `orchestra_executions` (Wave 4) gains real, queryable `model`/`provider`/`prompt_tokens`/`completion_tokens`/`cost_usd` columns — previously this data, when captured at all, was only ever stuffed into the free-form `output` jsonb by the one existing call site (`api/ai/orchestrate`'s own `logOrchestraExecution`). Two small additive items rode along: `assistant_memories` gains MemPalace-inspired temporal validity-window columns (`valid_from`/`valid_until`/`superseded_by_memory_id` — built ahead of need per the user's explicit choice, no consumer wired yet); `module_registry` gains an Agent Skills/Awesome-LLM-Apps-inspired secondary `tool_type` taxonomy axis (`data_access`/`calculation`/`validation`/`reporting`/`orchestration`), backfilled for all 40 modules, orthogonal to the existing free-text `domain` path. Pure additive infrastructure — no existing call site behavior changed. | migration `wave22_prompt_os_and_observability_schema` (+ a follow-up `wave22_fix_missing_fk_indexes`, folded into the same file for history accuracy); commit `ae051e3` | `npx tsc --noEmit` and `eslint` both zero issues. Seed counts confirmed via Supabase MCP: 8 templates, 8 versions, 40/40 modules tagged with a `tool_type`. `get_advisors` (security) clean — nothing new flagged against these tables. **Global-catalog reads verified over genuine live HTTP** (no `orgId`/`withTenantContext` dependency, same as Wave 20's `module_registry`): `GET /api/settings/prompts` and the re-confirmed `GET /api/settings/modules` both correctly return `401` for an unauthenticated request post-deploy. |
| 73 | 2026-07-03 | GitHub + Supabase | **Wave 23: wired the Prompt Operating System + real AI Observability into all 5 real LLM call sites.** `llm-client.ts`'s `callLLM`/`callLLMJson` now parse and return token usage from every provider's own response shape (OpenAI-compatible `usage.prompt_tokens`/`completion_tokens`, Anthropic `usage.input_tokens`/`output_tokens`, Google `usageMetadata.promptTokenCount`/`candidatesTokenCount`) — previously discarded entirely — plus a new `MODEL_PRICING`/`estimateCostUsd()` approximate reference table (an honest, manually-maintained constant; no live pricing API exists for any of the 4 providers). This is a **breaking change to both functions' return shape**, applied to every one of the 5 real call sites in the same commit — confirmed by a clean `tsc` pass across the whole repo (the primary safety net for this wave, since a missed call site would be a compile error, not a silent regression) and a direct `grep` sweep confirming no stale call sites remain. New `orchestra-execution-logger.ts` generalizes `api/ai/orchestrate/route.ts`'s own local `logOrchestraExecution()` (Wave 4) into a shared `recordOrchestraExecution()` every site now calls, populating the real `model`/`provider`/`promptTokens`/`completionTokens`/`costUsd` columns from Wave 22 instead of stuffing that data into free-form `output` jsonb. Every hardcoded prompt string across `chat-service.ts`, `task-execution-engine.ts`, `loop-engineering-audit.ts`, `instruction-mismatch-audit.ts`, and `api/ai/orchestrate/route.ts` is replaced with `resolvePromptTemplate()` against the seeded `'production'` version. **Real architectural mismatch caught and resolved honestly, not forced through:** `loop-engineering-audit.ts`'s meta-loop LLM call is deliberately **not** wired into `recordOrchestraExecution()` — `orchestra_executions.org_id` is `NOT NULL`, and this is a genuinely cross-tenant, platform-level call with no single org to attribute it to; forcing a fit would mean either violating that constraint or introducing a fake sentinel-org row, an anti-pattern this codebase has already rejected elsewhere (Wave 6's "first org in the DB" bug fix). `loop_executions` (Wave 5) remains the correct, already-existing observability record for that call site — the Prompt OS migration still applies to it, just not the new observability logging. **Hermes Agent-inspired addition, exercising the labeled-version mechanism itself as part of shipping it:** a `'staging'`-labeled v2 of the meta-synthesis prompt asks the LLM to suggest a likely root cause per silent loop instead of only reporting a count — inert until reviewed and explicitly promoted to `'production'` via `prompt-os-service.ts`. Found and fixed 2 more missing FK-covering indexes via `get_advisors` (`assistant_memories.superseded_by_memory_id`, `prompt_versions.created_by_id`), folded into the Wave 22 migration file. | migration `wave22_prompt_os_and_observability_schema` (staging-prompt seed + FK-index fixes appended to the same file); commit `b864e94` | `npx tsc --noEmit` and `eslint` both zero issues across every touched file. DB-level proof via Supabase MCP: confirmed `resolvePromptTemplate('chat.ai_thread_system')`'s query returns content byte-identical to the pre-Wave-23 hardcoded template literal (the behavior-preservation proof); simulated a full `orchestra_executions` insert with real `model`/`provider`/`prompt_tokens`/`completion_tokens`/`cost_usd` values inside a transaction, confirmed correct storage, then rolled back/cleaned up. `get_advisors` (security) clean after the 2 index fixes. Deployed (`dpl_HeNAWJy6P2uxFY9ciFkN4PzJSK7F`), zero new runtime errors; confirmed both new Wave 22/23 endpoints still return the expected `401` for unauthenticated requests post-deploy. |
| — | 2026-07-03 | Note | **PageAgent integration (Waves 24-26) paused mid-build, pending the user's decision on a real security finding.** Schema (Wave 24: `personal_model_config`, `organisations.page_agent_enabled`, seeded `page_agent_oa` orchestra layer) and backend (Wave 25: `personal-model-resolver.ts`, `/api/page-agent/proxy`, `/api/page-agent/config`) are built, migrated, and deployed. Wave 26 (frontend mounting) is in progress when a real problem was caught before finishing: the originally-approved "read-only mode" design for `/posh`/`/whistleblower` only blocked `clickElement`/`inputText` actions, but PageAgent's core mechanism — reading the DOM as text and sending it to the LLM — would still transmit actual POSH/whistleblower case content to Groq/OpenAI/a user's BYO endpoint regardless of whether actions are blocked, violating this codebase's own established confidentiality principle (case content never flows through any AI/audit path). The installed package's `transformPageContent()` hook (confirmed in its real TypeScript definitions) can fully redact page content before it ever reaches the LLM, which is the real fix — but this changes what "read-only" means from what was approved, so implementation is paused pending the user's confirmation before continuing. **Resolved 2026-07-03, see entry #76**: user confirmed disabling PageAgent entirely on both routes. |
| — | 2026-07-03 | Research | **VERIDIAN AI PMS (Project Management System) — open-source research pass, 3 repos studied for a new product branch.** User's brief: study `hcengineering/platform` (Huly), `opf/openproject` (OpenProject), `makeplane/plane` (Plane), extract modules/features VERIDIAN lacks, adapt (never copy code, never use their AI), avoid duplicating existing VERIDIAN modules, build a new "VERIDIAN AI PMS" product — VERIDIAN's first genuinely new `productBranches` row since `'grc'` (Wave 20). Full research + proposed module design in `PLATFORM_STRATEGY.md` §14. 3 parallel research passes, each independently grounded against VERIDIAN's actual schema (confirmed `tasks` is flat/single-assignee/no-priority/no-hierarchy, `projects` (Wave 19) has no lead/dates/health/issue-prefix, `documents` is compliance-coupled not a general wiki). Key finding, cross-verified by 2 of 3 agents independently: Plane's "Modules" concept (a themed issue grouping spanning cycles) would collide with VERIDIAN's own `moduleRegistry` terminology **and** would duplicate the Epic-issue-type concept within the new design itself — resolved by not building it as a separate table at all, letting the Epic issue-type's parent/child hierarchy cover the same need. Converged recommendation: a full issue-tracking core (types/statuses/priority/relations/labels/estimates, synthesizing Plane's clean model + Huly's hierarchy + OpenProject's per-type workflow), sprints, saved views, a genuinely new general-purpose wiki (kept separate from the compliance-coupled `documents` table), time tracking + billable rates and project budgeting + meeting management (OpenProject's unique traditional-PM depth, absent from the lighter tools), plus additive columns on the existing `projects` table (issue prefix, lead, dates, health, parent-project) rather than a new table. Kanban and Gantt/roadmap need **no new schema** — both are pure frontend views over data already modeled (confirmed this is also how OpenProject's own Gantt module works). Explicitly out of scope: collaborative CRDT rich-text editing, Huly's Drive/Chunter, any CRM/HR modules, and any AI feature ported from the three tools (future PMS AI touches must use VERIDIAN's own Prompt OS/Observability/Worker Agent stack). Next: scope the build (full PMS vs. a staged core-issue-tracking-first MVP) with the user before entering plan mode. |
| 74 | 2026-07-03 | GitHub + Supabase | **Wave 25 (VERIDIAN AI PMS): full schema foundation — org-branch enablement + core issue tracking + sprints/views/wiki/time/budget/meetings.** User confirmed: build the full design in one pass (not a staged MVP), as a separate, opt-in product branch disabled by default for existing GRC orgs. New `product_branches` row (`branch_key='pms'`) plus the missing org-adoption table `org_product_branch_enablements` (org-scoped RLS, explicit `is_enabled` default `false`, `enabled_at`/`enabled_by_id`/`disabled_at` audit columns) — `product_branch_modules` (Wave 20) is pure global catalog with no org dimension, so this is a genuinely new mechanism, not a reuse. `projects` (Wave 19) gains 7 additive PM columns (`issue_prefix`, `issue_sequence`, `lead_user_id`, `start_date`, `target_date`, `health_status`, `parent_project_id`) — every pre-existing GRC-only project simply leaves these null until an org actually enables PMS. ~24 new tables: core taxonomy (`pms_issue_types` org-wide, `pms_issue_statuses` per-project with a `group` enum for cross-project board columns, `pms_workflow_transitions` reusing the existing `user_role` enum rather than inventing a `roles` table), issues (`pms_issues`, `pms_issue_assignees`, `pms_issue_relations`, `pms_labels`/`pms_issue_labels`, `pms_estimate_schemes`/`pms_estimate_points`, `pms_milestones`), sprints (`pms_sprints`/`pms_sprint_issues`), saved views (`pms_saved_views` — private-vs-shared enforced by a real RLS branch, not a service-layer filter, mirroring `module_rule_configs`' `scope_type='user'` precedent from Wave 21), a genuinely new wiki (`pms_wiki_pages`, deliberately separate from the compliance-coupled `documents` table), time tracking + billable rates (`pms_time_entries`/`pms_billable_rates`), budgeting (`pms_budgets`/`pms_budget_line_items` — actuals computed live by summing time×rate, never a duplicated ledger), and meetings (`pms_meetings`/`pms_meeting_agenda_items`/`pms_meeting_outcomes`/`pms_meeting_participants`). Issue types/statuses are **copy-on-enable seeded** (real, org-owned default rows created when PMS is enabled for an org, per Plane/OpenProject's own fresh-workspace pattern) rather than resolved from a platform catalog — `module_rule_configs` is shaped for one resolved JSON value per rule, not multiple independently-FK-referenced rows a real `pms_issues.status_id` FK needs. Plane's "Modules" concept is deliberately not built as a separate table — the Epic issue-type's `parent_issue_id` hierarchy already covers the same need, avoiding both a VERIDIAN `moduleRegistry` naming collision and a duplicate representation within the new design itself. 7 new Module Registry rows (`domain='project_management'`, `is_core=false`) linked only to the `pms` branch, never `grc` — the whole point of the opt-in separation. `purpose-bound-ai.ts`'s `DOMAIN_ALLOWED_TOOLS` gains its first real second key, `project_management: new Set([])` — an empty allowlist, since no AI tool touches PMS this pass (explicit user instruction: do not use any of the 3 studied tools' AI, do not invent a new AI mechanism). | migrations `wave25_pms_enablement_and_core_issues`, `wave25_pms_sprints_views_wiki_time_budget_meetings`; commit `ca9a979` | `npx tsc --noEmit` and `eslint` both zero issues. `get_advisors` (security) clean — zero findings against any new `pms_*` table (performance advisor's only `pms_*` notices are expected "index never used yet" info on brand-new empty tables, not missing-index warnings — full FK coverage confirmed). **DB-level RLS/isolation proof via Supabase MCP, genuinely switched to the `app_runtime` role** (not just simulated as `postgres`/`service_role`, which bypass RLS entirely) — temporarily granted `postgres` the Postgres 16+ `SET` option on `app_runtime` membership (mirroring this session's Wave-1 precedent of a temporary test-only role grant), ran `SET ROLE app_runtime` with real org contexts via `set_config('app.current_org_id', ...)`, and reverted the grant to its exact original state afterward (confirmed via `pg_auth_members`). All 7 assertions passed: enablement absent for both test orgs by default (disabled-by-default proof); a second org's `pms_issues` row is fully invisible from the first org's `app_runtime` context and vice versa (bidirectional cross-tenant isolation on the core issues table); an unrelated user in the same org sees only `pms_saved_views` rows marked `shared`, while the owning user sees both their own private view and the shared one (the saved-view RLS branch proven, not just assumed). All test rows cleaned up. |
| 75 | 2026-07-03 | GitHub + Supabase | **Wave 26 (VERIDIAN AI PMS): service layer + core issue-tracking API.** 5 new services matching this codebase's established `hasRole`/`withTenantContext`/`ServiceError` conventions: `pms-enablement-service.ts` (`isPmsEnabledForOrg`/`requirePmsEnabled` — the shared 403 gate every other PMS route calls first; `enablePmsForOrg`/`disablePmsForOrg`, both admin-gated; enabling seeds the 4 default org-wide issue types (Task/Bug/Epic/Story) only if the org has none yet, so a disable-then-reenable cycle never duplicates them — disabling only flips `is_enabled`/`disabled_at`, never deletes data), `pms-issue-service.ts` (atomic per-project issue numbering via `UPDATE projects SET issue_sequence = issue_sequence + 1 ... RETURNING` inside the same transaction as the issue insert; `syncAssignees()` replaces the full `pms_issue_assignees` set and refreshes the denormalized `pms_issues.assignee_id` primary-assignee cache on every call, matching the service-layer-not-DB-trigger convention this codebase uses everywhere else), `pms-taxonomy-service.ts` (issue types/statuses/workflow transitions/labels/estimate schemes/milestones; `ensureDefaultStatusesForProject()` is the copy-on-*first-use* counterpart to org-level copy-on-enable — a project has no statuses until the first time it's actually used for PMS work, since statuses are per-project unlike the org-wide issue types), `pms-sprint-service.ts` (sprint-issue moves; `closeSprint()` computes and writes the `progress_snapshot` burndown once at close time by joining sprint issues against their status groups, never live-computed on every read), `pms-view-service.ts` (saved views; owner-only write checks layered on top of the real `access='shared'` RLS branch from migration 0022, not a replacement for it). 15 new thin-wrapper routes under `/api/pms/*` (`enablement`, `issue-types`, `issue-statuses`, `workflow-transitions`, `issues`+`[id]`+`[id]/relations`, `labels`, `estimate-schemes`, `milestones`, `sprints`+`[id]`+`[id]/issues`, `saved-views`+`[id]`), every one gated by `requirePmsEnabled(orgId)` before touching any PMS table — matches `src/app/api/products/[id]/projects/route.ts`'s established shape exactly. `GET /api/me` now returns `pmsEnabled`, reusing the same round-trip `AppShell.tsx` already polls (no new fetch). | no new migrations this wave (schema unchanged since Wave 25); commit `35a6460` | `npx tsc --noEmit` and `eslint` both zero issues (one real type error fixed: a hand-written structural type for the transaction client didn't match Drizzle's actual `PgTransaction` shape — replaced with the already-exported `TenantDb` type from `tenant-scoped.ts`, the correct fix rather than a cast). **DB-level functional/logic proof via Supabase MCP** (distinct from Wave 25's RLS/isolation proof — this wave's proof targets business logic correctness, not tenant isolation, which was already established): mirrored every service function's exact operations directly against the schema — enable-with-seeding (4 issue types created, confirmed not duplicated on a second run), copy-on-first-use default statuses (5 created for a fresh project), atomic per-project issue numbering (two issues created back-to-back in the same project correctly numbered 1 then 2), multi-assignee sync + denormalized primary-assignee cache (2 assignee rows, cache correctly set to the first), sprint close computing a correct burndown snapshot (1 of 2 issues completed, captured exactly), and saved-view creation. All 7 assertions passed exactly as expected; all test rows cleaned up and confirmed removed. `get_advisors` (security) re-run: identical to Wave 25's baseline, zero new findings. |
| 76 | 2026-07-03 | GitHub + Supabase | **Wave 27 (VERIDIAN AI PMS): frontend — enablement settings + core issue tracking.** New Settings → Project Management tab (`PmsEnablementSection.tsx`, admin-gated enable/disable `Switch`, shows the enabled-module list via the already-working `GET /api/settings/modules?branch=pms`). `AppSidebar.tsx`/`AppShell.tsx` gain a `pmsEnabled` param following the exact same conditional-nav-section pattern already used for `accountType`'s Clients link — a new "PROJECTS" section appears only once an org enables the branch, absent by default for existing GRC orgs. New `/pms` pages: project picker/home (with a "New Project" dialog), `[projectId]/issues` (table list), `[projectId]/board` (Kanban — columns are `pmsIssueStatuses` ordered by position, drag-and-drop between columns via `@dnd-kit/core`'s `useDraggable`/`useDroppable` changes `statusId` through `PATCH /api/pms/issues/[id]`, confirmed zero new dependency needed as planned), and a shared `IssueDetailPanel.tsx` slide-over (title/description/status/priority/type/assignee, each field saves inline on blur/change). Small necessary addition beyond the original plan: PMS projects aren't scoped to a single Wave-19 product, so new `GET/POST /api/projects` (+`[id]`) routes list/create projects org-wide — `createProjectDirect()` auto-resolves (or creates once) a hidden "General" default product per org so PMS users never have to understand the Product/Project hierarchy, matching how Plane/Huly/OpenProject present projects as the top-level concept. | no new migrations this wave; commit `e42be16` | `npx tsc --noEmit` zero issues. `eslint` zero issues in every new file except a pre-existing, codebase-wide `react-hooks/set-state-in-effect` lint rule that fires on the standard `useEffect(() => { load() }, [load])` data-fetch pattern — confirmed via direct test that this same rule already fires identically on `WebhookSection.tsx`, `AiConfigSection.tsx`, and `ApiKeySection.tsx` (none touched this wave), so this is pre-existing lint debt across the whole codebase, not a regression introduced here; left as-is rather than deviating from the established pattern in 3 new files alone. **Honest limitation on browser verification:** genuine interactive Preview-tool click-through (drag-and-drop, modal flows) could not be performed — confirmed no `.env` exists in this repo's local working copy (consistent with Wave 21's finding) and the only local dev config found (`veridian-orchestra` in a separate, long-stale scratchpad clone last committed pre-Wave-9) would need reconciling divergent local edits to be usable, which wasn't attempted given the risk of touching unfamiliar local state. Verified instead via the deployed Vercel app (this repo's established practice for anything depending on `withTenantContext`, per every prior wave's own "verified... post-deploy" language): confirmed the new commit builds and deploys `READY`, `get_advisors` unchanged, and the new pages/routes are reachable without server errors. Real interactive browser testing of the Kanban drag-and-drop and detail-panel editing remains an open gap, matching this session's long-standing, still-unresolved local-dev limitation — not something newly introduced by this wave. | Separately, resolved the PageAgent pause (entry above, 2026-07-03 Note): the user confirmed disabling PageAgent entirely on `/posh`/`/whistleblower` rather than attempting content redaction. `PageAgentInitializer.tsx` now skips constructing the agent at all on those routes (no DOM read, no proxy call, not just a restricted-tool client config); `/api/page-agent/proxy` hard-rejects any request whose `pathname` matches those prefixes before any provider call is made, removing the old response-side `filterMutatingToolCalls()` approach that never actually stopped the request's page content from reaching the LLM. Also fixed a `react-hooks/refs` violation in the same file (a ref mutated during render, pre-existing in this uncommitted work, moved into a `useEffect`). Commit `e43e308` — this is the first commit of the full PageAgent Waves 24-26 backend+schema+confidentiality-safe-frontend together (previously withheld pending this exact decision). `PmsEnablementSection`-equivalent PageAgent settings UI (a "My AI" tab, org-level toggle switch) remains unbuilt and deferred — not part of what was just confirmed, and PageAgent already defaults to enabled with a safe, working mount today. |
| 77 | 2026-07-04 | GitHub + Vercel | **Found and fixed a real, pre-existing security gap while live-verifying Wave 27: `middleware.ts`'s auth-redirect allowlist was missing 34 of 51 authenticated route groups.** Direct `curl` checks against the production deployment (the substitute for genuine Preview-tool browser testing, per Wave 27's entry above) found `/pms` returning `200` to a fully unauthenticated request instead of the expected redirect to `/login`. Widening the check confirmed this was never specific to `/pms` at all: `/posh`, `/whistleblower`, `/risks`, `/board`, `/clients`, `/approvals`, and every other GRC module page added since this file's `isAppRoute` chain was last touched (apparently frozen around Wave 9-11) all had the same gap — the hand-written `.startsWith()` chain simply never kept pace with `src/app/(app)/`'s directory growth across dozens of subsequent waves. **Confirmed no data actually leaked**: every one of these pages is a client component whose real data comes from `requireAuth()`-gated API routes, and those routes correctly returned `401` throughout (re-confirmed for `/api/pms/*` and `/api/projects` both before and after this fix) — but the middleware-level redirect-to-login layer, which every other route in this codebase is designed to rely on as its first line of defense, was silently absent for the large majority of pages, including the two (`/posh`, `/whistleblower`) this exact session has treated as maximally confidentiality-sensitive all along. Fixed by replacing the hand-written boolean chain with an explicit `PROTECTED_APP_ROUTE_PREFIXES` array enumerating all 51 directories under `src/app/(app)/`, checked via `.some()` — makes the next missing route a visible one-line diff instead of a silent gap, rather than just patching in the one route (`/pms`) this wave happened to add. | no schema changes; commit `db1afc9` | `npx tsc --noEmit` and `eslint` both zero issues. **Live-verified over genuine HTTP against the production deployment, not just DB-level or type-level checks**: before the fix, `curl` confirmed `/pms`, `/posh`, `/whistleblower`, `/risks`, `/board`, `/clients`, and `/approvals` all returned `200` (full page shell) to an unauthenticated request; after deploying the fix, the exact same 7 URLs all correctly return `307 Temporary Redirect` to `/login?redirectTo=<path>`, and `/api/pms/enablement`, `/api/pms/issues`, `/api/projects` continued returning `401` throughout, confirming the fix only restored the missing middleware layer without touching the API-level auth gate that was already correct. `get_deployment_build_logs` (errors-only filter): clean build, no errors. `get_runtime_errors` (last 15 min post-deploy): none. |
| 78 | 2026-07-04 | GitHub + Supabase + Vercel | **Wave 28 (VERIDIAN AI PMS, final wave): sprints, wiki, time tracking, budgeting, meetings, and a read-only Gantt/roadmap view — schema-complete since Wave 25, now fully built out.** 4 new services matching this codebase's established conventions: `pms-wiki-service.ts` (a genuinely new, general-purpose project wiki — deliberately kept separate from the compliance-coupled `documents` table; `createWikiPage()`'s slugify-with-collision-retry mirrors `product-service.ts`'s existing `createProduct()` slug pattern), `pms-time-service.ts` (`logTime()`, `resolveBillableRate()` — most-recent-`validFrom`-first, per-user rate falling back to the org default (`userId IS NULL`) row), `pms-budget-service.ts` (`getBudget()`'s stored `plannedTotal` vs. `getBudgetActuals()`'s live-computed actual labor cost — summed by joining every `pms_time_entries` row for the project's issues against `resolveBillableRate()` at read time, never a duplicated ledger, exactly as designed in Wave 25), `pms-meeting-service.ts` (meetings + ordered agenda items + outcomes/minutes + participants). 13 new `/api/pms/*` routes (`wiki`+`[id]`+`by-slug`, `time-entries`+`[id]`, `billable-rates`, `budgets`+`[id]`+`[id]/line-items`, `meetings`+`[id]`+`[id]/outcomes`), every one gated by `requirePmsEnabled()` first. UI: sprint board (start/close lifecycle, burndown display, issue add/remove reusing Wave 26's `closeSprint()`), wiki index + page tree + markdown editor, time log/list with a "Log Time" action folded directly into `IssueDetailPanel.tsx`, org-default billable-rate admin folded into the existing Settings → Project Management tab (not a new tab, per the plan), budgets showing live planned-vs-actual side by side, meetings with agenda + outcomes. Gantt/roadmap is hand-rolled as a percentage-positioned CSS timeline over issue `startDate`/`dueDate` and milestone `targetDate` — confirmed zero new dependency needed, exactly as scoped in the original Wave 25-28 plan. `ProjectNav.tsx` extended with all 6 new tabs. | no schema changes this wave (all tables already existed since Wave 25's migrations 0021/0022); commit `6ab8544` | `npx tsc --noEmit` and `eslint` both zero issues in every new file, aside from the same pre-existing, codebase-wide `react-hooks/set-state-in-effect` lint rule already confirmed in Wave 27's entry to fire identically on untouched files elsewhere in the codebase — not a regression, left as-is for the same reason. Deployment for commit `6ab8544` confirmed `READY` (`get_deployment_build_logs` errors-only: clean; `get_runtime_errors` last 15 min: none). **Live HTTP-reachability check against production**: all 6 new pages (`/pms/x/sprints`, `/wiki`, `/time`, `/budgets`, `/meetings`, `/roadmap`) correctly `307`-redirect to `/login?redirectTo=<path>` (middleware's Wave-27-fixed `PROTECTED_APP_ROUTE_PREFIXES` already covers `/pms` as a prefix, confirmed still working); all 4 new representative API routes (`/api/pms/wiki`, `/time-entries`, `/budgets`, `/meetings`) correctly return `401` unauthenticated. **DB-level functional proof via Supabase MCP** (same established pattern as Wave 26's business-logic verification, distinct from Wave 25's RLS/isolation proof): mirrored each new service's real logic directly against the schema — wiki slug-collision handling (two same-titled pages correctly got 2 distinct slugs), time-entry-times-resolved-rate math (3.5 hours × ₹1500/hr org-default rate = exactly ₹5250.00), budget planned-total summation (₹20,000 material + ₹10,000 labor = exactly ₹30,000.00), and a meeting with 2 ordered agenda items plus 1 outcome, all persisted and queryable correctly. All 5 assertions passed exactly as expected; all test rows cleaned up and confirmed removed (0 remaining). `get_advisors` (security): identical to the established baseline, zero new findings against any `pms_*` table. **Same honest limitation as Wave 27**: genuine interactive Preview-tool browser click-through (drag-and-drop, sprint start/close buttons, budget line-item forms) could not be performed, for the same pre-existing local-dev Supavisor-pooler/no-`.env` blocker noted there — server-rendered-page-reachability + API-auth checks + DB-level business-logic proof are the substitute verification used throughout Waves 25-28. | **This closes out the full VERIDIAN AI PMS effort (Waves 25-28)** — see the closing status update added to `PLATFORM_STRATEGY.md` §14 in this same commit round. |
| 79 | 2026-07-04 | GitHub + Supabase + Vercel | **Waves 29-31: Knowledge Base, Automation Rules, Custom Reports — a six-repo OSS evaluation (AppFlowy, NocoBase, n8n, Metabase, Superset, Peppermint, Mattermost) that adopted 3 and explicitly rejected 3, not a blind integration of all six.** Full research + adopt/reject decision record in `PLATFORM_STRATEGY.md` §15. Cross-referenced each repo's category against VERIDIAN's *actual* schema (not just assumed gaps): found `conversations`/`messages` (Wave 12) already covers Mattermost's core value, `instructionCommitments`+`pmsIssues` already cover Peppermint's ticketing concept, and NocoBase's low-code/app-builder is a different product category entirely with no unmet need pointing at it — all three rejected with reasoning, not silently skipped. Also found a real, previously-unfulfilled gap: `pms_wiki_pages` (Wave 25) was scoped in §14.2 as "reusable outside PMS too" but shipped PMS-only (`projectId NOT NULL`, `requirePmsEnabled()`-gated) — corrected via a genuinely separate, always-on `knowledge_base_pages` table. **Built**: `knowledge-base-service.ts` (org-wide page tree, slug-collision retry mirroring `pms-wiki-service.ts`), `automation-rule-service.ts` (deterministic `evaluateAndRunRules()` — single-condition trigger match, `notify_user`/`create_task` actions only, no AI, no node-graph, no code-execution action type, wired fire-and-forget into `notice-service.ts`'s `updateNotice()` and `pms-issue-service.ts`'s `updateIssue()` on status change), `custom-report-service.ts` (`runReport()` executes a per-`sourceEntity` whitelisted grouped-count query via an explicit switch — never raw SQL — rendered with the existing `recharts` dependency, no new BI engine). 3 new tables (`knowledge_base_pages`, `automation_rules`+`automation_rule_runs`, `saved_reports`) registered as `isCore=true` module-registry rows (always available, no enablement toggle, unlike PMS). 10 new `/api/*` routes, 3 new pages (`/knowledge-base`+`[slug]`, `/automation`), a `CustomReportsSection.tsx` folded into the existing `/reports` page, and 2 new sidebar nav entries. | migration `0023_wave29_31_kb_automation_reports.sql`; commits `57989f5`, `73b6e69`, `75ed848` | `npx tsc --noEmit`: zero errors. `eslint`: zero issues aside from the same pre-existing, codebase-wide `react-hooks/set-state-in-effect` rule already confirmed in Wave 27/28's entries to fire identically on untouched files (`pms/[projectId]/wiki/page.tsx` re-checked directly) — not a regression. Local `npm run build` could not complete past the "Collecting page data" phase for the same pre-existing local-dev no-`.env`/DATABASE_URL gap noted every wave — confirmed unrelated to this change (the failing route, `/api/assistants/[id]/memories/search`, was never touched). **RLS proven directly against a genuinely-switched `app_runtime` role** (not just `service_role`, which bypasses RLS): seeded a temporary second org + real rows in all 3 new tables, confirmed bidirectional cross-org isolation (`org_001` saw only its own `knowledge_base_pages`/`automation_rules` row, the temp org saw only its own), and confirmed the `saved_reports` private/shared branch both ways (owner sees their own private report + all shared; a second user in the same org sees only the shared one, never the other's private report). All test data and the temporary org cleaned up and confirmed removed (0 remaining); the temporary `SET` grant on `app_runtime` used to run the test was reverted to its exact original single-row state. `get_advisors` (security): identical pre-existing baseline, zero new findings against any of the 3 new tables. **Two real gaps found and fixed during live verification, not just claimed clean**: (1) `/knowledge-base` and `/automation` were missing from `middleware.ts`'s `PROTECTED_APP_ROUTE_PREFIXES` allowlist — the same class of drift Wave 27 fixed elsewhere — confirmed via live curl (unauthenticated requests got a 200 page shell instead of a redirect) and fixed immediately (commit `73b6e69`); re-audited the full allowlist against all 53 directories under `src/app/(app)/` and confirmed an exact match, zero missing/extra. (2) Even after that fix, both pages kept serving a stale cached 200 to anonymous requests — `X-Nextjs-Prerender: 1`/`X-Vercel-Cache: HIT`/`Age: ~200s` headers revealed Next.js had statically prerendered them at build time and Vercel's edge was serving that cached snapshot (captured on their very first production hit, before the middleware fix shipped) without re-invoking middleware; confirmed this was isolated to these 2 routes, not systemic (5 other existing pages — `/tasks`, `/penalties`, `/help`, `/team`, `/checklists` — all correctly redirect fresh with no cache headers). Fixed with `export const dynamic = "force-dynamic"` on both pages (commit `75ed848`), re-verified fresh with cache-busting query params: both now `307`-redirect on every request with no `X-Vercel-Cache`/`X-Nextjs-Prerender` headers at all. Final live-HTTP sweep: `/knowledge-base`, `/automation`, `/reports` all correctly `307` to `/login?redirectTo=<path>`; `/api/knowledge-base/pages`, `/api/automation-rules`, `/api/reports/saved` all correctly `401` unauthenticated. | Zero code copied from any of the 6 evaluated repos — architectural patterns only (page-hierarchy, trigger-condition-action, saved-query-as-chart), independently re-implemented against VERIDIAN's own schema/service/RLS conventions, per the user's explicit "adapt what's compatible, never their AI" instruction. See the closing status update added to `PLATFORM_STRATEGY.md` §15 in this same commit round. |
| 80 | 2026-07-04 | GitHub + Supabase + Vercel | **Waves 32-34: VERI Chat, VERI To Do, VERI Minutes of Meetings.** The user defined VERI Chat precisely (strictly business, connects to every module, plus WhatsApp/Telegram/Slack share) and asked first for a gap analysis of what Wave 12's existing `conversations`/`messages` system actually covers, then 3 new modules. Full gap analysis + design record in `PLATFORM_STRATEGY.md` §16. **Confirmed via live research, not assumed**: `wa.me`/`t.me` links only let a user compose a *new* outgoing message — no web link can extract an existing WhatsApp/Telegram conversation; the only documented way content leaves those apps is their own native "Export Chat"/Share Sheet. Slack is the one platform with a real API-backed permalink→content resolution path, but it needs a Slack App the user must register themselves. This drove the actual design: a tokenized, time-limited, read-only public share page (`conversation_share_links`) for share-out — never raw chat content in a URL — and a PWA Web Share Target (`src/app/manifest.ts` + `/api/veri-chat/share-target`) for share-in, which works from *any* app's native OS share sheet, not just WhatsApp/Telegram specifically. **Wave 32 (VERI Chat)**: extended `conversations` (`contextEntityType`/`contextEntityId` — the same polymorphic pattern already used by `embeddings`/`approval_requests`/`audit_logs`, not a new mechanism) and `messages` (`assistantId`, `sourcePlatform`/`sourceRef`), added `message_attachments` (reuses the existing `documents` table) and `conversation_share_links`. New `veri-chat-service.ts`; a Share button + dialog added to the existing `/chat` page (WhatsApp/Telegram hrefs + copy-link); a public `/shared/conversation/[token]` page and backing `/api/shared/conversation/[token]` route (intentionally outside the auth allowlist — the one legitimate unauthenticated surface in this codebase). **Wave 33 (VERI To Do)**: confirmed by reading `task-service.ts` that `listMyTodos()` only ever queried the bare `tasks` table despite Wave 15's Home page describing itself as a "universal To Do" — new `veri-todo-service.ts`'s `listVeriTodos()` genuinely unions `tasks` + pending `instruction_commitments` + assigned `pms_issues` (excluding completed/cancelled status groups), surfaced at a new `/veri-todo` page. `listMyTodos()` itself was left untouched (other callers depend on its exact shape) — this is a new, additive function. **Wave 34 (VERI Minutes of Meetings)**: new, general-purpose `veri_meetings`+`veri_meeting_action_items` — the same "genuinely new, standalone" call already made for Knowledge Base in Wave 29, since `board_meetings` (governance-only) and `pms_meetings` (PMS-project-only) are both real but scope-locked. Action items become real `tasks` rows (which VERI To Do already surfaces), not a parallel tracking mechanism; `minutesHistory` mirrors `board_meetings`' own amend-don't-overwrite precedent. All 3 registered as `isCore` module-registry rows. | migration `0024_wave32_34_veri_chat_todo_mom.sql` + `0025_wave32_fix_conversation_participant_org_scoping.sql`; commit `592f664` | `npx tsc --noEmit`: zero errors on first pass. `eslint`: zero issues aside from the same pre-existing, codebase-wide `react-hooks/set-state-in-effect` rule confirmed every prior wave to fire identically on untouched files — not a regression. `npm run build` compiled and typechecked successfully; blocked at the same pre-existing local-dev no-`.env`/`DATABASE_URL` page-data-collection gap noted every wave (this time on `/api/approvals`, never touched — confirmed unrelated). **A real security gap found and fixed during RLS verification, not just claimed clean**: `compliance.is_conversation_participant()` (Wave 12) checks only `conversation_participants.user_id = current_user_id()` — it never independently verifies `current_org_id()` at all. Proven live: impersonating a real participant's `user_id` under a *different* org's `current_org_id()` context returned `message_attachments`/`conversation_share_links`/`messages` rows that should have been invisible. Not exploitable through this app's own code paths today (every `withTenantContext` call gets its `orgId` server-side from the authenticated session via `requireAuth()`, never client-supplied) — but a real defense-in-depth hole in the RLS layer itself, inherited by Wave 32's 2 new tables and present all along in Wave 12's `messages`/`conversation_participants`. Fixed by strengthening all 4 policies to also join back to `conversations.org_id = current_org_id()` (migration `0025`), re-verified via the same genuinely-switched `app_runtime` role: the cross-org leak returned `0` rows everywhere after the fix, the legitimate same-org-participant case still correctly returned `1`, and a same-org-non-participant still correctly saw `0` (participant-gating intact). All test data (2 conversations, 1 message, 1 attachment, 1 share link, 2 meetings, 1 temp org) cleaned up and confirmed removed; the temporary `SET` grant on `app_runtime` reverted to its exact original single-row state. `get_advisors` (security): identical pre-existing baseline, zero new findings. **Two more gaps found live, learned from Wave 29-31's exact playbook**: `/veri-todo` and `/veri-meetings` were added to `middleware.ts`'s allowlist proactively this time (re-audited against all 55 `(app)/` directories, exact match) and every new page got `export const dynamic = "force-dynamic"` proactively, closing off the static-prerender/CDN-cache-bypass class of bug before it could recur. Final live-HTTP sweep: `/veri-todo`, `/veri-meetings`, `/chat` all `307` fresh (no `X-Vercel-Cache`/`X-Nextjs-Prerender` headers) to `/login?redirectTo=<path>`; `/api/veri-todo`, `/api/veri-meetings`, `/api/veri-chat/conversations/x/context` all `401`; `/shared/conversation/<bogus-token>` correctly `200`s the (client-rendered, always-reachable) page shell while its backing `/api/shared/conversation/<bogus-token>` correctly `404`s — confirming the actual security boundary (token validity) is enforced where it matters. | **Explicitly deferred, not silently dropped**: Slack permalink→content resolution (schema-ready via `sourcePlatform`/`sourceRef`, needs a Slack App the user must register in Slack's own admin console — cannot be created from inside this session). Live audio transcription for meetings (Meetily's category, confirmed via research to be MIT-licensed and genuinely useful, but a real self-hosted-Whisper infrastructure investment, not a schema/service addition). Wiring `assistantMemories` as a live consumer of conversation history (still zero consumers, exactly as Wave 22 left it). Full reasoning for all three in `PLATFORM_STRATEGY.md` §16.4. |
