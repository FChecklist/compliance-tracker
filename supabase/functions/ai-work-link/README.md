# ai-work-link (PROJEXA-BUILD-001 U-46b1, first half)

The universal AI work link. One capability URL per person and project:

`https://<project>.supabase.co/functions/v1/ai-work-link/pxa_<64 hex>`

That single address is a Markdown manual on GET, an MCP server on POST, the root of a REST API, and the server URL inside its own OpenAPI 3.0.3 and Swagger 2.0 documents. The token in the address is the credential: it resolves to exactly one person and one project, and the person never signs in to use it. This unit is the READ layer (spec `ai-os/projexa-build-001/UNIVERSAL_AI_WORK_LINK_SPEC.md`, sections 3 to 8 and 10). Writes, function reads and the confirm page are written in later units (U-46c, U-47).

No Vercel function is in the path, and no model runs: every read is a call to a `public.ai_work_link_*` SQL function of `drizzle/0624` to `0626`, made with the platform-injected service-role client.

## Files

| File | What it is |
| --- | --- |
| `index.ts` | `Deno.serve`, the settings and the service-role client. Nothing else. |
| `handler.ts` | The router: path mode and header mode, the order of checks, the call log, every response header. Run under bun by the tests, with the database passed in as `deps.rpc`. |
| `reads.ts` | What is read and checked, shared by REST and MCP: context, records, history, search and fetch, `check` and `propose`. The only place that calls `rpc`. |
| `api-definition.ts` | The ONE definition: endpoints, MCP tools, errors. Reads the two generated JSON files below. |
| `manual.ts`, `render.ts` | The manual, its manifest, the paste card and card data; the Markdown and CSV renderings. |
| `mcp.ts` | MCP, both protocol eras on one endpoint, no session. |
| `openapi.ts` | The OpenAPI 3.0.3 and Swagger 2.0 documents, cut from the same operation list as the router. |
| `config.ts` | The settings, one pure function of the environment. |
| `jwt.ts` | The JWT settings for the signed-in app routes (the PROJEXA and verdian-ai issuers and key sets), and `VERIFY_JWT = false`. |
| `session.ts` | Verifies a signed-in person's access token with jose against the published ES256 key sets (U-47b). |
| `confirm.ts` | `POST /drafts/{id}/confirm`, the Level-2 confirmation (U-47b): session, person, then `ai_work_link_draft_confirm`. |
| `record-kinds.generated.json`, `function-registry.generated.json` | Generated by `scripts/gen-ai-link-registry.*` (CI checks they are current). Read here, never edited. |
| `../_shared/ai-link/core.ts` | The pure helpers: address grammar, `negotiateFormat`, `paginate`, `errorBody`, headers, rate-limit arithmetic, filter allow-list, money nulling, data fencing. |

## Routes

Relative to the link base `B` (`F/<token>`) or the header-mode base `F/header` (token in `Link-Token` or `Authorization: Bearer`; never a query string).

| Route | Answer |
| --- | --- |
| `GET /`, `/manual.md`, `/manual.json` | the manual (Markdown unless JSON is asked for) |
| `GET /card.md`, `/card-data.md?kinds=` | the token-free paste card (at most 8,000 bytes) and data snapshot (at most 100,000 bytes) |
| `GET /openapi.json`, `/swagger.json` (`?mode=header`) | the API documents |
| `GET /context`, `/records/{kind}`, `/records/{kind}/{id}`, `/functions`, `/history`, `/intents/{id}` | reads; Markdown with no `Accept` header, JSON on request, CSV for records |
| `GET /propose`, `POST /check` | dry runs: nothing is recorded |
| `POST /` or `/mcp` | MCP (`initialize`, `tools/list`, `tools/call`, and `server/discover` for 2026-07-28) |
| `POST /functions/{fn}` | scope is checked (403), then 503 "not switched on yet" (the reads run on the exec host, a later unit) |
| `POST /actions` | scope (403), then the true reason: 403 `LEVEL_NOT_ALLOWED` (this link's level or a level-2 function), 422, 403 `WRITES_NOT_ENABLED` (the switch is off), 503 `EXECUTOR_NOT_AVAILABLE`; with the switch and the exec function on: record, `ai_work_link_intent_claim`, the exec function, 201 (BUILD-002 WP-09a, `drafts.ts`) |
| `POST /drafts`, `GET /drafts/{id}` | records a draft LIVE at level 0 (scope 403, body 400/413, parameters 422, `CONFIRM_HOST_NOT_SET` 503, caps 429 `WRITE_CAP_HOUR` / `WRITE_CAP_DAY`) and answers 201 `{draft_id, confirm_url, expires_at}` (the confirm code is in the URL fragment only); a replay is 200 with no code; `GET` is the state of this link's own draft (`drafts.ts`) |
| `POST /drafts/{id}/confirm` | 401 no or bad session, 403 not the draft's person, 409 wrong or reused code, 410 expired, 429 over 10 a minute per person, 503 writes off or any failure, 200 confirmed (U-47b) |
| `POST /mint` | signed-in person; fresh session (issued within 15 minutes, else 401 `SESSION_STALE`); 201 with the link and token, shown once; 404 a project the person cannot read; 403 level or function above their rank; 429 caps (10 an hour, 30 a day; 5 a minute brake) (BUILD-002 WP-08) |
| `GET /links[?project=]`, `POST /links/{id}/revoke` | the person's own links (no token, no hash); revoke by the link's person or an org admin |
| `GET` or `POST /warning?level=&project=` | the warning sentence, true for the current state: it promises direct entries only when writes are switched on |
| `POST /new-project` | "New project with my AI": a shell project named "New project (AI setup)" and a level 0 link for the same person in one transaction; rank 2 and above; fresh session; 5 a day |
| `GET|POST /drafts/{id}/preview` | a signed-in person and the confirm code: the function, every parameter and a BOQ total, before confirming; 401, 403 not the draft's person, 409 wrong code, 429 (shared brake), 503; works while writes are off (`drafts.ts`) |
| `OPTIONS` | 204 preflight; `PUT`, `PATCH`, `DELETE` 405 |

One switch: a change runs only when the SQL flag `writes_enabled` AND the exec function (`config.ts` `EXEC_FUNCTION_PRESENT`, false until a later unit) are both on; `reads.ts` `availabilityOf` is the one place that decides, and `/context`, the manual, `/functions` and `/check` all report from it (`level` is the effective level, beside `authority_level`, `writes_enabled`, `level_note`, `drafts_open`, `direct_open`, `reads_open`).

Order of checks: query-string token 400, token shape 404, call log 503, rate limit 429, link 410, scope 403, body 400/413/422, availability 503. The call log is written before anything is answered; if it fails nothing is read.

## Settings (all optional; none is a secret)

Platform-injected: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (the key is read in `index.ts` only). Optional: `AWL_CONFIRM_HOST` (the static confirm page host, decision OD-3; the default is a name that never resolves, on purpose), `AWL_APP_BASE` (PROJEXA app origin for search and fetch deep links, default `https://projexa-ai.com`), `AWL_CLIENT_ADDR_POSITION` (position counted from the right of `x-forwarded-for` for the unknown-token throttle; unset means one shared bucket that no header can rotate; set it only after spike S-3 shows what the gateway appends).

## verify_jwt is false

The caller is an AI tool holding a link, not a signed-in browser, so there is no Supabase JWT to check; the platform's own check would refuse every call. The link token is checked in SQL on every call instead (`ai_work_link__resolve`), together with the person's live role, so a demotion or revocation takes effect on the next call.

## How the PM deploys it

After the U-46a migrations (`drizzle/0621` to `0628`, and for the write path `0629` and `0630`, applied in that order BEFORE the code that names the two `submissions` columns is merged) are applied to production and the ACTIVE-CLAIMS entry for this function is on `main`: deploy the folder as the function `ai-work-link` with `verify_jwt` false, through the Supabase MCP or the CLI. The bundle needs `../_shared/ai-link/core.ts` and both generated JSON files beside the sources (whether the MCP file list accepts a `../_shared` path was not tried; the CLI bundles it). Then run the harness of `scripts/verify/ai-link/` against a test link (later unit U-46b2). Not deployed by this unit.

## Tests

`bun test --isolate src/lib/services/ai-work-link-router.test.ts src/lib/services/ai-work-link-mcp.test.ts src/lib/services/ai-work-link-openapi.test.ts src/lib/services/ai-work-link-index.test.ts`
