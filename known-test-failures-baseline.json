{
  "generatedAt": "2026-09-04",
  "generatedBy": "R72 Phase 7 Deploy Ritual -- regenerated via scripts/_regenerate-test-baseline.mjs after the hand-built version was found truncated",
  "provenance": "Byte-identical across R71 Phase 4 (claude_log id 176, verified pre-merge-checkout too), R72 Phase 3 (claude_log id 193), and this regeneration run. Deterministic cross-test-file pollution, full-suite-only, reproduces on isolated pre-merge main. Not a regression from any change in this project's history so far.",
  "totalTests": 5676,
  "pass": 5602,
  "skip": 5,
  "fail": 69,
  "knownFailures": [
    "withTenantContext: the D-06 nesting guard > a single-level call still resolves, and sets the org GUC",
    "withTenantContext: the D-06 nesting guard > entering it from inside its own callback rejects with 'nested withTenantContext'",
    "withTenantContext: the D-06 nesting guard > the rejection names both orgs and says what to do instead",
    "withTenantContext: the D-06 nesting guard > the second transaction is never opened -- the guard runs before a connection is taken",
    "withTenantContext: the D-06 nesting guard > isInsideTenantContext() reports the truth, in and out",
    "withTenantContext: production warns instead of throwing > nesting in production logs both stacks at warn level and lets the request finish",
    "the app_runtime pool options this guard exists to protect > still max 5, with the timeouts R46 added -- the guard is the fix, not a bigger pool",
    "F-16: the 30 s idle-in-transaction safety net travels with the connection > postgres() is given connection.options carrying the timeout as a -c startup option",
    "F-16: a transaction the server terminated is logged, not swallowed > a 25P03 failure inside withTenantContext warns with its SQL text, and still rejects",
    "createProgressEntry -- R48_PROGRESS_ENTRY_NO_PROJECT_MEMBERSHIP_CHECK_01 > activityId belonging to a DIFFERENT project than the supplied projectId is rejected (404), not silently accepted",
    "createProgressEntry -- R48_PROGRESS_ENTRY_NO_PROJECT_MEMBERSHIP_CHECK_01 > activityId belonging to the SAME supplied project succeeds -- the fix does not break the legitimate same-project case",
    "createProgressEntry -- R48_PROGRESS_ENTRY_NO_PROJECT_MEMBERSHIP_CHECK_01 > boqLineItemId belonging to a DIFFERENT project's BOQ than the supplied projectId is rejected (404), even though the activity itself is valid for that project",
    "createProgressEntry -- R67 B-09: the BOQ-line rule > a project WITH a BOQ rejects an entry that names no line, with code BOQ_LINE_REQUIRED and missing ['boqLine']",
    "createProgressEntry -- R67 B-09: the BOQ-line rule > a project WITHOUT a BOQ accepts the entry and reports linkedToBoq false",
    "createProgressEntry -- R67 B-09: the BOQ-line rule > a project WITH a BOQ and a real line succeeds and reports linkedToBoq true",
    "createProgressEntry rolls the site records up onto the linked schedule activity > recording 50% of a linked BOQ line sets the linked issue's completion_percentage to 50 and its completion_source to 'site_records'",
    "createProgressEntry rolls the site records up onto the linked schedule activity > a percent-only entry (no quantity surveyed yet) is worth the same, not zero",
    "createProgressEntry rolls the site records up onto the linked schedule activity > the roll-up runs on the CALLER's transaction -- never a nested withTenantContext (programme decision D-06)",
    "R67 E-06: the Project Status report and the budget-variance report state ONE budget > ACCEPTANCE: a BOQ whose root lines carry computedBudget totalling 2193.75 gives the project-status report that figure, and the budget-variance report the same one",
    "R67 E-06: the Project Status report and the budget-variance report state ONE budget > the ERP annual ledger figure is still returned, under its own name, so nothing that wants it has lost it",
    "R67 E-06: the Project Status report and the budget-variance report state ONE budget > ACCEPTANCE: a project with no BOQ reports budget null -- rendered as an en dash, never 0",
    "resolveBranding (never a broken/blank UI for an unconfigured org) > a configured logo path resolves to a real public URL under the org-branding bucket",
    "updateBrandingAsset / getBrandingAssetPath > updateBrandingAsset('logo', path) writes the logo column and returns resolved branding",
    "translatePromptVersion > rejects an unknown locale before touching the DB or an LLM",
    "translatePromptVersion > 404s for an unknown prompt version",
    "translatePromptVersion > returns the cached translation instead of calling the LLM again",
    "translatePromptVersion > calls the LLM and persists a new translation when none is cached",
    "translatePromptVersion > force=true re-translates and updates the existing row instead of inserting",
    "translatePromptVersion > throws when no AI model is configured",
    "R68 Phase 6 boolean 1 -- a real caller context must exist > refuses a caller that resolves to neither a live user nor a live api key, and writes NOTHING",
    "R68 Phase 6 boolean 1 -- a real caller context must exist > refuses a deactivated user even though the row exists",
    "R68 Phase 6 boolean 1 -- a real caller context must exist > refuses a revoked api key",
    "R68 Phase 6 boolean 1 -- a real caller context must exist > 'chain row exists': a declared chain that is not in platform.dynamic_chains refuses the write",
    "R68 Phase 6 boolean 2 -- the write's inputs must resolve against real data > a DEPARTMENT-scoped write naming a department that does not exist in this org is refused, and writes NOTHING",
    "R68 Phase 6 boolean 2 -- the write's inputs must resolve against real data > a USER-scoped write naming a user who is not in this org is refused",
    "R68 Phase 6 boolean 2 -- the write's inputs must resolve against real data > a supersede whose target row belongs to a different org is refused before any write",
    "R68 Phase 6 boolean 3 -- the caller must hold the minimum role for this write > a viewer cannot write institutional memory at all, and writes NOTHING",
    "R68 Phase 6 boolean 3 -- the caller must hold the minimum role for this write > an external_auditor cannot write institutional memory either (rank 1, same floor)",
    "R68 Phase 6 boolean 3 -- the caller must hold the minimum role for this write > a member CAN write an ordinary org-scoped memory (the real run-submission path still works)",
    "R68 Phase 6 boolean 3 -- the caller must hold the minimum role for this write > a member cannot write a DEPARTMENT-scoped memory (that needs manager)",
    "R68 Phase 6 boolean 3 -- the caller must hold the minimum role for this write > a manager CAN write a DEPARTMENT-scoped memory",
    "R68 Phase 6 boolean 3 -- the caller must hold the minimum role for this write > a member cannot write into ANOTHER user's USER-scoped memory (that needs admin)",
    "R68 Phase 6 boolean 3 -- the caller must hold the minimum role for this write > a member CAN write their OWN USER-scoped memory (the real chat-service path still works)",
    "R68 Phase 6 boolean 3 -- the caller must hold the minimum role for this write > promoteMemoryRecord refuses a member promoting an ORGANIZATION-scoped memory, and writes NOTHING",
    "R68 Phase 6 boolean 3 -- the caller must hold the minimum role for this write > archiveMemoryRecord refuses a viewer, and writes NOTHING",
    "R68 Phase 6 -- a client-supplied authorization claim is refused, not ignored > rejects an actor carrying `authorized` before it touches the database",
    "R68 Phase 6 -- a client-supplied authorization claim is refused, not ignored > rejects an actor carrying `isAuthorized` before it touches the database",
    "R68 Phase 6 -- a client-supplied authorization claim is refused, not ignored > rejects an actor carrying `role` before it touches the database",
    "R68 Phase 6 -- a client-supplied authorization claim is refused, not ignored > rejects an actor carrying `permissions` before it touches the database",
    "R68 Phase 6 -- a client-supplied authorization claim is refused, not ignored > rejects an actor carrying `bypassAuthorization` before it touches the database",
    "R68 Phase 6 -- an AI write with no attribution is refused (R-IMG-07) > a REAL AI write persists model, prompt hash, caller and chain onto the row",
    "R68 Phase 6 -- an AI write with no attribution is refused (R-IMG-07) > a non-AI write records the caller and chain with NULL model/prompt -- truthful, not a gap",
    "R68 Phase 6 -- an AI write with no attribution is refused (R-IMG-07) > existing metadata keys survive the attribution stamp",
    "R68 Phase 6 -- an AI write with no attribution is refused (R-IMG-07) > supersedeMemoryRecord persists model_id/prompt_hash onto the memory_versions snapshot",
    "R68 Phase 6 -- an AI write with no attribution is refused (R-IMG-07) > promoteMemoryRecord records model/prompt/chain in the lifecycle history it appends",
    "R68 Phase 6 -- an unauthorized caller cannot use the byte-identical no-op as a probe > supersede with UNCHANGED content is still refused for an unauthorized caller",
    "PROOF 1 (cont.): a non-entitled org is refused on every write path > createMemoryRecord() refuses, and writes nothing",
    "PROOF 2a (behavioural): an org entitled to IMG and NOTHING ELSE can recall and write > createMemoryRecord() completes a real write with only the IMG branch enabled",
    "resolveViewerScope > member is forced to their own ownerId, ignoring any requested ownerId",
    "resolveViewerScope > viewer is forced to their own ownerId too (below manager rank)",
    "GET /api/v1/projexa/accounts -- role gate (API_READ_WITHOUT_ROLE_CHECK) > a rank-1 role (external_auditor) is blocked with 403 before listAccounts runs",
    "GET /api/v1/projexa/accounts -- role gate (API_READ_WITHOUT_ROLE_CHECK) > another rank-1 role (client_viewer) is also blocked with 403",
    "GET /api/v1/projexa/ar-aging > a rank-1 role (external_auditor) is blocked with 403, arAgingReport is never called -- exposing real customer AR balances to the most-restricted tier is the exact gap this closes",
    "GET /api/v1/projexa/ar-aging > a rank-1 role (client_viewer) is blocked with 403",
    "GET /api/v1/projexa/dunning-list > a rank-1 role (external_auditor) is blocked with 403, dunningList is never called",
    "GET /api/v1/projexa/dunning-list > a rank-1 role (client_viewer) is blocked with 403, dunningList is never called",
    "GET /api/v1/projexa/finance-dashboard (R58 Lane 2 role-check fix) > a rank-1 role (external_auditor) is rejected with 403, getFinanceDashboard never called",
    "GET /api/v1/projexa/finance-dashboard (R58 Lane 2 role-check fix) > a rank-1 role (client_viewer) is rejected with 403, getFinanceDashboard never called",
    "GET /api/v1/projexa/finance-dashboard (R58 Lane 2 role-check fix) > an API key without read scope is rejected with 403, getFinanceDashboard never called"
  ]
}
